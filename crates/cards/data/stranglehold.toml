# Your opponents can't search libraries.
# If an opponent would begin an extra turn, that player skips that turn instead.
name = "Stranglehold"
otags = ["hate-tutor", "prevent-extra-turns", "single-english-word-name"]
id = "b66c6e3a-daed-43a3-95d6-4f9d6223bbf1"
default_print = "10a4d051-8e13-47c1-bfd6-17ab44670250"
oracle = "Your opponents can't search libraries.\nIf an opponent would begin an extra turn, that player skips that turn instead."
approximates = "\"If an opponent would begin an extra turn, that player skips that turn instead\" is dropped — no card in the pool grants an extra turn, so the engine has no extra-turn machinery to skip."

[cost]
generic = 3
red = 1

[kind]
type = "enchantment"

# Your opponents can't search libraries.
[[abilities]]
timing = "static"

# Your opponents can't search libraries.
[[abilities.effects]]
type = "static"
mode = "opponents_cant_search_libraries"
