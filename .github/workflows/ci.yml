name: CI

# Deploy PRD §GitHub Actions — PR verify via reusable verify-jobs.yml; terraform validates iac/.
# Mirrors verify in verify-and-release.yml; no release steps here.

on:
  pull_request:
    branches:
      - main
      - master

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      iac: ${{ steps.filter.outputs.iac }}
    steps:
      - uses: actions/checkout@v7

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            iac:
              - 'iac/**'
              - '.github/workflows/ci.yml'

  # Squash-merge uses the PR title as the main commit subject (semantic-release).
  # Branch commits are gated locally / in Cloud via Husky commit-msg — not here.
  commitlint:
    name: Commitlint (PR title)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v5
        with:
          node-version-file: .node-version
          cache: npm

      - run: npm clean-install

      - name: Lint PR title
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: printf '%s\n' "$TITLE" | npx commitlint

  verify:
    name: Verify
    uses: ./.github/workflows/verify-jobs.yml

  terraform:
    name: Terraform (iac/)
    needs: changes
    if: needs.changes.outputs.iac == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: terraform fmt -check
        working-directory: iac
        run: terraform fmt -check -recursive

      - name: terraform init (no backend — this only validates syntax/config, no state access)
        working-directory: iac
        run: terraform init -backend=false

      - name: terraform validate
        working-directory: iac
        run: terraform validate
