<!-- Generated by `just engine-cr-index` — do not edit by hand. -->

# CR → Engine Index

Reverse index of Comprehensive Rules citations in `crates/engine` (source + tests).
Regenerate with `just engine-cr-index` after adding `CR …` comments.
Check freshness with `just engine-cr-index-check`.

**482** unique rules · **4379** rule→line hits · **3676** unique source lines

## Rules

### CR 101.4

- `crates/engine/src/pending/handlers/edict.rs:11` — (CR 101.4: council's dilemma, join forces), which is [`Self::apnap_order`] when `first` is
- `crates/engine/src/resolution/pause_edict.rs:140` — No "starting with you" wording, so CR 101.4 default APNAP order, unlike Fateful
- `crates/engine/src/resolution/pause_edict.rs:152` — Conundrum Sphinx's attack trigger: "each player chooses a card name" (CR 101.4
- `crates/engine/src/types/effect/choice.rs:49` — fan-out over every living player in APNAP order (CR 101.4 — no "starting with you"
- `crates/engine/tests/game.rs:34147` — ── Join forces (Collective Voyage): a shared X funded by every player (CR 101.4, CR 701.19) ──
- `crates/engine/tests/game.rs:34223` — "searches run in APNAP order (CR 101.4)"
- `crates/engine/tests/game.rs:54234` — APNAP order (CR 101.4), each under that player's own control (CR 701.19), not the Explorer's
- `crates/engine/tests/game.rs:92906` — Sphinx's controller (CR 101.4 APNAP), each seat pauses on a ChooseCardName in turn.

### CR 102.1

- `crates/engine/tests/game.rs:102076` — CR 102.1 — "each player" includes every seat, the controller's own included.

### CR 102.2

- `crates/engine/src/triggers.rs:732` — (CR 102.2), so fire each of their `OpponentGainsLife` abilities off the same
- `crates/engine/src/triggers.rs:1526` — doesn't (CR 102.2: in a multiplayer pod, every other player is an opponent, so

### CR 102.3

- `crates/engine/src/resolution/damage.rs:327` — but only to opponents of the ability's controller (CR 102.3) other than the one who
- `crates/engine/src/triggers.rs:2442` — of `source`'s controller (every other player is, CR 102.3). A spell's own damage never
- `crates/engine/src/triggers.rs:2905` — the watcher's own opponents (CR 102.3 — every other player).
- `crates/engine/src/types/effect/shared.rs:1566` — is an opponent (CR 102.3), but an eliminated seat no longer counts (CR 800.4a) — the
- `crates/engine/src/types/trigger.rs:341` — `source`); every player other than the controller is an opponent (CR 102.3). See
- `crates/engine/tests/game.rs:97559` — (CR 102.3 — every other player is an opponent; CR 800.4a — an eliminated player is no

### CR 103.8a

- `crates/engine/src/core.rs:100` — where they skip it (CR 103.8a/c) — armed here, spent in [`Game::perform_turn_based_actions`].
- `crates/engine/src/lib.rs:161` — first draw in a two-player game only (CR 103.8a; multiplayer skips no one, CR 103.8c).
- `crates/engine/src/priority.rs:1691` — The starting player skips their first draw step in a two-player game (CR 103.8a);
- `crates/engine/tests/game.rs:49757` — CR 103.8a: in a two-player game the starting player skips their first draw step.
- `crates/engine/tests/game.rs:76689` — draw (not skipped — only the very first turn of the game skips its draw, CR 103.8a) and
- `crates/engine/tests/game.rs:90643` — first non-skipped draw of the game, CR 103.8a) still fires it for "that player" — player 1.

### CR 103.8c

- `crates/engine/src/core.rs:100` — where they skip it (CR 103.8a/c) — armed here, spent in [`Game::perform_turn_based_actions`].
- `crates/engine/src/lib.rs:161` — first draw in a two-player game only (CR 103.8a; multiplayer skips no one, CR 103.8c).
- `crates/engine/src/priority.rs:1692` — in multiplayer no one skips (CR 103.8c). `begin_first_turn` arms the flag from the
- `crates/engine/tests/game.rs:49729` — CR 103.8c: in a multiplayer game no one skips their first draw. The turn-1 draw step must

### CR 104.3

- `crates/engine/src/priority.rs:1725` — SBA sweep and death triggers are handled by `submit` after this step, and a (CR 704, CR 603, CR 104…
- `crates/engine/tests/game.rs:3953` — death triggers resolve inside the first-strike step's priority window, before the regular (CR 117, …
- `crates/engine/tests/game.rs:41017` — Dogfood: empty declare rejected (goad), then both seats passed — the step used to advance (CR 701.3…
- `crates/engine/tests/game.rs:59228` — can't be used here because it `unwrap`s each pass and a pause makes further passes illegal. (CR 117…

### CR 104.3a

- `crates/engine/src/core.rs:981` — has lost makes no choices (CR 104.3a), so the declaration falls back to whoever would
- `crates/engine/src/priority.rs:101` — Leave the game (CR 104.3a). Cannot fail: a player may always quit, with or without priority,
- `crates/engine/src/types/effect/shared.rs:1730` — existential over the ability controller's opponents (CR 104.3a): holds when any living
- `crates/engine/src/types/stack.rs:456` — Leave the game (CR 104.3a). Legal at any time, with or without priority, and even while the
- `crates/engine/tests/game.rs:12441` — Concede is legal without priority — you may quit on someone else's turn. (CR 117, CR 500, CR 104.3a)
- `crates/engine/tests/game.rs:77236` — and disappears exactly as an opponent's life crosses 10 (CR 104.3a — an existential over
- `crates/engine/tests/game.rs:86320` — (a state-based loss, CR 104.3a) *while* Arena's own trigger is still queued behind that
- `crates/engine/tests/game.rs:86345` — "0 life is a state-based loss (CR 104.3a)"

### CR 104.3c

- `crates/engine/tests/game.rs:22774` — nobody decks out along the way (CR 104.3c).
- `crates/engine/tests/game.rs:55029` — non-empty so their draw on the intervening/next turn isn't a deck-out loss (CR 104.3c),
- `crates/engine/tests/game.rs:73462` — Every library has at least seven cards: a short library's clamped draw sets the CR 104.3c
- `crates/engine/tests/game.rs:73728` — draw step on the way back around to P0 — an empty-library draw loses the game (CR 104.3c),
- `crates/engine/tests/game.rs:90461` — loses to an empty draw along the way (CR 104.3c).

### CR 105.1

- `crates/engine/src/characteristics.rs:2159` — Balefire Liege's "cast a red spell" / "cast a white spell" — CR 105.1/202.2, the
- `crates/engine/src/types/filter.rs:244` — spell"). Reads the spell's own colors (CR 105.1/202.2, [`color_identity`]), so a

### CR 105.2

- `crates/engine/src/characteristics.rs:415` — The colors of `object` — its source card's colored cost pips (CR 105.2), plus any colors
- `crates/engine/src/characteristics.rs:442` — at least one color with it (CR 105.2). Only `target` itself is a real target — CR 608.2b
- `crates/engine/src/resolution/damage.rs:270` — target creature and each other creature that shares a color with it" (CR 105.2).
- `crates/engine/src/resolution/pump.rs:85` — of the chosen target (the old "Radiance" keyword action, CR 105.2), not just the
- `crates/engine/src/types/filter.rs:624` — A permanent's color restriction for a [`PermanentFilter`] (CR 105.2).
- `crates/engine/tests/game.rs:93585` — shares a color with it gain protection from the chosen color until end of turn." (CR 105.2,
- `crates/engine/tests/game.rs:93873` — other creature that shares a color with it." (CR 105.2)

### CR 105.2a

- `crates/engine/src/de.rs:353` — Explicit colors (CR 105.2a) overriding the cost-pip derivation — a token's stated
- `crates/engine/src/query.rs:1478` — Color-count (Vanishing Verse's "monocolored permanent", CR 105.2a) — a colorless
- `crates/engine/src/query.rs:1485` — Specific color (CR 105.2a — Oran-Rief, the Vastwood's "each green creature").
- `crates/engine/src/query.rs:1499` — Negated specific color (CR 105.2a's negation — Terror/Shriekmaw's "nonblack
- `crates/engine/src/types/card.rs:590` — Explicit colors (CR 105.2a: a color indicator, or CR 111.4's "colors are determined by
- `crates/engine/src/types/card.rs:1041` — A card's *colors* (CR 105.2a: mana-cost pips, or `def.colors` for a card whose color a cost
- `crates/engine/src/types/filter.rs:319` — graveyard"). [`Sorcery`](Self::Sorcery) plus a CR 105.2a color check, read off the card's
- `crates/engine/src/types/filter.rs:638` — Exactly one color (CR 105.2a — colorless has zero and doesn't qualify).
- `crates/engine/src/types/filter.rs:640` — Is white (CR 105.2a).
- `crates/engine/src/types/filter.rs:642` — Is blue (CR 105.2a).
- `crates/engine/src/types/filter.rs:644` — Is black (CR 105.2a).
- `crates/engine/src/types/filter.rs:646` — Is red (CR 105.2a).
- `crates/engine/src/types/filter.rs:648` — Is green (CR 105.2a) — Oran-Rief, the Vastwood's "each green creature".
- `crates/engine/src/types/filter.rs:650` — Does NOT have the named color (CR 105.2a's negation — Terror/Shriekmaw's "nonblack
- `crates/engine/tests/game.rs:13355` — axis (CR 105.2a: monocolored is exactly one color; colorless has zero and doesn't count).
- `crates/engine/tests/game.rs:35877` — Restless Spire: "this land becomes a 2/1 **blue and red** Elemental creature" (CR 105.2a —

### CR 105.2b

- `crates/engine/src/types/card.rs:1064` — A hybrid pip (CR 107.4e, {A/B}) contributes to both of its colors (CR 105.2b) — Balefire

### CR 105.4

- `crates/engine/src/characteristics.rs:648` — `ObjectId`) has — its color(s), a creature type (CR 702.16), or multicolored (CR 105.4).
- `crates/engine/src/characteristics.rs:2330` — "Multicolored" is two or more colors (CR 105.4) — a monocolored or colorless source
- `crates/engine/src/types/card.rs:192` — or "protection from multicolored" (Stonecoil Serpent, CR 105.4's ≥2-colors quality). Kept

### CR 106.4

- `crates/engine/src/cast.rs:2549` — "Add N mana of any one color" (CR 106.4 — Lotus Field, Kami of Whispered Hopes):
- `crates/engine/src/pending/handlers/library.rs:248` — Answer a [`PendingChoice::ChooseManaColor`] (CR 106.4's "add N mana of any one color" —
- `crates/engine/src/types/effect/static.rs:157` — "Add N mana of any one color" (CR 106.4 — Goldspan Dragon's granted Treasure ability):
- `crates/engine/src/types/stack.rs:416` — Answer a [`PendingChoice::ChooseManaColor`] (CR 106.4's "add N mana of any one color"):
- `crates/engine/src/types/stack.rs:1636` — color are added to their pool (CR 106.4's "add N mana of any one color" —
- `crates/engine/src/zones.rs:2` — Primary: CR 400 (zones), CR 121 (drawing a card), CR 106.4 (mana pool).
- `crates/engine/tests/game.rs:19976` — ── Chosen-single-color mana: "add N mana of any one color" (CR 106.4) ─────────────
- `crates/engine/tests/game.rs:65701` — "Any *one* color" (CR 106.4): both credits lock to the one color the controller names,

### CR 106.6

- `crates/engine/src/characteristics.rs:490` — colorless commander — CR 106.6 has no mana of no color).

### CR 106.9

- `crates/engine/src/cast.rs:489` — CR 106.9's "spent to cast" query (Court Hussar's "unless {W} was spent to cast it"):
- `crates/engine/src/cast.rs:2405` — just a spell's — Nin, the Pain Artist's own `{X}{U}{R}` activation qualifies (CR 106.9).
- `crates/engine/src/cast.rs:2731` — `events` (CR 106.9 — Court Hussar's "unless {W} was spent to cast it"), read off its trailing
- `crates/engine/src/core.rs:889` — The colors of mana spent to cast the spell at `id` (CR 106.9 — [`Spell::spent_colors`]),
- `crates/engine/src/types/card.rs:1641` — The colors of mana actually spent to cast this spell (CR 106.9 — Court Hussar's "unless
- `crates/engine/src/types/card.rs:1957` — The colors of mana spent to cast the spell that became this permanent (CR 106.9), fixed
- `crates/engine/src/types/effect/shared.rs:1782` — "if `color` was spent to cast this" (CR 106.9 — Court Hussar's "unless {W} was spent to
- `crates/engine/src/types/mana.rs:355` — A credit that may be spent only per `restriction` (CR 106.9's "restrictions" on mana,
- `crates/engine/src/types/mana.rs:377` — A restriction on when a [`Mana::Restricted`] credit may be spent (CR 106.9), checked by
- `crates/engine/src/types/mana.rs:395` — `{X}{U}{R}`, CR 106.9); `Game::activate_ability` feeds `allows` the ability's own cost's
- `crates/engine/src/types/mana.rs:416` — being cast, so CR 106.9's restricted credits can never fund one).
- `crates/engine/src/types/mana.rs:495` — Spend-restricted credits ([`Mana::Restricted`], CR 106.9), a small fixed set of
- `crates/engine/src/types/mana.rs:681` — The colors actually spent in this multiset (CR 106.9's "spent to cast" query — Court
- `crates/engine/src/types/mana.rs:745` — CR 106.9). Folds every restricted credit `spell` makes usable into the plain algorithm
- `crates/engine/src/types/stack.rs:2087` — The colors of mana actually spent to pay this cast's cost (CR 106.9); see
- `crates/engine/tests/game.rs:84294` — ── Court Hussar (CR 106.9 — mana-spent-to-cast tracking) #157 ─────────────────────────
- `crates/engine/tests/game.rs:84374` — means {W} was spent (CR 106.9) alongside the {U} pip, so it survives.

### CR 106.11

- `crates/engine/src/priority.rs:216` — "Tapped for mana" means it produced mana (CR 106.11) — the type this tap made, read back

### CR 107.3

- `crates/engine/src/apply.rs:570` — CR 107.3: a static cast-X modification (Unbound Flourishing) doubles the value of
- `crates/engine/src/cast.rs:2399` — CR 107.3/601.2b: the chosen `{X}` folds into the mana cost (paid once per `{X}` symbol);
- `crates/engine/src/cast.rs:2420` — later push dilutes the tail — Illusionary Mask's "the mana you spent on {X}" (CR 107.3)
- `crates/engine/src/cast.rs:2588` — The activation's own chosen `{X}` (CR 107.3/601.2b — Fungal Reaches' "Remove
- `crates/engine/src/cast.rs:2705` — threading the chosen `{X}` so `Amount::X` resolves against it (CR 107.3) and the spent
- `crates/engine/src/cast.rs:2744` — appended to `events` ([`ManaPool::spent_counts`] — Illusionary Mask's CR 107.3 "the mana you
- `crates/engine/src/characteristics.rs:2251` — for the announced `base`, after that caster's static cast-X modifications (CR 107.3 —
- `crates/engine/src/de.rs:599` — `{X}` symbols (`x = 3` for Astral Cornucopia's `{X}{X}{X}`, CR 107.3). `false`/absent means
- `crates/engine/src/effects.rs:97` — Flourishing's copied {X} ability, CR 107.3), which no pool `mv_max_x` reads.
- `crates/engine/src/effects.rs:124` — (CR 107.3). A pausing effect leaves a PendingChoice behind.
- `crates/engine/src/pending/handlers/optional.rs:205` — Answer a [`PendingChoice::PayCost`] whose `cost` carries a chosen `{X}` (CR 107.3 —
- `crates/engine/src/pending/handlers/optional.rs:237` — into generic per CR 107.3); unaffordable leaves the choice pending with nothing tapped.
- `crates/engine/src/resolution/mod.rs:62` — ([`StackItem::Ability::spent_mana`]), read by [`Effect::Choice(ChoiceEffect::CastCreatureFaceDown)`…
- `crates/engine/src/resolution/pause_hand.rs:84` — spent on this ability's `{X}` could pay (`ctx.spent_mana`, CR 107.3).
- `crates/engine/src/triggers.rs:4886` — [`push_ability_group`](Self::push_ability_group) threading a chosen `{X}` (CR 107.3) onto
- `crates/engine/src/triggers.rs:4890` — CR 107.3 payability test; all zeroes except a real activation payment). `activated` marks
- `crates/engine/src/types/card.rs:2055` — spell each turn" gate (Nev, Zimone Infinite Analyst). CR 107.3.
- `crates/engine/src/types/filter.rs:212` — [`Cost::x`]'s own "this cost contains {X}" predicate (CR 107.3).
- `crates/engine/src/types/mana.rs:11` — ability's activation cost alike (Nin, the Pain Artist's `{X}{U}{R}, {T}`). CR 107.3: every
- `crates/engine/src/types/mana.rs:16` — ponytail: `{X}` on a permanent's own characteristics (a CDA, CR 107.3) isn't modeled; grow
- `crates/engine/src/types/mana.rs:72` — This cost with a chosen `x` folded into its generic component (CR 601.2b/107.3: paying
- `crates/engine/src/types/mana.rs:88` — (Illusionary Mask's `{X}` test, CR 107.3). `spent` is [`ManaPool::spent_counts`]'s shape:
- `crates/engine/src/types/mana.rs:694` — The multiset of mana actually spent, counted per kind for Illusionary Mask's CR 107.3
- `crates/engine/src/types/stack.rs:215` — The chosen `{X}` for an activation cost that contains `{X}` (CR 107.3 — paid once per
- `crates/engine/src/types/stack.rs:267` — Answer a [`PendingChoice::PayCost`] whose `cost` carries a chosen `{X}` (CR 107.3 — Decree
- `crates/engine/src/types/stack.rs:1459` — could be paid by some amount of, or all of, the mana spent on the `{X}` paid (CR 107.3,
- `crates/engine/src/types/stack.rs:2006` — ([`ManaPool::spent_counts`]'s shape) — Illusionary Mask's CR 107.3 "the mana you spent
- `crates/engine/src/types/stack.rs:2196` — activated ability whose cost contains `{X}` (CR 107.3), or a copy of such an ability
- `crates/engine/src/types/stack.rs:2208` — [`StackItem::Ability::spent_mana`] (Illusionary Mask's CR 107.3 test). All zeroes for
- `crates/engine/tests/game.rs:7881` — {X}, double the value of X." (CR 107.3) — Hangarback Walker ({X}{X}, enters with X counters)
- `crates/engine/tests/game.rs:7954` — The cast-X doubling applies to *permanent* spells only (CR 107.3 — "a permanent spell"); an
- `crates/engine/tests/game.rs:24619` — turn, put X +1/+1 counters on Nev." CR 107.3: the chosen X is the same for every {X} symbol
- `crates/engine/tests/game.rs:69853` — Astral Cornucopia's printed cost is {X}{X}{X} (CR 107.3: every {X} in a cost is the same
- `crates/engine/tests/game.rs:80104` — The printed test (CR 107.3): "a creature card in your hand whose mana cost could be paid by
- `crates/engine/tests/game.rs:80244` — [`Cost::payable_from_multiset`] directly (CR 107.3's "could be paid by some amount of, or all

### CR 107.3b

- `crates/engine/src/types/mana.rs:92` — necessarily chosen as 0 for a cast it must fund at 0 (CR 107.3b), so its `x` pips need
- `crates/engine/tests/game.rs:80276` — The candidate's own {X} counts 0 (CR 107.3b), so its pips need nothing.

### CR 107.3c

- `crates/engine/src/cast.rs:2253` — of that kind (X = 0 is always legal, CR 107.3c, so this only ever rejects a too-large X,
- `crates/engine/src/types/effect/shared.rs:1439` — legal (CR 107.3c). `false` (every other counter-removal cost) leaves the fixed count above

### CR 107.3h

- `crates/engine/tests/game.rs:70304` — and/or {G}." CR 107.3h/CR 605.3a: X is chosen at activation, and the ability produces

### CR 107.3i

- `crates/engine/src/query.rs:851` — own locked-in cast X is it (CR 601.2b/107.3i). Shared by [`Game::place_targeted_ability`]
- `crates/engine/src/triggers.rs:1377` — cast `{X}` (CR 601.2b/107.3i) — the same value [`Game::ability_source_x`] returns for a
- `crates/engine/src/types/card.rs:1849` — spell has left the stack and X would otherwise revert to 0 (CR 107.3i). For a hydra-style

### CR 107.4

- `crates/engine/src/types/card.rs:168` — This color's mana-symbol letter (CR 107.4 — `{W}{U}{B}{R}{G}`), for rendering a

### CR 107.4c

- `crates/engine/tests/game.rs:20362` — Mana of a color is never colorless (CR 107.4c): the dual credit is always one of

### CR 107.4e

- `crates/engine/src/de.rs:639` — Hybrid mana pips (CR 107.4e — `{a/b}`): a list of two-color arrays, one per
- `crates/engine/src/types/card.rs:1064` — A hybrid pip (CR 107.4e, {A/B}) contributes to both of its colors (CR 105.2b) — Balefire
- `crates/engine/src/types/mana.rs:19` — Hybrid mana pips (CR 107.4e — `{a/b}`), one entry per symbol: each is payable by mana of
- `crates/engine/src/types/mana.rs:90` — pip needs a spent unit of its color; each hybrid pip (CR 107.4e — `{a/b}`) a unit of either
- `crates/engine/src/types/mana.rs:148` — Assign each hybrid pip (CR 107.4e — `{a/b}`) one remaining spent unit of either of its colors,
- `crates/engine/src/types/mana.rs:826` — Hybrid pips (CR 107.4e — `{a/b}`), tallied per unordered color pair like `either`
- `crates/engine/tests/game.rs:20433` — ── Hybrid mana pips in activation costs: "{a/b}, {T}: …" (CR 107.4e) ───────────────
- `crates/engine/tests/game.rs:20568` — A hybrid pip {B/G} (CR 107.4e) can be paid with either of its two colors: a single
- `crates/engine/tests/game.rs:20659` — colored pip (CR 107.4e: hybrid mana "can be paid with either" of its two colors).
- `crates/engine/tests/game.rs:80176` — A hybrid pip ({W/U}, CR 107.4e) is payable by spent mana of either of its colors: Azorius

### CR 107.4f

- `crates/engine/src/apply.rs:1909` — Compleated (CR 107.4f — Vraska, Betrayal's Sting): a {a/P} pip paid with life
- `crates/engine/src/cast.rs:493` — A Phyrexian pip (CR 107.4f — Vraska, Betrayal's Sting's `{B/P}`) `settle_payment` fell
- `crates/engine/src/cast.rs:2755` — How many of `cost`'s Phyrexian pips (CR 107.4f — `{a/P}`) the payment [`Game::settle_payment`]
- `crates/engine/src/de.rs:642` — Phyrexian mana pips (CR 107.4f — `{a/P}`): a list of colors, one per Phyrexian
- `crates/engine/src/types/card.rs:1070` — A Phyrexian pip (CR 107.4f, {A/P}) contributes its color regardless of how it's paid (CR
- `crates/engine/src/types/card.rs:1650` — How many of this spell's Phyrexian mana pips (CR 107.4f — Vraska, Betrayal's Sting's
- `crates/engine/src/types/card.rs:1654` — ([`Event::PermanentEntered`]) — CR 107.4f: "If life was paid, this planeswalker enters
- `crates/engine/src/types/mana.rs:25` — Phyrexian mana pips (CR 107.4f — `{a/P}`), one entry per symbol: each is payable with one
- `crates/engine/src/types/mana.rs:97` — A Phyrexian pip (CR 107.4f, `phyrexian`) never appears in this check: it's always payable
- `crates/engine/src/types/mana.rs:1010` — Phyrexian pips (CR 107.4f — `{a/P}`): CR 107.4f frames the mana-or-2-life choice as the
- `crates/engine/src/types/mana.rs:1017` — ponytail: no genuine choice offered (CR 107.4f) — a caster who'd rather bank the mana
- `crates/engine/src/types/mana.rs:1331` — Vraska, Betrayal's Sting: {4}{B}{B/P} (CR 107.4f).
- `crates/engine/src/types/mana.rs:1354` — .expect("black mana pays the phyrexian pip (CR 107.4f)");
- `crates/engine/src/types/mana.rs:1360` — CR 107.4f's "or 2 life" route: the planner never fails to cover a Phyrexian pip, even
- `crates/engine/src/types/stack.rs:2090` — How many Phyrexian mana pips were paid with life instead of mana (CR 107.4f); see
- `crates/engine/tests/game.rs:101090` — Vraska, Betrayal's Sting's {4}{B}{B/P} (CR 107.4f): one Swamp funds the plain {B} and four
- `crates/engine/tests/game.rs:101136` — Compleated (CR 107.4f): "If life was paid, this planeswalker enters with two fewer
- `crates/engine/tests/game.rs:102660` — Five Swamps are exactly {4}{B} — the {B/P} can only be paid the other way (CR 107.4f), so

### CR 108.3

- `crates/engine/src/amount.rs:112` — controller isn't you (CR 108.3/720). A permanent you own is controlled by you or an
- `crates/engine/src/characteristics.rs:596` — color-set union and still only needs one qualifying land to add a color (CR 605, CR 108.3, CR 113).
- `crates/engine/src/priority.rs:422` — (CR 605, CR 108.3, CR 113)
- `crates/engine/src/resolution/control.rs:248` — (apply.rs), leaving ownership with the donor (CR 108.3). A target that has left the
- `crates/engine/src/resolution/control.rs:284` — ownership untouched (CR 108.3). Both must still be on the battlefield — an exchange
- `crates/engine/src/resolution/pump.rs:300` — defender being someone other than the Aura's controller. An unattached Aura (mid-SBA) (CR 704, CR 3…
- `crates/engine/src/triggers.rs:1620` — No pool effect kills in sequence within one resolution; revisit if one ever does. (CR 704, CR 108.3…
- `crates/engine/src/triggers.rs:2872` — single `amount`. (CR 510, CR 111, CR 108.3)
- `crates/engine/src/types/trigger.rs:509` — field the moment a second card needs a different power/type predicate. (CR 510, CR 111, CR 108.3)
- `crates/engine/tests/game.rs:4122` — Curiosity Crafter (soc): "Whenever a creature token you control deals combat damage to a (CR 510, C…
- `crates/engine/tests/game.rs:4148` — Ohran Frostfang (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR 108…
- `crates/engine/tests/game.rs:4673` — Defiling Daemogoth (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR …
- `crates/engine/tests/game.rs:15196` — control have hexproof." Both statics are commander_only anthems — a commander creature (CR 702.11, …
- `crates/engine/tests/game.rs:16461` — counter on it gets trample, a bare creature you control doesn't. (CR 702, CR 108.3, CR 122)
- `crates/engine/tests/game.rs:19224` — controller — unlike protection/hexproof, there is no own-permanent bypass. (CR 702.11, CR 108.3)
- `crates/engine/tests/game.rs:30187` — own bear; P1 responds with a flash control-changing Aura on the same bear, which resolves (CR 702.8…
- `crates/engine/tests/game.rs:35511` — Tyvar's Stand ({X}{G}) — "Target creature you control gets +X/+X and gains hexproof (CR 702.11, CR …
- `crates/engine/tests/game.rs:39301` — min: 0 lets the controller decline — nothing enters the battlefield. (CR 603.6a, CR 108.3, CR 403.5)
- `crates/engine/tests/game.rs:43258` — timestamped permanent-control layer, while ownership stays with P0 (CR 108.3), and the new
- `crates/engine/tests/game.rs:43284` — "ownership stays with the donor (CR 108.3)"
- `crates/engine/tests/game.rs:43393` — "the original owner is unchanged by the donation (CR 108.3)"
- `crates/engine/tests/game.rs:43415` — land keeps its owner (CR 108.3), and each new controller can tap the swapped land for mana
- `crates/engine/tests/game.rs:43459` — "ownership is untouched by the exchange (CR 108.3)"
- `crates/engine/tests/game.rs:43464` — "ownership is untouched by the exchange (CR 108.3)"
- `crates/engine/tests/game.rs:43551` — owner never moved (CR 108.3).
- `crates/engine/tests/game.rs:43590` — "ownership never moved through the donation or the exchange (CR 108.3)"
- `crates/engine/tests/game.rs:43670` — "ownership is untouched (CR 108.3)"
- `crates/engine/tests/game.rs:43675` — "ownership is untouched (CR 108.3)"
- `crates/engine/tests/game.rs:43761` — "ownership is untouched by the swap (CR 108.3)"
- `crates/engine/tests/game.rs:43766` — "ownership is untouched by the swap (CR 108.3)"
- `crates/engine/tests/game.rs:43834` — "ownership never moved through the donation or the swap (CR 108.3)"
- `crates/engine/tests/game.rs:44016` — "ownership never moved through the donation or the steal (CR 108.3)"
- `crates/engine/tests/game.rs:57664` — trigger pauses on the mode choice. Returns the dragon's controller. (CR 108.3, CR 403.5, CR 603)
- `crates/engine/tests/game.rs:62132` — target). (CR 701.38, CR 108.3, CR 601.2c)
- `crates/engine/tests/game.rs:65043` — P1's Doomwake (a permanent control change — CR 108.3 keeps P1 as owner), an enchantment P0
- `crates/engine/tests/game.rs:65061` — "P1 still owns it (CR 108.3)"
- `crates/engine/tests/game.rs:65084` — (ownership stays with P2, CR 108.3), an attack on one of P0's opponents fires Breena for P0
- `crates/engine/tests/game.rs:65113` — "P2 still owns it (CR 108.3)"
- `crates/engine/tests/game.rs:65140` — steals P1's Sram (owner stays P1, CR 108.3), P0 casting an Aura fires Sram for P0 — P0
- `crates/engine/tests/game.rs:65152` — "P1 still owns it (CR 108.3)"
- `crates/engine/tests/game.rs:65171` — (owner stays P1, CR 108.3), an enchantment P0 controls dying fires it for P0. The
- `crates/engine/tests/game.rs:65186` — "P1 still owns it (CR 108.3)"
- `crates/engine/tests/game.rs:68655` — Silkguard (soc): "...modified creatures you control gain hexproof until end of turn. (CR 702.11, CR…
- `crates/engine/tests/game.rs:70178` — mid-P0's-turn (via a priority hand-off) without needing a full turn rotation. (CR 117, CR 111, CR 1…
- `crates/engine/tests/game.rs:71646` — creatures you control gain trample and get +X/+X until end of turn, where X is the (CR 702, CR 108.…
- `crates/engine/tests/game.rs:71749` — (a permanent control change — ownership stays with P1, CR 108.3), the trigger fires on P0's
- `crates/engine/tests/game.rs:71761` — "P1 still owns it (CR 108.3)"

### CR 108.4

- `crates/engine/src/triggers.rs:493` — If the dying creature's owner left the game in this same SBA sweep, the (CR 704, CR 108.4)
- `crates/engine/src/triggers.rs:1620` — No pool effect kills in sequence within one resolution; revisit if one ever does. (CR 704, CR 108.3…
- `crates/engine/src/types/card.rs:830` — zone tags — defer until one exists. (CR 603, CR 108.4, CR 403.5)

### CR 109.4

- `crates/engine/src/amount.rs:90` — "Creatures you control" is a *control* test (CR 109.4/CR 720) — a creature you own
- `crates/engine/src/triggers.rs:1182` — (CR 109.4 / 603.3d), not its owner — a stolen or reanimated permanent
- `crates/engine/tests/game.rs:98296` — control." CR 109.4/CR 720 — "you control" is a control test, not an ownership test, so a

### CR 110.1

- `crates/engine/src/triggers.rs:4201` — How many permanents `player` controls (CR 110.1 — every permanent type, tokens included;

### CR 111

- `crates/engine/src/triggers.rs:606` — A dying token fires its Dies trigger before vanishing; its arena slot is (CR 603.6, CR 111, CR 603)
- `crates/engine/src/triggers.rs:2872` — single `amount`. (CR 510, CR 111, CR 108.3)
- `crates/engine/src/triggers.rs:4239` — A source that has already left the game entirely (a token's own Dies trigger, fired (CR 603.6, CR 1…
- `crates/engine/src/types/card.rs:1796` — A token (CR 111): created directly on the battlefield, not from a card. When it
- `crates/engine/src/types/trigger.rs:509` — field the moment a second card needs a different power/type predicate. (CR 510, CR 111, CR 108.3)
- `crates/engine/tests/game.rs:4122` — Curiosity Crafter (soc): "Whenever a creature token you control deals combat damage to a (CR 510, C…
- `crates/engine/tests/game.rs:9171` — Shock the 1/1 token: lethal, so the SBA moves it off the battlefield. (CR 704, CR 111, CR 403.5)
- `crates/engine/tests/game.rs:14035` — green Fungus Beast creature token with trample." Loaded from the real TOML pool (not a (CR 702, CR …
- `crates/engine/tests/game.rs:70178` — mid-P0's-turn (via a priority hand-off) without needing a full turn rotation. (CR 117, CR 111, CR 1…
- `crates/engine/tests/game.rs:73721` — deal combat damage to a player, create a 0/0 green and blue Fractal creature token. Put a (CR 510, …

### CR 111.1

- `crates/engine/src/apply.rs:391` — (X = the exiled card's mana value, CR 111.1) and drop the link so it fires exactly once —
- `crates/engine/src/resolution/tokens.rs:35` — Mint sequential ids matching the order `apply` will push them (CR 111.1).

### CR 111.4

- `crates/engine/src/query.rs:815` — Every living player, as a `TargetSpec::Player` legal-target list (CR 111.4 — any player,
- `crates/engine/src/resolution/tokens.rs:52` — Calligrapher's tapped-and-attacking rider overrides all of that (CR 111.4): the
- `crates/engine/src/types/card.rs:590` — Explicit colors (CR 105.2a: a color indicator, or CR 111.4's "colors are determined by
- `crates/engine/src/types/card.rs:1204` — overrides ride along for a target that states its color outright (a token, CR 111.4).
- `crates/engine/src/types/filter.rs:891` — Who controls a token minted by [`Effect::Token(TokenEffect::Create)`] (CR 111.4's "under its contro…
- `crates/engine/src/types/filter.rs:915` — opponent under *each opponent*. CR 111.4.
- `crates/engine/src/types/filter.rs:918` — creates a 2/1 ... Inkling ... token" — CR 111.4). Makes [`Effect::target`](super::Effect::target)
- `crates/engine/src/types/filter.rs:924` — Questing Phelddagrif's "Target opponent creates a 1/1 ... Hippo ... token", CR 111.4). The
- `crates/engine/src/types/filter.rs:931` — target player creates a 5/5 red Dragon creature token with flying." — CR 111.4). Same
- `crates/engine/src/types/stack.rs:3420` — (Prismari Command's "target player draws two cards, then discards two cards" — CR 111.4's
- `crates/engine/tests/game.rs:8896` — opponents (CR 111.4).
- `crates/engine/tests/game.rs:44984` — Mode 1: "Target player draws two cards, then discards two cards" (CR 111.4/601.2c) — the
- `crates/engine/tests/game.rs:45033` — Mode 2: "Target player creates a Treasure token" (CR 111.4) — the Treasure enters under
- `crates/engine/tests/game.rs:70656` — controlled that were exiled this way." (CR 111.4, 122.1) P0 controls a 3/3 and a 2/2
- `crates/engine/tests/game.rs:70694` — P2 and P3 controlled no exiled creatures — each still creates a 0/0 Fractal (CR 111.4),
- `crates/engine/tests/game.rs:102799` — token creature, whose color is stated outright because it has no mana cost (CR 111.4), is

### CR 111.7

- `crates/engine/src/apply.rs:130` — A dying token ceases to exist rather than becoming a graveyard card (CR 111.7).
- `crates/engine/src/apply.rs:183` — CR 111.7: a token Aura (a Replicate copy, CR 707.10a) that falls off ceases to
- `crates/engine/src/cast.rs:2497` — hand (CR 111.7) — same branch `Effect::Zone(ZoneEffect::ReturnToHand)` takes for a targeted bounce.
- `crates/engine/src/cast.rs:2515` — artifact"). A token ceases to exist instead of reaching exile (CR 111.7) — the same
- `crates/engine/src/core.rs:410` — CR 111.7 / 603.10a last-known information: a ceased token's stack ability still
- `crates/engine/src/effects.rs:195` — CR 707.10a/111.7: a copy that fails to resolve never becomes a card — it
- `crates/engine/src/resolution/frame.rs:63` — CR 111.7) — written unconditionally whenever that event applies (`apply.rs`), read by
- `crates/engine/src/resolution/zones.rs:42` — CR 111.7: a token that leaves the battlefield ceases to exist rather than
- `crates/engine/src/resolution/zones.rs:110` — (CR 111.7) — it never reaches the hand.
- `crates/engine/src/resolution/zones.rs:366` — CR 111.7: a token can't exist in a library — it ceases to exist instead.
- `crates/engine/src/resolution/zones.rs:404` — CR 111.7: a token can't exist in a library — it ceases to exist instead.
- `crates/engine/src/resolution/zones.rs:485` — changing zones (CR 111.7), mirroring `ReturnToHand`'s own token branch — no pool
- `crates/engine/src/resolution/zones.rs:545` — CR 111.7: a token can't exist in a library — it ceases to exist instead, with
- `crates/engine/src/resolution/zones.rs:581` — Shuffle a target permanent into its owner's library (CR 111.7: a token ceases to exist
- `crates/engine/src/types/card.rs:2001` — The object left the game — a token that ceased to exist (CR 111.7), a spell copy that
- `crates/engine/src/types/effect/shared.rs:1468` — (a token ceases to exist instead, CR 111.7); the source is always payable since an
- `crates/engine/src/types/effect/shared.rs:1488` — instead (CR 111.7) — the same fork [`return_self`](Self::return_self) takes. Like
- `crates/engine/src/types/stack.rs:2112` — A spell *copy* finished resolving and ceased to exist (CR 707.10a / CR 111.7) — it leaves
- `crates/engine/src/types/stack.rs:2760` — A token left the battlefield and ceased to exist (CR 111.7) — a state-based action.
- `crates/engine/tests/game.rs:8609` — Chaos Warp targeting a token (CR 111.7): a token can't exist in a library, so it ceases to
- `crates/engine/tests/game.rs:8731` — A token tucked by this primitive ceases to exist instead of entering a library (CR 111.7) —
- `crates/engine/tests/game.rs:8790` — Oblation targeting a token (CR 111.7): a token is a legal "nonland permanent" target, but it
- `crates/engine/tests/game.rs:9200` — "a dead token ceases to exist — it never lingers in the graveyard (CR 111.7)"
- `crates/engine/tests/game.rs:9414` — resolve_top_of_stack(&mut game); // Shock resolves → SBA kills the Pest, which ceases to exist. (CR…
- `crates/engine/tests/game.rs:9481` — "a dead token copy ceases to exist — it never lingers in the graveyard (CR 111.7)"
- `crates/engine/tests/game.rs:32336` — leaves the battlefield ceases to exist (CR 111.7) rather than falling into the graveyard —
- `crates/engine/tests/game.rs:35295` — "a bounced token doesn't reach the hand — it ceases to exist (CR 111.7)"
- `crates/engine/tests/game.rs:54643` — exist rather than reaching hand (CR 111.7); a nontoken creature is untouched.
- `crates/engine/tests/game.rs:54890` — A sacrificed token hits the graveyard as an SBA and then ceases to exist (CR 111.7), so it
- `crates/engine/tests/game.rs:55143` — A token among the exiled ceases to exist rather than reaching exile (CR 111.7).
- `crates/engine/tests/game.rs:55168` — "the exiled token ceased to exist rather than landing in exile (CR 111.7)",
- `crates/engine/tests/game.rs:55641` — (CR 111.7), never lingering as a graveyard card.
- `crates/engine/tests/game.rs:56447` — A token that leaves the battlefield ceases to exist (CR 111.7) rather than actually
- `crates/engine/tests/game.rs:73883` — A sacrificed token hits the graveyard as an SBA and then ceases to exist (CR 111.7), so

### CR 112.6

- `crates/engine/src/cast.rs:2085` — permanent with that activated ability, or (CR 112.6/603.6e) owns a `functions_in_graveyard`
- `crates/engine/src/cast.rs:2099` — CR 112.6/603.6e: a card whose def is flagged `functions_in_graveyard` activates its
- `crates/engine/src/query.rs:99` — CR 112.6/603.6e: a `functions_in_graveyard` card's activated ability
- `crates/engine/src/resolution/zones.rs:143` — from the graveyard directly (CR 112.6) with `tapped = true`.
- `crates/engine/tests/game.rs:22902` — tapped." An activated ability functioning from the graveyard (CR 112.6/603.6e).
- `crates/engine/tests/game.rs:22942` — Same {{B}}{{G}} ability, guarded to the graveyard (CR 112.6/603.6e): a live Teacher's Pest

### CR 112.7a

- `crates/engine/src/apply.rs:1036` — CR 701.5c/112.7a: a countered activated ability ceases to exist — remove the
- `crates/engine/src/query.rs:1092` — ability", CR 112.7a). Any controller's — Azorius counters an opponent's just as
- `crates/engine/src/resolution/misc.rs:78` — Counter target activated ability (CR 701.5c/112.7a — Azorius Guildmage). The target
- `crates/engine/src/triggers.rs:4929` — ability; `activated` marks which (CR 112.7a — an activated donation is still counterable as
- `crates/engine/src/types/filter.rs:94` — activated ability", CR 112.7a). Targets the ability's stack item by its `source` id, not a

### CR 113

- `crates/engine/src/amount.rs:144` — `CastSpell`-triggered ability's effect is contextualized at placement), so a live read (CR 603, CR …
- `crates/engine/src/apply.rs:2227` — Quintorius's activated ability: grant the free-cast permission for the chosen (CR 602, CR 601, CR 1…
- `crates/engine/src/cast.rs:2228` — ability): an ordinary activated ability restricted to a legal sorcery-speed moment. (CR 602, CR 113)
- `crates/engine/src/cast.rs:2274` — An activated ability carries no {X} (mirrors `run`'s "abilities (CR 602, CR 113)
- `crates/engine/src/cast.rs:2535` — mana ability, resolving instantly below, so the placement is indistinguishable. (CR 605, CR 113)
- `crates/engine/src/cast.rs:2551` — exempts a mana ability from the stack, not from choices made while it resolves, so (CR 605, CR 405,…
- `crates/engine/src/cast.rs:2575` — Mana abilities resolve immediately — no stack, no priority change. Goes through (CR 117, CR 405, CR…
- `crates/engine/src/cast.rs:2576` — `Game::run` so a composite mana ability (CR 605, CR 113) (Brass Infiniscope's
- `crates/engine/src/characteristics.rs:596` — color-set union and still only needs one qualifying land to add a color (CR 605, CR 108.3, CR 113).
- `crates/engine/src/priority.rs:148` — land's *non*-mana ability (which finds none, and rejects below). Delegate so the one (CR 605, CR 11…
- `crates/engine/src/priority.rs:422` — (CR 605, CR 108.3, CR 113)
- `crates/engine/src/query.rs:156` — A non-mana activated ability the player can afford, or a prepared back-face cast. (CR 602, CR 601, …
- `crates/engine/src/query.rs:556` — An activated ability granted by an Aura attached to `source` (Fallen Ideal's "Sacrifice (CR 602, CR…
- `crates/engine/src/resolution/mana.rs:22` — `single_color` is handled by `Game::activate_ability` before a mana ability ever (CR 605, CR 113)
- `crates/engine/src/resolution/mill.rs:77` — permission (the free-cast permission comes later, from the activated ability). (CR 602, CR 601, CR …
- `crates/engine/src/resolution/pause_exile_cast.rs:28` — Quintorius's activated ability pauses on a card-pick choice over this source's (CR 602, CR 113)
- `crates/engine/src/resolution/tokens.rs:26` — this arm only reaches direct `execute_effect` callers (a mana ability, a (CR 605, CR 113)
- `crates/engine/src/triggers.rs:690` — ponytail: the ability resolves at the next priority window, not synchronously (CR 117, CR 113)
- `crates/engine/src/types/mana.rs:397` — so both stay at their default/`false`). (CR 602, CR 601, CR 113)
- `crates/engine/src/types/mana.rs:484` — pair to an arbitrary WUBRG bitmask, carried per exact mask value in `of_colors`. (CR 605, CR 113)
- `crates/engine/src/types/stack.rs:2289` — object id) starts with a clean cap — correct, since a new object is a new game object. (CR 602, CR …
- `crates/engine/tests/game.rs:11793` — The colored mode (ability 1) is a mana ability — it produces immediately, uses no stack and (CR 605…
- `crates/engine/tests/game.rs:16169` — The activated ability's index on a fixture whose only ability is that one activated ability. (CR 60…
- `crates/engine/tests/game.rs:17162` — Sacrifice one Treasure for its own mana ability (a mana ability, no stack — avoids (CR 605, CR 405,…
- `crates/engine/tests/game.rs:19193` — The {W} activated ability is the third ability (etb choose-color, static grant, activated). (CR 602…
- `crates/engine/tests/game.rs:22928` — resolve_top_of_stack(&mut game); // resolve the activated ability off the stack (CR 602, CR 405, CR…
- `crates/engine/tests/game.rs:22944` — flag (Llanowar Elves' mana ability, sitting in the graveyard) can't activate anything either. (CR 6…
- `crates/engine/tests/game.rs:28625` — g.cast(spell).submit(); // keeps priority — the armed watch's ability is now stacked above it (CR 1…
- `crates/engine/tests/game.rs:29996` — (exercises the `granted_attachment_abilities` enumeration in `push_activatable_abilities`). (CR 602…
- `crates/engine/tests/game.rs:30045` — The grant is read live off the attachment scan (like a granted mana ability): while Fallen (CR 605,…
- `crates/engine/tests/game.rs:30123` — +2/+1 until end of turn" activated ability — proves the `granted_ability` field is wired, (CR 602, …
- `crates/engine/tests/game.rs:34609` — mana ability activates. (CR 605, CR 113)
- `crates/engine/tests/game.rs:46804` — Guard: without `sorcery_speed`, an ordinary activated ability keeps working at instant (CR 602, CR …
- `crates/engine/tests/game.rs:53453` — has from the hand, not a permanent's activated ability. (CR 602, CR 402.5, CR 113)
- `crates/engine/tests/game.rs:55628` — {T}, Sacrifice this artifact: add one mana of any color (a mana ability — resolves at once). (CR 60…
- `crates/engine/tests/game.rs:58368` — resolve_top_of_stack(&mut game); // the activated ability resolves, pausing on the choice (CR 602, …
- `crates/engine/tests/game.rs:58428` — resolve_top_of_stack(&mut game); // the activated ability resolves, pausing on the choice (CR 602, …
- `crates/engine/tests/game.rs:58583` — Quintorius, Loremaster's activated ability: "{1}{R}{W}, {T}, Sacrifice a Spirit: Choose (CR 602, CR…
- `crates/engine/tests/game.rs:60609` — ability is index 0). (CR 118.7, CR 113, CR 121)
- `crates/engine/tests/game.rs:62411` — A non-capped activated ability is unaffected by another permanent's once-per-turn cap. (CR 602, CR …
- `crates/engine/tests/game.rs:65456` — Tap `object` for its `[[abilities]]`-authored mana ability (index 0) — Fellwar Stone's shape. (CR 6…
- `crates/engine/tests/game.rs:65679` — no commander, the tap still legally happens (a mana ability never fails to activate) but (CR 605, C…
- `crates/engine/tests/game.rs:70076` — resolve_top_of_stack(&mut g); // the draw ability itself (not a mana ability) (CR 605, CR 113, CR 1…
- `crates/engine/tests/game.rs:90614` — "Sacrifice this token: Add {C}." — a mana ability, resolves at once (CR 605, CR 113).

### CR 113.6

- `crates/engine/src/cast.rs:1261` — Activate a hand card's [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e — a
- `crates/engine/src/cast.rs:1345` — CR 113.6/602: this is an activated ability — its authored payload goes on the stack (a
- `crates/engine/src/de.rs:490` — A hand-activated, discard-this-card ability (CR 113.6/602.5e, Magma Opus) — zero or
- `crates/engine/src/types/card.rs:687` — A hand-activated, discard-this-card ability (CR 113.6/602.5e — an activated ability that
- `crates/engine/src/types/card.rs:985` — A hand-activated, discard-this-card ability (CR 113.6/602.5e), carried by
- `crates/engine/src/types/stack.rs:120` — Activate a hand card's [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e — a
- `crates/engine/src/types/stack.rs:3103` — Activate `card`'s [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e): pay its
- `crates/engine/tests/game.rs:53737` — CR 113.6/602.5e — "{U/R}{U/R}, Discard this card: Create a Treasure token." functions
- `crates/engine/tests/game.rs:53755` — CR 113.6/602: a hand-activated ability resolves off the stack, not inline.
- `crates/engine/tests/game.rs:53802` — "the ability functions only from hand (CR 113.6)",
- `crates/engine/tests/game.rs:82756` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.
- `crates/engine/tests/game.rs:82805` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.
- `crates/engine/tests/game.rs:82846` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.

### CR 113.6a

- `crates/engine/src/query.rs:1153` — on the battlefield (CR 113.6a), so e.g. a pro-black creature card in a graveyard is
- `crates/engine/tests/game.rs:18402` — A keyword ability only functions on the battlefield (CR 113.6a): an opponent's

### CR 114

- `crates/engine/src/query.rs:1343` — Ids of every emblem `player` owns (CR 114). An emblem is an ownerless-but-controlled,
- `crates/engine/tests/game.rs:102252` — ── Garruk, Cursed Huntsman's emblem (#13b, CR 114) ─────────────────────────────────────────

### CR 114.1

- `crates/engine/src/apply.rs:1768` — CR 114.1/114.3: the emblem is created in its owner's command zone with only the
- `crates/engine/src/query.rs:1344` — unremovable, non-permanent object that exists only in the command zone (CR 114.1) and has
- `crates/engine/src/resolution/misc.rs:25` — Garruk, Cursed Huntsman's −6: "You get an emblem with …" (CR 114.1) — the emblem is
- `crates/engine/src/types/effect/misc.rs:42` — "You get an emblem with …" (CR 114.1, Garruk, Cursed Huntsman's −6): create the emblem
- `crates/engine/src/types/stack.rs:2698` — "You get an emblem with …" (CR 114.1): `controller` got the emblem object `emblem`, whose
- `crates/engine/tests/game.rs:102273` — have trample.\"" — CR 114.1: the emblem is created in the command zone under its owner's
- `crates/engine/tests/game.rs:102286` — "CR 114.1: an emblem is in the command zone"

### CR 114.2

- `crates/engine/src/types/stack.rs:2699` — `def` carries its abilities and nothing else. Public — CR 114.2, an emblem is visible to

### CR 114.3

- `crates/engine/src/apply.rs:1768` — CR 114.1/114.3: the emblem is created in its owner's command zone with only the
- `crates/engine/src/characteristics.rs:1587` — Emblem anthems (CR 114.3 — Garruk, Cursed Huntsman's "Creatures you control get +3/+3
- `crates/engine/src/characteristics_cache.rs:143` — A new emblem's static abilities (CR 114.3 — Garruk, Cursed Huntsman's "Creatures
- `crates/engine/tests/game.rs:102320` — CR 114.3: an emblem's abilities are continuous, not a one-shot pump — a creature that

### CR 114.5

- `crates/engine/src/apply.rs:1773` — emblem is never castable. Nothing ever removes it (CR 114.5).
- `crates/engine/src/query.rs:1345` — no characteristics other than its abilities (CR 114.5), so it is stored as a command-zone
- `crates/engine/src/query.rs:1349` — removes, copies, or targets an emblem (CR 114.5), so there is no counterpart remover.
- `crates/engine/src/types/effect/shared.rs:876` — CR 114.5: an emblem can't be targeted, and getting one targets nothing either.
- `crates/engine/src/types/stack.rs:2700` — everyone; nothing can remove, copy, or target it afterwards (CR 114.5).
- `crates/engine/tests/game.rs:102338` — CR 114.5: nothing can remove an emblem — paying the last of Garruk's loyalty puts him in

### CR 114.6

- `crates/engine/src/cast.rs:783` — legality is evaluated from (the bent/copied spell's own controller — CR 114.6/707.10a);
- `crates/engine/src/core.rs:808` — Whether the spell at `id` currently has exactly one target (CR 114.6's "single target" —
- `crates/engine/src/query.rs:1079` — single target", CR 114.6). Any controller's spell — Willbender bends an opponent's
- `crates/engine/src/resolution/copy.rs:152` — Willbender (CR 114.6 / 702.37f) / Wild Ricochet (CR 114.6a). The bent spell is this
- `crates/engine/src/resolution/copy.rs:182` — targets computed for the SPELL's controller (CR 114.6 — the new target must be
- `crates/engine/src/types/filter.rs:86` — with a single target", CR 114.6). Targets the stack object; used by
- `crates/engine/tests/game.rs:79313` — --- Willbender (CR 114.6 / 702.37f — turned face up, change the target of a single-target spell) ---

### CR 114.6a

- `crates/engine/src/cast.rs:781` — copy's or a bent original's CR 707.10c/114.6a retarget — same shape, just against an
- `crates/engine/src/resolution/copy.rs:152` — Willbender (CR 114.6 / 702.37f) / Wild Ricochet (CR 114.6a). The bent spell is this
- `crates/engine/src/resolution/copy.rs:163` — Wild Ricochet (CR 114.6a): "you may choose new targets for target instant or
- `crates/engine/tests/game.rs:79595` — --- Wild Ricochet (cmr, CR 114.6a / 707.10c — "You may choose new targets for target instant or
- `crates/engine/tests/game.rs:79702` — The retarget pause reaches the whole target set, and — CR 114.6a, unlike Willbender's
- `crates/engine/tests/game.rs:79790` — CR 114.6a: "you may choose new targets" never requires the new target(s) differ from the

### CR 114.6b

- `crates/engine/src/resolution/copy.rs:183` — legal for *that* spell), minus its current target (CR 114.6b — the target must
- `crates/engine/src/resolution/copy.rs:204` — CR 114.6b: no legal alternate — the target is left unchanged (no pause).
- `crates/engine/tests/game.rs:79447` — "CR 114.6b: the current target isn't offered — the target must change"
- `crates/engine/tests/game.rs:79473` — CR 114.6b: if the only legal target is the one the spell already has, the target is left
- `crates/engine/tests/game.rs:79506` — "the target is left unchanged (CR 114.6b)"

### CR 115

- `crates/engine/src/query.rs:1138` — them (CR 115, 702.11/702.16b/702.18). A hexproof Hangarback Walker still pumps itself.
- `crates/engine/src/types/filter.rs:123` — only applies to true targets (CR 115, 702.11/702.16b/702.18).
- `crates/engine/tests/game.rs:51398` — self-target-spec: "this creature" is a fixed reference, not a real target (CR 115) —
- `crates/engine/tests/game.rs:51464` — counter on Gorma." #72 self-target-spec (CR 121, CR 115): the counter always lands on
- `crates/engine/tests/game.rs:82874` — targeted ability (`target = "this"`, CR 115).

### CR 115.4

- `crates/engine/src/query.rs:1195` — The seats still in the game (a spell can only target a living player, CR 115.4).
- `crates/engine/src/types/filter.rs:46` — "Any target": a creature, a player, or a planeswalker (modern wording, CR 115.4).
- `crates/engine/tests/game.rs:50849` — its target spec is `"any"` (creature/player/planeswalker, CR 115.4).

### CR 117

- `crates/engine/src/cast.rs:634` — Casting is an action: it resets the pass count and the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1707` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1804` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1900` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1986` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:2034` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:2575` — Mana abilities resolve immediately — no stack, no priority change. Goes through (CR 117, CR 405, CR…
- `crates/engine/src/core.rs:114` — Untap has no priority window, so this rolls straight on to the upkeep and stops there. (CR 117, CR …
- `crates/engine/src/lib.rs:2` — Primary: CR 117 (priority), CR 405 (stack), CR 903 (Commander).
- `crates/engine/src/lib.rs:529` — diffing — revisit only if profiling ever shows this hot. (CR 117, CR 601, CR 500)
- `crates/engine/src/pending/handlers/combat.rs:44` — Chain to the next multi-blocked attacker's division, or hand back priority. (CR 117, CR 402.5, CR 5…
- `crates/engine/src/pending/raise/library.rs:69` — any excess slot (CR 117, CR 406.5, CR 402.5) is silently dropped (CR 120.3-style "as
- `crates/engine/src/priority.rs:1431` — by `submit`.) Eliminated seats never hold priority, so they don't count. (CR 117)
- `crates/engine/src/priority.rs:1726` — priority window opens between them. (CR 117)
- `crates/engine/src/query.rs:66` — step) still has no affordance; add "hold priority" if it's ever wanted. (CR 702.8, CR 117, CR 301.5)
- `crates/engine/src/triggers.rs:688` — going on the stack at the next priority window (the following upkeep) rather (CR 117, CR 405, CR 50…
- `crates/engine/src/triggers.rs:690` — ponytail: the ability resolves at the next priority window, not synchronously (CR 117, CR 113)
- `crates/engine/src/types/mana.rs:913` — ponytail: candidates are tried in a fixed priority order rather than an exhaustive (CR 117)
- `crates/engine/tests/game.rs:46` — an all-pass loop. (CR 701.38, CR 117)
- `crates/engine/tests/game.rs:75` — Everyone passes priority until the active player changes (one whole turn elapses). (CR 117, CR 500)
- `crates/engine/tests/game.rs:394` — In Main1 priority starts with the active player and rotates around the table; (CR 117)
- `crates/engine/tests/game.rs:2856` — pass priority → resolve". `TestGame` folds that into a small fluent surface. It derefs to (CR 117)
- `crates/engine/tests/game.rs:3607` — resolve_top_of_stack(&mut game); // P0.command_casts == 1; priority returns to active player P0. (C…
- `crates/engine/tests/game.rs:3609` — Hand P1 priority (still P0's turn, empty stack — one pass just hands off, doesn't end the step). (C…
- `crates/engine/tests/game.rs:3641` — resolve_top_of_stack(&mut game); // priority returns to active player P0. (CR 117)
- `crates/engine/tests/game.rs:3916` — and players get priority in it before the regular combat damage step. (CR 510, CR 117, CR 120.3)
- `crates/engine/tests/game.rs:3953` — death triggers resolve inside the first-strike step's priority window, before the regular (CR 117, …
- `crates/engine/tests/game.rs:11794` — holds no priority round — and its self-damage rider is *not* a cost (it never gates the tap). (CR 1…
- `crates/engine/tests/game.rs:12441` — Concede is legal without priority — you may quit on someone else's turn. (CR 117, CR 500, CR 104.3a)
- `crates/engine/tests/game.rs:14689` — Resolve a creature onto the battlefield and hand priority back to player 0. (CR 117, CR 402.5, CR 4…
- `crates/engine/tests/game.rs:18186` — Once P0 passes, priority reaches P1 and the same cast is legal. (CR 117, CR 601)
- `crates/engine/tests/game.rs:18333` — it (with mana to spare) is not worth stopping priority for. (CR 117)
- `crates/engine/tests/game.rs:22206` — Pass priority (all four seats) until the whole chain resolves: gainer enters → its ETB (CR 117)
- `crates/engine/tests/game.rs:23841` — P0 (active player) holds priority first; pass it so P1 can cast in the reaction window. (CR 117, CR…
- `crates/engine/tests/game.rs:23932` — still P0's turn, so P1 needs priority via a pass, and P1's spell must be castable then.) (CR 117, C…
- `crates/engine/tests/game.rs:24706` — .unwrap(); // P1 passes so P0 (the instant's caster) gets priority. (CR 117)
- `crates/engine/tests/game.rs:24736` — immediately (before either player gets priority), so its target choice pauses the game (CR 117, CR …
- `crates/engine/tests/game.rs:24737` — right away — the caster picks a target before `resolve_top_of_stack`'s priority passes. (CR 117, CR…
- `crates/engine/tests/game.rs:25161` — .unwrap(); // P1 passes so P0 (the instant's caster) gets priority. (CR 117)
- `crates/engine/tests/game.rs:25188` — Resolve the whole stack (each item in turn) by passing priority. (CR 117, CR 405, CR 500)
- `crates/engine/tests/game.rs:25446` — P0 passes so P1 gets priority to respond with Twincast. (CR 117)
- `crates/engine/tests/game.rs:25879` — P1 casts both counters at the same bear (casting keeps priority, so no pass between them). (CR 117,…
- `crates/engine/tests/game.rs:28308` — .unwrap(); // P0 passes so P1 gets priority to cast at instant speed. (CR 117, CR 601)
- `crates/engine/tests/game.rs:28371` — Roll forward until P0 holds priority again during P1's turn — the exact moment doesn't (CR 117, CR …
- `crates/engine/tests/game.rs:28625` — g.cast(spell).submit(); // keeps priority — the armed watch's ability is now stacked above it (CR 1…
- `crates/engine/tests/game.rs:28631` — .unwrap(); // hand priority to P1 to respond (CR 117, CR 402.5)
- `crates/engine/tests/game.rs:30300` — placement, before priority can pass) — decline it here since this test only cares about (CR 117)
- `crates/engine/tests/game.rs:30380` — before priority can pass. Accepting draws a card. (CR 117)
- `crates/engine/tests/game.rs:30596` — Respond with a lethal bolt at the would-be host (instant speed, caster keeps priority). (CR 117)
- `crates/engine/tests/game.rs:30901` — Resolve Scriv (multi-player: pass priority all the way around), landing on its ETB trigger's (CR 11…
- `crates/engine/tests/game.rs:30973` — Resolve the enchanted-creature-attacks trigger (3-player: pass priority all the way around). (CR 11…
- `crates/engine/tests/game.rs:33033` — generalization of [`resolve_top_of_stack`] (which hardcodes two passes for a 2-player game). (CR 11…
- `crates/engine/tests/game.rs:33700` — Resolve the spell — every player must pass priority (a 3-player table, not just two passes). (CR 11…
- `crates/engine/tests/game.rs:33907` — vote round begins (the first `CastVote` pause). (CR 117, CR 601)
- `crates/engine/tests/game.rs:46366` — .unwrap(); // CR 117 — P1 passes so P0 gets priority.
- `crates/engine/tests/game.rs:47039` — automatically as priority passes. (CR 117)
- `crates/engine/tests/game.rs:59020` — leaves the spell on the stack (P0 keeps priority) so the next cast stacks on top, then the (CR 117,…
- `crates/engine/tests/game.rs:59228` — can't be used here because it `unwrap`s each pass and a pause makes further passes illegal. (CR 117…
- `crates/engine/tests/game.rs:59340` — The turn actually rolled on: player 1 is now active, paused at their upkeep's priority (CR 117, CR …
- `crates/engine/tests/game.rs:61277` — ability — it keeps priority in Main1 — so it triggers a refresh without changing the phase. (CR 117…
- `crates/engine/tests/game.rs:61600` — Put a spell on the stack and pass so P1 holds priority. (CR 117, CR 405, CR 601)
- `crates/engine/tests/game.rs:61645` — After P0 casts and passes, P1 holds priority with one consecutive pass. Cycling must reset (CR 702.…
- `crates/engine/tests/game.rs:61647` — getting priority again. (CR 117)
- `crates/engine/tests/game.rs:61697` — One pass from the cycler must hand priority back — not resolve the stack. (CR 117, CR 402.5, CR 405)
- `crates/engine/tests/game.rs:70178` — mid-P0's-turn (via a priority hand-off) without needing a full turn rotation. (CR 117, CR 111, CR 1…
- `crates/engine/tests/game.rs:70195` — Hand priority to P1 (still P0's turn, empty stack) so P1 can cast the instant. (CR 117, CR 402.5, C…
- `crates/engine/tests/game.rs:70613` — .unwrap(); // P1 passes so P0 (the caster below) gets priority. (CR 117)
- `crates/engine/tests/game.rs:70631` — Priority reverts to the active player (P1) once the stack empties — hand it back to P0. (CR 117, CR…
- `crates/engine/tests/game.rs:73675` — twice (Primo itself, and the `ZERO_POWER_WITH_COUNTER` fixture). (CR 117, CR 402.5, CR 405)
- `crates/engine/tests/game.rs:76625` — turn"). Casting still requires priority, so roll to P0's main phase next turn. (CR 117, CR 601, CR …
- `crates/engine/tests/game.rs:102102` — All four seats must pass in succession (CR 117) for a 4-player game's spell to resolve —

### CR 117.1a

- `crates/engine/src/cast.rs:1612` — Casting requires priority (CR 117.1a).
- `crates/engine/src/cast.rs:1727` — Casting requires priority (CR 117.1a).
- `crates/engine/src/cast.rs:1823` — Casting requires priority (CR 117.1a).
- `crates/engine/src/cast.rs:1918` — Casting requires priority (CR 117.1a).
- `crates/engine/src/cast.rs:2007` — Casting requires priority (CR 117.1a).
- `crates/engine/src/playable.rs:322` — priority (CR 117.1a). The post-attack declare-attackers window is a reaction window for
- `crates/engine/tests/game.rs:6828` — requires priority (CR 117.1a), so roll forward until P1 has passed and P0 holds it.
- `crates/engine/tests/game.rs:18134` — CR 117.1a: a player may cast a spell only when they have priority. The reaction window

### CR 117.1b

- `crates/engine/src/cast.rs:1187` — Cycling is an activated ability (CR 702.29) — requires priority (CR 117.1b).
- `crates/engine/src/cast.rs:1280` — (CR 117.1b).
- `crates/engine/src/cast.rs:1435` — Encore is an activated ability (CR 702.140) — requires priority (CR 117.1b).
- `crates/engine/tests/game.rs:61634` — "cycling without priority is rejected (CR 117.1b)",

### CR 117.3

- `crates/engine/tests/game.rs:18058` — The reaction window (CR 117.3): once a spell is on the stack, a player who could cast an

### CR 117.3a

- `crates/engine/tests/game.rs:101479` — counters, they mill that many cards." A turn-based action (CR 117.3a) — it uses no stack

### CR 117.3c

- `crates/engine/src/cast.rs:1254` — An action resets the pass count; the cycler keeps priority (CR 117.3c) — overriding the
- `crates/engine/src/cast.rs:1359` — An action resets the pass count; the activator keeps priority (CR 117.3c) — overriding
- `crates/engine/src/cast.rs:1405` — An action resets the pass count; the player keeps priority (CR 117.3c).
- `crates/engine/src/cast.rs:1517` — A special action resets the pass count; the player keeps priority (CR 117.3c).
- `crates/engine/src/cast.rs:1564` — A special action resets the pass count; the player keeps priority (CR 117.3c).
- `crates/engine/tests/game.rs:18161` — After casting, the caster retains priority (CR 117.3c) — P1 must wait for it.
- `crates/engine/tests/game.rs:61646` — that count (CR 117.3c) — otherwise P1's next pass would resolve the stack without P0

### CR 117.4

- `crates/engine/tests/game.rs:84534` — resolves once *every* seat has passed in succession (CR 405.5, CR 117.4).

### CR 118

- `crates/engine/src/cast.rs:3` — Primary: CR 601 (casting spells), CR 602 (activating abilities), CR 118 (costs / payments).
- `crates/engine/src/cast.rs:2197` — A remove-a-counter cost (CR 118 — Steelbane Hydra's "Remove a +1/+1 counter from this
- `crates/engine/src/de.rs:1891` — +1/+1 counters removed from the source as part of the activation cost (CR 118
- `crates/engine/src/types/effect/shared.rs:1423` — +1/+1 counters removed from the ability's own source as part of the cost (CR 118 "remove
- `crates/engine/src/types/effect/shared.rs:1466` — "Return this to its owner's hand" as part of the cost (CR 118 — Rootha, Mercurial
- `crates/engine/src/types/effect/shared.rs:1472` — "Mill a card" as part of the cost (CR 701.13/118 — Millikin's "{T}, Mill a card: Add
- `crates/engine/src/types/effect/shared.rs:1477` — "Discard a card" as part of the cost (CR 602.2b/118 — Wild Mongrel's "Discard a card:
- `crates/engine/src/types/effect/shared.rs:1485` — "Exile this artifact"/"exile this permanent" as part of the cost (CR 118 — Perpetual

### CR 118.4

- `crates/engine/src/resolution/misc.rs:66` — Yavimaya Bloomsage's Channel back face: "Until end of turn, any time you could (CR 605, CR 118.4)
- `crates/engine/src/types/effect/shared.rs:1417` — Life paid as part of the cost (CR 118.4 — fetchlands' "Pay 1 life", War Room's "pay life

### CR 118.5

- `crates/engine/src/apply.rs:2240` — Cleanup: every free-cast permission expires at once (CR 118.5's "this turn" — no
- `crates/engine/src/cast.rs:214` — Quintorius, Loremaster's free-cast permission (CR 118.5 "without paying its mana
- `crates/engine/src/cast.rs:218` — Intet, the Dreamer's grant is the same CR 118.5 "without paying its mana cost", just with
- `crates/engine/src/cast.rs:226` — A printed conditional free-cast permission (CR 118.5, `CardDef::free_cast_if` —
- `crates/engine/src/combat.rs:193` — CR 118.5). Deliberately *not* folded into
- `crates/engine/src/combat.rs:210` — Whether `player` has an active free-cast permission (CR 118.5, "without paying its mana
- `crates/engine/src/de.rs:369` — A printed conditional free-cast permission (CR 118.5) — `free_cast_if = { .. }`
- `crates/engine/src/pending/handlers/dig.rs:53` — (CR 118.5, "without paying its mana cost") for the chosen card, or decline
- `crates/engine/src/pending/handlers/dig.rs:146` — (CR 118.5) for the chosen card — it stays in exile — or decline (`choice = None`). Either
- `crates/engine/src/pending/handlers/dig.rs:321` — Offer `controller` up to `count` free casts (CR 118.5) over the castable (nonland) cards in
- `crates/engine/src/pending/handlers/dig.rs:471` — caster may cast any number of the exiled (nonland) cards for free (CR 118.5) — raising
- `crates/engine/src/priority.rs:1799` — Quintorius's free-cast permission lasts only until end of turn (CR 118.5),
- `crates/engine/src/resolution/life.rs:173` — among all players (CR 118.5 — a set is a gain/loss of the difference). A player
- `crates/engine/src/resolution/resolve_misc.rs:243` — the free-cast permission (CR 118.5) for it — the same `CastFromExileFreePermissionGranted`
- `crates/engine/src/state.rs:132` — Intet remains on the battlefield" (CR 118.5 plus a live, source-scoped duration): each entry
- `crates/engine/src/state.rs:140` — Free-cast-from-exile (CR 118.5, "without paying its mana cost") — each entry is `(an
- `crates/engine/src/state.rs:159` — same CR 118.5 "this turn" expiry, no separate lifetime.
- `crates/engine/src/types/card.rs:621` — board-state [`Condition`] checked fresh at cast time (CR 118.5 — Massacre: "If an
- `crates/engine/src/types/stack.rs:1478` — grant the free-cast permission (CR 118.5), or decline ([`Effect::Dig(DigEffect::CastExiledWithThisF…
- `crates/engine/src/types/stack.rs:1488` — (CR 118.5), or decline (Herald of Amity's "exile the top eight … you may cast an Aura
- `crates/engine/src/types/stack.rs:1585` — cards among the exile pile `exiled` — to grant the free-cast permission (CR 118.5). Answered
- `crates/engine/src/types/stack.rs:2582` — long as that permanent remains on the battlefield (CR 118.5), a permission with no cleanup
- `crates/engine/src/types/stack.rs:2593` — Herald of Amity's dig (CR 118.5 / 701.17): the top library card `from` was exiled — face-up
- `crates/engine/src/types/stack.rs:2840` — (a card in its exiled-with pile) this turn without paying its mana cost (CR 118.5),
- `crates/engine/src/types/stack.rs:2854` — Every active free-cast-from-exile permission expired (cleanup, CR 118.5 "this turn").
- `crates/engine/tests/game.rs:23566` — highest life total among all players." CR 118.5 models the set as a gain/loss of the
- `crates/engine/tests/game.rs:23629` — "P0's own watcher never fires — a zero delta isn't a life change (CR 118.5)"
- `crates/engine/tests/game.rs:58495` — ── Free-cast-from-exile (CR 118.5) — Quintorius, Loremaster ────────────────────────
- `crates/engine/tests/game.rs:58652` — "no mana was spent — the cast was free (CR 118.5)"
- `crates/engine/tests/game.rs:58715` — its owner's library instead." (CR 118.5 free cast, CR 614.6 replacement)
- `crates/engine/tests/game.rs:58869` — "If an opponent controls a Plains and you control a Swamp, you may cast this spell (CR 118.5)
- `crates/engine/tests/game.rs:58901` — "no mana was spent — the cast was free (CR 118.5)"
- `crates/engine/tests/game.rs:76382` — (CR 118.5) every peer free-cast card uses.

### CR 118.6

- `crates/engine/src/cast.rs:4` — Also: alternative costs (CR 702.34 flashback, CR 702.19 escape), impulse play (CR 118.6).
- `crates/engine/src/cast.rs:13` — permission (CR 118.6). Shared by [`Game::cast`], [`Game::play_land`], and
- `crates/engine/src/combat.rs:175` — (CR 118.6). The permission gates casting/playing from exile; timing is checked separately.
- `crates/engine/src/priority.rs:1788` — Impulse-draw permissions last only until end of turn (CR 118.6) — an `extended`
- `crates/engine/src/query.rs:96` — player's commander), or impulse-exiled with permission (CR 118.6).
- `crates/engine/src/state.rs:122` — Impulse draw (CR 118.6): each entry is `(an exiled card, the player who may play it,
- `crates/engine/src/types/stack.rs:2576` — Impulse draw (CR 118.6): the top library card `from` was exiled face-up as the card `card`,

### CR 118.7

- `crates/engine/src/types/effect/shared.rs:1449` — source in the pool cares about the damage/life-loss distinction on these riders. (CR 605, CR 120.3,…
- `crates/engine/tests/game.rs:857` — Deep Analysis's flashback: {1}{U}, Pay 3 life. (CR 702.34, CR 118.7)
- `crates/engine/tests/game.rs:3891` — H2 repro: a first-strike 2/2 attacks a player at 2 life. The between-substeps SBA sweep (CR 704, CR…
- `crates/engine/tests/game.rs:9420` — resolve_top_of_stack(&mut game); // the Pest's Dies trigger resolves: gain 1 life. (CR 603.6, CR 11…
- `crates/engine/tests/game.rs:51162` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: gain life, then draw, both = 3. (CR 603…
- `crates/engine/tests/game.rs:60609` — ability is index 0). (CR 118.7, CR 113, CR 121)

### CR 118.9

- `crates/engine/src/apply.rs:562` — Serra Paragon (CR 118.9): a permanent spell cast from the graveyard by neither
- `crates/engine/src/apply.rs:1140` — Serra Paragon (CR 118.9): a land can only be played from the graveyard under its
- `crates/engine/src/apply.rs:1890` — Serra Paragon (CR 118.9): a permanent cast from the graveyard this way carries
- `crates/engine/src/apply.rs:2051` — Serra Paragon's granted rider (CR 118.9): last-known information for the
- `crates/engine/src/apply.rs:2462` — A life *loss* (CR 118.9/119.3 — a decrease only, not a gain) — feeds
- `crates/engine/src/cast.rs:26` — cast-from-graveyard alternative cost for a permanent (CR 118.9, Raffine's
- `crates/engine/src/cast.rs:28` — Serra Paragon (CR 118.9) also lets its controller play a land / cast a permanent
- `crates/engine/src/cast.rs:45` — Whether Serra Paragon's permission (CR 118.9) lets `player` play/cast `def` from their
- `crates/engine/src/cast.rs:141` — from there, static cost reducers (CR 118.9 — generic only, floored at 0), the spell's own
- `crates/engine/src/cast.rs:237` — cost for a permanent (CR 118.9, Raffine's Guidance) casts for that alternative cost,
- `crates/engine/src/cast.rs:243` — (CR 702.34, CR 702.19, CR 702.83, CR 118.9)
- `crates/engine/src/cast.rs:1066` — Playable from hand, exile, or (via Serra Paragon, CR 118.9) the graveyard — a land is
- `crates/engine/src/cast.rs:1114` — Validate a [`SacrificeCost`]'s named picks (CR 118.9/602.2b — checked before anything is
- `crates/engine/src/cast.rs:1150` — Pay a validated sacrifice cost's events (CR 118.9): each of `sacrificed` goes to the
- `crates/engine/src/cast.rs:1202` — Resolve the cycling sacrifice cost up front (CR 118.9/602.2b), same choke an ordinary
- `crates/engine/src/cast.rs:2340` — Resolve the sacrifice cost up front (CR 118.9 — a cost, checked before anything is
- `crates/engine/src/characteristics.rs:2009` — (CR 118.9 — a live [`Effect::Static(StaticEffect::PlayFromGraveyardOncePerTurn)`] static ability). …
- `crates/engine/src/characteristics.rs:2054` — they control (CR 118.9 — reduces generic mana only, so the caller floors generic at 0).
- `crates/engine/src/de.rs:443` — Cast-from-graveyard alternative cost for a permanent (CR 118.9) — `[graveyard_cast_cost]`
- `crates/engine/src/query.rs:667` — A sacrifice cost is chosen as it's paid (CR 118.9) and rides the activating intent, so an
- `crates/engine/src/resolution/life.rs:159` — Vandal's Edit: "Each player loses 2 life" — one simultaneous loss (CR 118.9)
- `crates/engine/src/resolution/zones.rs:507` — Serra Paragon's rider (CR 118.9) — see the variant's doc. Guard-return no-op if
- `crates/engine/src/spawn.rs:152` — Serra Paragon's granted rider (CR 118.9 — "When this permanent is put into a graveyard
- `crates/engine/src/state.rs:289` — graveyard from the battlefield this batch — Serra Paragon's granted rider (CR 118.9). A
- `crates/engine/src/triggers.rs:537` — Serra Paragon's granted rider (CR 118.9 — "When this permanent is put into
- `crates/engine/src/triggers.rs:748` — fires only when this is the turn's *first* loss. CR 118.9/119.3.
- `crates/engine/src/types/card.rs:712` — from its owner's graveyard for `cost` (an alternative cost, CR 118.9) via [`Game::cast`];
- `crates/engine/src/types/card.rs:775` — CR 118.9) plus exiling [`EscapeCost::exile`] other graveyard cards as an additional cost
- `crates/engine/src/types/card.rs:787` — Cast-from-graveyard alternative cost for a permanent (CR 118.9, Raffine's Guidance):
- `crates/engine/src/types/card.rs:1614` — Whether this spell was cast from a graveyard under Serra Paragon's permission (CR 118.9 —
- `crates/engine/src/types/card.rs:1899` — (CR 118.9) and so carries the granted rider "when this permanent is put into a graveyard
- `crates/engine/src/types/card.rs:2065` — *negative* amount bumps this (CR 118.9/119.3 — only a decrease is a life loss; gaining
- `crates/engine/src/types/card.rs:2126` — (turn-scoped; reset each turn at untap) — CR 118.9's "once during each of your turns."
- `crates/engine/src/types/effect/shared.rs:1144` — A sacrifice requirement in an ability's activation cost (CR 118.9 — sacrifice as a cost).
- `crates/engine/src/types/mana.rs:32` — A spell's own board-derived generic reduction (CR 601.2f/118.9) — "This spell costs {1}
- `crates/engine/tests/game.rs:957` — ── Alternative cast cost from graveyard, permanent (CR 118.9, Raffine's Guidance) ────
- `crates/engine/tests/game.rs:977` — tap_plains(&mut game, 1); // the alt cost's {W} (CR 118.9): {2}{W}, not the printed {W}
- `crates/engine/tests/game.rs:1694` — The exile-and-gain-2-life rider is a real placed trigger (CR 118.9/603.6): the recurred
- `crates/engine/tests/game.rs:12362` — CR 118.9 — "Sacrifice another creature" excludes the ability's own source. Izoni's outlet
- `crates/engine/tests/game.rs:34465` — CR 118.9/602.2b — "Sacrifice two other creatures" needs exactly two, both distinct from the
- `crates/engine/tests/game.rs:40000` — ── Static cost reduction (CR 118.9) ────────────────────────────────────────────
- `crates/engine/tests/game.rs:46487` — CR 118.9: a cost reduction never reduces a spell's mana cost below {0} for any given
- `crates/engine/tests/game.rs:47727` — the active caster included, in seat order — not each-opponent-then-you (CR 118.9).

### CR 119.3

- `crates/engine/src/apply.rs:2462` — A life *loss* (CR 118.9/119.3 — a decrease only, not a gain) — feeds
- `crates/engine/src/resolution/damage.rs:158` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source
- `crates/engine/src/resolution/damage.rs:385` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source deals.
- `crates/engine/src/resolution/damage.rs:407` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source deals.
- `crates/engine/src/resolution/damage.rs:420` — it, CR 119.3 "is dealt damage").
- `crates/engine/src/resolution/damage.rs:444` — Either form of dealt damage counts (CR 119.3): an infect source's hit lands as -1/-1
- `crates/engine/src/triggers.rs:748` — fires only when this is the turn's *first* loss. CR 118.9/119.3.
- `crates/engine/src/types/card.rs:2065` — *negative* amount bumps this (CR 118.9/119.3 — only a decrease is a life loss; gaining
- `crates/engine/src/types/effect/misc.rs:34` — target creature you don't control" is one-directional — CR 701.12/119.3 fight
- `crates/engine/tests/game.rs:11081` — life at all (CR 119.3/702.15e — lifelink triggers on ANY damage the source deals).

### CR 119.4

- `crates/engine/src/cast.rs:64` — hand cards for a discard rider, and enough life for a pay-life / pay-X-life rider (CR 119.4).
- `crates/engine/src/cast.rs:1084` — affordable (CR 119.4 — life down to and including 0); below the cost the land just
- `crates/engine/src/cast.rs:2088` — needs an untapped, non-sick body; a life cost needs that much life (CR 119.4). Returns
- `crates/engine/src/cast.rs:2193` — much life (CR 119.4).
- `crates/engine/src/priority.rs:286` — payment (CR 119.4).
- `crates/engine/src/triggers.rs:4125` — choice was declined, or never offered — CR 119.4's life floor) or about to be untapped
- `crates/engine/src/types/card.rs:615` — controller's life total is greater than or equal to `life` (CR 119.4 — a player may pay
- `crates/engine/src/types/effect/shared.rs:1419` — player who can't pay this much life can't activate the ability (CR 119.4). Resolved via
- `crates/engine/src/types/mana.rs:210` — life instead (CR 119.4 — capped by the caster's life total).
- `crates/engine/src/types/mana.rs:215` — (CR 119.4) and otherwise pays it alongside the mana cost. TOML `pay_life = 3`.
- `crates/engine/tests/game.rs:926` — A player with too little life can't pay, and nothing is spent (CR 119.4).
- `crates/engine/tests/game.rs:11856` — The self-damage is a rider, not a cost (CR 119.4 gates only life *costs*), so a painland
- `crates/engine/tests/game.rs:64198` — CR 119.4: a player can't pay more life than they have as a cost — choosing X = 3 with
- `crates/engine/tests/game.rs:97502` — CR 119.4: a player may pay life down to and including 0 — at exactly 2 life the choice

### CR 120

- `crates/engine/src/apply.rs:2350` — card. A library with fewer than one card lands it on top (CR 120 "as close
- `crates/engine/tests/game.rs:71793` — ── Reveal-until-a-count (#84, CR 701.30/120): open_the_way ────────────────────────────────

### CR 120.1

- `crates/engine/src/resolution/damage.rs:391` — ability's own controller — the player twin of `DealDamageToSelf` (CR 120.1), routed
- `crates/engine/src/triggers.rs:958` — Noncombat damage dealt to a player (CR 120.1) — the marker
- `crates/engine/src/types/stack.rs:2679` — `source` dealt `amount` *noncombat* damage to `player` (CR 120.1) — a marker distinct
- `crates/engine/tests/game.rs:85129` — you." The self-damage rider is real damage (CR 120.1), routed through

### CR 120.3

- `crates/engine/src/apply.rs:2064` — state-based action (lethal damage) or a direct effect (Destroy), the Aura's own (CR 704, CR 303.4, …
- `crates/engine/src/pending/handlers/dig.rs:514` — CR 120.3 "as many as possible" — an empty library reveals nothing and raises no pause),
- `crates/engine/src/pending/handlers/dig.rs:551` — what's there, CR 120.3 "as many as possible" — an empty library reveals nothing and raises
- `crates/engine/src/pending/handlers/dig.rs:1030` — matching `filter` or the library runs out (CR 120.3), collecting every non-match along the
- `crates/engine/src/priority.rs:1454` — Skip the first-strike combat damage step unless a first/double striker is in (CR 510, CR 120.3, CR …
- `crates/engine/src/priority.rs:1456` — window, so with no first strikers there's exactly one combat damage step. (CR 510, CR 120.3, CR 506)
- `crates/engine/src/resolution/damage.rs:12` — being marked; everything else marks damage as usual (CR 120.3/506).
- `crates/engine/src/resolution/damage.rs:19` — `damaged_this_turn` tally (`triggers.rs`) — don't see it, though CR 120.3 says the damage
- `crates/engine/src/resolution/damage.rs:54` — fact that it was dealt (CR 120.3).
- `crates/engine/src/resolution/damage.rs:121` — Damage to a creature is marked (an SBA later checks it against toughness), (CR 704, CR 120.3)
- `crates/engine/src/resolution/damage.rs:166` — Mass damage: mark `amount` on every creature; the SBA sweep clears the dead. (CR 704, CR 120.3)
- `crates/engine/src/resolution/misc.rs:34` — combat damage to a player this combat (see `Game::fire_combat_damage_watch_triggers`). (CR 510, CR …
- `crates/engine/src/resolution/reveal.rs:130` — `RevealUntil`'s sibling), stopping early on a short library (CR 120.3 "as many as
- `crates/engine/src/resolution/zones.rs:555` — CR 120.3: an empty library reveals nothing — a clean no-op.
- `crates/engine/src/triggers.rs:967` — Damage marked on a creature (CR 120.3/506) — `Game::deal_creature_damage` is the
- `crates/engine/src/triggers.rs:1111` — `events` directly (no scratch accumulator needed — combat damage is already fully (CR 510, CR 120.3…
- `crates/engine/src/types/card.rs:298` — damage is still dealt (CR 120.3) at its original size — lifelink, deathtouch, the
- `crates/engine/src/types/card.rs:2061` — CR 120.3, you don't draw if the library is empty.
- `crates/engine/src/types/effect/shared.rs:1449` — source in the pool cares about the damage/life-loss distinction on these riders. (CR 605, CR 120.3,…
- `crates/engine/tests/game.rs:3916` — and players get priority in it before the regular combat damage step. (CR 510, CR 117, CR 120.3)
- `crates/engine/tests/game.rs:3950` — first-strike combat damage step, one blocker being Blood Artist. The look-back lets the (CR 510, CR…
- `crates/engine/tests/game.rs:3986` — Both blockers were dead before the regular combat damage step, so the attackers live. (CR 510, CR 1…
- `crates/engine/tests/game.rs:4148` — Ohran Frostfang (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR 108…
- `crates/engine/tests/game.rs:4149` — player, draw a card." An unblocked attacker draws; a blocked attacker deals combat damage (CR 510, …
- `crates/engine/tests/game.rs:4176` — Leitmotif Composer (soc): "Whenever this creature deals combat damage to a player, draw a (CR 510, …
- `crates/engine/tests/game.rs:4450` — CR 120.3/506 noncombat alike) — an attacking dragon blocked by a tough creature marks
- `crates/engine/tests/game.rs:4673` — Defiling Daemogoth (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR …
- `crates/engine/tests/game.rs:4718` — Venerable Warsinger (soc): "...Whenever this creature deals combat damage to a player, (CR 510, CR …
- `crates/engine/tests/game.rs:4721` — power 3 deals 3 combat damage, so X = 3: a mana-value-3 graveyard creature is a legal (CR 510, CR 4…
- `crates/engine/tests/game.rs:11794` — holds no priority round — and its self-damage rider is *not* a cost (it never gates the tap). (CR 1…
- `crates/engine/tests/game.rs:14758` — Shock marks 2 damage; 2 >= toughness 2, so the SBA moves the bear to the graveyard. (CR 704, CR 403…
- `crates/engine/tests/game.rs:15140` — Roll forward until combat damage is dealt; keep the events of the submit that kills the lord. (CR 5…
- `crates/engine/tests/game.rs:18440` — enchanted *blocker* takes no combat damage from the creature it blocks. (CR 510, CR 120.3, CR 506)
- `crates/engine/tests/game.rs:20864` — resolve_top_of_stack(&mut game); // Shock resolves → lethal damage → SBA kills the 1/1. (CR 704, CR…
- `crates/engine/tests/game.rs:30774` — resolve_top_of_stack(&mut game); // the enchanted-creature-attacks drain resolves (before combat da…
- `crates/engine/tests/game.rs:36687` — `graveyard_or_command` choke point: lethal marked damage (the SBA death sweep) and a (CR 704, CR 12…
- `crates/engine/tests/game.rs:36715` — resolve_top_of_stack(&mut game); // Shock resolves → SBA sweep finds lethal damage. (CR 704, CR 120…
- `crates/engine/tests/game.rs:65355` — resolve_top_of_stack(&mut game); // Shock resolves → lethal damage → SBA kills the 1/1. (CR 704, CR…
- `crates/engine/tests/game.rs:72284` — No Aura anywhere in the library: the reveal runs off the end (CR 120.3 "as many as
- `crates/engine/tests/game.rs:72806` — CR 120.3: X larger than the library reveals every remaining card instead of panicking.
- `crates/engine/tests/game.rs:73721` — deal combat damage to a player, create a 0/0 green and blue Fractal creature token. Put a (CR 510, …
- `crates/engine/tests/game.rs:75180` — "Whenever that creature deals combat damage to a player this combat, this creature (CR 510, CR 120.…
- `crates/engine/tests/game.rs:75182` — trigger, not a static ability — only *that* creature dealing combat damage arms it. (CR 510, CR 120…
- `crates/engine/tests/game.rs:75205` — A creature that never got the deathtouch grant dealing combat damage doesn't arm the (CR 510, CR 12…
- `crates/engine/tests/game.rs:75263` — deathtoucher's now-legitimate combat damage is the only thing that could wrongly prepare (CR 510, C…
- `crates/engine/tests/game.rs:90558` — CR 120.3 "as many as possible" — a one-card library reveals only that one; the opponent's
- `crates/engine/tests/game.rs:99463` — the size of the damage (CR 120.3).
- `crates/engine/tests/game.rs:99542` — CR 120.3: infect changes the form of the damage, not the fact that it was dealt — lifelink

### CR 120.3c

- `crates/engine/src/combat.rs:1041` — loyalty counters are removed (CR 120.3c/306.8), never marked damage — a planeswalker has no
- `crates/engine/src/query.rs:970` — Burn that also reaches planeswalkers (Rip Apart, CR 120.3c).
- `crates/engine/src/resolution/damage.rs:133` — being marked (CR 120.3c/306.9) — checked ahead of Tajic's creature-only
- `crates/engine/src/resolution/damage.rs:219` — Volcanic Torrent's "and planeswalker" (CR 120.3c/306.9) — `include_planeswalkers`
- `crates/engine/src/resolution/damage.rs:248` — being marked (CR 120.3c/306.9), ahead of Phantom Centaur's shield below
- `crates/engine/tests/game.rs:50473` — CR 120.3c/306.9: damage to a planeswalker is removed as loyalty counters, not marked.
- `crates/engine/tests/game.rs:50496` — "not marked damage (CR 120.3c) — a planeswalker has no toughness to mark against"
- `crates/engine/tests/game.rs:50521` — ── Planeswalkers as attack defenders (CR 506.2/508.1a, damage redirect CR 120.3c/306.8) ──
- `crates/engine/tests/game.rs:50547` — CR 120.3c/306.8: combat damage dealt to a planeswalker removes that many loyalty counters;
- `crates/engine/tests/game.rs:50842` — "3 damage to a planeswalker removes 3 loyalty (CR 120.3c)"
- `crates/engine/tests/game.rs:60392` — swept alongside creatures, taking loyalty loss instead of marked damage (CR 120.3c/306.9).

### CR 120.4

- `crates/engine/src/pending/handlers/optional.rs:127` — Answer a [`PendingChoice::MayDrawUpTo`] (CR 120.4 / 601.2c — Arcane Denial's "may draw up to
- `crates/engine/src/resolution/pause_may.rs:141` — (CR 120.4 / 601.2c). Pause the resolving controller on a count choice `0..=max`;
- `crates/engine/src/resolution/pause_may.rs:160` — (CR 120.4 / 601.2c). The mandatory opponent draw is a preceding `TargetPlayerDraws`
- `crates/engine/src/types/stack.rs:248` — `0..=max` (CR 120.4 / 601.2c — Arcane Denial's "may draw up to two cards"). An out-of-range
- `crates/engine/src/types/stack.rs:875` — `player` chooses how many cards to draw — any number `0..=max` (CR 120.4 / 601.2c —
- `crates/engine/tests/game.rs:27883` — `Effect::Choice(ChoiceEffect::MayDrawUpTo)` is a CR 120.4 / 601.2c declinable draw: its controller …

### CR 120.8

- `crates/engine/src/amount.rs:198` — the return fizzled — CR 120.8 turns that into "no damage at all", which is the
- `crates/engine/src/resolution/damage.rs:36` — 0 or less damage is never dealt (CR 120.8) — and never placed as counters.
- `crates/engine/src/resolution/damage.rs:68` — 0 or less damage is never dealt (CR 120.8) — and never placed as counters.
- `crates/engine/src/resolution/damage.rs:151` — 0 damage is never dealt (CR 120.8) — no marker, no trigger.
- `crates/engine/src/resolution/damage.rs:311` — 0 damage is never dealt (CR 120.8) — no marker, no trigger.
- `crates/engine/src/resolution/damage.rs:336` — 0 damage is never dealt (CR 120.8) — no marker, no trigger.
- `crates/engine/src/resolution/damage.rs:378` — 0 damage is never dealt (CR 120.8) — no marker, no trigger.
- `crates/engine/src/resolution/damage.rs:400` — 0 damage is never dealt (CR 120.8) — no marker, no trigger.
- `crates/engine/src/types/effect/shared.rs:239` — card" gate: a source that would deal 0 damage deals none at all (CR 120.8).

### CR 121

- `crates/engine/src/zones.rs:2` — Primary: CR 400 (zones), CR 121 (drawing a card), CR 106.4 (mana pool).
- `crates/engine/tests/game.rs:20867` — resolve_top_of_stack(&mut game); // The Dies trigger resolves: draw a card. (CR 603.6, CR 603, CR 1…
- `crates/engine/tests/game.rs:51464` — counter on Gorma." #72 self-target-spec (CR 121, CR 115): the counter always lands on
- `crates/engine/tests/game.rs:60609` — ability is index 0). (CR 118.7, CR 113, CR 121)
- `crates/engine/tests/game.rs:70076` — resolve_top_of_stack(&mut g); // the draw ability itself (not a mana ability) (CR 605, CR 113, CR 1…

### CR 121.2

- `crates/engine/src/resolution/counters.rs:196` — "Each opponent loses all counters" (Final Act) — CR 122.1/121.2: every counter of
- `crates/engine/src/types/effect/counters.rs:96` — "Each opponent loses all counters" (Final Act) — CR 122.1/121.2: every counter of every
- `crates/engine/src/types/stack.rs:899` — replaceable event (CR 121.2), so after this one resolves the answer handler re-enters the draw
- `crates/engine/src/zones.rs:149` — (CR 702.52 — every draw is its own event, CR 121.2). Pauses on the first draw for which
- `crates/engine/tests/game.rs:101287` — Final Act's fifth mode, "Each opponent loses all counters" (CR 122.1/121.2): every counter

### CR 121.4

- `crates/engine/src/types/effect/shared.rs:1210` — A -1/-1 counter (CR 121.4/122.1 — Wickerbough Elder), tracked in the same kind-keyed map as
- `crates/engine/tests/game.rs:87086` — CR 121.4/122.1: a -1/-1 counter reduces power and toughness by 1 each, the mirror of a
- `crates/engine/tests/game.rs:98563` — ── Contagion Clasp, Evolution Sage, Cathedral Acolyte (CR 121.4, CR 701.27) ───────────────────

### CR 121.6

- `crates/engine/tests/game.rs:100526` — CR 121.6/614: the upkeep ability's own "double the number of counters" step places as many

### CR 122

- `crates/engine/tests/game.rs:16461` — counter on it gets trample, a bare creature you control doesn't. (CR 702, CR 108.3, CR 122)
- `crates/engine/tests/game.rs:16482` — its last counter loses trample the same recompute. (CR 702, CR 122)
- `crates/engine/tests/game.rs:25879` — P1 casts both counters at the same bear (casting keeps priority, so no pass between them). (CR 117,…
- `crates/engine/tests/game.rs:51519` — ability_index: 2, // 0 = enters-with-counters static, 1 = the new dies trigger. (CR 603.6, CR 122, …
- `crates/engine/tests/game.rs:68698` — no counters — same mass-hexproof grant, no +1/+1 counter this time. (CR 702.11, CR 122)
- `crates/engine/tests/game.rs:70926` — ── Increment #93: counter-choice-verbs — proliferate, move counters, mass counter removal ── (CR 70…
- `crates/engine/tests/game.rs:70930` — Expansion Algorithm (soc): "Proliferate X times." A creature with a +1/+1 counter and a (CR 701.27,…
- `crates/engine/tests/game.rs:97919` — Drown in Ichor ({1}{B}) — "Target creature gets -4/-4 until end of turn. Proliferate." (CR 701.27, …

### CR 122.1

- `crates/engine/src/amount.rs:269` — every living opponent, CR 122.1.
- `crates/engine/src/amount.rs:276` — single player this amount is relative to, CR 122.1.
- `crates/engine/src/characteristics.rs:1750` — `PlayerCountersPlaced` rad counter (CR 122.1) per player per counter removed. Empty when
- `crates/engine/src/characteristics.rs:2182` — The number of counters of a *named* kind (CR 122.1 — charge, -1/-1, …) actually placed when
- `crates/engine/src/characteristics.rs:2201` — player half of CR 122.1 (poison, rad, experience), reached by Winding Constrictor's "if you
- `crates/engine/src/combat.rs:362` — attacker owes it nothing (CR 122.1: only creatures with counters are taxed).
- `crates/engine/src/combat.rs:560` — Vow counters (CR 122.1 — Promise of Loyalty): a creature marked with a vow counter
- `crates/engine/src/combat.rs:574` — Vow auras (Vow of Duty/Flight/Lightning — CR 122.1 sibling): a live attached Aura can
- `crates/engine/src/core.rs:194` — How many counters of `kind` sit on `player` (CR 122.1). Public information — ten or more
- `crates/engine/src/core.rs:647` — Whether the permanent at `id` has any counter on it at all — CR 122.1's unqualified
- `crates/engine/src/core.rs:658` — The total number of counters on the permanent at `id` — CR 122.1's unqualified count (Nils,
- `crates/engine/src/priority.rs:1716` — Rad counters (CR 122.1, Fallout): "At the beginning of each player's precombat main
- `crates/engine/src/priority.rs:1835` — The rad-counter turn-based action for `player`'s precombat main phase (CR 122.1, Fallout):
- `crates/engine/src/query.rs:1522` — Counter axis (CR 122.1 — Innkeeper's Talent's "with counters on them", Inspiring
- `crates/engine/src/replacements.rs:3` — What a would-be counter placement is aimed at (CR 122.1 — counters sit on permanents and on
- `crates/engine/src/resolution/counters.rs:147` — on any permanent (CR 122.1). A player who has already lost is no longer in the game
- `crates/engine/src/resolution/counters.rs:196` — "Each opponent loses all counters" (Final Act) — CR 122.1/121.2: every counter of
- `crates/engine/src/resolution/pump.rs:362` — counter its controller has" (CR 122.1) gives each opponent's creatures a
- `crates/engine/src/types/card.rs:1707` — Named non-P/T counters (CR 122.1 — charge, story, …), indexed by [`CounterKind`] as
- `crates/engine/src/types/card.rs:2143` — Named counters sitting on this *player* (CR 122.1), one slot per [`PlayerCounterKind`] —
- `crates/engine/src/types/effect/counters.rs:86` — gets a poison counter" (Ichor Rats) — CR 122.1. Places `count` counters of `kind` on every
- `crates/engine/src/types/effect/counters.rs:96` — "Each opponent loses all counters" (Final Act) — CR 122.1/121.2: every counter of every
- `crates/engine/src/types/effect/shared.rs:340` — The total number of poison counters (CR 122.1) sitting on the effect controller's living
- `crates/engine/src/types/effect/shared.rs:345` — The number of poison counters (CR 122.1) on the player this amount is resolved *relative
- `crates/engine/src/types/effect/shared.rs:1173` — A named counter kind (CR 122.1) tracked on [`Permanent::kind_counters`] — distinct from the
- `crates/engine/src/types/effect/shared.rs:1191` — A vow counter (CR 122.1 — Promise of Loyalty): a functional reminder counter marking a
- `crates/engine/src/types/effect/shared.rs:1202` — A scream counter (All Hallow's Eve — CR 122.1's functional-reminder family). Mechanically a
- `crates/engine/src/types/effect/shared.rs:1210` — A -1/-1 counter (CR 121.4/122.1 — Wickerbough Elder), tracked in the same kind-keyed map as
- `crates/engine/src/types/effect/shared.rs:1216` — A strife counter (CR 122.1 — Crescendo of War): placed on the source itself at each
- `crates/engine/src/types/effect/shared.rs:1220` — An age counter (CR 122.1, CR 702.24a — cumulative upkeep, Jotun Grunt): placed on the
- `crates/engine/src/types/effect/shared.rs:1224` — A storage counter (CR 122.1 — storage lands, e.g. Fungal Reaches' "{1}, {T}: Put a
- `crates/engine/src/types/effect/shared.rs:1238` — kind that sits on a *player* (poison, CR 122.1) doesn't belong here at all — it has its own
- `crates/engine/src/types/effect/shared.rs:1257` — A named counter kind that sits on a *player* rather than a permanent (CR 122.1), tracked on
- `crates/engine/src/types/effect/shared.rs:1270` — A poison counter (CR 122.1, CR 704.5c — ten or more loses the game). Placed by infect
- `crates/engine/src/types/effect/shared.rs:1273` — A rad counter (CR 122.1, Fallout). No lose-the-game threshold — instead a turn-based
- `crates/engine/src/types/effect/static.rs:123` — Which recipients the replacement reaches (CR 122.1 — counters sit on permanents and on
- `crates/engine/src/types/effect/static.rs:296` — on players (CR 122.1), and a card names one or both: Hardened Scales only permanents, Winding
- `crates/engine/src/types/filter.rs:801` — Counter axis (CR 122.1) narrower than `modified` above — `modified` also matches an
- `crates/engine/src/types/filter.rs:809` — the pool currently needs on a [`PermanentFilter`] (CR 122.1's unqualified "counter" vs the
- `crates/engine/src/types/stack.rs:2269` — *player* (CR 122.1 — poison) — the player-side twin of
- `crates/engine/src/types/stack.rs:2474` — A vow counter (CR 122.1 — Promise of Loyalty) was placed on `object`, marking `protected`
- `crates/engine/tests/game.rs:41663` — ── Vow counters (CR 122.1 — Promise of Loyalty) ─────────────────────────────────────
- `crates/engine/tests/game.rs:69653` — ── Increment #75: counter-kinds — a kind-keyed counter map (CR 122.1) for named non-P/T
- `crates/engine/tests/game.rs:69668` — land in the kind-keyed map (CR 122.1), separate from `plus_counters`.
- `crates/engine/tests/game.rs:70533` — CR 122.1: study counters are a named kind, tracked separately from +1/+1 counters.
- `crates/engine/tests/game.rs:87086` — CR 121.4/122.1: a -1/-1 counter reduces power and toughness by 1 each, the mirror of a
- `crates/engine/tests/game.rs:98706` — many of those counters on that permanent instead" — *counters*, of every kind (CR 122.1),
- `crates/engine/tests/game.rs:99237` — add, they don't replace (CR 122.1).
- `crates/engine/tests/game.rs:100010` — for any counter kind, not only +1/+1 (CR 122.1's unqualified "counter").
- `crates/engine/tests/game.rs:101287` — Final Act's fifth mode, "Each opponent loses all counters" (CR 122.1/121.2): every counter
- `crates/engine/tests/game.rs:101630` — "Each opponent loses all counters" is every *kind* (CR 122.1), not just poison — an
- `crates/engine/tests/game.rs:101678` — CR 122.1 counts poison per player, and "its controller" is read once per affected creature —

### CR 122.1e

- `crates/engine/tests/game.rs:70260` — "{1}, {T}: Put a storage counter on this land." CR 122.1e: a storage land's ability adds

### CR 122.3g

- `crates/engine/src/core.rs:635` — Whether the permanent at `id` has a finality counter (CR 122.3g), i.e. it's exiled
- `crates/engine/src/types/card.rs:1810` — Whether this permanent has a finality counter (CR 122.3g/614.12): if it would be put into

### CR 201.2

- `crates/engine/src/pending/handlers/fanout.rs:270` — return, letting the enclosing sequence resume. Naming is mandatory (CR 201.2), so unlike a
- `crates/engine/src/pending/handlers/fanout.rs:284` — puts it into their hand, a miss puts it on the bottom of their library (CR 201.2/703.2j) —
- `crates/engine/src/pending/handlers/fanout.rs:303` — Trust boundary: bounded, non-blank shape only (CR 201.2 — a real name is never blank);
- `crates/engine/src/query.rs:1531` — Printed name (CR 201.2 — Leitmotif Composer's "creatures named Leitmotif Composer").
- `crates/engine/src/types/filter.rs:764` — *named* Leitmotif Composer can't be blocked this turn" — CR 201.2, matched against
- `crates/engine/src/types/stack.rs:426` — Answer a [`PendingChoice::ChooseCardName`] (CR 201.2/703.2j "choose a card name" —
- `crates/engine/src/types/stack.rs:1670` — fan-out (CR 201.2/703.2j "choose a card name" — Conundrum Sphinx's attack trigger).
- `crates/engine/tests/game.rs:4638` — this turn." (CR 702.10, CR 201.2)
- `crates/engine/tests/game.rs:93032` — card name to choose (CR 201.2 — a real name is never blank).

### CR 201.3

- `crates/engine/src/pending/handlers/fanout.rs:282` — against any real card list (CR 201.3 lets a player name a nonexistent card). Reveals the

### CR 202

- `crates/engine/src/types/effect/shared.rs:305` — types, CR 202 mana value) — Furygale Flocking's "costs {1} less to cast for each instant

### CR 202.2

- `crates/engine/src/characteristics.rs:2159` — Balefire Liege's "cast a red spell" / "cast a white spell" — CR 105.1/202.2, the
- `crates/engine/src/pending/handlers/fanout.rs:203` — "Any amount of mana" is paid as that much generic (CR 202.2 — generic accepts any type).
- `crates/engine/src/types/filter.rs:244` — spell"). Reads the spell's own colors (CR 105.1/202.2, [`color_identity`]), so a

### CR 202.3

- `crates/engine/src/types/card.rs:1003` — This card's mana value (CR 202.3): the total pips in its mana cost — generic plus every
- `crates/engine/src/types/effect/shared.rs:132` — The mana value (CR 202.3) of the spell that fired a `Trigger::CastSpell` (magecraft)
- `crates/engine/src/types/effect/shared.rs:1913` — CR 603.4/202.3: a `CastSpell` (magecraft) trigger's `Amount::TriggeringSpellManaValue`
- `crates/engine/src/types/mana.rs:117` — Render this cost's mana pips as cost text (CR 202.3) — `{X}`, the generic number, `{C}`
- `crates/engine/src/types/trigger.rs:703` — The triggering spell's mana value (CR 202.3), for a `Trigger::CastSpell` (magecraft)
- `crates/engine/src/types/trigger.rs:711` — The mana actually spent to cast the triggering spell (CR 601.2h/202.3), for a

### CR 202.3b

- `crates/engine/src/types/card.rs:1005` — (CR 202.3b), which is exactly how [`Cost`] stores it (the `x` marker adds nothing to the
- `crates/engine/src/types/card.rs:1029` — reads the printed cost (CR 202.3b treats `{X}` as 0 off the stack), which is safe here
- `crates/engine/src/types/effect/shared.rs:142` — (which reads the printed mana value, treating `{X}` as 0 per CR 202.3b — divergent from
- `crates/engine/tests/game.rs:55793` — (CR 202.3b: `{X}` counts as 0 outside the stack) for `Amount::TriggeringSpellManaSpent` probes
- `crates/engine/tests/game.rs:56361` — value (CR 202.3b treats {X} as 0 outside the stack). Casting a {1}{X} spell with X=3 spends

### CR 202.3f

- `crates/engine/src/types/card.rs:1007` — correctly. Each color/color hybrid pip counts 1 (CR 202.3f — both halves are one mana;
- `crates/engine/tests/game.rs:20747` — CR 202.3f: a color/color hybrid pip counts 1 toward mana value. Balefire Liege's

### CR 205

- `crates/engine/src/types/effect/shared.rs:304` — The number of instant and sorcery cards in the effect's controller's graveyard (CR 205 card

### CR 205.3k

- `crates/engine/tests/game.rs:85317` — An Arcane sorcery (CR 205.3k — spell subtype), functionally identical to Cultivate.

### CR 205.3m

- `crates/engine/src/types/stack.rs:1759` — ponytail: the pool's own creature types, not the CR 205.3m full type list (which is much
- `crates/engine/tests/game.rs:17096` — Patchwork Banner (msc): "As this artifact enters, choose a creature type." CR 205.3m's

### CR 205.4

- `crates/engine/src/types/card.rs:1205` — `legendary` stays because a supertype is not a card type (CR 205.4).

### CR 205.4a

- `crates/engine/src/de.rs:833` — The "Basic" supertype (CR 205.4a) — `basic = true` in TOML for the five
- `crates/engine/src/query.rs:1512` — Nonbasic land (CR 205.4a's "Basic" supertype — White Orchid Phantom's "target
- `crates/engine/src/query.rs:1547` — Nonlegendary exclusion (CR 205.4a — Muddle, the Ever-Changing's "nonlegendary
- `crates/engine/src/types/card.rs:1086` — Whether `def` is a basic land: has the "Basic" supertype (CR 205.4a). Reads
- `crates/engine/src/types/filter.rs:279` — "a basic Plains card" — the Basic supertype, CR 205.4a, excludes a nonbasic Plains-typed
- `crates/engine/src/types/filter.rs:376` — [`CardKind`] match, so an Aura counts (it's still an enchantment card, CR 205.4a) the same
- `crates/engine/src/types/filter.rs:757` — Excludes basic lands (CR 205.4a's "Basic" supertype — White Orchid Phantom's "target
- `crates/engine/src/types/filter.rs:769` — Excludes legendary permanents (CR 205.4a's "Legendary" supertype — Muddle, the
- `crates/engine/tests/game.rs:13382` — (CR 205.4a: the Basic supertype, not a subtype string) excludes a basic land from the
- `crates/engine/tests/game.rs:65262` — supertype (CR 205.4a) excludes a nonbasic Plains-typed land like Eclipsed Steppe
- `crates/engine/tests/game.rs:81652` — "a nonbasic Forest-typed dual is not basic (CR 205.4a)"
- `crates/engine/tests/game.rs:82042` — "a nonbasic Forest-typed dual is not basic (CR 205.4a)"

### CR 300

- `crates/engine/src/types/filter.rs:198` — Artifact or enchantment spells you cast (Quandrix Command's hard counter mode — CR 303/300:

### CR 301.5

- `crates/engine/src/query.rs:66` — step) still has no affordance; add "hold priority" if it's ever wanted. (CR 702.8, CR 117, CR 301.5)
- `crates/engine/src/types/card.rs:1799` — The permanent this is attached to, for an Aura/Equipment (CR 301.5/303.4). `None`
- `crates/engine/tests/game.rs:68567` — counter / enchanted by an Aura / equipped) feeding Silkguard's hexproof rider. ─────────── (CR 702.…

### CR 301.5c

- `crates/engine/src/pending/handlers/dig.rs:1168` — Aura/Equipment to `id` (CR 303.4f / CR 301.5c — the controller chooses among the objects
- `crates/engine/tests/game.rs:39217` — an optional attach (CR 301.5c); decline it here — the attach itself is exercised by the
- `crates/engine/tests/game.rs:39399` — battlefield this way, you may attach it to a creature you control" (CR 301.5c).
- `crates/engine/tests/game.rs:68876` — `opp_bonesplitter` stays unequipped — it's still an Equipment permanent (CR 301.5c) and

### CR 301.5g

- `crates/engine/src/triggers.rs:2350` — Queue attached-permanent attack triggers (CR 508.1, the Impetus cycle — and CR 301.5g's

### CR 302.6

- `crates/engine/src/apply.rs:1246` — CR 302.6/720.3: gaining control of a permanent (here via a control-changing
- `crates/engine/src/apply.rs:1252` — re-set (untested edge of CR 302.6) — add it if a card cares.
- `crates/engine/src/characteristics.rs:340` — Whether a permanent is subject to summoning sickness (CR 302.6): it entered under its
- `crates/engine/src/characteristics.rs:1324` — (CR 302.6) — an artifact/land (a Treasure, a fetchland) may tap the turn it enters.
- `crates/engine/src/types/card.rs:1680` — next untap (CR 302.6, [`Event::LostSummoningSickness`]): this instead clears for every
- `crates/engine/tests/game.rs:11693` — CR 302.6: summoning sickness only restricts creatures. A noncreature permanent (Armillary
- `crates/engine/tests/game.rs:18367` — A creature cast this turn can't attack (CR 302.6), so it isn't worth stopping at
- `crates/engine/tests/game.rs:32875` — CR 302.6: control just changed, so it's summoning-sick for its new controller.
- `crates/engine/tests/game.rs:82992` — sickness (CR 302.6), so lethal marked damage is dealt directly rather than through combat.

### CR 303

- `crates/engine/src/priority.rs:398` — Whether `object` is an enchantment currently on the battlefield (CR 303 — includes an
- `crates/engine/src/query.rs:1422` — Attached-to-creature: whether this (Aura) candidate's own host is a creature (CR 303 —
- `crates/engine/src/query.rs:1580` — Whether `host` has an Aura attached to it ("enchanted", CR 303). Equipment and other
- `crates/engine/src/query.rs:1865` — Sage's Reverie's "each Aura you control that's attached to a creature" (CR 303) — an
- `crates/engine/src/resolution/sequence_steps.rs:182` — ponytail: only an Aura can be attached (CR 303); a non-Aura minted token is a no-op
- `crates/engine/src/types/card.rs:394` — additional types (Artifact/Enchantment Creature). Auras count as enchantments (CR 303).
- `crates/engine/src/types/filter.rs:198` — Artifact or enchantment spells you cast (Quandrix Command's hard counter mode — CR 303/300:
- `crates/engine/src/types/filter.rs:688` — creature (CR 303 — Sage's Reverie's "each Aura you control that's attached to a

### CR 303.2

- `crates/engine/src/priority.rs:399` — Aura, CR 303.2). A phased-out permanent doesn't count, mirroring
- `crates/engine/src/types/card.rs:956` — — CR 303.2). `enter_as_copy = { of = "enchantment" }` in TOML; absent means `Creature`.

### CR 303.4

- `crates/engine/src/amount.rs:237` — CR 303.4: any Aura attached, regardless of controller — unlike
- `crates/engine/src/apply.rs:2064` — state-based action (lethal damage) or a direct effect (Destroy), the Aura's own (CR 704, CR 303.4, …
- `crates/engine/src/apply.rs:2065` — orphan-to-graveyard SBA hasn't run yet, so it's still attached right now. Read (CR 704, CR 303.4, C…
- `crates/engine/src/characteristics.rs:676` — The Auras `controller` controls that are currently attached to `host` (CR 303.4) —
- `crates/engine/src/characteristics_cache.rs:223` — own static reads its self (`object`), while Flickering Ward's Aura (`object`) grants (CR 702.21, CR…
- `crates/engine/src/effects.rs:192` — escape Aura's target realistically fizzles in a test, so this residual is (CR 702.19, CR 303.4, CR …
- `crates/engine/src/effects.rs:621` — permanent's escape spell resolves through the `CardKind::Creature`/`Aura` arms (CR 702.19, CR 303.4…
- `crates/engine/src/pending/handlers/library.rs:142` — hostless-Aura state-based action, same as before this pause existed. (CR 704, CR 303.4)
- `crates/engine/src/query.rs:556` — An activated ability granted by an Aura attached to `source` (Fallen Ideal's "Sacrifice (CR 602, CR…
- `crates/engine/src/resolution/pause_choose.rs:30` — Flickering Ward's "As this Aura enters, choose a color": pause on a ChooseColor for (CR 702.21, CR …
- `crates/engine/src/resolution/pump.rs:300` — defender being someone other than the Aura's controller. An unattached Aura (mid-SBA) (CR 704, CR 3…
- `crates/engine/src/triggers.rs:2180` — How many of `attackers` are each enchanted by an Aura `controller` controls (CR 303.4) —
- `crates/engine/src/types/card.rs:365` — (CR 303.4) and grants it a continuous effect while attached.
- `crates/engine/src/types/card.rs:1799` — The permanent this is attached to, for an Aura/Equipment (CR 301.5/303.4). `None`
- `crates/engine/src/types/effect/shared.rs:298` — The number of Auras (any controller) currently attached to the effect's source (CR 303.4)
- `crates/engine/src/types/filter.rs:186` — Aura spells you cast (Transcendent Envoy, CR 303.4). An Aura is its own [`CardKind`], so
- `crates/engine/tests/game.rs:18540` — Flickering Ward: "As this Aura enters, choose a color. Enchanted creature has protection (CR 702.21…
- `crates/engine/tests/game.rs:19168` — protection from the chosen color must not cause the Aura to fall off. (CR 702.16, CR 303.4)
- `crates/engine/tests/game.rs:30187` — own bear; P1 responds with a flash control-changing Aura on the same bear, which resolves (CR 702.8…
- `crates/engine/tests/game.rs:30330` — Equipment attached to it doesn't count — only Auras do (CR 303.4).
- `crates/engine/tests/game.rs:36513` — Animate Dead is a real Aura (CR 303.4): CR 704.5m's Aura-orphan state-based action applies
- `crates/engine/tests/game.rs:41953` — Destroy the Aura (Fracture); the goad is a live query over the attachment, not an (CR 701.38, CR 30…
- `crates/engine/tests/game.rs:68567` — counter / enchanted by an Aura / equipped) feeding Silkguard's hexproof rider. ─────────── (CR 702.…
- `crates/engine/tests/game.rs:93490` — it's on the battlefield (CR 702.16, CR 702.21, CR 303.4).
- `crates/engine/tests/game.rs:93535` — choice until end of turn." (CR 702.16, CR 702.21, CR 303.4)

### CR 303.4a

- `crates/engine/src/apply.rs:161` — ETB ability (CR 303.4a/608.2b) to reanimate and attach it. This SBA sweep runs
- `crates/engine/src/de.rs:314` — An Aura's enchant subject restriction (CR 303.4a) — `enchant = { … }`, the same
- `crates/engine/src/de.rs:319` — Animate Dead's cast-time "enchant creature card in a graveyard" (CR 303.4a) —
- `crates/engine/src/effects.rs:167` — Animate Dead (CR 303.4a's "enchant creature card in a graveyard"): a real Aura, but
- `crates/engine/src/effects.rs:372` — Animate Dead (CR 303.4a/608.2b): its own cast-time "enchant creature card in a
- `crates/engine/src/priority.rs:355` — Animate Dead (CR 303.4a's "enchant creature card in a graveyard"): the pool's one Aura
- `crates/engine/src/priority.rs:362` — An Aura is cast targeting the creature it will enchant (CR 303.4a), even though its
- `crates/engine/src/query.rs:1125` — Animate Dead's own cast-time graveyard target (CR 303.4a) — the choice already
- `crates/engine/src/resolution/copy.rs:103` — spell-timed effect (CR 303.4a/601.2c) — gets a real retarget spec too (Changing
- `crates/engine/src/resolution/copy.rs:112` — a count off — it's always exactly one target (CR 303.4a). An instant/sorcery
- `crates/engine/src/types/card.rs:514` — An Aura's enchant subject restriction (CR 303.4a — "Enchant creature you control"):
- `crates/engine/src/types/card.rs:523` — Animate Dead's own cast-time enchant target (CR 303.4a's "enchant creature card in a
- `crates/engine/src/types/card.rs:1863` — The graveyard-card object id this Aura targeted when cast (CR 303.4a's "enchant creature
- `crates/engine/src/types/filter.rs:132` — targeting (CR 303.4a's "enchant creature card in a graveyard"), captured on the permanent
- `crates/engine/src/types/filter.rs:194` — so an Aura spell matches too (CR 303.4a: an Aura *is* an enchantment) — the pool's white
- `crates/engine/tests/game.rs:30153` — Chains of Custody is "Enchant creature you control" (CR 303.4a) — an opponent's creature
- `crates/engine/tests/game.rs:32936` — filter (CR 303.4a) legally attaches to a noncreature host — the CR 704.5m/n legality SBA
- `crates/engine/tests/game.rs:36333` — Animate Dead's "enchant creature card in a graveyard" (CR 303.4a) is a real cast-time
- `crates/engine/tests/game.rs:45962` — An Aura is an enchantment (CR 303.4a), so it gets the discount too. Darksteel Mutation is

### CR 303.4f

- `crates/engine/src/apply.rs:14` — Aura's cast-time legality re-check (CR 303.4f) and by the CR 704.5m/n state-based action
- `crates/engine/src/apply.rs:43` — the Aura's `enchant` restriction (CR 303.4f) *and* not have protection that stops the Aura
- `crates/engine/src/effects.rs:186` — CR 303.4f: if the target is illegal as the Aura would resolve, the Aura
- `crates/engine/src/pending/handlers/dig.rs:1074` — (CR 303.4f) — see [`Self::maybe_pause_attach_deployed_aura`].
- `crates/engine/src/pending/handlers/dig.rs:1121` — creatures it could legally enchant (CR 303.4f), reusing the same `enchant`-restriction
- `crates/engine/src/pending/handlers/dig.rs:1168` — Aura/Equipment to `id` (CR 303.4f / CR 301.5c — the controller chooses among the objects
- `crates/engine/src/pending/handlers/dig.rs:1215` — already uses (CR 303.4f). Guard-returns with no events if this Aura has since left the
- `crates/engine/src/pending/handlers/edict.rs:223` — Copy Enchantment copying an Aura (CR 707.2 read with CR 303.4f): the copy entered
- `crates/engine/src/pending/handlers/library.rs:139` — An Aura among the deployed permanents may need a host chosen (CR 303.4f). Scoped to a
- `crates/engine/src/resolution/sequence_steps.rs:72` — CR 303.4f: "you may attach it to the token" is a real attach, so the moving
- `crates/engine/src/types/stack.rs:434` — 301.5c) — a mandatory Aura host (CR 303.4f) rejects `None`.
- `crates/engine/src/types/stack.rs:1730` — cast (CR 303.4f — Songbirds' Blessing's "you may put that card onto the battlefield,"
- `crates/engine/src/types/stack.rs:1733` — and the choice is mandatory once raised (CR 303.4f); a hostless Aura instead hits the
- `crates/engine/tests/game.rs:18914` — A copied Aura must enter attached (CR 707.2 read with CR 303.4f — an Aura not attached to
- `crates/engine/tests/game.rs:30185` — CR 303.4f: if the enchant restriction is no longer met when the Aura would resolve, it's
- `crates/engine/tests/game.rs:30570` — CR 303.4f: the Aura's target dies in response, so the Aura never enters — it's put
- `crates/engine/tests/game.rs:32040` — creature on the battlefield as the new host (CR 303.4f).
- `crates/engine/tests/game.rs:36393` — CR 608.2b/303.4f: exiling the chosen graveyard card after Animate Dead is cast but before
- `crates/engine/tests/game.rs:36395` — entering, the same fizzle CR 303.4f gives a battlefield-targeting Aura.
- `crates/engine/tests/game.rs:64633` — Ajani's Chosen's "you may attach it to the token" is a real attach (CR 303.4f), so the
- `crates/engine/tests/game.rs:71964` — library in a random order." CR 303.4f: an Aura put onto the battlefield this way attaches
- `crates/engine/tests/game.rs:72095` — When the deployed Aura has no legal host, CR 303.4f's choose-host pause never comes up —

### CR 303.4g

- `crates/engine/src/resolution/zones.rs:323` — CR 303.4g / 702.16e: this delayed "return this card attached to that creature"
- `crates/engine/tests/game.rs:29043` — step, exercising the non-cast Aura-return legality gate (CR 303.4g / 702.16e).
- `crates/engine/tests/game.rs:64724` — CR 303.4g / 702.16e: Gift of Immortality's delayed "return this card attached to that

### CR 305

- `crates/engine/src/de.rs:829` — Printed land types (CR 305 — "Forest", "Island", …). Empty for a land with
- `crates/engine/src/query.rs:1552` — Non-Lair land exclusion (CR 305 — Treva's Ruins' "non-Lair land"). Reads the printed
- `crates/engine/src/types/card.rs:378` — `subtypes` carries its printed land types (CR 305 — "Forest", "Island", …; empty for a
- `crates/engine/src/types/filter.rs:773` — Excludes the "Lair" land subtype (CR 305 — Treva's Ruins' "return a *non-Lair* land you
- `crates/engine/tests/game.rs:94890` — ── Zoetic Cavern: a Land with morph (CR 702.37 + CR 305) ──────────────────────────

### CR 305.1

- `crates/engine/tests/game.rs:94896` — doesn't touch the once-per-turn land drop (CR 305.1) — a real land is still playable this turn.
- `crates/engine/tests/game.rs:95006` — drop is consumed (CR 305.1) and it taps for {C} right away.

### CR 305.4

- `crates/engine/src/types/card.rs:354` — and artifact-type filters count them (CR 305.4 / #19). Empty for a plain creature.

### CR 305.9

- `crates/engine/src/types/stack.rs:1438` — (`tapped` if it enters tapped), or decline ("up to one" — CR 305.9 special action, an
- `crates/engine/src/types/stack.rs:2945` — permanent `permanent` (CR 305.9 "put onto the battlefield" — Eureka Moment, Zimone), tapped
- `crates/engine/tests/game.rs:53076` — CR 305.9: this is a special action ("put onto the battlefield"), not "play a land" — the

### CR 306

- `crates/engine/tests/game.rs:65202` — CR 306/307: a permanent's card types union — Doomwake Giant is an Enchantment

### CR 306.5b

- `crates/engine/src/pending/handlers/targets.rs:340` — A planeswalker's loyalty is loyalty counters (CR 306.5b), so it gets one more too.
- `crates/engine/src/pending/raise/optional.rs:22` — A planeswalker's loyalty is loyalty counters (CR 306.5b), so it always has counters.
- `crates/engine/tests/game.rs:101394` — CR 306.5b: a planeswalker's loyalty *is* loyalty counters, so proliferate gives it one more.

### CR 306.8

- `crates/engine/src/combat.rs:1041` — loyalty counters are removed (CR 120.3c/306.8), never marked damage — a planeswalker has no
- `crates/engine/tests/game.rs:50521` — ── Planeswalkers as attack defenders (CR 506.2/508.1a, damage redirect CR 120.3c/306.8) ──
- `crates/engine/tests/game.rs:50547` — CR 120.3c/306.8: combat damage dealt to a planeswalker removes that many loyalty counters;

### CR 306.9

- `crates/engine/src/resolution/damage.rs:133` — being marked (CR 120.3c/306.9) — checked ahead of Tajic's creature-only
- `crates/engine/src/resolution/damage.rs:219` — Volcanic Torrent's "and planeswalker" (CR 120.3c/306.9) — `include_planeswalkers`
- `crates/engine/src/resolution/damage.rs:248` — being marked (CR 120.3c/306.9), ahead of Phantom Centaur's shield below
- `crates/engine/tests/game.rs:50473` — CR 120.3c/306.9: damage to a planeswalker is removed as loyalty counters, not marked.
- `crates/engine/tests/game.rs:60392` — swept alongside creatures, taking loyalty loss instead of marked damage (CR 120.3c/306.9).

### CR 307

- `crates/engine/src/cast.rs:1931` — sorcery speed only (CR 601.3e/307), no flash.
- `crates/engine/src/playable.rs:3` — Primary: CR 601 (timing and zone legality), CR 307 (sorcery speed), flash and
- `crates/engine/tests/game.rs:65202` — CR 306/307: a permanent's card types union — Doomwake Giant is an Enchantment

### CR 307.1

- `crates/engine/tests/game.rs:18100` — sorcery-speed spells stay sorcery-only (CR 307.1) even in the reaction window.

### CR 307.1a

- `crates/engine/tests/game.rs:49058` — resolve_top_of_stack(game); // the underlying instant resolves, emptying the stack (CR 307.1a

### CR 400

- `crates/engine/src/zones.rs:2` — Primary: CR 400 (zones), CR 121 (drawing a card), CR 106.4 (mana pool).

### CR 400.3

- `crates/engine/src/pending/raise/library.rs:68` — if (CR 400.3) one ever does, slots are filled hand→bottom→exile in priority order and

### CR 400.4

- `crates/engine/src/pending/handlers/library.rs:458` — same library-to-exile choke a battlefield/graveyard exile uses (CR 400.4).

### CR 400.7

- `crates/engine/src/apply.rs:1317` — as it enters and never cleared at cleanup (resets with the object per CR 400.7).
- `crates/engine/src/apply.rs:1336` — token enters and never cleared at cleanup (resets with the object per CR 400.7).
- `crates/engine/src/apply.rs:1365` — An *indefinite* rewrite (CR 400.7) disarms any revert already armed on this
- `crates/engine/src/lib.rs:184` — new object (CR 400.7) and rightly won't match. Reset alongside
- `crates/engine/src/pending/handlers/edict.rs:236` — (CR 706/707.2; permanent, CR 400.7). `None` declines the "you may" and converts nothing.
- `crates/engine/src/pending/raise/optional.rs:141` — prior effect may have already moved one out (CR 400.7). No survivor skips the pause.
- `crates/engine/src/query.rs:1026` — for (CR 400.7).
- `crates/engine/src/resolution/zones.rs:35` — Flicker (CR 400.7 — a new object, Momentary Blink/Mistmeadow Witch): exile the
- `crates/engine/src/state.rs:119` — queried host (and object ids retire on zone change, CR 400.7), the same rationale the three
- `crates/engine/src/state.rs:154` — (CR 400.7), so the two stack→graveyard chokes
- `crates/engine/src/state.rs:183` — ponytail: entries are never removed. Object ids retire on zone change (CR 400.7), so a stale
- `crates/engine/src/types/card.rs:1770` — because a permanent that leaves the battlefield becomes a new object (CR 400.7). Runtime
- `crates/engine/src/types/card.rs:1778` — cleared at cleanup (resets with the object per CR 400.7).
- `crates/engine/src/types/filter.rs:500` — same-zone reorder, not a zone change (CR 400.7) — the card never leaves the library, so it
- `crates/engine/src/types/stack.rs:2324` — the object per CR 400.7), only when an until-end-of-turn copy reverts its `def`.
- `crates/engine/src/types/stack.rs:2354` — cleanup — it resets only when the object leaves the battlefield (CR 400.7). Public
- `crates/engine/src/types/stack.rs:2377` — cleanup (resets with the object per CR 400.7). A narrow base-P/T-only sibling of
- `crates/engine/src/types/stack.rs:2391` — indefinite (resets only when the object leaves the battlefield, CR 400.7). A copy is public
- `crates/engine/src/types/stack.rs:2868` — A flicker (CR 400.7 — a new object, [`Effect::Zone(ZoneEffect::FlickerTarget)`]/
- `crates/engine/src/types/stack.rs:2911` — identity (CR 400.7 mints a new object only on a zone change).
- `crates/engine/src/zones.rs:116` — Same-zone reorder, not a zone change (CR 400.7) — `card` keeps its object id.
- `crates/engine/tests/game.rs:19013` — copies are permanent (CR 400.7 — resets only on leaving).
- `crates/engine/tests/game.rs:83979` — ── Flicker (CR 400.7 — a new object): #147 ─────────────────────────────────────────────
- `crates/engine/tests/game.rs:83982` — (CR 400.7) re-fires it, without `TWO_ETB`'s incidental simultaneous-trigger-ordering pause
- `crates/engine/tests/game.rs:84033` — "the returned permanent is a new object (CR 400.7), not the same one that left"
- `crates/engine/tests/game.rs:84083` — assert_ne!(flickered, target, "returns as a new object (CR 400.7)");
- `crates/engine/tests/game.rs:99840` — leaves the battlefield and returns is a new object (CR 400.7) and starts unmonstrous again.

### CR 400.10a

- `crates/engine/src/apply.rs:2215` — The "exiled with" pattern (CR 400.10a): record the link — read back by
- `crates/engine/src/state.rs:342` — The "exiled with" pattern (CR 400.10a): each entry is `(source, exiled)` — a card exiled
- `crates/engine/src/types/card.rs:1333` — (CR 400.10a).
- `crates/engine/src/types/stack.rs:2829` — [`Event::MovedToCommandZone`] in the same batch) was exiled "with" `source` (CR 400.10a),
- `crates/engine/tests/game.rs:58065` — ── Source-linked exile pile (CR 400.10a "exiled with") — Currency Converter ────────

### CR 402.2

- `crates/engine/src/characteristics.rs:1987` — Whether `player` has no maximum hand size (CR 402.2): true if any permanent they control
- `crates/engine/src/priority.rs:1806` — A controlled "no maximum hand size" static (CR 402.2, e.g. Reliquary Tower)
- `crates/engine/tests/game.rs:59374` — A test-only permanent granting its controller "no maximum hand size" (CR 402.2) —

### CR 402.5

- `crates/engine/src/pending/handlers/combat.rs:44` — Chain to the next multi-blocked attacker's division, or hand back priority. (CR 117, CR 402.5, CR 5…
- `crates/engine/src/pending/raise/library.rs:69` — any excess slot (CR 117, CR 406.5, CR 402.5) is silently dropped (CR 120.3-style "as
- `crates/engine/src/query.rs:117` — Still offer cycling from hand even when the card isn't otherwise playable (CR 702.28, CR 402.5)
- `crates/engine/tests/game.rs:3609` — Hand P1 priority (still P0's turn, empty stack — one pass just hands off, doesn't end the step). (C…
- `crates/engine/tests/game.rs:14689` — Resolve a creature onto the battlefield and hand priority back to player 0. (CR 117, CR 402.5, CR 4…
- `crates/engine/tests/game.rs:28631` — .unwrap(); // hand priority to P1 to respond (CR 117, CR 402.5)
- `crates/engine/tests/game.rs:48995` — A qualifying MV-5+ instant cast via flashback — from the graveyard, not the hand. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:53453` — has from the hand, not a permanent's activated ability. (CR 602, CR 402.5, CR 113)
- `crates/engine/tests/game.rs:61697` — One pass from the cycler must hand priority back — not resolve the stack. (CR 117, CR 402.5, CR 405)
- `crates/engine/tests/game.rs:70195` — Hand priority to P1 (still P0's turn, empty stack) so P1 can cast the instant. (CR 117, CR 402.5, C…
- `crates/engine/tests/game.rs:70631` — Priority reverts to the active player (P1) once the stack empties — hand it back to P0. (CR 117, CR…
- `crates/engine/tests/game.rs:73675` — twice (Primo itself, and the `ZERO_POWER_WITH_COUNTER` fixture). (CR 117, CR 402.5, CR 405)

### CR 403.5

- `crates/engine/src/apply.rs:2065` — orphan-to-graveyard SBA hasn't run yet, so it's still attached right now. Read (CR 704, CR 303.4, C…
- `crates/engine/src/triggers.rs:487` — A creature dying (battlefield → graveyard) fires its own Dies trigger. (CR 603.6, CR 403.5, CR 603)
- `crates/engine/src/types/card.rs:830` — zone tags — defer until one exists. (CR 603, CR 108.4, CR 403.5)
- `crates/engine/src/types/effect/shared.rs:11` — match arm with no separate pure path. (CR 602, CR 403.5, CR 601)
- `crates/engine/tests/game.rs:741` — Same body, but no flashback — an ordinary sorcery in the graveyard can't be cast. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:4721` — power 3 deals 3 combat damage, so X = 3: a mana-value-3 graveyard creature is a legal (CR 510, CR 4…
- `crates/engine/tests/game.rs:9171` — Shock the 1/1 token: lethal, so the SBA moves it off the battlefield. (CR 704, CR 111, CR 403.5)
- `crates/engine/tests/game.rs:13616` — 3 >= toughness 2, so the SBA already moved the bear to the graveyard. (CR 704, CR 403.5)
- `crates/engine/tests/game.rs:14689` — Resolve a creature onto the battlefield and hand priority back to player 0. (CR 117, CR 402.5, CR 4…
- `crates/engine/tests/game.rs:14758` — Shock marks 2 damage; 2 >= toughness 2, so the SBA moves the bear to the graveyard. (CR 704, CR 403…
- `crates/engine/tests/game.rs:22944` — flag (Llanowar Elves' mana ability, sitting in the graveyard) can't activate anything either. (CR 6…
- `crates/engine/tests/game.rs:32437` — the same SBA sweep. Deaths mint graveyard cards first, then `PlayerLost` tombstones (CR 704, CR 403…
- `crates/engine/tests/game.rs:39301` — min: 0 lets the controller decline — nothing enters the battlefield. (CR 603.6a, CR 108.3, CR 403.5)
- `crates/engine/tests/game.rs:48995` — A qualifying MV-5+ instant cast via flashback — from the graveyard, not the hand. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:49274` — fires on a flashback cast from the graveyard — proves the new gate is opt-in. (CR 702.34, CR 403.5,…
- `crates/engine/tests/game.rs:50302` — resolve_top_of_stack(&mut game); // the planeswalker resolves and enters the battlefield (CR 603.6a…
- `crates/engine/tests/game.rs:57664` — trigger pauses on the mode choice. Returns the dragon's controller. (CR 108.3, CR 403.5, CR 603)
- `crates/engine/tests/game.rs:61277` — ability — it keeps priority in Main1 — so it triggers a refresh without changing the phase. (CR 117…

### CR 404

- `crates/engine/src/query.rs:1329` — Ids of every card in `player`'s graveyard (CR 404). The graveyard twin of

### CR 405

- `crates/engine/src/cast.rs:156` — our single-pass casting; grow the trigger form if ward-on-the-stack interaction is needed. (CR 702.…
- `crates/engine/src/cast.rs:2551` — exempts a mana ability from the stack, not from choices made while it resolves, so (CR 605, CR 405,…
- `crates/engine/src/cast.rs:2575` — Mana abilities resolve immediately — no stack, no priority change. Goes through (CR 117, CR 405, CR…
- `crates/engine/src/lib.rs:2` — Primary: CR 117 (priority), CR 405 (stack), CR 903 (Commander).
- `crates/engine/src/pending/handlers/optional.rs:43` — still-resolving spell rather than going on the stack as a new triggered ability — (CR 603, CR 405, …
- `crates/engine/src/triggers.rs:688` — going on the stack at the next priority window (the following upkeep) rather (CR 117, CR 405, CR 50…
- `crates/engine/src/types/card.rs:457` — of a several-ability simultaneous group grows from a real card (see wire-protocol-and-visibility sp…
- `crates/engine/tests/game.rs:3609` — Hand P1 priority (still P0's turn, empty stack — one pass just hands off, doesn't end the step). (C…
- `crates/engine/tests/game.rs:11793` — The colored mode (ability 1) is a mana ability — it produces immediately, uses no stack and (CR 605…
- `crates/engine/tests/game.rs:17162` — Sacrifice one Treasure for its own mana ability (a mana ability, no stack — avoids (CR 605, CR 405,…
- `crates/engine/tests/game.rs:22928` — resolve_top_of_stack(&mut game); // resolve the activated ability off the stack (CR 602, CR 405, CR…
- `crates/engine/tests/game.rs:25188` — Resolve the whole stack (each item in turn) by passing priority. (CR 117, CR 405, CR 500)
- `crates/engine/tests/game.rs:33033` — generalization of [`resolve_top_of_stack`] (which hardcodes two passes for a 2-player game). (CR 11…
- `crates/engine/tests/game.rs:51569` — unrelated stack item) — drain the whole stack, not just the indestructible grant. (CR 702.12, CR 40…
- `crates/engine/tests/game.rs:59020` — leaves the spell on the stack (P0 keeps priority) so the next cast stacks on top, then the (CR 117,…
- `crates/engine/tests/game.rs:61600` — Put a spell on the stack and pass so P1 holds priority. (CR 117, CR 405, CR 601)
- `crates/engine/tests/game.rs:61697` — One pass from the cycler must hand priority back — not resolve the stack. (CR 117, CR 402.5, CR 405)
- `crates/engine/tests/game.rs:70195` — Hand priority to P1 (still P0's turn, empty stack) so P1 can cast the instant. (CR 117, CR 402.5, C…
- `crates/engine/tests/game.rs:70631` — Priority reverts to the active player (P1) once the stack empties — hand it back to P0. (CR 117, CR…
- `crates/engine/tests/game.rs:73675` — twice (Primo itself, and the `ZERO_POWER_WITH_COUNTER` fixture). (CR 117, CR 402.5, CR 405)

### CR 405.5

- `crates/engine/tests/game.rs:84534` — resolves once *every* seat has passed in succession (CR 405.5, CR 117.4).

### CR 406

- `crates/engine/tests/game.rs:26719` — ── Vengeful Rebirth's self-exile rider (CR 406, exile_self_on_resolve) ──────────────

### CR 406.5

- `crates/engine/src/pending/raise/library.rs:69` — any excess slot (CR 117, CR 406.5, CR 402.5) is silently dropped (CR 120.3-style "as
- `crates/engine/src/playable.rs:570` — Wrong or missing delve/escape exile picks — not a mana shortfall. (CR 702.19, CR 702.66, CR 406.5)
- `crates/engine/tests/game.rs:32565` — Chains of Custody: same ETB exile clause as Sheltered by Ghosts, alongside its ward {2} (CR 702.21,…
- `crates/engine/tests/game.rs:57763` — The real pool card: its dies trigger is a "choose one" over impulse-exile and three (CR 603.6, CR 4…

### CR 500

- `crates/engine/src/apply.rs:563` — flashback nor escape can only be its once-per-turn permission — flashback/escape (CR 702.34, CR 702…
- `crates/engine/src/lib.rs:529` — diffing — revisit only if profiling ever shows this hot. (CR 117, CR 601, CR 500)
- `crates/engine/src/resolution/misc.rs:58` — Alchemist's Refuge: "You may cast spells this turn as though they had flash." (CR 702.8, CR 601, CR…
- `crates/engine/src/resolution/misc.rs:59` — ponytail: resolved as a one-shot turn-flag set (`Player::flash_permission_this_turn`) (CR 500)
- `crates/engine/tests/game.rs:75` — Everyone passes priority until the active player changes (one whole turn elapses). (CR 117, CR 500)
- `crates/engine/tests/game.rs:9846` — Tajic, Legion's Edge: "{R}{W}: Tajic gains first strike until end of turn." (CR 702.7, CR 500)
- `crates/engine/tests/game.rs:12441` — Concede is legal without priority — you may quit on someone else's turn. (CR 117, CR 500, CR 104.3a)
- `crates/engine/tests/game.rs:14410` — ability_index: 0, // {G}{U}, {T}: grant flash permission this turn (CR 702.8, CR 500)
- `crates/engine/tests/game.rs:23932` — still P0's turn, so P1 needs priority via a pass, and P1's spell must be castable then.) (CR 117, C…
- `crates/engine/tests/game.rs:25188` — Resolve the whole stack (each item in turn) by passing priority. (CR 117, CR 405, CR 500)
- `crates/engine/tests/game.rs:28371` — Roll forward until P0 holds priority again during P1's turn — the exact moment doesn't (CR 117, CR …
- `crates/engine/tests/game.rs:30123` — +2/+1 until end of turn" activated ability — proves the `granted_ability` field is wired, (CR 602, …
- `crates/engine/tests/game.rs:51547` — indestructible until end of turn." #72 self-target-spec: the grant always lands on (CR 702.12, CR 6…
- `crates/engine/tests/game.rs:55224` — Toxic Deluge cast for X=2 gives every creature -2/-2 until end of turn: a 2/2 dies to an SBA, (CR 7…
- `crates/engine/tests/game.rs:59228` — can't be used here because it `unwrap`s each pass and a pause makes further passes illegal. (CR 117…
- `crates/engine/tests/game.rs:59340` — The turn actually rolled on: player 1 is now active, paused at their upkeep's priority (CR 117, CR …
- `crates/engine/tests/game.rs:62411` — A non-capped activated ability is unaffected by another permanent's once-per-turn cap. (CR 602, CR …
- `crates/engine/tests/game.rs:68655` — Silkguard (soc): "...modified creatures you control gain hexproof until end of turn. (CR 702.11, CR…
- `crates/engine/tests/game.rs:71646` — creatures you control gain trample and get +X/+X until end of turn, where X is the (CR 702, CR 108.…
- `crates/engine/tests/game.rs:73151` — "can't have hexproof or shroud": a fresh grant applied the same turn is filtered right (CR 702.11, …
- `crates/engine/tests/game.rs:76625` — turn"). Casting still requires priority, so roll to P0's main phase next turn. (CR 117, CR 601, CR …

### CR 500.4

- `crates/engine/src/apply.rs:1806` — both pools (some persistent mana may already have been spent), CR 500.4's
- `crates/engine/src/types/card.rs:112` — Whether this step is one of combat's five (CR 500.4/601.3e — begin combat through end of
- `crates/engine/src/types/mana.rs:717` — This pool capped at `cap`, per bucket (CR 500.4's "until end of turn" mana exception,
- `crates/engine/src/types/stack.rs:2710` — mid-turn step/phase change — "until end of turn" persistent mana (CR 500.4 exception,
- `crates/engine/src/types/stack.rs:2721` — "Until end of turn, you don't lose this mana as steps and phases end" (CR 500.4
- `crates/engine/tests/game.rs:17640` — Tagged mana left unspent when a step/phase ends is cleared with the pool (CR 500.4), so a
- `crates/engine/tests/game.rs:48491` — Mana empties between steps (CR 500.4), so re-fund once there.
- `crates/engine/tests/game.rs:49187` — game.fund_mana(PlayerId(0)); // mana empties between steps (CR 500.4)
- `crates/engine/tests/game.rs:70314` — Floating mana empties at the end of every step/phase (CR 500.4) — fund each round's
- `crates/engine/tests/game.rs:70399` — every remaining step: mana empties at the end of a step/phase (CR 500.4), so the
- `crates/engine/tests/game.rs:78770` — (CR 500.4 exception): the red it adds is still floating after a mid-turn step boundary,
- `crates/engine/tests/game.rs:78820` — "Rousing Refrain's red mana survives a mid-turn step boundary (CR 500.4 exception)"
- `crates/engine/tests/game.rs:78825` — (CR 500.4), and Rousing Refrain's persistent red still empties once the turn actually ends
- `crates/engine/tests/game.rs:89462` — Mana empties at each step's end (CR 500.4), so fund right before paying, not before combat.
- `crates/engine/tests/game.rs:89551` — Mana empties at each step's end (CR 500.4), so fund right before paying.
- `crates/engine/tests/game.rs:91923` — gone too (CR 500.4 — unspent mana empties as steps/phases end).
- `crates/engine/tests/game.rs:91930` — "P0's earlier mana emptied at the next step/phase boundary (CR 500.4)"
- `crates/engine/tests/game.rs:93844` — Mana empties at each step's end (CR 500.4), so fund right before paying.
- `crates/engine/tests/game.rs:96976` — step/phase (CR 500.4), so funding any earlier would be cleared before the payment.

### CR 502

- `crates/engine/src/triggers.rs:686` — grants no priority (CR 502), so — like the every-player flavors above — this

### CR 502.1

- `crates/engine/src/core.rs:114` — Untap has no priority window, so this rolls straight on to the upkeep and stops there. (CR 117, CR …
- `crates/engine/src/state.rs:38` — expiry, the same reasoning `pending_next_cast`'s turn-boundary clear uses. (CR 508.1a, CR 502.1, CR…

### CR 502.2

- `crates/engine/src/de.rs:501` — "You may choose not to untap this during your untap step" (CR 502.2 — Rubinia
- `crates/engine/src/pending/handlers/fanout.rs:595` — Answer a [`PendingChoice::DeclineUntap`] (CR 502.2 — Rubinia Soulsinger's "you may choose
- `crates/engine/src/priority.rs:1557` — "You may choose not to untap this" (CR 502.2 — Rubinia Soulsinger): a tapped
- `crates/engine/src/priority.rs:1588` — Pause on the optional-untap decision (CR 502.2). `advance_step` returns on this so
- `crates/engine/src/types/card.rs:881` — "You may choose not to untap this during your untap step" (CR 502.2 — Rubinia Soulsinger):
- `crates/engine/src/types/stack.rs:252` — Answer a [`PendingChoice::DeclineUntap`] (CR 502.2 — Rubinia Soulsinger's "you may choose
- `crates/engine/src/types/stack.rs:887` — (CR 502.2 — Rubinia Soulsinger). Raised as a turn-based-action pause; answered by
- `crates/engine/tests/game.rs:42655` — "declining to untap leaves Rubinia tapped (CR 502.2)"

### CR 503

- `crates/engine/src/core.rs:114` — Untap has no priority window, so this rolls straight on to the upkeep and stops there. (CR 117, CR …
- `crates/engine/src/triggers.rs:688` — going on the stack at the next priority window (the following upkeep) rather (CR 117, CR 405, CR 50…
- `crates/engine/tests/game.rs:59340` — The turn actually rolled on: player 1 is now active, paused at their upkeep's priority (CR 117, CR …

### CR 505

- `crates/engine/src/types/trigger.rs:156` — At the beginning of the controller's first (precombat) main phase (CR 505/508 —

### CR 505.1a

- `crates/engine/src/apply.rs:557` — CR 505.1a/505.1b: read off ambient timing state (like `cast_from_hand` above),
- `crates/engine/src/apply.rs:661` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/apply.rs:746` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/apply.rs:949` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/core.rs:857` — main phase (CR 505.1a/505.1b — [`Spell::cast_during_main_phase`]), `false` if `id` isn't a
- `crates/engine/src/types/card.rs:1546` — (CR 505.1a/505.1b) — Sulfurous Blast's "If you cast this spell during your main phase..."

### CR 505.1b

- `crates/engine/src/apply.rs:557` — CR 505.1a/505.1b: read off ambient timing state (like `cast_from_hand` above),
- `crates/engine/src/apply.rs:661` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/apply.rs:746` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/apply.rs:949` — CR 505.1a/505.1b: same ambient-timing read `Event::SpellCast` uses above.
- `crates/engine/src/core.rs:857` — main phase (CR 505.1a/505.1b — [`Spell::cast_during_main_phase`]), `false` if `id` isn't a
- `crates/engine/src/types/card.rs:1546` — (CR 505.1a/505.1b) — Sulfurous Blast's "If you cast this spell during your main phase..."

### CR 506

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).
- `crates/engine/src/combat.rs:875` — but assigns no damage to it (CR 510.1a). Its *blockers* still deal theirs below. (CR 702.7, CR 506)
- `crates/engine/src/priority.rs:1454` — Skip the first-strike combat damage step unless a first/double striker is in (CR 510, CR 120.3, CR …
- `crates/engine/src/priority.rs:1456` — window, so with no first strikers there's exactly one combat damage step. (CR 510, CR 120.3, CR 506)
- `crates/engine/src/resolution/damage.rs:12` — being marked; everything else marks damage as usual (CR 120.3/506).
- `crates/engine/src/resolution/misc.rs:34` — combat damage to a player this combat (see `Game::fire_combat_damage_watch_triggers`). (CR 510, CR …
- `crates/engine/src/triggers.rs:967` — Damage marked on a creature (CR 120.3/506) — `Game::deal_creature_damage` is the
- `crates/engine/src/triggers.rs:1111` — `events` directly (no scratch accumulator needed — combat damage is already fully (CR 510, CR 120.3…
- `crates/engine/tests/game.rs:3950` — first-strike combat damage step, one blocker being Blood Artist. The look-back lets the (CR 510, CR…
- `crates/engine/tests/game.rs:3986` — Both blockers were dead before the regular combat damage step, so the attackers live. (CR 510, CR 1…
- `crates/engine/tests/game.rs:4149` — player, draw a card." An unblocked attacker draws; a blocked attacker deals combat damage (CR 510, …
- `crates/engine/tests/game.rs:4176` — Leitmotif Composer (soc): "Whenever this creature deals combat damage to a player, draw a (CR 510, …
- `crates/engine/tests/game.rs:4450` — CR 120.3/506 noncombat alike) — an attacking dragon blocked by a tough creature marks
- `crates/engine/tests/game.rs:4718` — Venerable Warsinger (soc): "...Whenever this creature deals combat damage to a player, (CR 510, CR …
- `crates/engine/tests/game.rs:15140` — Roll forward until combat damage is dealt; keep the events of the submit that kills the lord. (CR 5…
- `crates/engine/tests/game.rs:18440` — enchanted *blocker* takes no combat damage from the creature it blocks. (CR 510, CR 120.3, CR 506)
- `crates/engine/tests/game.rs:30774` — resolve_top_of_stack(&mut game); // the enchanted-creature-attacks drain resolves (before combat da…
- `crates/engine/tests/game.rs:75180` — "Whenever that creature deals combat damage to a player this combat, this creature (CR 510, CR 120.…
- `crates/engine/tests/game.rs:75182` — trigger, not a static ability — only *that* creature dealing combat damage arms it. (CR 510, CR 120…
- `crates/engine/tests/game.rs:75205` — A creature that never got the deathtouch grant dealing combat damage doesn't arm the (CR 510, CR 12…
- `crates/engine/tests/game.rs:75263` — deathtoucher's now-legitimate combat damage is the only thing that could wrongly prepare (CR 510, C…

### CR 506.2

- `crates/engine/src/types/stack.rs:6` — What an attacking creature is attacking (CR 506.2/508.1a): the defending player, or a
- `crates/engine/tests/game.rs:50521` — ── Planeswalkers as attack defenders (CR 506.2/508.1a, damage redirect CR 120.3c/306.8) ──

### CR 506.2c

- `crates/engine/src/types/stack.rs:7` — planeswalker that player controls. (Battles — CR 506.2c — have no card in the pool yet; add a

### CR 506.4

- `crates/engine/src/apply.rs:2655` — Drop `object` from the current combat's attacker and blocker lists (CR 506.4) — shared by
- `crates/engine/src/types/stack.rs:2234` — A permanent was removed from combat (CR 506.4 — [`Effect::Control(ControlEffect::RemoveFromCombat)`…

### CR 506.4c

- `crates/engine/src/apply.rs:1454` — CR 506.4c: any time a permanent's controller changes, it's removed from combat.
- `crates/engine/src/apply.rs:1470` — CR 506.4c: any time a permanent's controller changes, it's removed from combat
- `crates/engine/src/apply.rs:1483` — CR 506.4c: any time a permanent's controller changes, it's removed from combat.
- `crates/engine/src/apply.rs:2659` — [`Event::ConditionedControlGained`] — CR 506.4c, Goblin Cadets' "(This removes this
- `crates/engine/tests/game.rs:90898` — "CR 506.4c: the control change removes it from combat"

### CR 507

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).

### CR 508

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).
- `crates/engine/src/combat.rs:621` — attack at all ("if able") is simply not required — the same escape hatch goad uses. (CR 702.19, CR …
- `crates/engine/src/pending/handlers/combat.rs:44` — Chain to the next multi-blocked attacker's division, or hand back priority. (CR 117, CR 402.5, CR 5…
- `crates/engine/src/types/trigger.rs:156` — At the beginning of the controller's first (precombat) main phase (CR 505/508 —
- `crates/engine/tests/game.rs:3891` — H2 repro: a first-strike 2/2 attacks a player at 2 life. The between-substeps SBA sweep (CR 704, CR…
- `crates/engine/tests/game.rs:30973` — Resolve the enchanted-creature-attacks trigger (3-player: pass priority all the way around). (CR 11…
- `crates/engine/tests/game.rs:41144` — Tapped ⇒ can't attack ⇒ goad's "if able" clause doesn't require it. (CR 701.38, CR 508)
- `crates/engine/tests/game.rs:41184` — Summoning-sick (no haste) ⇒ can't attack ⇒ not forced by goad. (CR 701.38, CR 508)

### CR 508.1

- `crates/engine/src/apply.rs:1109` — this turn" (Agent Frank Horrigan's indestructible grant, CR 508.1) both
- `crates/engine/src/apply.rs:1499` — CR 508.1: turn-scoped "attacked this turn" flag (`Condition::SourceAttackedThisTurn`)
- `crates/engine/src/characteristics_cache.rs:210` — Horrigan's indestructible grant, CR 508.1) — same board-wide turn-boundary
- `crates/engine/src/combat.rs:717` — triggers (CR 508.1, "attack with two or more creatures"), rather than per single
- `crates/engine/src/de.rs:1811` — firing path (CR 508.1) — Fractal Harness's "whenever equipped creature attacks". The
- `crates/engine/src/triggers.rs:2198` — Queue the batch attack-count triggers (CR 508.1, "attack with two or more creatures"):
- `crates/engine/src/triggers.rs:2208` — watcher's own controller controls (CR 508.1, Killian, Decisive Mentor's second ability).
- `crates/engine/src/triggers.rs:2272` — "Whenever a creature attacks" (CR 508.1, Righteous Cause): once per attacker, any
- `crates/engine/src/triggers.rs:2350` — Queue attached-permanent attack triggers (CR 508.1, the Impetus cycle — and CR 301.5g's
- `crates/engine/src/types/card.rs:25` — What a creature is attacking (CR 508.1): a player, or a planeswalker an opponent controls.
- `crates/engine/src/types/card.rs:1689` — Whether this permanent was declared as an attacker this turn (CR 508.1, [`Event::AttackerDeclared`]…
- `crates/engine/src/types/trigger.rs:192` — combat, regardless of defender (CR 508.1, Firemane Commando's "whenever you attack with
- `crates/engine/src/types/trigger.rs:198` — control) with `at_least` or more creatures this combat (CR 508.1, Mangara/Tomik's "an
- `crates/engine/src/types/trigger.rs:211` — controller* (CR 508.1, Firemane Commando's second ability: "whenever another player
- `crates/engine/src/types/trigger.rs:220` — Whenever a creature attacks (CR 508.1, Righteous Cause) — any controller, any defender,
- `crates/engine/src/types/trigger.rs:228` — Whenever the creature this Aura is attached to is declared as an attacker (CR 508.1, the
- `crates/engine/tests/game.rs:30626` — ── Enchanted-creature-attacks triggers (the Impetus cycle, CR 508.1) ────────────────
- `crates/engine/tests/game.rs:49925` — ── Batch attack-count triggers (CR 508.1, "attack with two or more creatures") ──────
- `crates/engine/tests/game.rs:94812` — ── "Whenever a creature attacks" watch-others trigger (CR 508.1, Righteous Cause) ────
- `crates/engine/tests/game.rs:97843` — `Condition::SourceAttackedThisTurn` is a turn-scoped flag (CR 508.1), not

### CR 508.1a

- `crates/engine/src/combat.rs:121` — What a declared attacker is attacking (CR 508.1a), or `None` if the defending player has
- `crates/engine/src/combat.rs:132` — planeswalker (CR 508.1a). `None` once the planeswalker has left the battlefield, which is
- `crates/engine/src/combat.rs:413` — it isn't a legal attack target (CR 508.1a): the defending player must be a living opponent,
- `crates/engine/src/combat.rs:496` — opponent's planeswalkers (CR 508.1a); each taps unless it has vigilance.
- `crates/engine/src/combat.rs:513` — Every attack resolved to its defending player (CR 508.1a) — the form every restriction
- `crates/engine/src/combat.rs:617` — Must-attack requirements (CR 508.1a — Furygale Flocking's minted tokens "attack that
- `crates/engine/src/combat.rs:641` — their planeswalkers doesn't discharge the requirement (CR 508.1a).
- `crates/engine/src/combat.rs:647` — A global "creatures attack each combat if able" static (CR 508.1a — Avatar of
- `crates/engine/src/combat.rs:1025` — Deal `amount` combat damage from `source` to whatever it's attacking (CR 508.1a): the
- `crates/engine/src/core.rs:944` — Each declared attacker paired with what it is attacking (CR 508.1a).
- `crates/engine/src/core.rs:965` — Who makes this turn's attack declaration — the active player (CR 508.1a) unless a live
- `crates/engine/src/resolution/tokens.rs:40` — 1v1 game; with more opponents, still just the first one found — CR 508.1a).
- `crates/engine/src/state.rs:32` — "Attacks … this turn if able" (CR 508.1a — Furygale Flocking's minted tokens): each entry
- `crates/engine/src/state.rs:38` — expiry, the same reasoning `pending_next_cast`'s turn-boundary clear uses. (CR 508.1a, CR 502.1, CR…
- `crates/engine/src/state.rs:63` — "You choose which creatures attack this turn" (CR 508.1a — Master Warcraft): the player who
- `crates/engine/src/triggers.rs:471` — Every attack watcher is scoped to the defending *player* (CR 508.1a), which
- `crates/engine/src/types/stack.rs:6` — What an attacking creature is attacking (CR 506.2/508.1a): the defending player, or a
- `crates/engine/src/types/stack.rs:219` — The active player declares attackers, each attacking a chosen defender (CR 508.1a — a
- `crates/engine/src/types/stack.rs:2449` — A creature was declared as an attacker (CR 508.1a). `defender` is always the *defending
- `crates/engine/src/types/stack.rs:2488` — The creature `object` must attack `defender` this turn if able (CR 508.1a "attacks … if
- `crates/engine/src/types/trigger.rs:72` — Whenever this creature attacks or blocks (Mana-Charged Dragon, CR 508.1a / CR 509.3a) —
- `crates/engine/src/types/trigger.rs:207` — defending *player*, which an attack on one of their planeswalkers resolves to (CR 508.1a).
- `crates/engine/tests/game.rs:41265` — ── "Attacks this turn if able" requirement (CR 508.1a — Furygale Flocking) ───────────
- `crates/engine/tests/game.rs:41581` — ── Choose an opponent at random, must attack (CR 508.1a — Ruhan of the Fomori) ──────
- `crates/engine/tests/game.rs:41601` — same must-attack way Furygale Flocking's tokens are (CR 508.1a "if able").
- `crates/engine/tests/game.rs:43841` — same turn (CR 702.10b haste + slice-1's combat gate, CR 508.1a — attackers are the active
- `crates/engine/tests/game.rs:44023` — controller the same turn (CR 702.10b haste + slice-1's combat gate, CR 508.1a — attackers are
- `crates/engine/tests/game.rs:50521` — ── Planeswalkers as attack defenders (CR 506.2/508.1a, damage redirect CR 120.3c/306.8) ──
- `crates/engine/tests/game.rs:50525` — CR 508.1a: each attacking creature chooses a defending player, a planeswalker that player
- `crates/engine/tests/game.rs:50571` — CR 508.1a: the defender must be an opponent's planeswalker — your own is never a legal
- `crates/engine/tests/game.rs:93217` — is aimed at a planeswalker (CR 508.1a), that's the planeswalker's controller.
- `crates/engine/tests/game.rs:95898` — ── Global combat-static anthems (CR 508.1a, CR 702.4b — Avatar of Slaughter) ────────
- `crates/engine/tests/game.rs:96899` — ── Mana-Charged Dragon: "attacks or blocks" join forces pump (CR 508.1a, CR 509.3a) ──
- `crates/engine/tests/game.rs:97021` — ── Master Warcraft: someone else declares this turn's attacks and blocks (CR 508.1a, CR 509.1a) ──

### CR 508.1g

- `crates/engine/src/combat.rs:4` — Also: CR 701.38 (goad), CR 508.1g (attack costs / pillow-fort).
- `crates/engine/src/combat.rs:279` — "If able" is gated on the attack tax (CR 508.1g): only defenders whose tax `player`
- `crates/engine/src/combat.rs:344` — The total generic mana `declarer` must pay to declare `attackers` (CR 508.1g), summed across
- `crates/engine/src/combat.rs:387` — `defender` (CR 508.1g), evaluated in isolation via [`Game::plan_auto_taps`] — the exact
- `crates/engine/src/combat.rs:394` — declaration itself. (CR 508.1g, CR 701.38)
- `crates/engine/src/combat.rs:592` — "If able" includes affording the tax to reach a legal defender (CR 508.1g + CR
- `crates/engine/src/combat.rs:665` — Pillow-fort attack taxes (CR 508.1g / CR 802, Ghostly Prison): the sum owed across the
- `crates/engine/src/combat.rs:669` — declaring; can't-afford ⇒ illegal declaration (CR 508.1g), rather than offered as an
- `crates/engine/src/combat.rs:674` — afford. (CR 701.38, CR 508.1g)
- `crates/engine/tests/game.rs:3112` — ── Attack tax / pillow-fort statics (CR 508.1g declare-attackers costs, Ghostly Prison) ──
- `crates/engine/tests/game.rs:41194` — CR 701.38a "if able" + CR 508.1g: a goaded creature must attack, but only if its controller

### CR 508.4

- `crates/engine/src/pending/handlers/library.rs:583` — that opponent (CR 508.4, reusing the token put-in-attacking path) and keeps it.
- `crates/engine/src/pending/handlers/library.rs:618` — Kaalia: the creature enters attacking the opponent Kaalia attacks — CR 508.4, not a
- `crates/engine/src/resolution/tokens.rs:140` — attacker against the baked defender — CR 508.4, not a declared attack,
- `crates/engine/src/resolution/tokens.rs:321` — never `AttackerDeclared` — CR 508.4, so a minted copy can't re-trigger myriad), then
- `crates/engine/src/resolution/tokens.rs:365` — the attacker's own battlefield for the matching tokens (CR 508.4: each mint enters
- `crates/engine/src/types/effect/choice.rs:197` — enters tapped and attacking that opponent (CR 508.4).
- `crates/engine/src/types/effect/shared.rs:2120` — put-in creature tapped and attacking it (CR 508.4).
- `crates/engine/src/types/effect/shared.rs:2130` — under the attacker and enters tapped and attacking it, per CR 508.4.
- `crates/engine/src/types/stack.rs:1449` — set, enters the put-in creature tapped and attacking that opponent (Kaalia, CR 508.4).
- `crates/engine/src/types/stack.rs:2461` — Calligrapher's minted Inkling), *not* via the declare-attackers step. CR 508.4: such a
- `crates/engine/tests/game.rs:10289` — Regression: the token's own entry does not re-fire Combat Calligrapher (CR 508.4 — a
- `crates/engine/tests/game.rs:10389` — Regression: a minted copy's own entry does not re-trigger Redoubled (CR 508.4 — a token
- `crates/engine/tests/game.rs:10798` — never re-triggers `Trigger::Attacks` (CR 508.4) — no infinite loop / extra copies.

### CR 509

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).
- `crates/engine/src/combat.rs:767` — Goblin Cadets' "whenever this creature blocks or becomes blocked" (CR 509): scan the
- `crates/engine/src/triggers.rs:2306` — Queue [`Trigger::BlocksOrBecomesBlocked`] (Goblin Cadets, CR 509): `blocks` is one
- `crates/engine/src/types/trigger.rs:65` — Whenever this creature blocks or becomes blocked (Goblin Cadets, CR 509/CR 509.1h): fires
- `crates/engine/tests/game.rs:90736` — Gomazoa / Goblin Cadets (CR 509, #221): a block-assignment read ("each creature it's blocking",

### CR 509.1a

- `crates/engine/src/combat.rs:11` — `player` (CR 509.1a): an untapped creature of `player`'s, against a declared attacker
- `crates/engine/src/combat.rs:30` — CR 509.1a: blockers are the defending *controller*'s creatures — a creature you've
- `crates/engine/src/combat.rs:39` — "This creature can't block" (CR 509.1a — Bloodghast): never a legal blocker.
- `crates/engine/src/combat.rs:43` — Global "[filter] creatures can't block" (CR 509.1a — Razorjaw Oni's "Black creatures
- `crates/engine/src/combat.rs:65` — that controller's planeswalkers (CR 509.1a: the defending player declares the blocks).
- `crates/engine/src/combat.rs:151` — through a planeswalker they control (CR 509.1a: either way they declare the blocks).
- `crates/engine/src/combat.rs:239` — so a restriction beats a requirement (CR 509.1a) — a creature that can't attack
- `crates/engine/src/combat.rs:453` — [`Effect::Static(StaticEffect::CantBlockFilter)`] static matching `blocker` (CR 509.1a —
- `crates/engine/src/combat.rs:528` — Attack-restriction statics (CR 509.1a — Combat Calligrapher, Eriette of the Charmed
- `crates/engine/src/combat.rs:734` — controller, so no one gains a block they couldn't otherwise make (CR 509.1a).
- `crates/engine/src/combat.rs:794` — CR 509.1a) or empty; with a live Master Warcraft it's the whole undeclared table for its
- `crates/engine/src/core.rs:973` — Who makes this turn's block declarations — each attacked player for themselves (CR 509.1a)
- `crates/engine/src/state.rs:71` — "You choose which creatures block this turn and how those creatures block" (CR 509.1a —
- `crates/engine/src/types/card.rs:27` — "attacks you" trigger (CR 509.1a) — is the player for `Player`, or the planeswalker's
- `crates/engine/src/types/card.rs:274` — "This creature can't block" (CR 509.1a — Bloodghast is never a legal blocker). Read by
- `crates/engine/tests/game.rs:10111` — Bloodghast: "This creature can't block." (CR 509.1a — it's never a legal blocker.)
- `crates/engine/tests/game.rs:42902` — ── CR 800.4a control-effect timestamp precedence + CR 602.2/509.1a controller gates ────────
- `crates/engine/tests/game.rs:42905` — creature), so one card exercises CR 800.4a timestamp precedence and the CR 602.2/509.1a
- `crates/engine/tests/game.rs:43014` — CR 509.1a: blockers are the *defending controller's* creatures. A stolen creature blocks for
- `crates/engine/tests/game.rs:43052` — "the owner can't block with a creature it no longer controls (CR 509.1a)",
- `crates/engine/tests/game.rs:43058` — .expect("the thief blocks with the stolen creature (CR 509.1a)");
- `crates/engine/tests/game.rs:50599` — CR 509.1a: the defending player (the planeswalker's controller) declares blockers against
- `crates/engine/tests/game.rs:82250` — Jungle Lion: "This creature can't block." (CR 509.1a — never a legal blocker.)
- `crates/engine/tests/game.rs:95972` — ── Global combat-static restriction: black creatures can't block (CR 509.1a — Razorjaw Oni) ──
- `crates/engine/tests/game.rs:95999` — "other", so it applies to its own source too (CR 509.1a), the same way a plain anthem
- `crates/engine/tests/game.rs:96431` — A restriction beats a requirement (CR 509.1a): P0 cast a spell with an opponent's Angelic
- `crates/engine/tests/game.rs:97021` — ── Master Warcraft: someone else declares this turn's attacks and blocks (CR 508.1a, CR 509.1a) ──
- `crates/engine/tests/game.rs:97201` — The seat making the choice moves; the block restrictions don't (CR 509.1a). A creature can

### CR 509.1b

- `crates/engine/src/combat.rs:755` — Menace (CR 509.1b): an attacker with menace must be blocked by two or more creatures.
- `crates/engine/tests/game.rs:7268` — turn." (CR 509.1b, CR 702.10)

### CR 509.1h

- `crates/engine/src/triggers.rs:2310` — keeps a multiply-blocked attacker's "becomes blocked" to a single fire (CR 509.1h). Called
- `crates/engine/src/types/trigger.rs:65` — Whenever this creature blocks or becomes blocked (Goblin Cadets, CR 509/CR 509.1h): fires

### CR 509.3a

- `crates/engine/src/triggers.rs:2330` — Queue [`Trigger::AttacksOrBlocks`]'s block half (Mana-Charged Dragon, CR 509.3a): unlike
- `crates/engine/src/types/trigger.rs:72` — Whenever this creature attacks or blocks (Mana-Charged Dragon, CR 508.1a / CR 509.3a) —
- `crates/engine/tests/game.rs:96899` — ── Mana-Charged Dragon: "attacks or blocks" join forces pump (CR 508.1a, CR 509.3a) ──

### CR 510

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).
- `crates/engine/src/priority.rs:1454` — Skip the first-strike combat damage step unless a first/double striker is in (CR 510, CR 120.3, CR …
- `crates/engine/src/priority.rs:1456` — window, so with no first strikers there's exactly one combat damage step. (CR 510, CR 120.3, CR 506)
- `crates/engine/src/resolution/misc.rs:34` — combat damage to a player this combat (see `Game::fire_combat_damage_watch_triggers`). (CR 510, CR …
- `crates/engine/src/triggers.rs:1111` — `events` directly (no scratch accumulator needed — combat damage is already fully (CR 510, CR 120.3…
- `crates/engine/src/triggers.rs:2872` — single `amount`. (CR 510, CR 111, CR 108.3)
- `crates/engine/src/types/trigger.rs:509` — field the moment a second card needs a different power/type predicate. (CR 510, CR 111, CR 108.3)
- `crates/engine/tests/game.rs:3916` — and players get priority in it before the regular combat damage step. (CR 510, CR 117, CR 120.3)
- `crates/engine/tests/game.rs:3950` — first-strike combat damage step, one blocker being Blood Artist. The look-back lets the (CR 510, CR…
- `crates/engine/tests/game.rs:3986` — Both blockers were dead before the regular combat damage step, so the attackers live. (CR 510, CR 1…
- `crates/engine/tests/game.rs:4122` — Curiosity Crafter (soc): "Whenever a creature token you control deals combat damage to a (CR 510, C…
- `crates/engine/tests/game.rs:4148` — Ohran Frostfang (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR 108…
- `crates/engine/tests/game.rs:4149` — player, draw a card." An unblocked attacker draws; a blocked attacker deals combat damage (CR 510, …
- `crates/engine/tests/game.rs:4176` — Leitmotif Composer (soc): "Whenever this creature deals combat damage to a player, draw a (CR 510, …
- `crates/engine/tests/game.rs:4673` — Defiling Daemogoth (soc): "...Whenever a creature you control deals combat damage to a (CR 510, CR …
- `crates/engine/tests/game.rs:4718` — Venerable Warsinger (soc): "...Whenever this creature deals combat damage to a player, (CR 510, CR …
- `crates/engine/tests/game.rs:4721` — power 3 deals 3 combat damage, so X = 3: a mana-value-3 graveyard creature is a legal (CR 510, CR 4…
- `crates/engine/tests/game.rs:15140` — Roll forward until combat damage is dealt; keep the events of the submit that kills the lord. (CR 5…
- `crates/engine/tests/game.rs:18440` — enchanted *blocker* takes no combat damage from the creature it blocks. (CR 510, CR 120.3, CR 506)
- `crates/engine/tests/game.rs:30774` — resolve_top_of_stack(&mut game); // the enchanted-creature-attacks drain resolves (before combat da…
- `crates/engine/tests/game.rs:73721` — deal combat damage to a player, create a 0/0 green and blue Fractal creature token. Put a (CR 510, …
- `crates/engine/tests/game.rs:75180` — "Whenever that creature deals combat damage to a player this combat, this creature (CR 510, CR 120.…
- `crates/engine/tests/game.rs:75182` — trigger, not a static ability — only *that* creature dealing combat damage arms it. (CR 510, CR 120…
- `crates/engine/tests/game.rs:75205` — A creature that never got the deathtouch grant dealing combat damage doesn't arm the (CR 510, CR 12…
- `crates/engine/tests/game.rs:75263` — deathtoucher's now-legitimate combat damage is the only thing that could wrongly prepare (CR 510, C…

### CR 510.1a

- `crates/engine/src/combat.rs:133` — exactly when its attacker assigns no combat damage (CR 510.1a).
- `crates/engine/src/combat.rs:875` — but assigns no damage to it (CR 510.1a). Its *blockers* still deal theirs below. (CR 702.7, CR 506)

### CR 510.1c

- `crates/engine/src/combat.rs:906` — is attacking if it tramples (CR 510.1c — the player *or* the planeswalker, whichever it
- `crates/engine/src/combat.rs:968` — CR 510.1c / 702.19e: trample overflow is power minus *assigned*, not minus *dealt* — a
- `crates/engine/src/types/card.rs:1555` — division shape combat's [`Event::CombatDamageDivided`] uses (CR 510.1c) — a divided
- `crates/engine/src/types/stack.rs:3367` — How a multi-blocked attacker's combat damage is divided among its blockers (CR 510.1c).
- `crates/engine/tests/game.rs:4036` — CR 510.1c / 702.19e: damage *assigned* to a blocker satisfies trample's "assign at least
- `crates/engine/tests/game.rs:50620` — CR 510.1c: excess damage from a trampling attacker is assigned to the *defender* it's

### CR 510.2

- `crates/engine/src/combat.rs:1001` — CR 510.2: this is combat damage to a creature — a `DealsCombatDamageToCreature`
- `crates/engine/src/combat.rs:1143` — CR 510.2: combat damage to a creature (blocker → attacker) also fires a
- `crates/engine/src/lib.rs:180` — noncombat alike (CR 510.2 / 120.3/506) — feeding
- `crates/engine/src/triggers.rs:915` — Combat damage to a player (CR 510.2) — the combat-damage-to-player watch family
- `crates/engine/src/triggers.rs:937` — Combat damage to a creature (CR 510.2) — still needs the eliminated-source
- `crates/engine/src/triggers.rs:1110` — Primo, the Unbounded's batch combat-damage Fractal trigger (CR 510.2/603.3b): reads
- `crates/engine/src/triggers.rs:2856` — Queue combat-damage-to-a-player watch triggers (CR 510.2): `source` (a creature) just
- `crates/engine/src/types/effect/shared.rs:1935` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's reanimation target bound resolves
- `crates/engine/src/types/effect/shared.rs:1942` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's "its controller may draw" payoff
- `crates/engine/src/types/effect/shared.rs:1949` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's "each other opponent" splash (Hydra
- `crates/engine/src/types/effect/shared.rs:1956` — CR 510.2/603.10a: an `Attacks` trigger's reanimation target bound resolves against the
- `crates/engine/src/types/effect/shared.rs:2853` — dealt to that player") — CR 510.2/603.10a last-known information, locked in when the trigger
- `crates/engine/src/types/effect/shared.rs:2896` — placeholder with the attacker's power, read at trigger placement (CR 510.2/603.10a last-known
- `crates/engine/src/types/filter.rs:358` — trigger placement (CR 510.2/603.10a last-known information), same posture as
- `crates/engine/src/types/stack.rs:2659` — A creature dealt combat damage to a player (CR 510.2) — a marker distinct from the
- `crates/engine/src/types/stack.rs:2668` — A creature dealt combat damage to another creature (CR 510.2) — a marker distinct from the
- `crates/engine/src/types/trigger.rs:298` — Whenever a creature deals combat damage to a player (CR 510.2), scoped by `who`:
- `crates/engine/src/types/trigger.rs:307` — Whenever this permanent deals combat damage to a *creature* (CR 510.2, Stinkweed Imp:
- `crates/engine/src/types/trigger.rs:328` — creature-damage choke — combat or noncombat alike (CR 510.2 / 120.3/506) — not just
- `crates/engine/src/types/trigger.rs:498` — power 0, deal combat damage to a player (CR 510.2/603.3b, Primo, the Unbounded: "Whenever
- `crates/engine/src/types/trigger.rs:738` — CR 510.2/603.10a last-known information: the amount of combat damage the source just
- `crates/engine/src/types/trigger.rs:745` — CR 510.2/603.10a last-known information: the player the source just dealt combat damage
- `crates/engine/src/types/trigger.rs:751` — CR 510.2/603.10a last-known information: who controlled the creature that just dealt the
- `crates/engine/src/types/trigger.rs:761` — field is specifically CR 510.2 combat damage *to a player*). See
- `crates/engine/src/types/trigger.rs:773` — CR 510.2/603.10a last-known information: the creature a [`Trigger::DealsCombatDamageToCreature`]
- `crates/engine/src/types/trigger.rs:798` — CR 510.2/603.10a last-known information: the trigger's own source permanent's power at the
- `crates/engine/tests/game.rs:4124` — hit in the same combat does not (CR 510.2 x the your_tokens scope).
- `crates/engine/tests/game.rs:4198` — ── Combat damage to a creature (CR 510.2, fidelity increment #193) ─────────────────────
- `crates/engine/tests/game.rs:4284` — Deals-combat-damage-to-a-creature is creature-scoped (CR 510.2): an unblocked Stinkweed
- `crates/engine/tests/game.rs:4449` — The damaged-by set records damage from either creature-damage choke (CR 510.2 combat and
- `crates/engine/tests/game.rs:4722` — target, a mana-value-4 one is not (CR 510.2/603.10a last-known information).
- `crates/engine/tests/game.rs:4848` — target; a mana-value-5 one and a land are not (CR 510.2/603.10a last-known information).
- `crates/engine/tests/game.rs:45733` — Both powers are read before either amount is marked (CR 510.2/701.12c), so a "dead" 2/2
- `crates/engine/tests/game.rs:73722` — number of +1/+1 counters on it equal to the damage dealt." CR 510.2/603.3b: the whole
- `crates/engine/tests/game.rs:73768` — CR 510.2: attacking two different players fires Primo's batch trigger once per defending
- `crates/engine/tests/game.rs:93051` — routes the draw back to themself (CR 510.2).
- `crates/engine/tests/game.rs:93148` — (CR 510.2); the other two opponents take the same amount from the trigger, and the Hydra's

### CR 510.5

- `crates/engine/src/combat.rs:844` — damage step begins (CR 510.5) — the condition for creating a separate first-strike
- `crates/engine/src/priority.rs:1455` — combat (CR 510.5): advance the step marker without a StepBegan or a priority
- `crates/engine/src/priority.rs:1724` — The two combat damage steps deal their own batch (CR 510.5). The between-steps
- `crates/engine/src/types/card.rs:77` — The extra combat damage step for first/double strikers (CR 510.5); created only when
- `crates/engine/tests/game.rs:3915` — CR 510.5: a first-striker in combat creates a separate first-strike combat damage step,
- `crates/engine/tests/game.rs:3949` — Cross-check of CR 510.5 × CR 603.6c/603.10: two first strikers kill both blockers in the
- `crates/engine/tests/game.rs:3994` — CR 510.5: with no first/double striker in combat there is exactly one combat damage

### CR 511

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).

### CR 511.3

- `crates/engine/src/priority.rs:1514` — combat (CR 511.3) and cleanup's damage/boost/control housekeeping (CR 514.2) belong to

### CR 514

- `crates/engine/src/resolution/pump.rs:354` — TempBoost, cleared at cleanup). A 0-or-less-toughness creature dies to the next SBA. (CR 704, CR 51…

### CR 514.2

- `crates/engine/src/apply.rs:1397` — Revert an until-EOT enter-as-copy to the printed permanent (CR 514.2 — Cursed
- `crates/engine/src/apply.rs:1800` — The turn actually ending (CR 514.2 cleanup) — even "until end of turn"
- `crates/engine/src/priority.rs:1514` — combat (CR 511.3) and cleanup's damage/boost/control housekeeping (CR 514.2) belong to
- `crates/engine/src/priority.rs:1770` — step (CR 514.2); control reverts to the owner (or a still-attached
- `crates/engine/src/types/card.rs:1952` — cleared back to `None` (CR 514.2). `None` for an ordinary permanent or a *permanent* copy
- `crates/engine/src/types/card.rs:2180` — pools (some may have been spent since); the turn-ending boundary (CR 514.2 cleanup) clears
- `crates/engine/src/types/stack.rs:2390` — restored at cleanup ([`Event::TempBoostsEnded`], CR 514.2); otherwise the copy is
- `crates/engine/src/types/stack.rs:2417` — An until-end-of-turn control override on `object` ended (cleanup, CR 514.2); control
- `crates/engine/src/types/stack.rs:2425` — Every until-end-of-turn ability grant ended (cleanup, CR 514.2 / 702.166) — clears
- `crates/engine/src/types/stack.rs:2709` — Whether this boundary is the turn actually ending (CR 514.2 cleanup) rather than a
- `crates/engine/tests/game.rs:34839` — Wild Mongrel's color-set is until end of turn (CR 514.2 cleanup): once the turn passes, the
- `crates/engine/tests/game.rs:42516` — "control reverts to the owner at cleanup (CR 514.2)"
- `crates/engine/tests/game.rs:43215` — "control reverts to the owner at cleanup (CR 514.2)"
- `crates/engine/tests/game.rs:43726` — 108.3) — and control reverts to the original controllers at cleanup (CR 514.2).
- `crates/engine/tests/game.rs:43779` — "the until-EOT swap ends at cleanup and control reverts (CR 514.2)"
- `crates/engine/tests/game.rs:43924` — untapped, and hasted; control reverts to the original controllers at cleanup (CR 514.2).
- `crates/engine/tests/game.rs:43966` — "the until-EOT steal ends at cleanup and control reverts (CR 514.2)"
- `crates/engine/tests/game.rs:68931` — `TempBoostsEnded` cleanup path reaches a noncreature grant too. (CR 514.2)
- `crates/engine/tests/game.rs:78826` — (CR 514.2 cleanup) — the exception is "until end of turn," not "forever."

### CR 514.3

- `crates/engine/src/priority.rs:1815` — Discard down to the hand-size limit (CR 514.3): the player chooses which cards.
- `crates/engine/src/state.rs:38` — expiry, the same reasoning `pending_next_cast`'s turn-boundary clear uses. (CR 508.1a, CR 502.1, CR…
- `crates/engine/src/types/stack.rs:1406` — `player` must discard down to the hand-size limit at cleanup (CR 514.3): choose exactly
- `crates/engine/tests/game.rs:59224` — ── Cleanup-step discard to hand size (CR 514.3) ─────────────────────────────────────

### CR 601

- `crates/engine/src/apply.rs:554` — stack (CR 601's default cast zone — Dirgur Focusmage's "from your hand").
- `crates/engine/src/apply.rs:678` — Cast from the card's owner's hand (CR 601's default cast zone).
- `crates/engine/src/apply.rs:763` — Cast from the card's owner's hand (CR 601's default cast zone).
- `crates/engine/src/apply.rs:2227` — Quintorius's activated ability: grant the free-cast permission for the chosen (CR 602, CR 601, CR 1…
- `crates/engine/src/cast.rs:3` — Primary: CR 601 (casting spells), CR 602 (activating abilities), CR 118 (costs / payments).
- `crates/engine/src/cast.rs:634` — Casting is an action: it resets the pass count and the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1707` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1804` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1900` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:1986` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/cast.rs:2034` — Casting is an action: reset the pass count; the caster keeps priority. (CR 117, CR 601)
- `crates/engine/src/characteristics.rs:637` — through `legal_targets_for` if one ever does. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/characteristics.rs:2157` — hand …" — the only arm that reads the cast-from zone (CR 601).
- `crates/engine/src/effects.rs:193` — untested; extend with an `spell.escape` check if one needs it. (CR 702.19, CR 601)
- `crates/engine/src/effects.rs:501` — actually cast via escape (a card with escape usually has a normal cast mode (CR 702.19, CR 601)
- `crates/engine/src/effects.rs:621` — permanent's escape spell resolves through the `CardKind::Creature`/`Aura` arms (CR 702.19, CR 303.4…
- `crates/engine/src/lib.rs:529` — diffing — revisit only if profiling ever shows this hot. (CR 117, CR 601, CR 500)
- `crates/engine/src/pending/handlers/optional.rs:43` — still-resolving spell rather than going on the stack as a new triggered ability — (CR 603, CR 405, …
- `crates/engine/src/playable.rs:3` — Primary: CR 601 (timing and zone legality), CR 307 (sorcery speed), flash and
- `crates/engine/src/query.rs:156` — A non-mana activated ability the player can afford, or a prepared back-face cast. (CR 602, CR 601, …
- `crates/engine/src/query.rs:1191` — lenient form never rejects a legal play. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/resolution/damage.rs:97` — through the spell path, never a triggered/activated ability's). (CR 602, CR 601, CR 603)
- `crates/engine/src/resolution/mill.rs:77` — permission (the free-cast permission comes later, from the activated ability). (CR 602, CR 601, CR …
- `crates/engine/src/resolution/misc.rs:58` — Alchemist's Refuge: "You may cast spells this turn as though they had flash." (CR 702.8, CR 601, CR…
- `crates/engine/src/resolution/resolve_misc.rs:245` — genuinely *cast* it (CR 601) at their next opportunity, firing real "whenever you
- `crates/engine/src/types/card.rs:1538` — Whether this spell was cast from its controller's hand (CR 601's default cast zone) —
- `crates/engine/src/types/effect/shared.rs:11` — match arm with no separate pure path. (CR 602, CR 403.5, CR 601)
- `crates/engine/src/types/filter.rs:238` — from a graveyard, an impulse-play from exile, a command-zone commander cast (CR 601). The
- `crates/engine/src/types/mana.rs:397` — so both stay at their default/`false`). (CR 602, CR 601, CR 113)
- `crates/engine/src/types/stack.rs:2289` — object id) starts with a clean cap — correct, since a new object is a new game object. (CR 602, CR …
- `crates/engine/src/types/stack.rs:3038` — A `Trigger::YouDiscard` payoff (CR 601 impulse play): the graveyard card `from` was
- `crates/engine/src/types/trigger.rs:360` — Restrict to a spell cast from its controller's hand (CR 601's default cast zone) —
- `crates/engine/tests/game.rs:741` — Same body, but no flashback — an ordinary sorcery in the graveyard can't be cast. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:801` — Printed {5}, flashback {1}: with only one mana available, casting can only succeed if the (CR 702.3…
- `crates/engine/tests/game.rs:6819` — Indestructible is live: the destroy spell resolves, but the bear survives. (CR 702.12, CR 601)
- `crates/engine/tests/game.rs:18186` — Once P0 passes, priority reaches P1 and the same cast is legal. (CR 117, CR 601)
- `crates/engine/tests/game.rs:18525` — id with the choice already made as `color`. (CR 702.21, CR 601)
- `crates/engine/tests/game.rs:23841` — P0 (active player) holds priority first; pass it so P1 can cast in the reaction window. (CR 117, CR…
- `crates/engine/tests/game.rs:23932` — still P0's turn, so P1 needs priority via a pass, and P1's spell must be castable then.) (CR 117, C…
- `crates/engine/tests/game.rs:25879` — P1 casts both counters at the same bear (casting keeps priority, so no pass between them). (CR 117,…
- `crates/engine/tests/game.rs:28308` — .unwrap(); // P0 passes so P1 gets priority to cast at instant speed. (CR 117, CR 601)
- `crates/engine/tests/game.rs:33700` — Resolve the spell — every player must pass priority (a 3-player table, not just two passes). (CR 11…
- `crates/engine/tests/game.rs:33907` — vote round begins (the first `CastVote` pause). (CR 117, CR 601)
- `crates/engine/tests/game.rs:49274` — fires on a flashback cast from the graveyard — proves the new gate is opt-in. (CR 702.34, CR 403.5,…
- `crates/engine/tests/game.rs:55224` — Toxic Deluge cast for X=2 gives every creature -2/-2 until end of turn: a 2/2 dies to an SBA, (CR 7…
- `crates/engine/tests/game.rs:59020` — leaves the spell on the stack (P0 keeps priority) so the next cast stacks on top, then the (CR 117,…
- `crates/engine/tests/game.rs:61600` — Put a spell on the stack and pass so P1 holds priority. (CR 117, CR 405, CR 601)
- `crates/engine/tests/game.rs:61645` — After P0 casts and passes, P1 holds priority with one consecutive pass. Cycling must reset (CR 702.…
- `crates/engine/tests/game.rs:74944` — fires off the free cast — proof it's a real cast (CR 601), not a copy-only mint.
- `crates/engine/tests/game.rs:76625` — turn"). Casting still requires priority, so roll to P0's main phase next turn. (CR 117, CR 601, CR …
- `crates/engine/tests/game.rs:93443` — with the choice already made as `color`. (CR 614.12, CR 601)

### CR 601.2

- `crates/engine/src/lib.rs:200` — by [`Game::resolve_spell`] when the spell becomes a permanent (CR 601.2 — the "enters with"

### CR 601.2a

- `crates/engine/tests/game.rs:63261` — or the card would be moved to the graveyard *and* onto the stack (CR 601.2a: the spell is

### CR 601.2b

- `crates/engine/src/cast.rs:257` — A pay-X-life additional cost (CR 601.2b/601.2f — Toxic Deluge) chooses `{X}` for the
- `crates/engine/src/cast.rs:855` — CR 601.2b: X is chosen before targets are chosen, so an `x_scaled` count (Curse of the
- `crates/engine/src/cast.rs:1637` — CR 601.2b: {X} is chosen ahead of targets, same clamp `Game::validate_cast` applies.
- `crates/engine/src/cast.rs:1744` — CR 601.2b: {X} is chosen ahead of targets, same clamp `Game::validate_cast` applies.
- `crates/engine/src/cast.rs:1840` — CR 601.2b: {X} is chosen ahead of targets, same clamp `Game::validate_cast` applies.
- `crates/engine/src/cast.rs:2251` — A player-declared-X counter-removal cost (CR 601.2b/602.2b — Fungal Reaches' "Remove X
- `crates/engine/src/cast.rs:2399` — CR 107.3/601.2b: the chosen `{X}` folds into the mana cost (paid once per `{X}` symbol);
- `crates/engine/src/cast.rs:2588` — The activation's own chosen `{X}` (CR 107.3/601.2b — Fungal Reaches' "Remove
- `crates/engine/src/cast.rs:2654` — A modal activated ability (CR 601.2b — Cankerbloom's "Choose one — Destroy target
- `crates/engine/src/de.rs:513` — A non-mana cast-time cap on {X} (CR 601.2b — Open the Way's player-count bound) —
- `crates/engine/src/de.rs:1320` — needs neither. `x_scaled` (CR 601.2b, default `false`) marks `min`/`max` as placeholders the
- `crates/engine/src/pending/raise/mod.rs:56` — (CR 601.2b — Cankerbloom). See [`crate::PendingChoice::ChooseMode`].
- `crates/engine/src/playable.rs:163` — CR 601.2b: {X} (and modes) are chosen before targets (CR 601.2c) — computed here,
- `crates/engine/src/playable.rs:170` — A printed non-mana ceiling on X (CR 601.2b — Open the Way's "X can't be greater than
- `crates/engine/src/priority.rs:329` — `x` is the caster's chosen `{X}` (CR 601.2b — chosen before targets), read by a
- `crates/engine/src/priority.rs:685` — A card's printed non-mana ceiling on {X} (CR 601.2b — Open the Way's player-count bound),
- `crates/engine/src/query.rs:843` — client asks for X first (CR 601.2b already requires that ordering).
- `crates/engine/src/query.rs:851` — own locked-in cast X is it (CR 601.2b/107.3i). Shared by [`Game::place_targeted_ability`]
- `crates/engine/src/query.rs:897` — casting spell's chosen `{X}` (CR 601.2b — chosen before targets), read by a
- `crates/engine/src/triggers.rs:1377` — cast `{X}` (CR 601.2b/107.3i) — the same value [`Game::ability_source_x`] returns for a
- `crates/engine/src/types/card.rs:906` — (CR 601.2b — Open the Way's "X can't be greater than the number of players in the game").
- `crates/engine/src/types/card.rs:914` — A cast-time ceiling on a spell's announced {X} that isn't derived from mana (CR 601.2b).
- `crates/engine/src/types/card.rs:1604` — zero times. Settled before the spell hits the stack (CR 601.2b) and recorded here the way
- `crates/engine/src/types/card.rs:1846` — The {X} chosen for the spell that became this permanent (CR 601.2b), fixed for the rest
- `crates/engine/src/types/effect/shared.rs:1434` — Whether the counter-removal cost's count is a player-declared `{X}` (CR 601.2b) instead
- `crates/engine/src/types/effect/shared.rs:1734` — "if X is `at_least` or more" (CR 601.2b; Kinetic Ooze's "If X is 5 or more, you draw a
- `crates/engine/src/types/mana.rs:29` — An additional cost paid alongside mana, before the spell hits the stack (CR 601.2b/
- `crates/engine/src/types/mana.rs:72` — This cost with a chosen `x` folded into its generic component (CR 601.2b/107.3: paying
- `crates/engine/src/types/mana.rs:207` — Whether this spell's chosen `{X}` (CR 601.2b) is paid as life rather than mana (CR
- `crates/engine/src/types/stack.rs:37` — (CR 601.2b); it is 0 (and ignored) for a spell with no `{X}`.
- `crates/engine/src/types/stack.rs:147` — non-`{X}` back face, same CR 601.2b default `Intent::Cast`'s `x` uses — Braingeyser).
- `crates/engine/src/types/stack.rs:159` — chosen `{X}` (0 for a non-`{X}` adventure — same CR 601.2b default `Intent::Cast` uses). On
- `crates/engine/src/types/stack.rs:1202` — `activated` is the third window (CR 601.2b — Cankerbloom): an *activated* modal ability
- `crates/engine/src/types/stack.rs:3164` — chosen `{X}` (CR 601.2b — X is fixed before targets are chosen): `Game::choose_spell_targets`
- `crates/engine/src/types/stack.rs:3174` — instead *defined* by an additional cost (CR 601.2b/601.2f) — Immoral Bargain's "As an
- `crates/engine/tests/game.rs:44273` — ── Entered-with-X target filter (CR 601.2b, kinetic_ooze #99) ─────────────────────
- `crates/engine/tests/game.rs:44326` — ── Source's locked cast X threshold rider (CR 601.2b, kinetic_ooze #99) ───────────
- `crates/engine/tests/game.rs:44411` — rider must keep reading the spell's *locked* cast X (CR 601.2b), not the live count, so no
- `crates/engine/tests/game.rs:64154` — get -X/-X until end of turn." — {2}{B} sorcery. The chosen `{X}` (CR 601.2b) funds the
- `crates/engine/tests/game.rs:68467` — spell's own chosen X at the `choose_spell_targets` choke point (CR 601.2b/601.2c). ────────────
- `crates/engine/tests/game.rs:100044` — target enchantment. / Proliferate.): mode 0 is chosen at activation (CR 601.2b), then that

### CR 601.2c

- `crates/engine/src/apply.rs:41` — attached to that creature"). A cast Aura gets its legality from a target choice (CR 601.2c),
- `crates/engine/src/cast.rs:156` — our single-pass casting; grow the trigger form if ward-on-the-stack interaction is needed. (CR 702.…
- `crates/engine/src/cast.rs:158` — aren't folded into ward/cost-reduction — no pool modal card targets a warded permanent. (CR 702.21,…
- `crates/engine/src/cast.rs:614` — A multi-target spell now chooses its targets (CR 601.2c): auto-fill when the choice is
- `crates/engine/src/cast.rs:648` — (CR 601.2c), so `modal_target_clauses` below is where that mode's clause(s) are read.
- `crates/engine/src/cast.rs:691` — clause-pause chain (CR 601.2c), if it has one. `None` for the overwhelming
- `crates/engine/src/cast.rs:699` — A non-modal spell's independent target clauses, in printed order (CR 601.2c) — the ones
- `crates/engine/src/cast.rs:733` — The chosen mode's independent target clauses (CR 601.2c/700.2), in printed order, if it
- `crates/engine/src/cast.rs:777` — Record a just-cast multi-target spell's chosen targets (CR 601.2c), starting at its first
- `crates/engine/src/cast.rs:799` — After clause `clause` of `spell` is settled, choose the next independent clause (CR 601.2c —
- `crates/engine/src/cast.rs:831` — `count.max` distinct legal targets — but capped at how many legal targets exist (CR 601.2c:
- `crates/engine/src/cast.rs:867` — CR 601.2c/601.2f/702.42: Strive's own sibling — Twinflame's target count is the
- `crates/engine/src/cast.rs:870` — CR 601.2c/702.33c: Multikicker's own sibling, "1 + N" rather than "exactly N" — see the
- `crates/engine/src/cast.rs:875` — CR 601.2c: a main-phase-conditional target (Return to Dust's "you may exile up to one
- `crates/engine/src/cast.rs:888` — CR 601.2c/702.33c: Multikicker's own sibling — Comet Storm's "choose any target,
- `crates/engine/src/cast.rs:949` — After a multi-target spell's targets are finalized (CR 601.2c), also settle CR 601.2d's
- `crates/engine/src/cast.rs:995` — After a multi-target spell's targets are finalized (CR 601.2c), also settle CR 601.2d's
- `crates/engine/src/cast.rs:1640` — creatures" — CR 601.2c): its targets are chosen *after* the cast, the same
- `crates/engine/src/cast.rs:2310` — A multi-target loyalty ability (CR 601.2c — Garruk Wildspeaker's "+1: Untap two
- `crates/engine/src/cast.rs:2315` — ponytail: CR 601.2c chooses targets *before* CR 601.2h pays costs; here the
- `crates/engine/src/cast.rs:2364` — once every other cost is paid — CR 601.2c's targets, unlike a plain untargeted choice.
- `crates/engine/src/cast.rs:2677` — A two-target activated ability (Zedruu's donation, CR 601.2c): its first target (the
- `crates/engine/src/characteristics.rs:637` — through `legal_targets_for` if one ever does. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/de.rs:1316` — A [`TargetCount`] (CR 601.2c). Two spellings:
- `crates/engine/src/effects.rs:11` — (CR 601.2c) — or, when its count is fully declinable (`min == 0`), the caster chose none, and
- `crates/engine/src/effects.rs:32` — `destroy_target` steps — CR 601.2c) into one `(ability, target)` step per targeted `Sequence`
- `crates/engine/src/effects.rs:192` — escape Aura's target realistically fizzles in a test, so this residual is (CR 702.19, CR 303.4, CR …
- `crates/engine/src/effects.rs:815` — ability placed with no target (declined, or none legal — CR 601.2c/603.3c, see
- `crates/engine/src/pending/handlers/edict.rs:403` — [`ChoiceRequest::ChooseTargetPlayers`] (CR 601.2c/608.2b: zero is a legal choice); once
- `crates/engine/src/pending/handlers/fanout.rs:396` — (Zimone's Hypothesis' primer, CR 601.2c). Non-targeted, so a `Some` id must be a currently
- `crates/engine/src/pending/handlers/library.rs:803` — CR 601.2c: a mode with no legal target can't be chosen. The `ChooseMode` pause was
- `crates/engine/src/pending/handlers/targets.rs:124` — "Up to N" (`count.min == 0`): an empty answer declines. CR 601.2c treats choosing zero
- `crates/engine/src/pending/handlers/targets.rs:161` — A set-level mana-value budget (CR 601.2c — Rampaging Yao Guai's "destroy any number of
- `crates/engine/src/pending/handlers/targets.rs:184` — More than one target chosen for the ability's own (first) clause (CR 601.2c —
- `crates/engine/src/pending/handlers/targets.rs:409` — `legal` (CR 601.2c).
- `crates/engine/src/pending/handlers/targets.rs:427` — Distinct (CR 601.2c: "the same target can't be chosen twice") and all legal.
- `crates/engine/src/pending/handlers/targets.rs:450` — Advocate's targeted graveyard-exile cost, CR 601.2c/602.2b): exactly `count` distinct legal
- `crates/engine/src/pending/handlers/targets.rs:472` — Distinct, legal, and all owned by the same graveyard's owner (CR 601.2c's "an
- `crates/engine/src/pending/handlers/targets.rs:511` — Validate and record a multi-target spell's chosen targets (CR 601.2c): between
- `crates/engine/src/pending/handlers/targets.rs:529` — Distinct (CR 601.2c: "the same target can't be chosen twice") and all legal.
- `crates/engine/src/playable.rs:157` — Clause 0 of the non-modal spell's post-cast target clauses (CR 601.2c) — `None` for the
- `crates/engine/src/playable.rs:163` — CR 601.2b: {X} (and modes) are chosen before targets (CR 601.2c) — computed here,
- `crates/engine/src/playable.rs:223` — Strive (CR 601.2c/702.42): the caster commits to a target count before
- `crates/engine/src/playable.rs:235` — Multikicker (CR 601.2c/702.33c): "choose any target, then choose another
- `crates/engine/src/priority.rs:350` — effect that needs one; for a modal spell it's the *chosen* mode's effect (CR 601.2c) — so
- `crates/engine/src/query.rs:1137` — chosen — so shroud/hexproof/protection (which only restrict *targeting*) don't filter (CR 702.11, C…
- `crates/engine/src/query.rs:1167` — ponytail: checked only at target *selection* time (here), matching how protection is (CR 601.2c)
- `crates/engine/src/query.rs:1191` — lenient form never rejects a legal play. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/resolution/copy.rs:103` — spell-timed effect (CR 303.4a/601.2c) — gets a real retarget spec too (Changing
- `crates/engine/src/resolution/counters.rs:381` — Kinetic Ooze's X≥10 rider (CR 601.2c/603.3d): double the +1/+1 counters on each of the
- `crates/engine/src/resolution/pause_fight.rs:22` — (CR 601.2c — no damage, no pause) rather than picking an illegal target.
- `crates/engine/src/resolution/sequence_steps.rs:116` — The reflexive ability's own resolution: return the chosen graveyard card (CR 601.2c
- `crates/engine/src/triggers.rs:2070` — target chosen at placement (CR 601.2c) — exactly like any triggered ability fired by a
- `crates/engine/src/triggers.rs:2105` — stack as a real, respondable object with its target chosen at placement (CR 601.2c) — after
- `crates/engine/src/triggers.rs:4630` — effect (Goblin Cadets' "target opponent gains control of it", CR 601.2c) still carries
- `crates/engine/src/triggers.rs:4657` — (min 0) isn't mandatory — CR 601.2c already treats choosing zero of "up to N" as a
- `crates/engine/src/triggers.rs:4720` — target clauses (CR 601.2c) — its first clause is the permanent (`Effect::target`); its
- `crates/engine/src/triggers.rs:4834` — number" of zero). No pause. Otherwise the controller chooses (CR 601.2c).
- `crates/engine/src/types/card.rs:457` — of a several-ability simultaneous group grows from a real card (see wire-protocol-and-visibility sp…
- `crates/engine/src/types/card.rs:1499` — The chosen targets (CR 601.2c). A single-target spell fills one slot; Aether Gale fills up
- `crates/engine/src/types/card.rs:1504` — A *second* independent target clause's chosen targets (CR 601.2c — Magma Opus's "Tap two
- `crates/engine/src/types/card.rs:1597` — the spell has no Strive cost. Settled before the spell hits the stack (CR 601.2c precedes
- `crates/engine/src/types/effect/choice.rs:137` — A resolution-time optional "you may put a +1/+1 counter on a creature" (CR 601.2c —
- `crates/engine/src/types/effect/shared.rs:560` — (CR 601.2c), before the condition is evaluated at resolution, so an `otherwise`
- `crates/engine/src/types/effect/shared.rs:1017` — How many targets this effect chooses (CR 601.2c). Most targeted effects take a single
- `crates/engine/src/types/effect/shared.rs:1036` — "return up to one target Aura or Equipment card" (CR 601.2c — a declinable target).
- `crates/engine/src/types/effect/shared.rs:1074` — one" ability with no target chosen (declined, or none legal — CR 601.2c/603.3c) still goes
- `crates/engine/src/types/effect/shared.rs:1492` — "Exile N target cards from an opponent's graveyard" as an additional cost (CR 601.2c/
- `crates/engine/src/types/effect/shared.rs:1494` — …"). Unlike `sacrifice`/`discard_cost`'s untargeted choices, CR 601.2c treats these as
- `crates/engine/src/types/effect/shared.rs:3003` — Targets are chosen on announcement (CR 601.2c), before a `Conditional`'s condition is
- `crates/engine/src/types/effect/zone.rs:122` — later frame; its target is chosen when it goes on the stack (CR 601.2c), after the fan-out.
- `crates/engine/src/types/filter.rs:877` — "Any number of target players" (Priest of Forgotten Gods, CR 601.2c/608.2b: choosing zero
- `crates/engine/src/types/mana.rs:252` — not), Strive's total depends on *how many* targets the caster commits to: CR 601.2c
- `crates/engine/src/types/stack.rs:78` — The caster's declared Strive target count ([`AdditionalCost::strive`] — CR 601.2c/
- `crates/engine/src/types/stack.rs:240` — target players" (CR 601.2c/608.2b — a subset, possibly empty, of the offered `legal` set).
- `crates/engine/src/types/stack.rs:842` — - an ability's first target clause before it goes on the stack (CR 601.2c/603.3c),
- `crates/engine/src/types/stack.rs:843` — - an already-on-the-stack spell's independent target clause (CR 601.2c / CR 707.10c
- `crates/engine/src/types/stack.rs:1138` — graveyard to pay an activated ability's own targeted exile cost (CR 601.2c/602.2b —
- `crates/engine/src/types/stack.rs:2094` — A multi-target spell's chosen targets (CR 601.2c) were recorded onto `spell` — either
- `crates/engine/src/types/stack.rs:3154` — How many distinct targets an effect chooses (CR 601.2c): between `min` and `max`, inclusive.
- `crates/engine/src/types/stack.rs:3181` — Strive's own sibling of [`Self::sacrifice_scaled`] (CR 601.2c/601.2f/702.42) — Twinflame's
- `crates/engine/src/types/stack.rs:3185` — caster commits to *before* the stack (CR 601.2c precedes 601.2f) — carried on
- `crates/engine/src/types/stack.rs:3192` — A set-level cap on the chosen targets' *summed* mana value (CR 601.2c: legality is
- `crates/engine/src/types/stack.rs:3207` — Multikicker's own sibling (CR 601.2c/702.33c) — Comet Storm's "Choose any target, then
- `crates/engine/src/types/stack.rs:3228` — The timing-conditional sibling of [`Self::kicked_scaled`] (CR 601.2c's general
- `crates/engine/src/types/stack.rs:3238` — count range, not two), so the same-instance distinctness CR 601.2c already gives a
- `crates/engine/src/types/stack.rs:3282` — A spell's chosen targets (CR 601.2c), in the order chosen. A single-target spell fills just
- `crates/engine/src/types/stack.rs:3412` — One ability's independent target clauses, in printed order (CR 601.2c/700.2) — Hull Breach's
- `crates/engine/tests/de.rs:104` — value X or less" (CR 601.2c): the budget rides on the count table, and a dropped
- `crates/engine/tests/game.rs:24736` — immediately (before either player gets priority), so its target choice pauses the game (CR 117, CR …
- `crates/engine/tests/game.rs:24737` — right away — the caster picks a target before `resolve_top_of_stack`'s priority passes. (CR 117, CR…
- `crates/engine/tests/game.rs:27760` — Drain both delayed draws; P0's is a declinable up-to-two (CR 601.2c) — P0 takes the full two.
- `crates/engine/tests/game.rs:27796` — Arcane Denial's countered-spell rider is "may draw up to two cards" (CR 601.2c) — a
- `crates/engine/tests/game.rs:33025` — Answer the pending "any number of target players" choice for `player` (CR 601.2c/608.2b —
- `crates/engine/tests/game.rs:34304` — "Any number of target players" (CR 601.2c) — the caster's own chosen subset, not
- `crates/engine/tests/game.rs:34384` — CR 601.2c/608.2b: "any number" permits zero — no life loss, no sacrifice, but the
- `crates/engine/tests/game.rs:35511` — Tyvar's Stand ({X}{G}) — "Target creature you control gets +X/+X and gains hexproof (CR 702.11, CR …
- `crates/engine/tests/game.rs:35647` — "Up to one" (CR 601.2c) — declining the enemy pick still leaves the creature pumped.
- `crates/engine/tests/game.rs:35694` — is pumped, and no fight happens (CR 601.2c — no legal target, no pause).
- `crates/engine/tests/game.rs:36334` — target — a respondable CR 601.2c choice, like any other Aura's enchant subject — not a
- `crates/engine/tests/game.rs:37265` — target for the copy." A flashback cast (from the graveyard) reanimates its own target, (CR 702.34, …
- `crates/engine/tests/game.rs:38005` — CR 601.2c — Life from the Loam's "Return up to three target land cards from your graveyard to
- `crates/engine/tests/game.rs:38075` — CR 601.2c — with fewer than three lands available, "up to three" still lets the caster take
- `crates/engine/tests/game.rs:43234` — The donation's second, independent target clause (CR 601.2c): the target opponent.
- `crates/engine/tests/game.rs:44565` — CR 601.2c — "any number of other target creatures" includes zero: declining every doubling
- `crates/engine/tests/game.rs:44984` — Mode 1: "Target player draws two cards, then discards two cards" (CR 111.4/601.2c) — the
- `crates/engine/tests/game.rs:45753` — No creature I control: the fight resolves with no damage and no pending choice (CR 601.2c).
- `crates/engine/tests/game.rs:49234` — "up to X" at X=0 (CR 601.2c) is a forced empty target set: no counters or flying, but the
- `crates/engine/tests/game.rs:51547` — indestructible until end of turn." #72 self-target-spec: the grant always lands on (CR 702.12, CR 6…
- `crates/engine/tests/game.rs:62132` — target). (CR 701.38, CR 108.3, CR 601.2c)
- `crates/engine/tests/game.rs:64791` — target (CR 601.2c — chosen as it goes on the stack).
- `crates/engine/tests/game.rs:64855` — Declining the "up to one" return is legal (CR 601.2c/608.2b) — the token isn't undone.
- `crates/engine/tests/game.rs:66173` — ── Multi-target count (CR 601.2c / 608.2b) — "N target" / "up to N target" ──────────────
- `crates/engine/tests/game.rs:66208` — CR 601.2c — "Return six target nonland permanents to their owners' hands." With more than
- `crates/engine/tests/game.rs:66261` — CR 601.2c — with fewer than six legal targets the caster targets as many as possible; the
- `crates/engine/tests/game.rs:66284` — CR 601.2c — the six chosen targets must be distinct and legal, and there must be six of them.
- `crates/engine/tests/game.rs:67102` — (CR 601.2c — up-to-one, chosen during resolution and never a stack target): answered by an
- `crates/engine/tests/game.rs:67913` — CR 601.2c/601.2d — Magma Opus's two independent target clauses on one spell: "4 damage
- `crates/engine/tests/game.rs:67995` — CR 601.2c — Volcanic Salvo's "up to two target creatures and/or planeswalkers": each chosen
- `crates/engine/tests/game.rs:68046` — living inside one mode of a modal spell (CR 700.2 + CR 601.2c together).
- `crates/engine/tests/game.rs:68318` — choice (CR 601.2c's "maximum possible number") auto-fills both, no pause needed.
- `crates/engine/tests/game.rs:68467` — spell's own chosen X at the `choose_spell_targets` choke point (CR 601.2b/601.2c). ────────────
- `crates/engine/tests/game.rs:68523` — Silkguard at X=0: "up to zero" is a forced empty set (CR 601.2c) — no target may be chosen
- `crates/engine/tests/game.rs:69149` — CR 601.2c — "exile X target creatures" with fewer legal creatures than X caps at the maximum
- `crates/engine/tests/game.rs:73991` — Gyome, Master Chef: "{1}, Sacrifice a Food: Target creature gains indestructible until (CR 702.12, …
- `crates/engine/tests/game.rs:75010` — CR 601.2c treats declining "up to one" as a complete, legal choice.
- `crates/engine/tests/game.rs:76879` — "any number of other target creatures you control" (CR 601.2c/608.2b — zero is legal), and
- `crates/engine/tests/game.rs:79315` — A single-target burn spell (CR 601.2c — "deal 3 damage to target creature"), the spell
- `crates/engine/tests/game.rs:79598` — A multi-target burn spell (CR 601.2c — "deal 1 damage to each of up to two target creatures"),
- `crates/engine/tests/game.rs:85157` — Exactly two legal targets exist for a "choose exactly two" clause (CR 601.2c's "maximum
- `crates/engine/tests/game.rs:89448` — ── Triggered-ability up-to-N target count (CR 601.2c, #218 multi-target-abilities) ───
- `crates/engine/tests/game.rs:89512` — CR 601.2c: "up to two" is fully declinable — choosing none is a legal answer once the
- `crates/engine/tests/game.rs:89669` — ── Activated-ability independent second target clause (CR 601.2c, #218) ──────────────
- `crates/engine/tests/game.rs:91770` — ability goes on the stack (CR 601.2c).
- `crates/engine/tests/game.rs:92692` — Two independent single-target clauses (CR 601.2c) on one non-modal spell — the graveyard
- `crates/engine/tests/game.rs:92750` — clause's target is still declared at cast (CR 601.2c: targets are chosen as the spell is
- `crates/engine/tests/game.rs:92756` — A decoy so clause 0 is a real choice (a lone legal target auto-fills, CR 601.2c).
- `crates/engine/tests/game.rs:92793` — A decoy so clause 0 is a real choice (a lone legal target auto-fills, CR 601.2c).
- `crates/engine/tests/game.rs:93645` — doesn't widen what's legal to target at cast time (CR 601.2c).
- `crates/engine/tests/game.rs:94895` — (Land). Casting face down is casting a spell, not playing a land (CR 702.37b/601.2c), so it
- `crates/engine/tests/game.rs:96540` — Declaring Multikicker paid twice needs 1 + 2 = 3 legal targets (CR 601.2c/702.33c); with
- `crates/engine/tests/game.rs:96644` — A decoy so clause 0 is a real choice (a lone legal target auto-fills, CR 601.2c).
- `crates/engine/tests/game.rs:96683` — Decoys so both clauses are real choices (a lone legal target auto-fills, CR 601.2c).
- `crates/engine/tests/game.rs:96726` — widens to 2; CR 601.2c's built-in same-clause distinctness gives the oracle's "other" for
- `crates/engine/tests/game.rs:96813` — carries one (CR 601.2c), so a client reading a target need here would stage a click that can
- `crates/engine/tests/game.rs:97951` — ── Set-level total-mana-value target budget (CR 601.2c, rampaging_yao_guai #9) ────
- `crates/engine/tests/game.rs:101219` — The trigger says "target opponent" — choose it as the ability goes on the stack (CR 601.2c).

### CR 601.2d

- `crates/engine/src/cast.rs:801` — CR 601.2d divided-damage/counter split over the finished target sets. A modal spell reads
- `crates/engine/src/cast.rs:949` — After a multi-target spell's targets are finalized (CR 601.2c), also settle CR 601.2d's
- `crates/engine/src/cast.rs:974` — "Any number of targets" (CR 601.2d) admits creatures *and* players — collect both.
- `crates/engine/src/cast.rs:995` — After a multi-target spell's targets are finalized (CR 601.2c), also settle CR 601.2d's
- `crates/engine/src/cast.rs:2778` — Build an [`Event::SpellDamageDivided`] from `(target, amount)` pairs (CR 601.2d), splitting
- `crates/engine/src/pending/handlers/combat.rs:59` — is split among its already-chosen targets (CR 601.2d — Magma Opus's "4 damage divided as
- `crates/engine/src/pending/handlers/combat.rs:84` — CR 601.2d: each target must receive at least one point of the divided total.
- `crates/engine/src/pending/handlers/combat.rs:102` — is split among its already-chosen targets (CR 601.2d — Grove's Bounty's "Distribute X
- `crates/engine/src/pending/handlers/combat.rs:125` — CR 601.2d: each target must receive at least one of the divided total.
- `crates/engine/src/pending/handlers/combat.rs:149` — counters across any subset of `legal` (CR 601.2d — Forgotten Ancient's "move any number of
- `crates/engine/src/pending/handlers/common.rs:97` — ([`Effect::Counters(CountersEffect::MoveCounters)`]'s `distributed` mode, CR 601.2d): one combined …
- `crates/engine/src/pending/handlers/targets.rs:546` — Chain into the next independent target clause, if any, before the CR 601.2d split runs.
- `crates/engine/src/resolution/counters.rs:46` — A divided spell's per-target count was already settled (CR 601.2d) right after
- `crates/engine/src/resolution/damage.rs:93` — A divided spell's per-target amount was already settled (CR 601.2d) right
- `crates/engine/src/resolution/damage.rs:100` — `damage_division`, player shares on `damage_division_players` (CR 601.2d).
- `crates/engine/src/resolution/pause_fight.rs:133` — creatures" (CR 601.2d): pause on a target→amount map capped at `from`'s live
- `crates/engine/src/types/card.rs:1551` — CR 601.2d's damage division for a `divided: true` `Effect::Damage(DamageEffect::Target)` on this sp…
- `crates/engine/src/types/card.rs:1559` — CR 601.2d's *player* shares of a `divided: true` `Effect::Damage(DamageEffect::Target)`'s division …
- `crates/engine/src/types/card.rs:1566` — CR 601.2d's counter division for a `divided: true` `Effect::Counters(CountersEffect::PutCounters)` …
- `crates/engine/src/types/stack.rs:1024` — chosen `targets` (CR 601.2d — Magma Opus's "4 damage divided as you choose among any number
- `crates/engine/src/types/stack.rs:1036` — chosen `targets` (CR 601.2d — Grove's Bounty's "Distribute X +1/+1 counters among any
- `crates/engine/src/types/stack.rs:1047` — across any number of `legal` destinations (CR 601.2d — Forgotten Ancient's "move any
- `crates/engine/src/types/stack.rs:2623` — A divided-damage spell's total was split among its chosen targets (CR 601.2d — see
- `crates/engine/src/types/stack.rs:2633` — A divided-counters spell's total was split among its chosen targets (CR 601.2d — see
- `crates/engine/tests/game.rs:48342` — CR 601.2d — Grove's Bounty (the real pool card's adventure half): "Distribute X +1/+1
- `crates/engine/tests/game.rs:48391` — CR 601.2d — a division must cover exactly the chosen targets, give each at least one
- `crates/engine/tests/game.rs:48433` — A target gets zero — CR 601.2d requires at least one each.
- `crates/engine/tests/game.rs:50848` — CR 601.2d: Magma Opus's "any number of targets" division reaches a planeswalker too, since
- `crates/engine/tests/game.rs:67447` — (`ReturnToHand`) spreads to `deal_damage` (with CR 601.2d division), `tap_target`, and
- `crates/engine/tests/game.rs:67630` — CR 601.2d — Magma Opus's "4 damage divided as you choose among any number of targets": the
- `crates/engine/tests/game.rs:67689` — CR 601.2d — a division must cover exactly the chosen targets, give each at least one point,
- `crates/engine/tests/game.rs:67741` — A target gets zero — CR 601.2d requires at least one each.
- `crates/engine/tests/game.rs:67758` — CR 601.2d — a single chosen target needs no division choice; it takes the whole amount.
- `crates/engine/tests/game.rs:67812` — CR 601.2d — Magma Opus's "any number of targets" includes players: the 4 damage may be split
- `crates/engine/tests/game.rs:67867` — CR 601.2d — a single chosen *player* target needs no division choice; it takes the whole 4.
- `crates/engine/tests/game.rs:67913` — CR 601.2c/601.2d — Magma Opus's two independent target clauses on one spell: "4 damage
- `crates/engine/tests/game.rs:71241` — CR 601.2d — Forgotten Ancient (soc): "At the beginning of your upkeep, you may move any
- `crates/engine/tests/game.rs:71276` — CR 601.2d's "any number" permits zero — declining to move any counters is a legal answer to

### CR 601.2f

- `crates/engine/src/cast.rs:90` — Retrace (CR 702.83a): can't pay without a land card in hand to discard (CR 601.2f —
- `crates/engine/src/cast.rs:109` — without a creature card in hand to reveal (CR 601.2f — an unpayable additional cost
- `crates/engine/src/cast.rs:142` — board-derived reduction (CR 601.2f, e.g. Blasphemous Act), and ward (CR 702.21). Shared by
- `crates/engine/src/cast.rs:184` — printed cost (CR 601.2f), same opt-in shape as `evoked` — `false` for a spell with none, or
- `crates/engine/src/cast.rs:187` — (CR 601.2f's "rather than pay this spell's mana cost").
- `crates/engine/src/cast.rs:206` — A printed alternative cost (CR 601.2f — Invigorate): declaring it replaces the mana
- `crates/engine/src/cast.rs:257` — A pay-X-life additional cost (CR 601.2b/601.2f — Toxic Deluge) chooses `{X}` for the
- `crates/engine/src/cast.rs:315` — Strive (CR 601.2f/702.42): "{2}{R} more to cast for each target beyond the first" —
- `crates/engine/src/cast.rs:479` — A printed alternative cost's non-mana rider (CR 601.2f — Invigorate's "you may have an
- `crates/engine/src/cast.rs:537` — A delve or escape graveyard-exile payment (CR 601.2f/601.2h — paid pre-stack, before
- `crates/engine/src/cast.rs:544` — An optional additional sacrifice cost (CR 601.2f — Plumb the Forbidden's "you may
- `crates/engine/src/cast.rs:560` — Pay-X-life additional cost (CR 601.2f — Toxic Deluge): the chosen `{X}` funds this
- `crates/engine/src/cast.rs:572` — Fixed pay-life additional cost (CR 601.2f — Deep Analysis's flashback "Pay 3 life"),
- `crates/engine/src/cast.rs:862` — CR 601.2f: the sacrifice-defined sibling of the above — Immoral Bargain's X is settled
- `crates/engine/src/cast.rs:867` — CR 601.2c/601.2f/702.42: Strive's own sibling — Twinflame's target count is the
- `crates/engine/src/characteristics.rs:2091` — increments the tally (CR 601.2f applies the reduction as the spell is cast), so
- `crates/engine/src/core.rs:823` — copy-per-sacrifice rider reads once one exists (CR 601.2f's "copy this spell for each
- `crates/engine/src/de.rs:374` — A printed non-mana alternative cost (CR 601.2f) — `alternative_cost = { condition =
- `crates/engine/src/de.rs:645` — `[cost.additional]` — an additional cost paid alongside mana (CR 601.2f).
- `crates/engine/src/de.rs:688` — fixed-count sacrifice cost (Dread Return's Flashback—Sacrifice three creatures, CR 601.2f/
- `crates/engine/src/playable.rs:36` — Whether the caster is casting the spell for its printed alternative cost (CR 601.2f —
- `crates/engine/src/playable.rs:574` — An additional sacrifice cost (CR 601.2f), either optional (Plumb the Forbidden: 0 up to
- `crates/engine/src/playable.rs:639` — Alternative cost (CR 601.2f — Invigorate): only declarable if the card actually has one,
- `crates/engine/src/resolution/life.rs:27` — Invigorate's alternative-cost rider (CR 601.2f — see `LifeEffect::OpponentGains`'s
- `crates/engine/src/triggers.rs:3805` — the conditions above, reading the resolving spell's own recorded count (CR 601.2f).
- `crates/engine/src/types/card.rs:474` — A printed alternative cost that pays something other than mana (CR 601.2f — Invigorate: "If
- `crates/engine/src/types/card.rs:492` — The non-mana cost paid instead of the printed mana cost, fired at cast time (CR 601.2f —
- `crates/engine/src/types/card.rs:629` — A printed alternative cost that isn't a mana cost at all (CR 601.2f — Invigorate: "If you
- `crates/engine/src/types/card.rs:769` — generic cost by {1} (floored at 0, CR 601.2f). `delve = true` in TOML; `false` for every
- `crates/engine/src/types/card.rs:776` — (CR 601.2f); the resolved spell is exiled like flashback's (CR 702.19d — only relevant to a
- `crates/engine/src/types/card.rs:1573` — How many permanents were sacrificed to pay [`AdditionalCost::sacrifice`] (CR 601.2f —
- `crates/engine/src/types/effect/shared.rs:150` — (CR 601.2f) — Plumb the Forbidden's "copy this spell for each creature sacrificed this
- `crates/engine/src/types/effect/shared.rs:1806` — sacrifice cost (CR 601.2f): the reflexive trigger doesn't happen at all when nothing was
- `crates/engine/src/types/mana.rs:32` — A spell's own board-derived generic reduction (CR 601.2f/118.9) — "This spell costs {1}
- `crates/engine/src/types/mana.rs:36` — floored at 0 (CR 601.2f — cost reduction never touches colored pips).
- `crates/engine/src/types/mana.rs:170` — An additional cost to cast a spell (CR 601.2f), on top of its mana cost. Paid synchronously
- `crates/engine/src/types/mana.rs:212` — A *fixed* life payment (CR 601.2f) — Deep Analysis's flashback "Pay 3 life" rider. Distinct
- `crates/engine/src/types/mana.rs:217` — A sacrifice rider on the cost (CR 601.2f), either [`SacrificeAdditionalCostCount::OneOrMore`]'s
- `crates/engine/src/types/mana.rs:220` — Flashback—Sacrifice three creatures, CR 601.2f/602.2b). `None` for no sacrifice cost on
- `crates/engine/src/types/mana.rs:253` — chooses targets before CR 601.2f locks the total cost, but this engine puts a spell on
- `crates/engine/src/types/mana.rs:295` — A sacrifice rider on a spell's mana cost (CR 601.2f — [`AdditionalCost::sacrifice`]): the
- `crates/engine/src/types/mana.rs:303` — How many permanents [`SacrificeAdditionalCost`] requires (CR 601.2f).
- `crates/engine/src/types/mana.rs:310` — the cast names exactly N distinct matching permanents (CR 601.2f/602.2b) or is rejected.
- `crates/engine/src/types/stack.rs:49` — Hand cards chosen to pay the spell's additional discard cost (CR 601.2f;
- `crates/engine/src/types/stack.rs:100` — Whether the caster is casting the spell for its printed alternative cost (CR 601.2f —
- `crates/engine/src/types/stack.rs:476` — Hand cards paying a cast's additional discard cost (CR 601.2f) or an activation's
- `crates/engine/src/types/stack.rs:3174` — instead *defined* by an additional cost (CR 601.2b/601.2f) — Immoral Bargain's "As an
- `crates/engine/src/types/stack.rs:3181` — Strive's own sibling of [`Self::sacrifice_scaled`] (CR 601.2c/601.2f/702.42) — Twinflame's
- `crates/engine/tests/game.rs:2951` — Name the hand cards paying an additional discard cost (CR 601.2f). Unset (empty) for a
- `crates/engine/tests/game.rs:2965` — Name the permanents paying an additional sacrifice cost (CR 601.2f). Unset (empty) for a
- `crates/engine/tests/game.rs:3014` — Cast for the spell's printed alternative cost (CR 601.2f — [`CardDef::alternative_cost`])
- `crates/engine/tests/game.rs:40524` — {R} pip is owed (CR 601.2f — cost reduction never goes below 0 remaining generic).
- `crates/engine/tests/game.rs:40656` — controller's creatures, floored at 0 (CR 601.2f — never negative, never overpays).
- `crates/engine/tests/game.rs:60493` — unpayable additional cost makes the cast illegal (CR 601.2f) — with no other creature card
- `crates/engine/tests/game.rs:63186` — ── Additional cast costs (#11): "discard a card" (CR 601.2f) ─────────────────────────
- `crates/engine/tests/game.rs:63325` — ── Additional cast costs (#92): optional "sacrifice any number" (CR 601.2f) ────────────
- `crates/engine/tests/game.rs:63706` — three creatures (CR 601.2f/602.2b) ────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:64155` — additional life payment (CR 601.2f), not the mana cost: casting with X = 3 spends exactly
- `crates/engine/tests/game.rs:92818` — ── Printed alternative cost with a non-mana rider (#190, CR 601.2f, Invigorate) ────────────
- `crates/engine/tests/game.rs:92850` — No `fund_mana` — the alternative cost must pay nothing at all (CR 601.2f).

### CR 601.2g

- `crates/engine/src/cast.rs:108` — "Reveal a creature card from your hand" (CR 601.2g — Disaster Radius): can't be cast
- `crates/engine/src/cast.rs:124` — (CR 601.2g — Disaster Radius). 0 if there is no such card; [`Game::cast_additional_cost_gate`]
- `crates/engine/src/cast.rs:508` — "Reveal a creature card from your hand" (CR 601.2g — Disaster Radius): read before any
- `crates/engine/src/core.rs:833` — [`AdditionalCost::reveal_creature_from_hand`] (CR 601.2g — Disaster Radius's "reveal a
- `crates/engine/src/de.rs:697` — (CR 601.2g — Disaster Radius).
- `crates/engine/src/de.rs:732` — "Reveal a creature card from your hand" (CR 601.2g — Disaster Radius) —
- `crates/engine/src/resolution/damage.rs:209` — Disaster Radius's "X is the revealed card's mana value" (CR 601.2g) reads
- `crates/engine/src/types/card.rs:1579` — [`AdditionalCost::reveal_creature_from_hand`] (CR 601.2g — Disaster Radius's "reveal a
- `crates/engine/src/types/effect/shared.rs:160` — [`AdditionalCost::reveal_creature_from_hand`] (CR 601.2g) — Disaster Radius's "X is the
- `crates/engine/tests/game.rs:60530` — "the revealed card stays in hand — a reveal, not a discard (CR 601.2g)",

### CR 601.2h

- `crates/engine/src/cast.rs:517` — ponytail: the additional discard is a *cost* (CR 601.2h — paid pre-stack, before
- `crates/engine/src/cast.rs:525` — CR 701.8/601.2h: a cost discard is still a discard — fires "whenever you discard"
- `crates/engine/src/cast.rs:537` — A delve or escape graveyard-exile payment (CR 601.2f/601.2h — paid pre-stack, before
- `crates/engine/src/cast.rs:2315` — ponytail: CR 601.2c chooses targets *before* CR 601.2h pays costs; here the
- `crates/engine/src/triggers.rs:3041` — CR 601.2h: the mana actually spent on this cast, locked in when the trigger
- `crates/engine/src/types/effect/shared.rs:139` — The mana actually spent (CR 601.2h) to cast the spell that fired a `Trigger::CastSpell`
- `crates/engine/src/types/effect/shared.rs:1921` — CR 601.2h: a `CastSpell` trigger's `Amount::TriggeringSpellManaSpent` reads resolve against
- `crates/engine/src/types/effect/shared.rs:2821` — actually spent on the triggering spell (CR 601.2h) — Manaform Hellkite's "X is the amount of
- `crates/engine/src/types/trigger.rs:711` — The mana actually spent to cast the triggering spell (CR 601.2h/202.3), for a
- `crates/engine/tests/game.rs:34262` — "each player may pay any amount of mana" — any amount they can actually pay (CR 601.2h).
- `crates/engine/tests/game.rs:56360` — "X is the amount of mana spent to cast that spell" (CR 601.2h) — not the spell's mana
- `crates/engine/tests/game.rs:79027` — Encore's mana cost is checked before any exile (CR 601.2h / 702.140a): an unfunded encore is
- `crates/engine/tests/game.rs:96610` — as part of casting (CR 601.2h), before targets are chosen — two bystander creatures give

### CR 601.2i

- `crates/engine/src/triggers.rs:804` — "When you cast this spell" (CR 601.2i/603.3): scanned off the cast card's
- `crates/engine/src/types/trigger.rs:464` — "When you cast this spell" (CR 601.2i/603.3): a triggered ability on the spell's *own*
- `crates/engine/src/types/trigger.rs:727` — (CR 601.2i), so it resolves even if the spell is later countered. See
- `crates/engine/tests/game.rs:24780` — Animar." CR 601.2i: the trigger goes on the stack above the spell that caused it, so it
- `crates/engine/tests/game.rs:69723` — placed above the spell (CR 601.2i) and resolved separately (covered on its own in
- `crates/engine/tests/game.rs:93170` — (CR 601.2i/603.3), so the reanimated creature arrives first.
- `crates/engine/tests/game.rs:93194` — The cast trigger goes on the stack above the spell (CR 601.2i/603.3): "you may" is answered

### CR 601.3a

- `crates/engine/src/playable.rs:361` — Alchemist's Refuge's "you may cast spells this turn as though they had flash" (CR 601.3a)
- `crates/engine/src/types/card.rs:2116` — reset each turn at untap) — CR 601.3a, granted by [`Effect::Misc(MiscEffect::GrantFlashThisTurn)`]
- `crates/engine/src/types/stack.rs:2646` — `player` may cast spells this turn as though they had flash (CR 601.3a — Alchemist's
- `crates/engine/tests/game.rs:14376` — flash." — CR 601.3a timing permission, unfiltered (every spell, not just a subset).
- `crates/engine/tests/game.rs:14444` — The permission is granted "this turn" only (CR 601.3a) — gone by the granting player's

### CR 601.3e

- `crates/engine/src/cast.rs:1931` — sorcery speed only (CR 601.3e/307), no flash.
- `crates/engine/src/combat.rs:195` — waives timing (CR 601.3e — it arrives mid-resolution), while Intet's grant is an ordinary
- `crates/engine/src/de.rs:378` — "Cast this spell only during combat" (CR 601.3e) — `cast_only_during_combat = true`;
- `crates/engine/src/de.rs:382` — "Cast this spell only before attackers are declared" (CR 601.3e — Master Warcraft)
- `crates/engine/src/playable.rs:331` — "Cast this spell only during combat" (CR 601.3e — Cauldron Dance) is a restriction
- `crates/engine/src/playable.rs:338` — "Cast this spell only before attackers are declared" (CR 601.3e — Master Warcraft): the
- `crates/engine/src/playable.rs:365` — timing (CR 601.3e — the permission comes mid-resolution, e.g. with the cascading spell
- `crates/engine/src/types/card.rs:112` — Whether this step is one of combat's five (CR 500.4/601.3e — begin combat through end of
- `crates/engine/src/types/card.rs:635` — "Cast this spell only during combat" (CR 601.3e's named-window restriction — Cauldron
- `crates/engine/src/types/card.rs:642` — "Cast this spell only before attackers are declared" (CR 601.3e's named-window restriction
- `crates/engine/tests/game.rs:598` — CR 601.3e timing gate, trivialized to isolate the restriction from its two effect clauses
- `crates/engine/tests/game.rs:14312` — Cauldron Dance (CR 601.3e): "Cast this spell only during combat." An instant with the
- `crates/engine/tests/game.rs:14342` — The same restricted instant IS castable once combat has begun (CR 601.3e's named window

### CR 602

- `crates/engine/src/apply.rs:2227` — Quintorius's activated ability: grant the free-cast permission for the chosen (CR 602, CR 601, CR 1…
- `crates/engine/src/cast.rs:3` — Primary: CR 601 (casting spells), CR 602 (activating abilities), CR 118 (costs / payments).
- `crates/engine/src/cast.rs:1279` — A hand-activated ability is still an activated ability (CR 602) — requires priority
- `crates/engine/src/cast.rs:1345` — CR 113.6/602: this is an activated ability — its authored payload goes on the stack (a
- `crates/engine/src/cast.rs:2228` — ability): an ordinary activated ability restricted to a legal sorcery-speed moment. (CR 602, CR 113)
- `crates/engine/src/cast.rs:2274` — An activated ability carries no {X} (mirrors `run`'s "abilities (CR 602, CR 113)
- `crates/engine/src/query.rs:156` — A non-mana activated ability the player can afford, or a prepared back-face cast. (CR 602, CR 601, …
- `crates/engine/src/query.rs:556` — An activated ability granted by an Aura attached to `source` (Fallen Ideal's "Sacrifice (CR 602, CR…
- `crates/engine/src/resolution/damage.rs:97` — through the spell path, never a triggered/activated ability's). (CR 602, CR 601, CR 603)
- `crates/engine/src/resolution/mill.rs:77` — permission (the free-cast permission comes later, from the activated ability). (CR 602, CR 601, CR …
- `crates/engine/src/resolution/pause_exile_cast.rs:28` — Quintorius's activated ability pauses on a card-pick choice over this source's (CR 602, CR 113)
- `crates/engine/src/types/effect/shared.rs:11` — match arm with no separate pure path. (CR 602, CR 403.5, CR 601)
- `crates/engine/src/types/mana.rs:397` — so both stay at their default/`false`). (CR 602, CR 601, CR 113)
- `crates/engine/src/types/stack.rs:1990` — Whether this is an *activated* ability (CR 602 — a permanent's activated ability,
- `crates/engine/src/types/stack.rs:2211` — Whether this is an *activated* ability (CR 602) rather than a triggered one (CR 603) —
- `crates/engine/src/types/stack.rs:2289` — object id) starts with a clean cap — correct, since a new object is a new game object. (CR 602, CR …
- `crates/engine/tests/game.rs:16169` — The activated ability's index on a fixture whose only ability is that one activated ability. (CR 60…
- `crates/engine/tests/game.rs:19193` — The {W} activated ability is the third ability (etb choose-color, static grant, activated). (CR 602…
- `crates/engine/tests/game.rs:22928` — resolve_top_of_stack(&mut game); // resolve the activated ability off the stack (CR 602, CR 405, CR…
- `crates/engine/tests/game.rs:29996` — (exercises the `granted_attachment_abilities` enumeration in `push_activatable_abilities`). (CR 602…
- `crates/engine/tests/game.rs:30123` — +2/+1 until end of turn" activated ability — proves the `granted_ability` field is wired, (CR 602, …
- `crates/engine/tests/game.rs:46804` — Guard: without `sorcery_speed`, an ordinary activated ability keeps working at instant (CR 602, CR …
- `crates/engine/tests/game.rs:53453` — has from the hand, not a permanent's activated ability. (CR 602, CR 402.5, CR 113)
- `crates/engine/tests/game.rs:53755` — CR 113.6/602: a hand-activated ability resolves off the stack, not inline.
- `crates/engine/tests/game.rs:53809` — "{0}: Draw a card" — a non-mana activated ability (CR 602), so it uses the stack — and P1
- `crates/engine/tests/game.rs:58368` — resolve_top_of_stack(&mut game); // the activated ability resolves, pausing on the choice (CR 602, …
- `crates/engine/tests/game.rs:58428` — resolve_top_of_stack(&mut game); // the activated ability resolves, pausing on the choice (CR 602, …
- `crates/engine/tests/game.rs:58583` — Quintorius, Loremaster's activated ability: "{1}{R}{W}, {T}, Sacrifice a Spirit: Choose (CR 602, CR…
- `crates/engine/tests/game.rs:62411` — A non-capped activated ability is unaffected by another permanent's once-per-turn cap. (CR 602, CR …
- `crates/engine/tests/game.rs:82756` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.
- `crates/engine/tests/game.rs:82805` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.
- `crates/engine/tests/game.rs:82846` — CR 113.6/602: the landcycling ability resolves off the stack before it searches.

### CR 602.2

- `crates/engine/src/cast.rs:1619` — CR 602.2: casting a prepared permanent's back face is its controller's action — a
- `crates/engine/src/cast.rs:2113` — CR 602.2: only a permanent's *controller* may activate its abilities — a stolen
- `crates/engine/src/priority.rs:160` — CR 602.2/605.3: tapping a permanent for mana is its *controller*'s action — a stolen
- `crates/engine/src/priority.rs:433` — CR 602.2/605.3: a player's available mana counts the permanents they *control*, not
- `crates/engine/tests/game.rs:34522` — (CR 602.2), and the copy effect still resolves off the stack afterward.
- `crates/engine/tests/game.rs:42902` — ── CR 800.4a control-effect timestamp precedence + CR 602.2/509.1a controller gates ────────
- `crates/engine/tests/game.rs:42905` — creature), so one card exercises CR 800.4a timestamp precedence and the CR 602.2/509.1a
- `crates/engine/tests/game.rs:43063` — CR 602.2/605.3: tapping a permanent for mana is its controller's action. P0 steals two of
- `crates/engine/tests/game.rs:43096` — "the owner can't tap a land it no longer controls (CR 602.2)",
- `crates/engine/tests/game.rs:43102` — CR 602.2: only a permanent's controller may activate its abilities. P0 steals two of P1's
- `crates/engine/tests/game.rs:43141` — "the owner can't activate a permanent it no longer controls (CR 602.2)",
- `crates/engine/tests/game.rs:43259` — controller can activate it (CR 602.2 — slice-1's gate flip). Four players, so "target
- `crates/engine/tests/game.rs:43300` — .expect("the new controller activates the donated Sol Ring (CR 602.2)");
- `crates/engine/tests/game.rs:43416` — (slice-1's controller gate, CR 602.2).
- `crates/engine/tests/game.rs:43467` — Slice-1's mana gate (CR 602.2): P0 taps the Forest it gained; it can't tap the Island it lost.
- `crates/engine/tests/game.rs:43485` — "P0 can't tap the Island it no longer controls (CR 602.2)"

### CR 602.2b

- `crates/engine/src/apply.rs:1077` — "Activate only once each turn" (CR 602.2b) resets at the start of every
- `crates/engine/src/cast.rs:1114` — Validate a [`SacrificeCost`]'s named picks (CR 118.9/602.2b — checked before anything is
- `crates/engine/src/cast.rs:1202` — Resolve the cycling sacrifice cost up front (CR 118.9/602.2b), same choke an ordinary
- `crates/engine/src/cast.rs:2199` — source has that many counters of the right kind on it (CR 602.2b — an uncompletable
- `crates/engine/src/cast.rs:2210` — A "mill a card" additional cost (CR 701.13/602.2b — Millikin's "{T}, Mill a card:
- `crates/engine/src/cast.rs:2215` — "Activate only once each turn" (CR 602.2b — an activation restriction; Beledros
- `crates/engine/src/cast.rs:2251` — A player-declared-X counter-removal cost (CR 601.2b/602.2b — Fungal Reaches' "Remove X
- `crates/engine/src/cast.rs:2344` — A "discard a card" cost (CR 602.2b — Wild Mongrel's "Discard a card") names exactly
- `crates/engine/src/cast.rs:2477` — "Discard a card" as part of the cost (CR 602.2b — Wild Mongrel's "Discard a card").
- `crates/engine/src/de.rs:1908` — "Activate only once each turn" (CR 602.2b) on an activated ability, or "this
- `crates/engine/src/pending/handlers/targets.rs:450` — Advocate's targeted graveyard-exile cost, CR 601.2c/602.2b): exactly `count` distinct legal
- `crates/engine/src/playable.rs:577` — distinct matches or the cast is rejected, CR 602.2b). No such cost on this spell rejects
- `crates/engine/src/types/card.rs:682` — [`ActivationCost::sacrifice`] uses (CR 602.2b — an uncompletable/unnamed cost makes the
- `crates/engine/src/types/effect/shared.rs:1426` — can't activate the ability (CR 602.2b — an uncompletable cost makes activation illegal).
- `crates/engine/src/types/effect/shared.rs:1431` — bloom's "remove a charge counter") — gated the same way (CR 602.2b: fewer than
- `crates/engine/src/types/effect/shared.rs:1438` — that kind (CR 602.2b — an uncompletable cost makes activation illegal); `X = 0` is always
- `crates/engine/src/types/effect/shared.rs:1456` — ability). CR 602.2b — an unmet activation restriction makes the activation illegal.
- `crates/engine/src/types/effect/shared.rs:1474` — Paid on activation; a library with fewer than this many cards can't pay it (CR 602.2b —
- `crates/engine/src/types/effect/shared.rs:1477` — "Discard a card" as part of the cost (CR 602.2b/118 — Wild Mongrel's "Discard a card:
- `crates/engine/src/types/effect/shared.rs:1482` — hand, or names the same card twice — can't pay it (CR 602.2b — an uncompletable/illegal
- `crates/engine/src/types/effect/shared.rs:1813` — "if this ability has been activated `at_least` or more times this turn" (CR 602.2b —
- `crates/engine/src/types/mana.rs:220` — Flashback—Sacrifice three creatures, CR 601.2f/602.2b). `None` for no sacrifice cost on
- `crates/engine/src/types/mana.rs:310` — the cast names exactly N distinct matching permanents (CR 601.2f/602.2b) or is rejected.
- `crates/engine/src/types/stack.rs:211` — Hand cards named to pay a `discard_cost` (CR 602.2b — Wild Mongrel's "Discard a
- `crates/engine/src/types/stack.rs:477` — `discard_cost` (CR 602.2b) — whichever the resolved action's kind actually charges;
- `crates/engine/src/types/stack.rs:1138` — graveyard to pay an activated ability's own targeted exile cost (CR 601.2c/602.2b —
- `crates/engine/src/types/stack.rs:2283` — Any activated ability was activated (CR 602.2b), recorded for every activation, not just
- `crates/engine/tests/game.rs:34465` — CR 118.9/602.2b — "Sacrifice two other creatures" needs exactly two, both distinct from the
- `crates/engine/tests/game.rs:34645` — CR 602.2b — an uncompletable "mill a card" cost makes the activation illegal.
- `crates/engine/tests/game.rs:34712` — CR 602.2b — an uncompletable/illegal "discard a card" cost makes the activation illegal:
- `crates/engine/tests/game.rs:34766` — Paying Wild Mongrel's discard cost is a real discard (CR 701.8/602.2b): it fires "whenever
- `crates/engine/tests/game.rs:51030` — "CR 602.2b: no +1/+1 counter on Benevolent Hydra to pay the removal cost"
- `crates/engine/tests/game.rs:61713` — sacrifice; CR 602.2b — an unnamed or illegal sacrifice pick makes the activation illegal.
- `crates/engine/tests/game.rs:62384` — "Activate only once each turn" cap (CR 602.2b, an activation restriction), even though
- `crates/engine/tests/game.rs:63706` — three creatures (CR 601.2f/602.2b) ────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:63755` — "CR 602.2b — Sacrifice three creatures means exactly three, not two"
- `crates/engine/tests/game.rs:63775` — "CR 602.2b — Sacrifice three creatures means exactly three, not four"
- `crates/engine/tests/game.rs:64048` — CR 602.2b: an activation cost that can't be paid makes the activation illegal.
- `crates/engine/tests/game.rs:69957` — "Remove a charge counter from this enchantment: Add one mana of any color." CR 602.2b: an
- `crates/engine/tests/game.rs:70051` — CR 602.2b: removing a story counter is uncompletable with zero on it.
- `crates/engine/tests/game.rs:70090` — the first Staff's `taps_self` state so this specifically exercises CR 602.2b.
- `crates/engine/tests/game.rs:70433` — CR 602.2b: an activation cost that can't be fully paid is illegal — removing more storage
- `crates/engine/tests/game.rs:87126` — The remove-a-counter activation cost path (CR 602.2b), keyed to the -1/-1 kind rather than

### CR 602.5b

- `crates/engine/src/cast.rs:2154` — or more lands" — Temple of the False God). CR 602.5b: an unmet restriction makes the
- `crates/engine/src/cast.rs:2227` — "Activate only as a sorcery" (CR 602.5b — Ozolith, the Shattered Spire's counter
- `crates/engine/src/cast.rs:2230` — phase" variant exists because every pool card spelling this restriction means CR 602.5b.
- `crates/engine/src/de.rs:1914` — "Activate only as a sorcery" (CR 602.5b): restricts activation to a legal
- `crates/engine/src/types/effect/shared.rs:1460` — An activation timing restriction: "Activate only as a sorcery" (CR 602.5b — Ozolith, the
- `crates/engine/src/types/effect/shared.rs:1792` — Sphere's "Activate only if …", CR 602.5b) — [`Game::ability_activation_gate`] evaluates the
- `crates/engine/tests/game.rs:46717` — sorcery" ability is a legal sorcery-speed moment (CR 602.5b), so it may be activated.
- `crates/engine/tests/game.rs:78026` — Temple of the False God uses (CR 602.5b), repurposed to observe the turn-scoped flag.
- `crates/engine/tests/game.rs:80284` — "Activate only as a sorcery" (CR 602.5b): the `{X}` ability is not activatable outside a legal
- `crates/engine/tests/game.rs:100893` — activation restriction (Glistening Sphere, CR 602.5b); `Amount::OpponentsPoisonCounters` sums
- `crates/engine/tests/game.rs:100916` — opponent has three or more poison counters." CR 602.5b makes an activation with the

### CR 602.5e

- `crates/engine/src/cast.rs:1261` — Activate a hand card's [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e — a
- `crates/engine/src/de.rs:490` — A hand-activated, discard-this-card ability (CR 113.6/602.5e, Magma Opus) — zero or
- `crates/engine/src/types/card.rs:687` — A hand-activated, discard-this-card ability (CR 113.6/602.5e — an activated ability that
- `crates/engine/src/types/card.rs:985` — A hand-activated, discard-this-card ability (CR 113.6/602.5e), carried by
- `crates/engine/src/types/stack.rs:120` — Activate a hand card's [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e — a
- `crates/engine/src/types/stack.rs:3103` — Activate `card`'s [`CardDef::hand_ability`] entry at `index` (CR 113.6/602.5e): pay its
- `crates/engine/tests/game.rs:53737` — CR 113.6/602.5e — "{U/R}{U/R}, Discard this card: Create a Treasure token." functions

### CR 602.8a

- `crates/engine/tests/game.rs:16715` — non-active player holding priority can legally cast it (CR 602.8a).

### CR 603

- `crates/engine/src/amount.rs:144` — `CastSpell`-triggered ability's effect is contextualized at placement), so a live read (CR 603, CR …
- `crates/engine/src/apply.rs:395` — real ability is a triggered ability that uses the stack (CR 603) and can be responded to;
- `crates/engine/src/pending/handlers/optional.rs:43` — still-resolving spell rather than going on the stack as a new triggered ability — (CR 603, CR 405, …
- `crates/engine/src/pipeline.rs:8` — Primary: CR 704 (SBA fixpoint), CR 603 (trigger enqueue / APNAP placement), CR 608
- `crates/engine/src/priority.rs:1725` — SBA sweep and death triggers are handled by `submit` after this step, and a (CR 704, CR 603, CR 104…
- `crates/engine/src/resolution/damage.rs:97` — through the spell path, never a triggered/activated ability's). (CR 602, CR 601, CR 603)
- `crates/engine/src/state.rs:350` — `illusion_on_source_leave`, this places a real CR 603 triggered ability rather than an
- `crates/engine/src/triggers.rs:3` — Primary: CR 603 (triggered abilities). Also: CR 603.6c/603.10 look-back, CR 603.3c no
- `crates/engine/src/triggers.rs:487` — A creature dying (battlefield → graveyard) fires its own Dies trigger. (CR 603.6, CR 403.5, CR 603)
- `crates/engine/src/triggers.rs:606` — A dying token fires its Dies trigger before vanishing; its arena slot is (CR 603.6, CR 111, CR 603)
- `crates/engine/src/triggers.rs:4239` — A source that has already left the game entirely (a token's own Dies trigger, fired (CR 603.6, CR 1…
- `crates/engine/src/types/card.rs:457` — of a several-ability simultaneous group grows from a real card (see wire-protocol-and-visibility sp…
- `crates/engine/src/types/card.rs:830` — zone tags — defer until one exists. (CR 603, CR 108.4, CR 403.5)
- `crates/engine/src/types/stack.rs:1991` — cycling/hand activation, or a copy of one) rather than a *triggered* ability (CR 603).
- `crates/engine/src/types/stack.rs:2211` — Whether this is an *activated* ability (CR 602) rather than a triggered one (CR 603) —
- `crates/engine/tests/game.rs:3451` — Any submitted intent triggers the SBA sweep that eliminates the doomed players. (CR 704, CR 603)
- `crates/engine/tests/game.rs:3953` — death triggers resolve inside the first-strike step's priority window, before the regular (CR 117, …
- `crates/engine/tests/game.rs:9114` — "the SBA sweep eliminated P2 by the time the cast was processed (CR 704, CR 603)"
- `crates/engine/tests/game.rs:9420` — resolve_top_of_stack(&mut game); // the Pest's Dies trigger resolves: gain 1 life. (CR 603.6, CR 11…
- `crates/engine/tests/game.rs:20867` — resolve_top_of_stack(&mut game); // The Dies trigger resolves: draw a card. (CR 603.6, CR 603, CR 1…
- `crates/engine/tests/game.rs:30901` — Resolve Scriv (multi-player: pass priority all the way around), landing on its ETB trigger's (CR 11…
- `crates/engine/tests/game.rs:30973` — Resolve the enchanted-creature-attacks trigger (3-player: pass priority all the way around). (CR 11…
- `crates/engine/tests/game.rs:38280` — Any submitted intent triggers the SBA sweep that eliminates the doomed player. (CR 704, CR 603)
- `crates/engine/tests/game.rs:51162` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: gain life, then draw, both = 3. (CR 603…
- `crates/engine/tests/game.rs:51194` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: create 2 Thopters. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51223` — Dying source: Lifeblood Hydra's Dies trigger fills `SourcePower` from LKI. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51274` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: create 3 Treasures. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51519` — ability_index: 2, // 0 = enters-with-counters static, 1 = the new dies trigger. (CR 603.6, CR 122, …
- `crates/engine/tests/game.rs:57664` — trigger pauses on the mode choice. Returns the dragon's controller. (CR 108.3, CR 403.5, CR 603)
- `crates/engine/tests/game.rs:57687` — resolve_top_of_stack(game); // Shock resolves → SBA kills the dragon → dies trigger placed. (CR 704…
- `crates/engine/tests/game.rs:57763` — The real pool card: its dies trigger is a "choose one" over impulse-exile and three (CR 603.6, CR 4…
- `crates/engine/tests/game.rs:57786` — resolve_top_of_stack(&mut game); // the destroy resolves → Atsushi dies → dies trigger placed. (CR …
- `crates/engine/tests/game.rs:61277` — ability — it keeps priority in Main1 — so it triggers a refresh without changing the phase. (CR 117…
- `crates/engine/tests/game.rs:69249` — resolve_whole_stack(&mut game); // the destroy, then its own dies trigger (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:75549` — resolve_whole_stack(&mut game); // the destroy, then the Pest's own dies trigger (CR 603.6, CR 603)

### CR 603.2b

- `crates/engine/tests/game.rs:4822` — With no legal graveyard target, the "you may" trigger still offers its yes/no (CR 603.2b),

### CR 603.2c

- `crates/engine/src/de.rs:1944` — The cost to accept an `optional` trigger (CR 603.2c "you may pay …"), e.g. Trudge
- `crates/engine/src/triggers.rs:3493` — Queue [`Trigger::BecomesTargeted`] triggers (CR 603.2c "becomes the target of a spell"): a
- `crates/engine/src/triggers.rs:3555` — Queue [`Trigger::SpellTargetsThisOnly`] triggers (Mirrorwing Dragon — CR 603.2c narrowed
- `crates/engine/src/types/trigger.rs:453` — Whenever a permanent becomes the target of a spell (CR 603.2c "becomes the target"), fired
- `crates/engine/src/types/trigger.rs:529` — (CR 603.2c "becomes the target" narrowed to "targets only" — Mirrorwing Dragon: "Whenever
- `crates/engine/src/types/trigger.rs:646` — source/controller (CR 603.2c).
- `crates/engine/tests/game.rs:24230` — Treasure token" — the Goldspan Dragon shape (CR 603.2c "becomes the target").
- `crates/engine/tests/game.rs:101193` — target opponent gets a poison counter." CR 603.2c — the watch is over every creature its

### CR 603.3

- `crates/engine/src/triggers.rs:804` — "When you cast this spell" (CR 601.2i/603.3): scanned off the cast card's
- `crates/engine/src/triggers.rs:2437` — Queue "deals damage to an opponent" triggers (CR 603.3, Looter il-Kor: "Whenever this
- `crates/engine/src/types/trigger.rs:335` — noncombat alike (CR 603.3, Looter il-Kor: "Whenever this creature deals damage to an
- `crates/engine/src/types/trigger.rs:464` — "When you cast this spell" (CR 601.2i/603.3): a triggered ability on the spell's *own*
- `crates/engine/tests/game.rs:82672` — CR 603.3 — "whenever this creature deals damage to an opponent": noncombat damage the
- `crates/engine/tests/game.rs:92179` — "Another … creature you control enters" excludes Riku's own entry — CR 603.3's implicit
- `crates/engine/tests/game.rs:93170` — (CR 601.2i/603.3), so the reanimated creature arrives first.
- `crates/engine/tests/game.rs:93194` — The cast trigger goes on the stack above the spell (CR 601.2i/603.3): "you may" is answered

### CR 603.3b

- `crates/engine/src/cast.rs:2718` — placed ability — the copy trigger lands above it (CR 603.3b) and, on resolution, mints
- `crates/engine/src/resolution/copy.rs:355` — sits directly above the original (CR 603.3b), so it's the topmost stack ability
- `crates/engine/src/resolution/pause_edict.rs:58` — ponytail: this "when you do" is CR 603.3b's separate reflexive trigger, modeled here
- `crates/engine/src/resolution/pause_may.rs:187` — (this is a real triggered ability, not Echo — CR 603.3b, not CR 702.31).
- `crates/engine/src/resolution/sequence_steps.rs:87` — A reflexive "when you do" trigger (CR 603.3b — Forum Filibuster): the "you do" is
- `crates/engine/src/resolution/sequence_steps.rs:102` — A reflexive "when one or more nonland cards are exiled this way" trigger (CR 603.3b —
- `crates/engine/src/state.rs:216` — shape (CR 603.3b's "one or more"). Pushed by [`Game::enqueue_triggers`]'s
- `crates/engine/src/triggers.rs:415` — simultaneous triggers (CR 603.3b) — the two are queued as separate
- `crates/engine/src/triggers.rs:461` — tokens" (CR 603.3b): record the controller now, deduped and fired once per
- `crates/engine/src/triggers.rs:1012` — CR 603.3b "one or more creature tokens": the whole batch, not each token, is the
- `crates/engine/src/triggers.rs:1031` — CR 603.3b "one or more creatures you control deal combat damage to one or more
- `crates/engine/src/triggers.rs:1056` — Conspiracy Theorist's "one or more nonland cards" (CR 701.8/603.3b): the whole
- `crates/engine/src/triggers.rs:1110` — Primo, the Unbounded's batch combat-damage Fractal trigger (CR 510.2/603.3b): reads
- `crates/engine/src/triggers.rs:2064` — Enqueue a reflexive "when you do" triggered ability (CR 603.3b — Forum Filibuster). Called
- `crates/engine/src/triggers.rs:2101` — (CR 603.3b — Augusta, Order Returned), the count-gated twin of
- `crates/engine/src/triggers.rs:2937` — controller's watcher ability once per defending player — CR 603.3b's "one or more", a
- `crates/engine/src/triggers.rs:4223` — APNAP (CR 603.3b): waiting triggers go on the stack active-player-first, then each
- `crates/engine/src/triggers.rs:4503` — instances they then order among themselves (CR 603.3b).
- `crates/engine/src/types/card.rs:763` — an ETB payoff (Mulldrifter's draw two) still resolves first (CR 702.74a, CR 603.3b — see
- `crates/engine/src/types/effect/shared.rs:1085` — Thread the token a reflexive trigger's parent just minted (CR 603.3b — Forum Filibuster)
- `crates/engine/src/types/effect/zone.rs:113` — "When one or more nonland cards are exiled this way, …" (CR 603.3b — Augusta, Order
- `crates/engine/src/types/stack.rs:984` — it — Rupture Spire's own ETB triggered ability (CR 603.3b), NOT Echo, though it shares
- `crates/engine/src/types/trigger.rs:443` — Whenever the controller creates one or more creature tokens (CR 603.3b's "one or more" —
- `crates/engine/src/types/trigger.rs:498` — power 0, deal combat damage to a player (CR 510.2/603.3b, Primo, the Unbounded: "Whenever
- `crates/engine/src/types/trigger.rs:636` — (Contaminant Grafter) — CR 603.3b's batch wording: **one** trigger per combat damage step
- `crates/engine/src/types/trigger.rs:839` — finds that ability still on the stack (its trigger sits directly above it, CR 603.3b) by
- `crates/engine/tests/game.rs:14184` — trigger simultaneously and must be ordered (CR 603.3b); CR 603.3d then chooses each one's
- `crates/engine/tests/game.rs:14223` — Order: the bounce first, the graveyard exile second (CR 603.3b, the printed order).
- `crates/engine/tests/game.rs:27000` — ── ETB sacrifice-unless (CR 603.3b, rupture_spire / trevas_ruins) ───────────────────
- `crates/engine/tests/game.rs:44496` — window open (CR 603.3b) — proof the doubling targets were chosen at placement.
- `crates/engine/tests/game.rs:58126` — The batch nonland-discard trigger is a real stack object (CR 603.3b), not an
- `crates/engine/tests/game.rs:64777` — genuine reflexive triggered ability (CR 603.3b): it goes on the stack as its own object,
- `crates/engine/tests/game.rs:70110` — also exercises CR 603.3b's "one or more" batch semantics: two tokens made in one resolution
- `crates/engine/tests/game.rs:70126` — "two creature tokens made in one resolution put exactly one story counter (CR 603.3b)"
- `crates/engine/tests/game.rs:71314` — triggered ability on the stack (CR 603.3b), synchronously with the Cast itself.
- `crates/engine/tests/game.rs:73722` — number of +1/+1 counters on it equal to the damage dealt." CR 510.2/603.3b: the whole
- `crates/engine/tests/game.rs:83315` — (CR 603.3b ordering), each with its own "may" gate and its own color-filtered target.
- `crates/engine/tests/game.rs:88512` — CR 603.3b: when two different controllers' abilities trigger off the same event, they're
- `crates/engine/tests/game.rs:88560` — "the active player's death watch is offered first (CR 603.3b APNAP)"
- `crates/engine/tests/game.rs:101254` — proliferate" — CR 603.3b's batch wording: one trigger per combat damage step, however many

### CR 603.3c

- `crates/engine/src/effects.rs:815` — ability placed with no target (declined, or none legal — CR 601.2c/603.3c, see
- `crates/engine/src/resolution/resolve_misc.rs:241` — cost." "Up to one": no chosen target (declined, or none legal — CR 603.3c already
- `crates/engine/src/triggers.rs:3` — Primary: CR 603 (triggered abilities). Also: CR 603.6c/603.10 look-back, CR 603.3c no
- `crates/engine/src/triggers.rs:15` — Dropped: the ability targets but has no legal target (CR 603.3c).
- `crates/engine/src/triggers.rs:4220` — Trigger doubling (CR 603.3c — Harmonic Prodigy / Veyran): before placement, an on-board
- `crates/engine/src/triggers.rs:4379` — stack, or drop it if it targets with no legal target (CR 603.3c — continue the loop).
- `crates/engine/src/triggers.rs:4480` — Trigger doubling (CR 603.3c — Harmonic Prodigy, Veyran, Voice of Duality): for each pending
- `crates/engine/src/triggers.rs:4508` — source permanent is `source` trigger an additional time (CR 603.3c). Both example cards
- `crates/engine/src/triggers.rs:4571` — `player` — Veyran's magecraft cause (CR 603.3c). A spell copy is always an instant/sorcery,
- `crates/engine/src/triggers.rs:4591` — ([`Placement::NoLegalTarget`], CR 603.3c); if it's targetless, push it onto the stack
- `crates/engine/src/triggers.rs:4656` — CR 603.3c drops a *mandatory*-target ability with no legal target. "Up to one"
- `crates/engine/src/types/card.rs:1868` — once it's left the graveyard (CR 603.3c: the ETB ability then has no legal target and is
- `crates/engine/src/types/effect/shared.rs:1074` — one" ability with no target chosen (declined, or none legal — CR 601.2c/603.3c) still goes
- `crates/engine/src/types/filter.rs:137` — choice; empty (CR 603.3c: the ability is dropped) if the captured card has since left the
- `crates/engine/src/types/stack.rs:842` — - an ability's first target clause before it goes on the stack (CR 601.2c/603.3c),
- `crates/engine/src/types/stack.rs:1970` — Whether [`Game::place_pending_triggers`] has already run trigger-doubling (CR 603.3c —
- `crates/engine/tests/game.rs:4823` — but accepting fizzles harmlessly (CR 603.3c) rather than forcing an action or panicking.
- `crates/engine/tests/game.rs:13459` — harmlessly (CR 603.3c), same as declining.
- `crates/engine/tests/game.rs:31083` — With no creature an opponent controls, the ETB trigger has no legal target (CR 603.3c) — it
- `crates/engine/tests/game.rs:51633` — anything has no legal target — the exile ability can't be activated (CR 603.3c).
- `crates/engine/tests/game.rs:74990` — legal target and is dropped (CR 603.3c) rather than pausing or panicking.
- `crates/engine/tests/game.rs:80463` — --- Trigger doubling (CR 603.3c — Harmonic Prodigy, Veyran, Voice of Duality) ---
- `crates/engine/tests/game.rs:80597` — "Harmonic doubles another Wizard's ETB trigger (CR 603.3c)",
- `crates/engine/tests/game.rs:80693` — Veyran's own trigger-doubling static doubles her own magecraft trigger too (CR 603.3c,
- `crates/engine/tests/game.rs:80748` — "two doublers stack additively (CR 603.3c): one original + two extra instances",
- `crates/engine/tests/game.rs:83341` — First ordered trigger: the "may" gate survives ordering (CR 603.3c).

### CR 603.3d

- `crates/engine/src/pending/handlers/fanout.rs:23` — the chooser counters for `target_player` (empty declines — CR 603.3d). Put one +1/+1 counter
- `crates/engine/src/pending/handlers/targets.rs:21` — CR 603.3d: each ability's target is chosen as *it* goes on the stack, in the chosen
- `crates/engine/src/pending/handlers/targets.rs:137` — is still chosen at placement (CR 603.3d) before the ability goes on the stack.
- `crates/engine/src/pending/handlers/targets.rs:236` — doubling) is chosen next at placement (CR 603.3d) before the ability hits the stack.
- `crates/engine/src/pending/handlers/targets.rs:405` — Validate and record a triggered ability's *second* independent target clause (CR 603.3d —
- `crates/engine/src/resolution/copy.rs:153` — ability's own chosen target (CR 603.3d for Willbender's trigger; the cast target for
- `crates/engine/src/resolution/counters.rs:381` — Kinetic Ooze's X≥10 rider (CR 601.2c/603.3d): double the +1/+1 counters on each of the
- `crates/engine/src/resolution/mod.rs:57` — A triggered ability's second independent target clause's chosen targets (CR 603.3d), read by
- `crates/engine/src/triggers.rs:1215` — The triggered ability belongs to the permanent's controller (CR 603.3d),
- `crates/engine/src/triggers.rs:1227` — Controller-scoped, not owner-scoped (CR 603.3d): both the "except this
- `crates/engine/src/triggers.rs:1708` — Controller-scoped watcher (CR 603.3d): a stolen/reanimated Starfield Mystic
- `crates/engine/src/triggers.rs:2533` — Controller-scoped (CR 603.3d): a stolen/reanimated Hateful Eidolon fires for its
- `crates/engine/src/triggers.rs:2758` — Controller-scoped (CR 603.3d / 603.6e): a battlefield watcher's "an enchantment you
- `crates/engine/src/triggers.rs:2820` — (CR 603.3d) — not its owner, which matters if it entered under another player's control.
- `crates/engine/src/triggers.rs:3035` — Controller-scoped (CR 603.3d): a stolen/reanimated cast-watcher (Sram, Kor
- `crates/engine/src/triggers.rs:3455` — Controller-scoped (CR 603.3d): a stolen/reanimated draw-watcher (Pearl-Ear) fires
- `crates/engine/src/triggers.rs:4329` — "Choose one —" on a triggered ability (CR 603.3d / 700.2): the mode is chosen as the
- `crates/engine/src/triggers.rs:4705` — A triggered ability's *second* independent target clause (CR 603.3d — an ability may target
- `crates/engine/src/triggers.rs:4789` — or `None`), choose its *second* independent target clause too (CR 603.3d — Kinetic Ooze's
- `crates/engine/src/types/effect/shared.rs:1059` — (`StackItem::Ability::targets_second`, CR 603.3d) rather than the ability's one shared
- `crates/engine/src/types/stack.rs:1195` — `at_placement` distinguishes the two rules windows this pause serves (CR 603.3d vs CR 608.2):
- `crates/engine/src/types/stack.rs:1310` — (`options`), or declines (CR 603.3d "up to one" — Nils, Discipline Enforcer's per-player
- `crates/engine/src/types/stack.rs:1997` — The chosen targets of a *second* independent target clause (CR 603.3d — Kinetic Ooze's
- `crates/engine/src/types/stack.rs:2203` — A second independent target clause's chosen targets (CR 603.3d — Kinetic Ooze's X≥10
- `crates/engine/tests/game.rs:14184` — trigger simultaneously and must be ordered (CR 603.3b); CR 603.3d then chooses each one's
- `crates/engine/tests/game.rs:14230` — The bounce is placed first — its own target choice pauses immediately (CR 603.3d),
- `crates/engine/tests/game.rs:44440` — CR 603.3d — Kinetic Ooze's ETB carries two independent target clauses, both chosen as the
- `crates/engine/tests/game.rs:44472` — CR 603.3d — the X ≥ 10 rider's "other target creatures" are chosen as the trigger goes on
- `crates/engine/tests/game.rs:55060` — The mode is chosen as the dies trigger goes on the stack (CR 603.3d) — the pause is live now.
- `crates/engine/tests/game.rs:57688` — The mode is chosen as the trigger goes on the stack (CR 603.3d), so the mode pause is
- `crates/engine/tests/game.rs:57787` — The mode is chosen as the trigger goes on the stack (CR 603.3d), so the two-mode pause is
- `crates/engine/tests/game.rs:57797` — Increment #1 (CR 603.3d / 700.2): a *triggered* "choose one" picks its mode as the ability
- `crates/engine/tests/game.rs:76915` — self-death modal trigger. The mode is chosen as the trigger goes on the stack (CR 603.3d), so

### CR 603.4

- `crates/engine/src/amount.rs:29` — this to `Fixed` at trigger placement (CR 603.4). The arm exists only so this match
- `crates/engine/src/core.rs:292` — its owner's turn-scoped "a card left your graveyard this turn" flag — the CR 603.4
- `crates/engine/src/effects.rs:1016` — `SourceUntapped` (Howling Mine's CR 603.4 *second* check), and `SourcePowerAtMost`
- `crates/engine/src/effects.rs:1053` — Howling Mine's CR 603.4 *second* check: re-read the source's own tapped
- `crates/engine/src/resolution/copy.rs:241` — CR 603.4: the triggering spell may have left the stack (countered in
- `crates/engine/src/resolution/copy.rs:276` — different one of those creatures." Same CR 603.4 "already left the stack" guard as
- `crates/engine/src/resolution/copy.rs:353` — CR 603.4/707.10c: the triggering ability may have left the stack (countered in
- `crates/engine/src/triggers.rs:1888` — value") bakes correctly at placement (CR 603.4) whether the trigger fired off a cast or a
- `crates/engine/src/triggers.rs:3046` — CR 603.4: the triggering spell's chosen {X} is locked in when the trigger goes
- `crates/engine/src/triggers.rs:3132` — CR 603.4: the triggering ability's source, locked in when the watch fires — the
- `crates/engine/src/triggers.rs:3256` — cast's own chosen `{X}` via [`TriggerContext::cast_x`], same CR 603.4 last-known-
- `crates/engine/src/triggers.rs:3779` — [`Condition::SourceUntapped`] (Howling Mine's CR 603.4 *first* check — the resolution-time
- `crates/engine/src/triggers.rs:3938` — through `Game::ability_condition_holds` (Howling Mine's CR 603.4 first check, at
- `crates/engine/src/triggers.rs:4016` — (Dread Cacodemon's/Reiver Demon's ETB intervening-if, CR 603.4), which intercepts it
- `crates/engine/src/triggers.rs:4021` — the Forbidden's reflexive "When you do" copy gate, CR 603.4), which intercepts it
- `crates/engine/src/triggers.rs:4712` — printed order, honoring each intervening-if gate (CR 603.4) evaluated now at placement.
- `crates/engine/src/triggers.rs:4753` — CR 603.4: a gated clause is only a real target clause when its intervening-if
- `crates/engine/src/triggers.rs:4769` — Whether an intervening-if `condition` holds as a trigger goes on the stack (CR 603.4).
- `crates/engine/src/types/card.rs:461` — An intervening-if condition (CR 603.4): the trigger only goes on the stack when this
- `crates/engine/src/types/card.rs:1969` — hand" ETB intervening-if, CR 603.4). `false` for a token, a reanimated/searched/flickered/
- `crates/engine/src/types/effect/shared.rs:1503` — An intervening-if condition on a triggered ability (CR 603.4): checked once, *when the
- `crates/engine/src/types/effect/shared.rs:1505` — ponytail: the CR 603.4 *second* check (re-evaluated as the ability resolves) is skipped — a
- `crates/engine/src/types/effect/shared.rs:1525` — step (the CR 603.4 *second* check, re-checked fresh at resolution — a creature created in
- `crates/engine/src/types/effect/shared.rs:1671` — nested in an [`Effect::Conditional`] (checked fresh at resolution) — CR 603.4 requires
- `crates/engine/src/types/effect/shared.rs:1680` — [`Effect::Conditional`] baked to its `then`/no-op at trigger placement (CR 603.4 — the
- `crates/engine/src/types/effect/shared.rs:1742` — A composed AND of every arm in `conditions` (CR 603.4 — Zimone, All-Questioning's "if a
- `crates/engine/src/types/effect/shared.rs:1791` — (Contaminant Grafter's end step, CR 603.4) and an activation restriction (Glistening
- `crates/engine/src/types/effect/shared.rs:1795` — "if you cast it from your hand" (CR 603.4 — Dread Cacodemon's/Reiver Demon's ETB
- `crates/engine/src/types/effect/shared.rs:1803` — "When you do [sacrifice one or more creatures]" (CR 603.4 — Plumb the Forbidden's reflexive
- `crates/engine/src/types/effect/shared.rs:1913` — CR 603.4/202.3: a `CastSpell` (magecraft) trigger's `Amount::TriggeringSpellManaValue`
- `crates/engine/src/types/effect/shared.rs:1928` — CR 603.4: a `YouCastThis` self-cast trigger's `Amount::X`/`Amount::HalfXRoundedDown` reads
- `crates/engine/src/types/effect/shared.rs:2593` — CR 603.4 resolution-time re-check wrapper still gets its nested draw filled.
- `crates/engine/src/types/effect/shared.rs:2740` — of those tokens instead") — CR 603.4: the triggering spell's mana value is locked in when the
- `crates/engine/src/types/trigger.rs:708` — same CR 603.4 reasoning as `dying_source_stats` above. See
- `crates/engine/src/types/trigger.rs:716` — preceding `Event::ManaSpent` in the same batch, same CR 603.4 last-known-information
- `crates/engine/src/types/trigger.rs:720` — The casting spell's chosen `{X}` (CR 603.4), for a [`Trigger::YouCastThis`] self-cast
- `crates/engine/src/types/trigger.rs:785` — Drake's "when you next cast an instant or sorcery spell this turn, copy it") — CR 603.4
- `crates/engine/src/types/trigger.rs:793` — trigger goes on the stack — same CR 603.4 last-known-information shape as `cast_x` above,
- `crates/engine/tests/game.rs:28584` — CR 603.4: if the triggering spell leaves the stack (countered) before the delayed
- `crates/engine/tests/game.rs:44609` — CR 603.4 intervening-if — at X=9 the "if X is 10 or more" double-counters clause is off, so the
- `crates/engine/tests/game.rs:46586` — ── Intervening-if conditions (CR 603.4) and watch-others attack triggers ─────────────
- `crates/engine/tests/game.rs:55870` — instead." (CR 603.4 intervening-if, CR 700.4 magecraft-shaped trigger)
- `crates/engine/tests/game.rs:62620` — inflate the count (CR 603.4 last-known information).
- `crates/engine/tests/game.rs:71510` — an intervening-if (CR 603.4) — with zero counters the trigger never fires, so there is no
- `crates/engine/tests/game.rs:88591` — makes the intervening-if (CR 603.4) fail at resolution: nothing is returned.
- `crates/engine/tests/game.rs:88620` — "intervening-if (CR 603.4): the Aura is not on the battlefield, so nothing is returned"
- `crates/engine/tests/game.rs:90637` — with a CR 603.4 resolution-time second check. ─────────────────────────────────────────────
- `crates/engine/tests/game.rs:90672` — "if this artifact is untapped" (CR 603.4 intervening-if, first check): a tapped Howling
- `crates/engine/tests/game.rs:90701` — CR 603.4's *second* check: Howling Mine triggers while untapped (the deck can tap it in
- `crates/engine/tests/game.rs:93746` — The "if you cast it from your hand" intervening-if (CR 603.4) is false when Reiver enters
- `crates/engine/tests/game.rs:93801` — Not cast from hand, so the CR 603.4 intervening-if is false and the ETB ability never
- `crates/engine/tests/game.rs:95675` — CR 603.4's *second* check: Pyrohemia's end-step trigger fires while the battlefield has
- `crates/engine/tests/game.rs:100938` — three or more poison counters, draw a card, ..." — CR 603.4's intervening-if, so below the

### CR 603.5

- `crates/engine/tests/game.rs:43681` — The turned-face-up trigger is a "you may" (CR 603.5): declining it exchanges nothing.

### CR 603.6

- `crates/engine/src/triggers.rs:487` — A creature dying (battlefield → graveyard) fires its own Dies trigger. (CR 603.6, CR 403.5, CR 603)
- `crates/engine/src/triggers.rs:488` — CR 700.4: "died" means specifically "put into a graveyard from the battlefield" (CR 700.4, CR 603.6)
- `crates/engine/src/triggers.rs:539` — CR 603.6 placed trigger, fabricated here rather than scanned off `def` since
- `crates/engine/src/triggers.rs:606` — A dying token fires its Dies trigger before vanishing; its arena slot is (CR 603.6, CR 111, CR 603)
- `crates/engine/src/triggers.rs:4239` — A source that has already left the game entirely (a token's own Dies trigger, fired (CR 603.6, CR 1…
- `crates/engine/tests/game.rs:1676` — Destroy resolves (the creature genuinely dies, CR 603.6), then the granted rider's own
- `crates/engine/tests/game.rs:1694` — The exile-and-gain-2-life rider is a real placed trigger (CR 118.9/603.6): the recurred
- `crates/engine/tests/game.rs:1781` — CR 603.6: the rider is a real triggered ability that goes on the stack and can be
- `crates/engine/tests/game.rs:9420` — resolve_top_of_stack(&mut game); // the Pest's Dies trigger resolves: gain 1 life. (CR 603.6, CR 11…
- `crates/engine/tests/game.rs:20867` — resolve_top_of_stack(&mut game); // The Dies trigger resolves: draw a card. (CR 603.6, CR 603, CR 1…
- `crates/engine/tests/game.rs:51162` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: gain life, then draw, both = 3. (CR 603…
- `crates/engine/tests/game.rs:51194` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: create 2 Thopters. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51223` — Dying source: Lifeblood Hydra's Dies trigger fills `SourcePower` from LKI. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51274` — resolve_top_of_stack(&mut g); // The Dies trigger resolves: create 3 Treasures. (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:51519` — ability_index: 2, // 0 = enters-with-counters static, 1 = the new dies trigger. (CR 603.6, CR 122, …
- `crates/engine/tests/game.rs:57763` — The real pool card: its dies trigger is a "choose one" over impulse-exile and three (CR 603.6, CR 4…
- `crates/engine/tests/game.rs:57786` — resolve_top_of_stack(&mut game); // the destroy resolves → Atsushi dies → dies trigger placed. (CR …
- `crates/engine/tests/game.rs:69249` — resolve_whole_stack(&mut game); // the destroy, then its own dies trigger (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:75549` — resolve_whole_stack(&mut game); // the destroy, then the Pest's own dies trigger (CR 603.6, CR 603)
- `crates/engine/tests/game.rs:76916` — the pause lands the moment the dies trigger is placed — no further resolution. (CR 603.6, CR 700.2)
- `crates/engine/tests/game.rs:86675` — ── Increment 183: "died" means put into a graveyard from the battlefield (CR 700.4/603.6) ────

### CR 603.6a

- `crates/engine/src/triggers.rs:458` — A created token is a permanent entering the battlefield (CR 603.6a) too.
- `crates/engine/src/types/trigger.rs:407` — (CR 603.6a's kin for "this permanent or another … enters"). Doomwake Giant's
- `crates/engine/tests/game.rs:39301` — min: 0 lets the controller decline — nothing enters the battlefield. (CR 603.6a, CR 108.3, CR 403.5)
- `crates/engine/tests/game.rs:50302` — resolve_top_of_stack(&mut game); // the planeswalker resolves and enters the battlefield (CR 603.6a…
- `crates/engine/tests/game.rs:65235` — Doomwake Giant (soc), CR 603.6a: "Constellation — Whenever THIS CREATURE or another

### CR 603.6c

- `crates/engine/src/apply.rs:1976` — CR 603.6c/704.5m last-known information: capture the Aura(s) attached to this
- `crates/engine/src/apply.rs:2062` — CR 603.6c/704.5m last-known information: capture the Aura(s) attached to this
- `crates/engine/src/state.rs:240` — Aura's def)` tuples — CR 603.6c last-known information for
- `crates/engine/src/triggers.rs:3` — Primary: CR 603 (triggered abilities). Also: CR 603.6c/603.10 look-back, CR 603.3c no
- `crates/engine/src/triggers.rs:374` — CR 603.6c/603.10.1 "look back in time": a watch-others death trigger reads the game
- `crates/engine/src/triggers.rs:1419` — this one in the same batch (`batch_deaths`, the CR 603.6c look-back) — minus the dying
- `crates/engine/src/triggers.rs:1614` — Feeds the CR 603.6c look-back in [`queue_watch_death_triggers`]. Mirrors the death arms of
- `crates/engine/src/triggers.rs:1670` — CR 603.6c look-back those feed.
- `crates/engine/src/triggers.rs:1672` — CR 603.6c simultaneous-death look-back (an enchantment dying alongside its own watcher in
- `crates/engine/src/triggers.rs:1730` — own last-known information (CR 603.6c/603.10 — the `*IncludingThis` self-fire convention).
- `crates/engine/src/triggers.rs:1738` — and no CR 603.6c simultaneous-death look-back across *other* nonland permanents dying in
- `crates/engine/src/triggers.rs:2499` — Queue watch-any-enchanted-creature-dies triggers (CR 603.6c, Hateful Eidolon: "Whenever
- `crates/engine/src/triggers.rs:2507` — same event batch (they're already off the battlefield) — the CR 603.6c look-back, same
- `crates/engine/src/triggers.rs:2536` — controller-at-death — the CR 603.6c look-back — so those stay as captured.)
- `crates/engine/src/types/trigger.rs:15` — Watch-death triggers honor the CR 603.6c look-back: a watcher that dies alongside other
- `crates/engine/src/types/trigger.rs:90` — creature's own last-known information (CR 603.6c/603.10). Blood Artist.
- `crates/engine/src/types/trigger.rs:256` — Whenever *any* enchanted creature dies (CR 603.6c, Hateful Eidolon: "Whenever an
- `crates/engine/tests/game.rs:3949` — Cross-check of CR 510.5 × CR 603.6c/603.10: two first strikers kill both blockers in the
- `crates/engine/tests/game.rs:21295` — fire the drain, reading its own last-known information (CR 603.6c/603.10).
- `crates/engine/tests/game.rs:22130` — CR 603.6c/603.10 look-back: Blood Artist dies in the same board wipe as two other
- `crates/engine/tests/game.rs:31124` — card to its owner's hand (CR 603.6c last-known information — by the time its own trigger
- `crates/engine/tests/game.rs:31152` — CR 603.6c last-known information, stress-tested: a mass destroy kills the host and orphans
- `crates/engine/tests/game.rs:31515` — CR 603.6c look-back: Hateful Eidolon dying in the same board wipe as another enchanted

### CR 603.6d

- `crates/engine/src/state.rs:412` — own share (CR 603.6d LKI-adjacent: the exile has already happened, so this reads a snapshot

### CR 603.6e

- `crates/engine/src/apply.rs:352` — The O-Ring pattern (CR 603.6e): for each `(source, exiled)` link still on
- `crates/engine/src/apply.rs:359` — leaves, it's simply skipped (CR 603.6e only returns a card that's "still exiled") and its
- `crates/engine/src/apply.rs:2170` — The O-Ring pattern (CR 603.6e): record the link — read back by
- `crates/engine/src/cast.rs:2085` — permanent with that activated ability, or (CR 112.6/603.6e) owns a `functions_in_graveyard`
- `crates/engine/src/cast.rs:2099` — CR 112.6/603.6e: a card whose def is flagged `functions_in_graveyard` activates its
- `crates/engine/src/characteristics.rs:1568` — (CR 603.6e continuous-analog — Anger's "as long as this card is in your graveyard …
- `crates/engine/src/de.rs:458` — CR 603.6e — this card's triggered abilities fire from its owner's graveyard rather
- `crates/engine/src/effects.rs:751` — every zone that effect's pool consumers actually fire from (CR 603.6e / 400.7: a
- `crates/engine/src/query.rs:99` — CR 112.6/603.6e: a `functions_in_graveyard` card's activated ability
- `crates/engine/src/query.rs:1331` — abilities (CR 603.6e).
- `crates/engine/src/resolution/zones.rs:127` — exiled by Nezumi Graverobber mid-trigger, say — per CR 603.6e / 400.7.
- `crates/engine/src/resolution/zones.rs:140` — battlefield under its owner's control (CR 603.6e). The self-return twin of
- `crates/engine/src/state.rs:303` — death is still visible to *other*, surviving players' death-watch (CR 603.6e; Hissing
- `crates/engine/src/state.rs:334` — The O-Ring pattern (CR 603.6e): each entry is `(source, exiled)` — an object exiled by
- `crates/engine/src/triggers.rs:496` — the death itself still happened (CR 603.6e): a *different*, surviving
- `crates/engine/src/triggers.rs:733` — choke, battlefield and graveyard-functional alike (CR 603.6e).
- `crates/engine/src/triggers.rs:1441` — the graveyard (CR 603.6e) — skip it on the battlefield; the graveyard scan below fires
- `crates/engine/src/triggers.rs:1667` — its owner controls. A narrow sibling of the creature death watch (CR 603.6e "leaves the
- `crates/engine/src/triggers.rs:1904` — Queue a controller-scoped trigger for `player`'s **graveyard-functional** cards (CR 603.6e):
- `crates/engine/src/triggers.rs:2693` — [`CardDef::functions_in_graveyard`] (CR 603.6e) — the `PermanentEnters` watch's graveyard
- `crates/engine/src/triggers.rs:2745` — graveyard (CR 603.6e) — the same battlefield exclusion `queue_controller_triggers`
- `crates/engine/src/types/card.rs:824` — rather than from the battlefield (CR 603.6e — Squee's upkeep self-return, Nether Traitor's
- `crates/engine/src/types/stack.rs:2792` — The O-Ring pattern (CR 603.6e): `object` (an exile-zone card, minted by a preceding
- `crates/engine/src/types/stack.rs:2858` — permanent `permanent`, under its owner's control (`controller`), per CR 603.6e. Fires ETB
- `crates/engine/src/types/trigger.rs:181` — gainer. Functions from the graveyard (CR 603.6e) for a card that sets
- `crates/engine/tests/game.rs:22459` — A card whose *only* triggered ability functions from the graveyard (CR 603.6e): at its
- `crates/engine/tests/game.rs:22564` — CR 603.6e: a triggered ability that functions while its source is in the graveyard. At the
- `crates/engine/tests/game.rs:22585` — A graveyard ability must NOT also fire from play (CR 603.6e): the same card on the
- `crates/engine/tests/game.rs:22664` — graveyard to the battlefield." Its death-watch trigger fires from the graveyard (CR 603.6e).
- `crates/engine/tests/game.rs:22768` — life to prove the graveyard-functioning opponent-lifegain watch (CR 603.6e).
- `crates/engine/tests/game.rs:22902` — tapped." An activated ability functioning from the graveyard (CR 112.6/603.6e).
- `crates/engine/tests/game.rs:22942` — Same {{B}}{{G}} ability, guarded to the graveyard (CR 112.6/603.6e): a live Teacher's Pest
- `crates/engine/tests/game.rs:22998` — enters-trigger (CR 603.6e), fired by another Spirit entering rather than by Vanguard's own
- `crates/engine/tests/game.rs:31212` — CR 603.6e / 400.7: a return-this effect only acts on the object in the zone the ability
- `crates/engine/tests/game.rs:32480` — ── Exile-with-return, the O-Ring pattern (#59, CR 603.6e) ───────────────────────────
- `crates/engine/tests/game.rs:32485` — controls until this Aura leaves the battlefield" (CR 603.6e linked exile — the O-Ring
- `crates/engine/tests/game.rs:32528` — "the exiled card returned when its exiling source left the battlefield (CR 603.6e)"
- `crates/engine/tests/game.rs:83726` — Oblivion Ring: the ETB exile is linked to the leave-return (CR 603.6e) — destroying the
- `crates/engine/tests/game.rs:83763` — "the exiled card returned when the Ring left (CR 603.6e)"
- `crates/engine/tests/game.rs:88399` — ── Increment 184: death-watch survives a coincident player loss (CR 603.6e/800.4a) ──────
- `crates/engine/tests/game.rs:88406` — death is still a real game event (CR 603.6e), so a *different*, surviving player's
- `crates/engine/tests/game.rs:89178` — owner's control." — the linked O-Ring pair (CR 603.6e), creature targets only.
- `crates/engine/tests/game.rs:89211` — "the exiled card returned when Journey to Nowhere left (CR 603.6e)"

### CR 603.7

- `crates/engine/src/apply.rs:1084` — ponytail: a `ScheduleNextCastTrigger` watch's CR 603.7 "this turn" duration
- `crates/engine/src/apply.rs:1090` — ponytail: `ScheduleThisTurnCombatDamageCopy`'s CR 603.7 "this turn" watch
- `crates/engine/src/lib.rs:154` — Pending CR 603.7 delayed triggered abilities not yet placed on the stack.
- `crates/engine/src/pipeline.rs:22` — Fire scheduled delayed triggers (CR 603.7) whose step has arrived.
- `crates/engine/src/pipeline.rs:24` — Fire event-armed delayed one-shots (CR 603.7) whose watched cast just happened.
- `crates/engine/src/pipeline.rs:26` — Fire event-armed delayed watches (CR 603.7) whose watched creature just dealt combat
- `crates/engine/src/pipeline.rs:29` — Fire this-turn, controller-scoped, repeatable delayed watches (CR 603.7) whose
- `crates/engine/src/resolution/misc.rs:93` — Schedule a CR 603.7 delayed trigger: resolve `who` to a concrete player now (the
- `crates/engine/src/resolution/misc.rs:110` — Scattering Stroke's win rider (CR 603.7): schedule a delayed one-shot for the
- `crates/engine/src/resolution/misc.rs:139` — Arm a CR 603.7 delayed one-shot: always the ability's own controller/source (Brass
- `crates/engine/src/resolution/sequence_steps.rs:205` — Gift of Immortality: schedule the delayed return of this Aura (CR 603.7), attached
- `crates/engine/src/resolution/sequence_steps.rs:232` — then schedule its return to hand at the next end step (CR 603.7). No such event
- `crates/engine/src/resolution/sequence_steps.rs:273` — (CR 603.7), mirroring `ScheduleReturnThisAuraAttachedToReanimated`'s emit shape. No
- `crates/engine/src/resolution/zones.rs:37` — (`return_at` absent) or schedule that return as a real CR 603.7 delayed triggered
- `crates/engine/src/resolution/zones.rs:303` — Gift of Immortality: the delayed CR 603.7 payoff scheduled by
- `crates/engine/src/state.rs:4` — Side state for goad (CR 701.38), delayed triggers (CR 603.7), exile links,
- `crates/engine/src/state.rs:356` — Pending CR 603.7 delayed triggered abilities not yet placed on the stack.
- `crates/engine/src/state.rs:365` — [`Effect::Misc(MiscEffect::ScheduleNextCastTrigger)`](crate::Effect::Misc(MiscEffect::ScheduleNextC…
- `crates/engine/src/state.rs:373` — [`Effect::Misc(MiscEffect::ArmCombatDamageWatch)`](crate::Effect::Misc(MiscEffect::ArmCombatDamageW…
- `crates/engine/src/state.rs:384` — (Surge to Victory) — CR 603.7's *repeatable* sibling of `pending_combat_damage_watch`
- `crates/engine/src/triggers.rs:4` — legal target, CR 603.7 delayed triggers. Numeric [`Amount`] evaluation lives in
- `crates/engine/src/triggers.rs:1993` — Drain every CR 603.7 delayed trigger whose `fire_at` step just began — the first time any
- `crates/engine/src/triggers.rs:2012` — "At the beginning of your next main phase" (Scattering Stroke, CR 603.7) is scoped to the
- `crates/engine/src/triggers.rs:3246` — Fire CR 603.7 delayed one-shots armed by [`Effect::Misc(MiscEffect::ScheduleNextCastTrigger)`] (Bra…
- `crates/engine/src/triggers.rs:3252` — [`Event::NextCastTriggerConsumed`] before its `TriggerGroup` is queued, CR 603.7's "next".
- `crates/engine/src/triggers.rs:3329` — Fire CR 603.7 delayed watches armed by [`Effect::Misc(MiscEffect::ArmCombatDamageWatch)`] (Stensian
- `crates/engine/src/triggers.rs:3336` — `TriggerGroup` is queued, CR 603.7's "this combat" one-shot. A sibling of
- `crates/engine/src/triggers.rs:3386` — Fire CR 603.7's *repeatable* delayed watches armed by
- `crates/engine/src/triggers.rs:3395` — [`Self::fire_combat_damage_watch_triggers`], entries are **not** removed here — CR 603.7's
- `crates/engine/src/types/card.rs:69` — The default [`Effect::Misc(MiscEffect::ScheduleAtNextUpkeep)`] `fire_at` — CR 603.7's "next upkeep".
- `crates/engine/src/types/filter.rs:938` — Who acts when a [`Effect::Misc(MiscEffect::ScheduleAtNextUpkeep)`] delayed trigger fires (CR 603.7).
- `crates/engine/src/types/stack.rs:2494` — A CR 603.7 delayed triggered ability was scheduled by [`Effect::Misc(MiscEffect::ScheduleAtNextUpke…
- `crates/engine/src/types/stack.rs:2505` — Every delayed trigger scheduled for `fire_at` fired at once (CR 603.7, drained in full the
- `crates/engine/src/types/stack.rs:2515` — (CR 603.7 continuous rider — "creatures your opponents control don't untap during their
- `crates/engine/src/types/stack.rs:2522` — A CR 603.7 delayed *one-shot* was armed by [`Effect::Misc(MiscEffect::ScheduleNextCastTrigger)`]:
- `crates/engine/src/types/stack.rs:2534` — cast) and is removed — CR 603.7's "next" is at most once. An unconsumed watch's "this
- `crates/engine/src/types/stack.rs:2541` — A CR 603.7 delayed watch was armed by [`Effect::Misc(MiscEffect::ArmCombatDamageWatch)`] (Stensian
- `crates/engine/src/types/stack.rs:2552` — combat damage to a player) and is removed — CR 603.7's "this combat" is at most once. An
- `crates/engine/src/types/stack.rs:2560` — A CR 603.7 *repeatable* delayed watch was armed by
- `crates/engine/tests/game.rs:27616` — ── Delayed triggered abilities (#27, CR 603.7) ──────────────────────────────────────
- `crates/engine/tests/game.rs:28191` — `Effect::Misc(MiscEffect::ScheduleNextCastTrigger)` arms a CR 603.7 delayed one-shot that fires the…
- `crates/engine/tests/game.rs:28238` — CR 603.7's "next" — the delayed one-shot fires at most once, even when the controller
- `crates/engine/tests/game.rs:28355` — CR 603.7's implicit "this turn" duration: an armed watch that sees no matching cast this

### CR 603.7b

- `crates/engine/src/resolution/tokens.rs:150` — Hellkite, CR 603.7b): schedule a delayed exile against this specific

### CR 603.10

- `crates/engine/src/characteristics.rs:1517` — no pool card chains grants, so this needs no recursion. (CR 603.10 / last-known info if
- `crates/engine/src/triggers.rs:3` — Primary: CR 603 (triggered abilities). Also: CR 603.6c/603.10 look-back, CR 603.3c no
- `crates/engine/src/triggers.rs:1730` — own last-known information (CR 603.6c/603.10 — the `*IncludingThis` self-fire convention).
- `crates/engine/src/triggers.rs:3733` — triggers still fire (CR 603.10).
- `crates/engine/src/types/trigger.rs:90` — creature's own last-known information (CR 603.6c/603.10). Blood Artist.
- `crates/engine/tests/game.rs:3949` — Cross-check of CR 510.5 × CR 603.6c/603.10: two first strikers kill both blockers in the
- `crates/engine/tests/game.rs:21295` — fire the drain, reading its own last-known information (CR 603.6c/603.10).
- `crates/engine/tests/game.rs:22130` — CR 603.6c/603.10 look-back: Blood Artist dies in the same board wipe as two other

### CR 603.10.1

- `crates/engine/src/triggers.rs:374` — CR 603.6c/603.10.1 "look back in time": a watch-others death trigger reads the game

### CR 603.10a

- `crates/engine/src/apply.rs:1991` — CR 603.10a last-known information — see `Game::dying_creature_stats`.
- `crates/engine/src/apply.rs:2004` — CR 603.10a last-known information: the host this token was attached to (if it
- `crates/engine/src/apply.rs:2044` — CR 603.10a last-known information: the host this permanent was attached to
- `crates/engine/src/apply.rs:2079` — CR 603.10a last-known information — see `Game::dying_creature_stats`.
- `crates/engine/src/apply.rs:2125` — CR 603.10a last-known information — see `MovedToGraveyard`'s
- `crates/engine/src/apply.rs:2299` — CR 603.10a last-known information — see `MovedToGraveyard`'s
- `crates/engine/src/apply.rs:2329` — CR 603.10a last-known information — see `MovedToGraveyard`'s
- `crates/engine/src/pending/handlers/dig.rs:1216` — graveyard (CR 603.10a last-known information — milled/exiled elsewhere in the meantime).
- `crates/engine/src/resolution/zones.rs:89` — other way (CR 603.10a last-known information).
- `crates/engine/src/resolution/zones.rs:306` — other way — CR 603.10a last-known information) or `creature` no longer resolves to
- `crates/engine/src/state.rs:200` — Each entry is `(owner, graveyard-object id that left)`; the ids are the CR 603.10a
- `crates/engine/src/state.rs:256` — `(dying creature's pre-move id, power, +1/+1 counters)` — CR 603.10a last-known
- `crates/engine/src/state.rs:282` — (Animate Dead). `host` is CR 603.10a last-known information — `Game::attached_to` read at
- `crates/engine/src/state.rs:298` — this batch — CR 603.10a last-known information for [`Game::queue_watch_death_triggers`],
- `crates/engine/src/triggers.rs:999` — Each owner's own graveyard-object ids that left (CR 603.10a last-known info),
- `crates/engine/src/triggers.rs:1308` — battlefield, sacrifice enchanted creature." Threads the captured host (CR 603.10a
- `crates/engine/src/triggers.rs:1490` — [`Game::damaged_this_turn`] tally recorded `dying` as a victim (CR 603.10a last-known
- `crates/engine/src/triggers.rs:1541` — The dead creature's id rides along (CR 603.10a last-known information) for a watch whose
- `crates/engine/src/triggers.rs:1816` — placement (CR 603.10a last-known information). Bespoke rather than
- `crates/engine/src/triggers.rs:2554` — watcher's own) having controlled at least one of them — CR 603.10a last-known
- `crates/engine/src/triggers.rs:2672` — graveyard-object ids (CR 603.10a last-known information), threaded into the
- `crates/engine/src/triggers.rs:2866` — `amount` is baked onto `This`-scoped watchers' context as CR 603.10a last-known
- `crates/engine/src/triggers.rs:2939` — summed damage baked into [`TriggerContext::combat_damage`] (CR 603.10a last-known
- `crates/engine/src/types/effect/shared.rs:47` — must resolve before the target leaves the battlefield (CR 613.6/603.10a last-known
- `crates/engine/src/types/effect/shared.rs:1891` — CR 603.10a last-known information: a Dies trigger's `Amount::SourcePower`/
- `crates/engine/src/types/effect/shared.rs:1899` — Same CR 603.10a last-known-information shape, one step over: an `AnEnchantedCreatureDies`
- `crates/engine/src/types/effect/shared.rs:1935` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's reanimation target bound resolves
- `crates/engine/src/types/effect/shared.rs:1942` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's "its controller may draw" payoff
- `crates/engine/src/types/effect/shared.rs:1949` — CR 510.2/603.10a: a `DealsCombatDamageToPlayer` trigger's "each other opponent" splash (Hydra
- `crates/engine/src/types/effect/shared.rs:1956` — CR 510.2/603.10a: an `Attacks` trigger's reanimation target bound resolves against the
- `crates/engine/src/types/effect/shared.rs:2321` — the dying permanent's own last-known card types (CR 603.10a — Martyr's Bond's "shares a card
- `crates/engine/src/types/effect/shared.rs:2700` — the dying source's CR 603.10a last-known-information snapshot (Lifeblood Hydra's "gain life
- `crates/engine/src/types/effect/shared.rs:2714` — placeholders to the watcher's controller's CR 603.10a last-known-information count (Hateful
- `crates/engine/src/types/effect/shared.rs:2853` — dealt to that player") — CR 510.2/603.10a last-known information, locked in when the trigger
- `crates/engine/src/types/effect/shared.rs:2896` — placeholder with the attacker's power, read at trigger placement (CR 510.2/603.10a last-known
- `crates/engine/src/types/filter.rs:350` — at trigger placement (CR 603.10a last-known information), same posture as `Amount::X`
- `crates/engine/src/types/filter.rs:358` — trigger placement (CR 510.2/603.10a last-known information), same posture as
- `crates/engine/src/types/filter.rs:793` — dying permanent's last-known card types (CR 603.10a) — a *dynamic* type gate whose type
- `crates/engine/src/types/trigger.rs:264` — [`Amount::AurasYouControlledAttachedToDyingCreature`] at placement — CR 603.10a
- `crates/engine/src/types/trigger.rs:315` — damaged creature's id rides in [`TriggerContext::damaged_creature`] (CR 603.10a
- `crates/engine/src/types/trigger.rs:321` — Whenever a creature dealt damage by this permanent *this turn* dies (CR 603.10a
- `crates/engine/src/types/trigger.rs:490` — attached to at the instant it left (CR 603.10a last-known information), carried in
- `crates/engine/src/types/trigger.rs:682` — CR 701.8/603.10a last-known information: the graveyard-object ids of the **nonland** cards
- `crates/engine/src/types/trigger.rs:697` — CR 603.10a last-known information: the dying source's power/+1/+1-counter count the
- `crates/engine/src/types/trigger.rs:734` — every other trigger. Locked in at placement — same CR 603.10a last-known-information
- `crates/engine/src/types/trigger.rs:738` — CR 510.2/603.10a last-known information: the amount of combat damage the source just
- `crates/engine/src/types/trigger.rs:745` — CR 510.2/603.10a last-known information: the player the source just dealt combat damage
- `crates/engine/src/types/trigger.rs:751` — CR 510.2/603.10a last-known information: who controlled the creature that just dealt the
- `crates/engine/src/types/trigger.rs:757` — CR 609.7/603.10a last-known information: the amount of damage the enchanted host just
- `crates/engine/src/types/trigger.rs:767` — control") — CR 603.10a last-known information's "that card". `None` for every other
- `crates/engine/src/types/trigger.rs:773` — CR 510.2/603.10a last-known information: the creature a [`Trigger::DealsCombatDamageToCreature`]
- `crates/engine/src/types/trigger.rs:798` — CR 510.2/603.10a last-known information: the trigger's own source permanent's power at the
- `crates/engine/src/types/trigger.rs:805` — CR 603.10a last-known information: the just-dead creature's graveyard-object id, for a
- `crates/engine/src/types/trigger.rs:813` — CR 603.10a last-known information: the dying permanent's own card types, for a
- `crates/engine/src/types/trigger.rs:820` — CR 603.10a last-known information: the graveyard-object ids of the cards that left this
- `crates/engine/src/types/trigger.rs:830` — CR 603.10a last-known information: the permanent this object was attached to the instant
- `crates/engine/tests/game.rs:4225` — CR 603.10a: bouncing the damaged creature in response to the (already-stacked) destroy
- `crates/engine/tests/game.rs:4255` — "the trigger's target already left the battlefield (CR 603.10a) — no re-destruction"
- `crates/engine/tests/game.rs:4335` — ── Turn-scoped damaged-by set + death-watch (CR 603.10a, fidelity increment #194) ──────
- `crates/engine/tests/game.rs:4722` — target, a mana-value-4 one is not (CR 510.2/603.10a last-known information).
- `crates/engine/tests/game.rs:4848` — target; a mana-value-5 one and a land are not (CR 510.2/603.10a last-known information).
- `crates/engine/tests/game.rs:15656` — CR 603.10a last-known information cuts both ways: if the exiled card has already left
- `crates/engine/tests/game.rs:31838` — control" (CR 603.10a last-known information — reanimates "that card", the specific
- `crates/engine/tests/game.rs:32196` — CR 603.10a last-known information: if the Aura itself is exiled out of the graveyard
- `crates/engine/tests/game.rs:32251` — CR 603.10a last-known information cuts both ways: if the dying card is exiled before the
- `crates/engine/tests/game.rs:51123` — ── Dies-trigger last-known information (CR 603.10a) ──────────────────────────────────
- `crates/engine/tests/game.rs:51139` — its power." CR 603.10a last-known information: the amount must read the hydra's power the
- `crates/engine/tests/game.rs:51181` — artifact creature token with flying for each +1/+1 counter on this creature." CR 603.10a
- `crates/engine/tests/game.rs:51265` — equal to its power." CR 603.10a last-known information lands the count.
- `crates/engine/tests/game.rs:53156` — hand is a no-op (CR 603.10a last-known information) — it stays in the graveyard rather
- `crates/engine/tests/game.rs:101596` — its power." Its power is last-known information (CR 603.10a) — the Ghoul is already in the

### CR 604.3

- `crates/engine/src/characteristics.rs:1485` — ponytail: `conditional_keywords` is a static keyword grant (CR 604.3), not a

### CR 605

- `crates/engine/src/cast.rs:2535` — mana ability, resolving instantly below, so the placement is indistinguishable. (CR 605, CR 113)
- `crates/engine/src/cast.rs:2551` — exempts a mana ability from the stack, not from choices made while it resolves, so (CR 605, CR 405,…
- `crates/engine/src/cast.rs:2576` — `Game::run` so a composite mana ability (CR 605, CR 113) (Brass Infiniscope's
- `crates/engine/src/characteristics.rs:596` — color-set union and still only needs one qualifying land to add a color (CR 605, CR 108.3, CR 113).
- `crates/engine/src/priority.rs:4` — advance), cleanup. Also: mana abilities / auto-tap planning (CR 605, turn-priority-and-stack spec).
- `crates/engine/src/priority.rs:148` — land's *non*-mana ability (which finds none, and rejects below). Delegate so the one (CR 605, CR 11…
- `crates/engine/src/priority.rs:283` — Pay 1 life to add {C} under Yavimaya Bloomsage's Channel grant (a CR 605 mana ability —
- `crates/engine/src/priority.rs:422` — (CR 605, CR 108.3, CR 113)
- `crates/engine/src/query.rs:3` — Actions worth stopping priority for (turn-priority-and-stack spec). Also: CR 605 mana-ability carve…
- `crates/engine/src/resolution/mana.rs:22` — `single_color` is handled by `Game::activate_ability` before a mana ability ever (CR 605, CR 113)
- `crates/engine/src/resolution/misc.rs:66` — Yavimaya Bloomsage's Channel back face: "Until end of turn, any time you could (CR 605, CR 118.4)
- `crates/engine/src/resolution/tokens.rs:26` — this arm only reaches direct `execute_effect` callers (a mana ability, a (CR 605, CR 113)
- `crates/engine/src/types/effect/shared.rs:989` — Whether an activated ability with this effect is a mana ability (CR 605):
- `crates/engine/src/types/effect/shared.rs:1308` — [`Effect::Static(StaticEffect::GrantToAttached)`] Aura imposes on its host (CR 605's mana-ability c…
- `crates/engine/src/types/effect/shared.rs:1323` — abilities." (CR 605.) A mana ability of the host's still activates; nothing else does.
- `crates/engine/src/types/effect/shared.rs:1449` — source in the pool cares about the damage/life-loss distinction on these riders. (CR 605, CR 120.3,…
- `crates/engine/src/types/mana.rs:484` — pair to an arbitrary WUBRG bitmask, carried per exact mask value in `of_colors`. (CR 605, CR 113)
- `crates/engine/tests/game.rs:11793` — The colored mode (ability 1) is a mana ability — it produces immediately, uses no stack and (CR 605…
- `crates/engine/tests/game.rs:17162` — Sacrifice one Treasure for its own mana ability (a mana ability, no stack — avoids (CR 605, CR 405,…
- `crates/engine/tests/game.rs:22944` — flag (Llanowar Elves' mana ability, sitting in the graveyard) can't activate anything either. (CR 6…
- `crates/engine/tests/game.rs:30045` — The grant is read live off the attachment scan (like a granted mana ability): while Fallen (CR 605,…
- `crates/engine/tests/game.rs:34609` — mana ability activates. (CR 605, CR 113)
- `crates/engine/tests/game.rs:42064` — abilities." (CR 605.)
- `crates/engine/tests/game.rs:55628` — {T}, Sacrifice this artifact: add one mana of any color (a mana ability — resolves at once). (CR 60…
- `crates/engine/tests/game.rs:65456` — Tap `object` for its `[[abilities]]`-authored mana ability (index 0) — Fellwar Stone's shape. (CR 6…
- `crates/engine/tests/game.rs:65679` — no commander, the tap still legally happens (a mana ability never fails to activate) but (CR 605, C…
- `crates/engine/tests/game.rs:70076` — resolve_top_of_stack(&mut g); // the draw ability itself (not a mana ability) (CR 605, CR 113, CR 1…
- `crates/engine/tests/game.rs:90614` — "Sacrifice this token: Add {C}." — a mana ability, resolves at once (CR 605, CR 113).

### CR 605.1a

- `crates/engine/tests/game.rs:70278` — resolve_top_of_stack(&mut g); // not a mana ability — it uses the stack (CR 605.1a, 601.2i)
- `crates/engine/tests/game.rs:70328` — resolve_top_of_stack(&mut g); // not a mana ability — uses the stack (CR 605.1a, 601.2i)
- `crates/engine/tests/game.rs:70394` — resolve_top_of_stack(&mut g); // not a mana ability — uses the stack (CR 605.1a, 601.2i)
- `crates/engine/tests/game.rs:95174` — resolve_top_of_stack(&mut g); // not a mana ability — it uses the stack (CR 605.1a, 601.2i)
- `crates/engine/tests/game.rs:95201` — resolve_top_of_stack(&mut g); // not a mana ability — uses the stack (CR 605.1a, 601.2i)

### CR 605.3

- `crates/engine/src/priority.rs:160` — CR 602.2/605.3: tapping a permanent for mana is its *controller*'s action — a stolen
- `crates/engine/src/priority.rs:185` — Fertile Ground / Mirari's Wake fire off the same tap (CR 605.3 — inline, no stack).
- `crates/engine/src/priority.rs:192` — pool batch. Mana abilities don't stack (CR 605.3), so the bonus resolves inline — no stack,
- `crates/engine/src/priority.rs:433` — CR 602.2/605.3: a player's available mana counts the permanents they *control*, not
- `crates/engine/tests/game.rs:43063` — CR 602.2/605.3: tapping a permanent for mana is its controller's action. P0 steals two of

### CR 605.3a

- `crates/engine/src/cast.rs:2132` — abilities]" restriction (Faith's Fetters, Prison Term; CR 605.3a exempts mana abilities
- `crates/engine/src/cast.rs:2550` — the controller names the color as part of resolving the ability. CR 605.3a only
- `crates/engine/src/cast.rs:2577` — `Sequence` of `AddMana` + `ScheduleNextCastTrigger` — CR 605.3a doesn't require a
- `crates/engine/src/types/effect/shared.rs:991` — CR 605.3a doesn't require *only* adding mana — an ability that could add mana and does
- `crates/engine/tests/game.rs:28216` — delayed trigger (CR 605.3a doesn't require a mana ability to do *only* that)"
- `crates/engine/tests/game.rs:70304` — and/or {G}." CR 107.3h/CR 605.3a: X is chosen at activation, and the ability produces

### CR 605.3b

- `crates/engine/src/types/filter.rs:95` — card in a zone. Mana abilities never reach the stack (CR 605.3b); triggered abilities are
- `crates/engine/tests/game.rs:53865` — "(Mana abilities can't be targeted.)" — CR 605.3b: a mana ability never uses the stack, so
- `crates/engine/tests/game.rs:53879` — "tapping a land for mana never puts an ability on the stack (CR 605.3b)",

### CR 606

- `crates/engine/src/cast.rs:2086` — card sitting in their graveyard with it; loyalty rules (CR 606: sorcery-speed, once per
- `crates/engine/src/cast.rs:2161` — A loyalty ability (CR 606): sorcery-speed, at most one per planeswalker per turn,
- `crates/engine/src/types/effect/shared.rs:1451` — A loyalty ability's loyalty cost (CR 606): `Some(+N/0/−N)` marks the ability as a
- `crates/engine/tests/game.rs:50164` — ── Planeswalkers & loyalty abilities (CR 606) ───────────────────────────────────────

### CR 606.3

- `crates/engine/src/priority.rs:1577` — A new turn frees each planeswalker to activate a loyalty ability again (CR 606.3).
- `crates/engine/src/types/card.rs:1807` — Whether a loyalty ability was activated on this planeswalker this turn (CR 606.3 — at most
- `crates/engine/src/types/stack.rs:2281` — ability is activated) or cleared (`active = false`, at its controller's untap). CR 606.3.
- `crates/engine/tests/game.rs:7155` — CR 606.3: only one loyalty ability of a given planeswalker may be activated each turn. After
- `crates/engine/tests/game.rs:7191` — "a second loyalty ability the same turn is illegal (CR 606.3)"
- `crates/engine/tests/game.rs:50365` — ability_index: MINUS2, // −2 with only 1 loyalty (CR 606.3)
- `crates/engine/tests/game.rs:50399` — A second loyalty ability this turn is illegal (CR 606.3).
- `crates/engine/tests/game.rs:50434` — Outside a main phase (here, begin-combat) it can't be activated (CR 606.3).
- `crates/engine/tests/game.rs:91848` — Starting loyalty 3 can't pay −4 (CR 606.3), and only one loyalty ability may be activated
- `crates/engine/tests/game.rs:91849` — per turn (CR 606.3) — tick up once, then come back next turn.

### CR 606.5b

- `crates/engine/src/types/card.rs:369` — A planeswalker: a permanent that enters with `loyalty` starting loyalty (CR 606.5b) and
- `crates/engine/src/types/card.rs:1162` — A planeswalker's printed starting loyalty (CR 606.5b — it enters with that many loyalty
- `crates/engine/src/types/card.rs:1805` — A planeswalker's current loyalty (its loyalty counters, CR 606.5b). 0 for a non-planeswalker.
- `crates/engine/tests/game.rs:7111` — battlefield as a planeswalker with its printed starting loyalty (CR 606.5b).
- `crates/engine/tests/game.rs:50309` — "it enters with its printed starting loyalty (CR 606.5b)",

### CR 608

- `crates/engine/src/effects.rs:3` — Primary: CR 608 (resolving spells and abilities). Owns stack entry
- `crates/engine/src/effects.rs:117` — The ability leaves the stack as it resolves (CR 608), *before* its effect
- `crates/engine/src/pipeline.rs:8` — Primary: CR 704 (SBA fixpoint), CR 603 (trigger enqueue / APNAP placement), CR 608
- `crates/engine/src/pipeline.rs:52` — What happens when every living player passes in succession (CR 608).
- `crates/engine/src/resolution/mod.rs:5` — Primary: CR 608. External seam: [`Game::run`] (in `effects`) is the sole Effect→board verb —
- `crates/engine/tests/game.rs:67196` — countering your own creature can save it from the chosen quality's sweep (CR 608: put the

### CR 608.1

- `crates/engine/src/priority.rs:1412` — stack (CR 608.1: all players passing in succession with a non-empty stack). The server
- `crates/engine/tests/game.rs:18215` — completes the round of priority with a non-empty stack (CR 608.1), nothing else.

### CR 608.2

- `crates/engine/src/resolution/zones.rs:511` — CR 608.2 "do as much as possible" reading gains 2 life even then (the two clauses are
- `crates/engine/src/types/effect/shared.rs:1774` — CR 608.2, evaluated as the ability resolves, after the same ability's exile step already
- `crates/engine/src/types/stack.rs:1195` — `at_placement` distinguishes the two rules windows this pause serves (CR 603.3d vs CR 608.2):
- `crates/engine/tests/game.rs:88265` — are no cards in that player's graveyard, flip Nezumi Graverobber." (CR 712 flip; CR 608.2 —
- `crates/engine/tests/game.rs:95872` — CR 608.2: an instruction that can't be performed is ignored — a graveyard with no creature
- `crates/engine/tests/game.rs:101729` — "Then" (CR 608.2) orders the two clauses within the one resolution: the poison counter is

### CR 608.2b

- `crates/engine/src/apply.rs:161` — ETB ability (CR 303.4a/608.2b) to reanimate and attach it. This SBA sweep runs
- `crates/engine/src/cast.rs:2284` — relies on an illegal target fizzling at resolution, CR 608.2b, rather than
- `crates/engine/src/characteristics.rs:442` — at least one color with it (CR 105.2). Only `target` itself is a real target — CR 608.2b
- `crates/engine/src/effects.rs:89` — CR 608.2b: an ability whose stored target is no longer legal fizzles —
- `crates/engine/src/effects.rs:148` — earlier resolution recorded (CR 608.2b — an illegal target's step simply doesn't happen).
- `crates/engine/src/effects.rs:260` — resolution loop below re-checks each for legality (CR 608.2b) and applies
- `crates/engine/src/effects.rs:297` — CR 608.2b: if the spell has at least one targeted step and every one of them
- `crates/engine/src/effects.rs:319` — CR 608.2b/c: a step whose stored target is no longer legal is skipped —
- `crates/engine/src/effects.rs:372` — Animate Dead (CR 303.4a/608.2b): its own cast-time "enchant creature card in a
- `crates/engine/src/effects.rs:773` — CR 608.2b: whether a spell/ability's stored target is still a legal choice for
- `crates/engine/src/pending/handlers/edict.rs:403` — [`ChoiceRequest::ChooseTargetPlayers`] (CR 601.2c/608.2b: zero is a legal choice); once
- `crates/engine/src/pending/handlers/targets.rs:187` — resolution (CR 608.2b) — the triggered-ability twin of how a multi-target *spell*
- `crates/engine/src/pending/raise/library.rs:158` — creature sibling of [`put_land_from_hand`] (CR 608.2b's "may" — no eligible creature in hand
- `crates/engine/src/query.rs:1110` — left the battlefield (CR 608.2b: nothing left to refer to).
- `crates/engine/src/resolution/control.rs:249` — battlefield since is skipped (CR 608.2b); with no chosen recipient the donation does
- `crates/engine/src/resolution/control.rs:285` — needs both, so a target that has left since (CR 608.2b) cancels the whole swap.
- `crates/engine/src/resolution/copy.rs:124` — themself; a permanent target's controller pays. A missing target (CR 608.2b) is
- `crates/engine/src/resolution/copy.rs:154` — Wild Ricochet), already re-checked legal by CR 608.2b before this ran (so a spell that
- `crates/engine/src/resolution/counters.rs:383` — clause). A target that has left the battlefield since is skipped (CR 608.2b).
- `crates/engine/src/resolution/misc.rs:45` — exile step never ran) is unreachable in practice — CR 608.2b already fizzles the
- `crates/engine/src/resolution/misc.rs:81` — (CR 608.2b) if it already left the stack is handled upstream by `target_still_legal`,
- `crates/engine/src/resolution/misc.rs:163` — resolves. A no-op if `spell` already left the stack (CR 608.2b) — a response emptied that
- `crates/engine/src/resolution/pause_counter_spell.rs:33` — nothing to hold hostage — same no-op as the unconditional counter (CR 608.2b).
- `crates/engine/src/resolution/pause_counter_spell.rs:52` — going to a graveyard anyway — already left the stack / uncounterable (CR 608.2b /
- `crates/engine/src/resolution/pause_fight.rs:59` — or stopped being a creature (CR 608.2b — a fizzled shared target) or there's no
- `crates/engine/src/resolution/pump.rs:341` — left the battlefield since is skipped (CR 608.2b).
- `crates/engine/src/resolution/resolve_misc.rs:264` — target is guaranteed by the time this runs (CR 608.2b already fizzled the whole
- `crates/engine/src/resolution/sequence_steps.rs:118` — `token`. Guard-return (CR 608.2b) if the token has left the battlefield since — with
- `crates/engine/src/resolution/zones.rs:399` — ability was activated (destroyed in response, CR 608.2b) — skip it.
- `crates/engine/src/types/effect/damage.rs:31` — a single creature — CR 608.2b legality/protection/hexproof gate only that choice); the
- `crates/engine/src/types/effect/shared.rs:229` — ability before either step resolves, CR 608.2b).
- `crates/engine/src/types/filter.rs:877` — "Any number of target players" (Priest of Forgotten Gods, CR 601.2c/608.2b: choosing zero
- `crates/engine/src/types/stack.rs:240` — target players" (CR 601.2c/608.2b — a subset, possibly empty, of the offered `legal` set).
- `crates/engine/tests/game.rs:7277` — fizzles on resolution (CR 608.2b) instead of granting anything.
- `crates/engine/tests/game.rs:8315` — Ward) fizzles at resolution (CR 608.2b) — this engine's established target-illegality model
- `crates/engine/tests/game.rs:15445` — legal (mandatory-once-legal) activation that simply fizzles on resolution (CR 608.2b),
- `crates/engine/tests/game.rs:26658` — Spell Crumple, whose only target the copy already countered, fizzles (CR 608.2b) and must go
- `crates/engine/tests/game.rs:33025` — Answer the pending "any number of target players" choice for `player` (CR 601.2c/608.2b —
- `crates/engine/tests/game.rs:34384` — CR 601.2c/608.2b: "any number" permits zero — no life loss, no sacrifice, but the
- `crates/engine/tests/game.rs:36393` — CR 608.2b/303.4f: exiling the chosen graveyard card after Animate Dead is cast but before
- `crates/engine/tests/game.rs:45178` — return does nothing at all (no pause, no wedge) — CR 608.2b.
- `crates/engine/tests/game.rs:53243` — "You may put a creature card from your hand onto the battlefield" (CR 608.2b): declining
- `crates/engine/tests/game.rs:53360` — "you may put …" (CR 608.2b): declining is legal and leaves the creature in hand.
- `crates/engine/tests/game.rs:59009` — ── Target legality at resolution (CR 608.2b "fizzle") ──────────────────────────────
- `crates/engine/tests/game.rs:59026` — target is illegal at resolution, so the bolt fizzles (CR 608.2b) — no panic, and the bolt
- `crates/engine/tests/game.rs:59050` — "a fizzled instant still goes to the graveyard (CR 608.2b)"
- `crates/engine/tests/game.rs:59123` — "CR 608.2b: the destroy's target was wholly illegal, so the whole spell fails to \
- `crates/engine/tests/game.rs:60962` — unless they discard a land card." — the discard's land escape valve (CR 608.2b resolution-time
- `crates/engine/tests/game.rs:64855` — Declining the "up to one" return is legal (CR 601.2c/608.2b) — the token isn't undone.
- `crates/engine/tests/game.rs:64888` — CR 608.2b: the reflexive ability's minted-token host is destroyed in response, before the
- `crates/engine/tests/game.rs:66321` — CR 608.2b — a chosen target that became illegal before resolution is skipped; the spell still
- `crates/engine/tests/game.rs:68308` — CR 608.2b: if one of mode 2's two independently-chosen targets becomes illegal before Hull
- `crates/engine/tests/game.rs:76879` — "any number of other target creatures you control" (CR 601.2c/608.2b — zero is legal), and
- `crates/engine/tests/game.rs:79510` — CR 608.2b: if Willbender's chosen spell has left the stack (here, countered) before the trigger
- `crates/engine/tests/game.rs:98943` — filter, so aiming the tap ability there fizzles on resolution (CR 608.2b) instead of

### CR 608.2c

- `crates/engine/src/effects.rs:319` — CR 608.2b/c: a step whose stored target is no longer legal is skipped —
- `crates/engine/src/resolution/pump.rs:42` — CR 608.2c: nothing to boost if the source has already left the battlefield —
- `crates/engine/src/resolution/pump.rs:223` — do if the source has already left (CR 608.2c).
- `crates/engine/src/resolution/pump.rs:237` — keywords, all on the source. Nothing to do if the source has left (CR 608.2c).
- `crates/engine/tests/game.rs:7012` — cards, then mill a card." — an effect-internal optional sub-action (CR 608.2c), not an
- `crates/engine/tests/game.rs:7069` — Declining "You may discard a card" (CR 608.2c) means "If you do" never triggers — no
- `crates/engine/tests/game.rs:66387` — CR 608.2c — when every chosen target is gone before resolution the spell is a clean no-op

### CR 608.2h

- `crates/engine/src/types/effect/shared.rs:1595` — … }` (a spell's own resolve-time check, CR 608.2h) since no pool card needs it as an
- `crates/engine/tests/game.rs:61793` — onto the battlefield tapped, then shuffle." — a resolve-time conditional (CR 608.2h), not

### CR 609.7

- `crates/engine/src/resolution/damage.rs:174` — creature) — CR 609.7 would want each creature as the damage's true source for
- `crates/engine/src/triggers.rs:2403` — Queue attached-host damage-watch triggers (CR 609.7/702, Armadillo Cloak: "Whenever
- `crates/engine/src/triggers.rs:2415` — not once per CR 609.7 simultaneous-combat-damage event summed. Net life is identical for
- `crates/engine/src/types/effect/shared.rs:326` — [`Trigger::EnchantedCreatureDealsDamage`] watch just dealt (CR 609.7, Armadillo Cloak:
- `crates/engine/src/types/effect/shared.rs:1906` — CR 609.7: an `EnchantedCreatureDealsDamage` trigger's `Amount::TriggeringDamageDealt` reads
- `crates/engine/src/types/effect/shared.rs:2726` — the enchanted host's just-dealt damage (CR 609.7, Armadillo Cloak's "you gain that much life")
- `crates/engine/src/types/trigger.rs:757` — CR 609.7/603.10a last-known information: the amount of damage the enchanted host just

### CR 611

- `crates/engine/src/characteristics.rs:1990` — effect (CR 611), so no event is needed — it just stops applying when the source leaves.

### CR 611.2b

- `crates/engine/src/apply.rs:325` — CR 611.2b: for each condition-scoped control override whose [`ControlCondition`] no longer
- `crates/engine/src/state.rs:9` — The CR 611.2b duration condition scoping a control-changing effect (Rubinia Soulsinger's "for
- `crates/engine/src/state.rs:100` — Condition-scoped control changes (CR 611.2b — Rubinia Soulsinger's "for as long as you
- `crates/engine/src/types/stack.rs:2435` — A condition-scoped control-changing effect (CR 611.2b — Rubinia Soulsinger's "for as long
- `crates/engine/src/types/stack.rs:2446` — (the source untapped, left the battlefield, or changed controller — CR 611.2b); control
- `crates/engine/tests/game.rs:42539` — ── Condition-scoped control duration (CR 611.2b — Rubinia Soulsinger) ──────────────
- `crates/engine/tests/game.rs:42615` — "Rubinia's controller now controls the creature (CR 611.2b)"
- `crates/engine/tests/game.rs:42634` — "the steal reverts the instant Rubinia untaps (CR 611.2b)"
- `crates/engine/tests/game.rs:42660` — "the steal persists while Rubinia stays tapped and controlled (CR 611.2b)"
- `crates/engine/tests/game.rs:42691` — "the loaded card gains control of the target creature (CR 611.2b)"
- `crates/engine/tests/game.rs:42706` — "the steal reverts the instant Rubinia leaves the battlefield (CR 611.2b)"

### CR 611.2c

- `crates/engine/src/apply.rs:1316` — Excava, the Risen Past (CR 611.2c): the reanimated permanent's indefinite set, written
- `crates/engine/src/resolution/zones.rs:198` — set (CR 611.2c). A plain reanimation (`becomes == None`) is just the one event.
- `crates/engine/src/types/card.rs:1765` — An *indefinite* base-P/T SET (CR 611.2c — Excava, the Risen Past's "It's a 1/1 Spirit
- `crates/engine/src/types/card.rs:1775` — Card types added indefinitely (CR 611.2c — Excava's "It's a … creature … in addition to its
- `crates/engine/src/types/effect/shared.rs:1371` — rider applies to the permanent it reanimates (CR 611.2c — Excava, the Risen Past's "It's a 1/1
- `crates/engine/src/types/stack.rs:2349` — A just-reanimated permanent took on an *indefinite* set of characteristics (CR 611.2c —
- `crates/engine/tests/game.rs:36894` — battlefield (CR 611.2c indefinite one-shot set).

### CR 613

- `crates/engine/src/apply.rs:1370` — copy's name/cost rather than the printed card's — a CR 613 layered
- `crates/engine/src/cast.rs:2121` — (indices past the printed slice) sit after the removal in CR 613 order, so stay active.
- `crates/engine/src/characteristics.rs:21` — One engine-internal CR 613 continuous-effect entry affecting an object's effective
- `crates/engine/src/characteristics.rs:68` — [`Game::modifier_provenance`]. Additive attribution only — not CR 613 layers (engine-core-and-event…
- `crates/engine/src/characteristics.rs:1237` — Reads printed types for a non-permanent (CR 613 applies only to the permanent).
- `crates/engine/src/characteristics.rs:1278` — printed subtypes for a non-permanent (CR 613 applies only to the permanent).
- `crates/engine/src/characteristics.rs:1330` — A creature's effective power: its printed base run through the CR 613 P/T layers
- `crates/engine/src/characteristics.rs:1371` — The printed base P/T to feed the CR 613 layers, or `None` if `object` has no P/T (not a
- `crates/engine/src/characteristics.rs:1388` — (not `p.def`) so the flipped numbers feed the CR 613 layers.
- `crates/engine/src/characteristics.rs:1397` — Every CR 613 P/T continuous-effect entry currently affecting `object`: base-set 7b entries
- `crates/engine/src/characteristics_cache.rs:4` — Invalidated on relevant [`Event`]s. engine-core-and-event-model spec additive recompute, not CR 613…
- `crates/engine/src/characteristics_cache.rs:13` — additive recompute, not CR 613 layers).
- `crates/engine/src/combat.rs:5` — Deferred / gaps: per-deck increments under `docs/fidelity/` (fidelity-grind skill); layers (CR 613)…
- `crates/engine/src/types/card.rs:1202` — types, subtypes and abilities are replaced by the Treasure profile. Color (CR 613 layer 5) is
- `crates/engine/src/types/card.rs:1949` — CR 706/613 — "become a copy … until end of turn"): when the copy is established, the
- `crates/engine/src/types/effect/shared.rs:94` — characteristic scan (CR 613) read at resolution, not at announcement, so a creature whose
- `crates/engine/tests/game.rs:29528` — activated ability (gain 1 life) — exercises the CR 613/701 "loses all abilities" removal:
- `crates/engine/tests/game.rs:29679` — The Aura's own grants still apply (they sit after the removal in CR 613 order).
- `crates/engine/tests/game.rs:36925` — A +1/+1 counter layers on top of the set 1/1 (CR 613: 7b base-set, then 7c delta → 2/2).
- `crates/engine/tests/game.rs:92605` — A +1/+1 counter layers on top of the SET base (CR 613: 7b base-set, then 7c delta) — the
- `crates/engine/tests/game.rs:92646` — Wave E CR 613 timestamp regression: Trench Gorger's ETB leaves an indefinite 7b base-set on
- `crates/engine/tests/game.rs:98265` — control." The scan is a live characteristic read (CR 613) — a +1/+1 counter placed after
- `crates/engine/tests/game.rs:102714` — "\"becomes\" (CR 613) keeps the object's own name, unlike a CR 707 copy effect"
- `crates/engine/tests/game.rs:102797` — CR 613: "becomes a Treasure artifact … and loses all other card types and abilities" sets
- `crates/engine/tests/game.rs:102829` — "a type/ability-setting effect leaves color alone (CR 613 layer 5)"
- `crates/engine/tests/game.rs:102835` — Vraska's −2 has no duration (CR 613, indefinite). Applying it over an until-end-of-turn

### CR 613.1d

- `crates/engine/src/resolution/pump.rs:339` — loses all other card types and abilities (CR 613.1d/613.1f) — an indefinite def
- `crates/engine/src/types/card.rs:1200` — (CR 613.1d/613.1f — Vraska, Betrayal's Sting's −2). Unlike a copy effect (CR 707) this is a
- `crates/engine/src/types/stack.rs:2393` — non-copy type/ability-SETTING rewrite (CR 613.1d/613.1f — Vraska, Betrayal's Sting's −2:

### CR 613.1e

- `crates/engine/src/cast.rs:2119` — CR 613.1e/701 "loses all abilities": a printed activated ability is suppressed while an
- `crates/engine/src/characteristics.rs:42` — CR 613.1f/613.1e: "loses all abilities" effect on the host's own printed abilities.
- `crates/engine/src/characteristics.rs:1199` — abilities and keywords (CR 613.1e/701). Only a battlefield permanent can be a host.
- `crates/engine/src/characteristics.rs:1210` — stripping them ([`Game::host_loses_all_abilities`], CR 613.1e/701 "loses all abilities"), in
- `crates/engine/src/characteristics.rs:1471` — CR 613.1e/701 "loses all abilities": a host under an ability-removing Aura (Darksteel
- `crates/engine/src/triggers.rs:3729` — CR 613.1e/701 "loses all abilities": a live host under an ability-removing Aura

### CR 613.1f

- `crates/engine/src/characteristics.rs:42` — CR 613.1f/613.1e: "loses all abilities" effect on the host's own printed abilities.
- `crates/engine/src/resolution/pump.rs:339` — loses all other card types and abilities (CR 613.1d/613.1f) — an indefinite def
- `crates/engine/src/types/card.rs:1200` — (CR 613.1d/613.1f — Vraska, Betrayal's Sting's −2). Unlike a copy effect (CR 707) this is a
- `crates/engine/src/types/stack.rs:2393` — non-copy type/ability-SETTING rewrite (CR 613.1d/613.1f — Vraska, Betrayal's Sting's −2:

### CR 613.3

- `crates/engine/src/apply.rs:1343` — Trench Gorger (CR 613.3(7b)): the indefinite base-P/T-only sibling of
- `crates/engine/src/characteristics.rs:44` — CR 613.3(7b): the creature's base P/T is set.
- `crates/engine/src/characteristics.rs:46` — CR 613.3(7c): a P/T modification added on top of the base.
- `crates/engine/src/characteristics.rs:1374` — 0/0 — the animation's until-EOT `BasePtSet` layer then supplies the real numbers (CR 613.3).
- `crates/engine/src/resolution/pump.rs:221` — toughness each equal to the number of cards exiled this way", CR 613.3(7b)): unlike
- `crates/engine/src/types/card.rs:1714` — An until-end-of-turn base-P/T SET (CR 613.3(7b) — Biomass Mutation, Quandrix Charm's
- `crates/engine/src/types/card.rs:1873` — the object its rewritten enchant ability names (CR 613.3/702: "it loses 'enchant creature
- `crates/engine/src/types/effect/shared.rs:1374` — (CR 613.3(7b)), and `keywords` are added — all for as long as it stays on the battlefield.
- `crates/engine/src/types/stack.rs:2329` — A permanent's base power/toughness was SET until end of turn (CR 613.3(7b) — Biomass
- `crates/engine/src/types/stack.rs:2374` — A permanent's base power and toughness were SET *indefinitely* (CR 613.3(7b) —
- `crates/engine/tests/game.rs:35732` — X/X until end of turn." A CR 613.3(7b) until-end-of-turn base-P/T SET on every creature
- `crates/engine/tests/game.rs:35770` — turn." A CR 613.3(7b) until-end-of-turn base-P/T SET on one target, with 7c deltas
- `crates/engine/tests/game.rs:36016` — toughness until end of turn." A CR 613.3(7b) until-end-of-turn base-P/T SET, snapshotting
- `crates/engine/tests/game.rs:92446` — cards are all fetched and exiled; the printed 6/6 becomes a 3/3 (CR 613.3(7b) base set).
- `crates/engine/tests/game.rs:92559` — matching lands still sets base P/T to 0/0 (CR 613.3(7b)), which then dies to the CR 704.5f

### CR 613.3c

- `crates/engine/src/characteristics.rs:421` — A CR 613.3c layer-5 color-SET ([`Permanent::set_color_eot`] — Wild Mongrel's "becomes the
- `crates/engine/src/pending/handlers/library.rs:310` — a resolution-time color-SET (CR 613.3c — Wild Mongrel: stores it on `source`'s
- `crates/engine/src/resolution/pause_choose.rs:40` — Wild Mongrel's "...and becomes the color of your choice until end of turn": the same (CR 613.3c)
- `crates/engine/src/types/card.rs:1741` — A CR 613.3c layer-5 color-SET until end of turn (Wild Mongrel's "becomes the color of
- `crates/engine/src/types/card.rs:1744` — colors rather than unioning with them (unlike `added_colors_eot`'s CR 613.3c layer-5 ADD).
- `crates/engine/src/types/stack.rs:1657` — — Flickering Ward's [`Effect::Choice(ChoiceEffect::ChooseColor)`]) or a resolution-time color-SET (…
- `crates/engine/src/types/stack.rs:2148` — A "becomes the color of your choice until end of turn" choice was answered (CR 613.3c
- `crates/engine/tests/game.rs:34796` — Wild Mongrel — "...and becomes the color of your choice until end of turn." (CR 613.3c layer

### CR 613.4

- `crates/engine/src/apply.rs:1335` — Hofri Ghostforge's minted copy (CR 613.4): the indefinite subtype set, written as the
- `crates/engine/src/characteristics.rs:33` — CR 613.4: type/subtype-changing effect.
- `crates/engine/src/characteristics.rs:1150` — The CR 613.4 type/subtype layer a [`Effect::Static(StaticEffect::SetAttachedTypes)`] Aura forces on…
- `crates/engine/src/characteristics.rs:1235` — A battlefield permanent's card types after the CR 613.4 type layer: its printed types plus
- `crates/engine/src/characteristics.rs:1253` — CR 613.4: a set-types Aura (Darksteel Mutation) replaces the host's printed card types
- `crates/engine/src/characteristics.rs:1275` — A battlefield permanent's creature subtypes after the CR 613.4 subtype layer: its printed
- `crates/engine/src/combat.rs:31` — stolen blocks for you, not for its owner. Creature-ness via the CR 613.4 type layer
- `crates/engine/src/combat.rs:227` — Creature-ness via the CR 613.4 type layer (`is_creature_on_battlefield`), so an animated
- `crates/engine/src/priority.rs:393` — CR 613.4 type layer, not the printed kind: a manland animated into a creature (Restless
- `crates/engine/src/resolution/zones.rs:248` — on the minted token (CR 613.4 subtype layer, indefinite).
- `crates/engine/src/types/card.rs:1723` — Card types added until end of turn by a self-animation (CR 613.4 — Restless Spire's "this
- `crates/engine/src/types/effect/shared.rs:1373` — unioned onto the reanimated object (CR 613.4), base P/T is SET to `base_power`/`base_toughness`
- `crates/engine/src/types/effect/static.rs:251` — CR 613.4: when `true`, `add_types` are the host's *complete* card types (replacing its
- `crates/engine/src/types/stack.rs:2338` — A permanent gained card types + creature subtypes + colors until end of turn (CR 613.4 —
- `crates/engine/src/types/stack.rs:2364` — A permanent gained an *indefinite* set of creature subtypes (CR 613.4 subtype layer —
- `crates/engine/tests/game.rs:29493` — CR 613.4: Darksteel Mutation makes the enchanted creature "an Insect artifact creature …
- `crates/engine/tests/game.rs:29518` — "it loses its enchantment card type (CR 613.4 'loses all other card types')",

### CR 613.6

- `crates/engine/src/types/effect/shared.rs:47` — must resolve before the target leaves the battlefield (CR 613.6/603.10a last-known
- `crates/engine/tests/game.rs:8689` — power, and read it before the tuck removes the creature from the battlefield (CR 613.6

### CR 613.7

- `crates/engine/src/characteristics.rs:23` — `CardDef` or serialized. `timestamp` is the CR 613.7 same-layer ordering key.
- `crates/engine/src/core.rs:528` — The next continuous-effect timestamp (CR 613.7), consuming it. Static-permanent effects
- `crates/engine/src/lib.rs:144` — Monotonic source of continuous-effect timestamps (CR 613.7 — later same-layer effects win
- `crates/engine/src/types/card.rs:1720` — The CR 613.7 timestamp of [`Permanent::base_pt_set_eot`], so a later 7b set (Darksteel
- `crates/engine/src/types/card.rs:1729` — The CR 613.7 timestamp of [`Permanent::added_types_eot`] / [`Permanent::added_subtypes_eot`].
- `crates/engine/src/types/card.rs:1773` — The CR 613.7 timestamp of [`Permanent::set_base_pt`].
- `crates/engine/src/types/card.rs:1780` — The CR 613.7 timestamp of [`Permanent::added_types`] / [`Permanent::added_subtypes`].
- `crates/engine/src/types/card.rs:1802` — The CR 613.7 timestamp of this permanent's own static continuous effects while on the

### CR 614

- `crates/engine/src/apply.rs:257` — an enter-as-a-copy replacement (CR 614/616 — Altered Ego) pauses with the object briefly
- `crates/engine/src/cast.rs:1474` — Doubling Season (CR 614): each copy enters under `player`.
- `crates/engine/src/characteristics.rs:4` — Also: CR 614 slices (counter replacements, enters-tapped). P/T is a CR 613-ordered layered
- `crates/engine/src/characteristics.rs:2166` — (CR 614 — Hardened Scales, Doubling Season).
- `crates/engine/src/characteristics.rs:2233` — `recipient`'s control, after that player's static token-creation replacements (CR 614 —
- `crates/engine/src/characteristics.rs:2242` — life-gain replacements (CR 614 — Pest Rescuer, "you gain that much life plus 1 instead").
- `crates/engine/src/combat.rs:1287` — replacements (Doubling Season, CR 614) the same way `Effect::Token(TokenEffect::Create)`'s mint is.
- `crates/engine/src/core.rs:202` — A real placement, so CR 614 replacements apply (Winding Constrictor, Vorinclex).
- `crates/engine/src/pending/handlers/common.rs:34` — doublers would apply to any other "put a counter" (CR 614); `all_kinds` also moves every
- `crates/engine/src/pending/handlers/common.rs:99` — replaceable-counters pipeline (CR 614) [`Self::move_counters`] uses for its single-
- `crates/engine/src/pending/handlers/edict.rs:89` — counters on `source` through [`Game::counters_after_replacements`] (CR 614 doublers apply).
- `crates/engine/src/pending/handlers/edict.rs:182` — through the CR 614 counter-replacement pipeline (Hardened Scales, a doubler). `x` is the
- `crates/engine/src/pending/handlers/targets.rs:307` — ponytail: a loyalty counter added here skips the CR 614 replacement pipeline — loyalty is
- `crates/engine/src/replacements.rs:5` — CR 614 counter replacement against.
- `crates/engine/src/resolution/counters.rs:71` — Double the target's +1/+1 counters: place as many more as it already has (CR 614).
- `crates/engine/src/resolution/counters.rs:361` — The shared core of "double `object`'s +1/+1 counters" (CR 614): as many more as it
- `crates/engine/src/resolution/counters.rs:410` — Equipment is attached to (CR 614) — a no-target sibling of [`CountersEffect::DoubleCounters`]
- `crates/engine/src/resolution/tokens.rs:119` — Doubling Season (CR 614): each batch may enter under a different player
- `crates/engine/src/resolution/tokens.rs:184` — Doubling Season doubles Treasures too — they are tokens (CR 614).
- `crates/engine/src/resolution/tokens.rs:226` — Doubling Season (CR 614): the copies enter under `controller`.
- `crates/engine/src/resolution/tokens.rs:334` — Doubling Season (CR 614): each copy is its own token creation.
- `crates/engine/src/types/card.rs:816` — through [`Game::counters_after_replacements`] so CR 614 doublers apply. `devour = N` in TOML.
- `crates/engine/src/types/effect/static.rs:98` — A counter-placement replacement (CR 614 — Hardened Scales, Doubling Season, Vorinclex).
- `crates/engine/src/types/stack.rs:1625` — through [`Game::counters_after_replacements`] so CR 614 doublers apply. Answered by
- `crates/engine/src/types/stack.rs:2251` — total (CR 614; see [`Game::counters_after_replacements`]). Modeling the whole placement as
- `crates/engine/tests/game.rs:36287` — CR 614 "as this enters" applies on non-cast battlefield entry too: Reanimate should return
- `crates/engine/tests/game.rs:50902` — ── +1/+1 counter replacement effects (CR 614) — the Quandrix core ──────────────────────
- `crates/engine/tests/game.rs:75441` — Doubling Season (a CR 614 +1/+1-counter doubler): the devoured counters route through
- `crates/engine/tests/game.rs:99819` — uses (CR 614, CR 701.28a) — Doubling Season doubles four counters to eight.
- `crates/engine/tests/game.rs:100526` — CR 121.6/614: the upkeep ability's own "double the number of counters" step places as many
- `crates/engine/tests/game.rs:102356` — ── Counter replacement beyond +1/+1 (CR 614): any kind, on a permanent or a player ──────

### CR 614.1

- `crates/engine/src/replacements.rs:260` — put* the counters (CR 614.1) — answers Vorinclex's "if **you would put** …" / "if an
- `crates/engine/src/types/effect/static.rs:118` — counters (CR 614.1), not off whose permanent receives them. `None` keys off the
- `crates/engine/src/types/effect/static.rs:311` — Whose *placement* a [`StaticEffect::CounterReplacement`] replaces (CR 614.1) — the axis
- `crates/engine/tests/game.rs:102123` — rider: nothing is removed, so nobody gets a rad counter. The CR 614.1 predicate itself
- `crates/engine/tests/game.rs:102215` — CR 614.1 — Bloatfly Swarm's replacement is *conditional*: "If damage would be dealt to this
- `crates/engine/tests/game.rs:102903` — who *would put* the counters (CR 614.1), not off whose permanent receives them — so your

### CR 614.1c

- `crates/engine/src/characteristics.rs:2220` — carry (CR 614.1c — Gorma, the Gullet's third ability). Each qualifying static's `count` is
- `crates/engine/src/effects.rs:458` — Gullet, CR 614.1c): scan the caster's own battlefield for every static

### CR 614.6

- `crates/engine/src/apply.rs:2234` — Quintorius, Loremaster's CR 614.6 rider (see `PlayPermissions::stack_object_bottoms_library_on_leav…
- `crates/engine/src/apply.rs:2361` — A no-op unless `from` was a stack object (Quintorius's CR 614.6 redirect) — the
- `crates/engine/src/effects.rs:689` — Quintorius, Loremaster's CR 614.6 rider: "If that spell would be put into a graveyard,
- `crates/engine/src/pending/handlers/dig.rs:77` — CR 614.6 replacement rider — see `PlayPermissions::stack_object_bottoms_library_on_leave`.
- `crates/engine/src/resolution/misc.rs:194` — Quintorius, Loremaster's CR 614.6 rider (see `finish_instant_sorcery_resolution`'s
- `crates/engine/src/state.rs:148` — Quintorius, Loremaster's replacement rider (CR 614.6): "If that spell would be put into a
- `crates/engine/src/types/stack.rs:2845` — (CR 614.6): "If that spell would be put into a graveyard, put it on the bottom of its
- `crates/engine/tests/game.rs:58715` — its owner's library instead." (CR 118.5 free cast, CR 614.6 replacement)
- `crates/engine/tests/game.rs:58789` — CR 614.6's "would be put into a graveyard" covers a countered spell too, not just a
- `crates/engine/tests/game.rs:58976` — Proves the CR 614.6 redirect is scoped to the one flagged free-cast object — an ordinary

### CR 614.12

- `crates/engine/src/apply.rs:1950` — Excava, the Risen Past (CR 614.12): the finality counter is present the instant
- `crates/engine/src/de.rs:365` — A CR 614.12 as-enters replacement choice (Overgrown Tomb) — `enters_tapped_unless_you_pay_life = 2`;
- `crates/engine/src/pending/handlers/library.rs:275` — Answer a [`PendingChoice::ChooseCreatureType`] (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/pending/handlers/library.rs:308` — Answer a [`PendingChoice::ChooseColor`] — either an as-enters choice (CR 614.12/700.9-style
- `crates/engine/src/pending/handlers/optional.rs:534` — untapped, or decline and have it enter tapped (CR 614.12 — Overgrown Tomb's "As this
- `crates/engine/src/pending/handlers/optional.rs:538` — permanent doesn't exist until this answer mints [`Event::LandPlayed`] (CR 614.12's
- `crates/engine/src/spawn.rs:142` — CR 614.12: a permanent with a finality counter that would die (be put into a graveyard
- `crates/engine/src/triggers.rs:318` — First, so a card carrying both makes its CR 614.12 as-enters choice before its ETB trigger
- `crates/engine/src/triggers.rs:4122` — `enters_tapped_unless_you_pay_life` (CR 614.12's pay-life-or-tapped choice) is
- `crates/engine/src/triggers.rs:4213` — exception is [`Trigger::AsEnters`] (CR 614.12), a replacement effect that runs inline here
- `crates/engine/src/triggers.rs:4286` — "As this permanent enters, …" (CR 614.12) is a replacement effect, not a triggered
- `crates/engine/src/types/card.rs:612` — A CR 614.12 as-enters replacement *choice*, not a `Condition` — Overgrown Tomb's "As this
- `crates/engine/src/types/card.rs:1810` — Whether this permanent has a finality counter (CR 122.3g/614.12): if it would be put into
- `crates/engine/src/types/card.rs:1835` — The creature type named by an as-enters choice (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/types/card.rs:1840` — The color named by an as-enters choice (CR 614.12/700.9-style "as this Aura enters, choose
- `crates/engine/src/types/card.rs:2154` — chooses war or peace", one `(Archangel, true = war)` entry per copy — CR 614.12 makes the
- `crates/engine/src/types/stack.rs:419` — Answer a [`PendingChoice::ChooseCreatureType`] (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/types/stack.rs:423` — Answer a [`PendingChoice::ChooseColor`] (CR 614.12/700.9-style "as ~ enters, choose a
- `crates/engine/src/types/stack.rs:995` — untapped, or decline and have it enter tapped (CR 614.12 — [`CardDef::enters_tapped_unless_you_pay_…
- `crates/engine/src/types/stack.rs:998` — minted (the land isn't on the battlefield yet — CR 614.12's replacement locks in before
- `crates/engine/src/types/stack.rs:1647` — (CR 614.12/700.9-style "as ~ enters, choose a creature type" — Patchwork Banner's
- `crates/engine/src/types/stack.rs:1656` — `player` must name a color for `source` — either an as-enters choice (CR 614.12/700.9-style
- `crates/engine/src/types/stack.rs:2138` — An as-enters "choose a creature type" choice was answered (CR 614.12/700.9-style —
- `crates/engine/src/types/stack.rs:2145` — An as-enters "choose a color" choice was answered (CR 614.12/700.9-style — Flickering
- `crates/engine/src/types/stack.rs:2741` — Whether the entering permanent gets a finality counter (CR 614.12 — a permanent with
- `crates/engine/src/types/trigger.rs:46` — "As this permanent enters, …" (CR 614.12) — a replacement effect, *not* a triggered
- `crates/engine/tests/game.rs:36685` — CR 614.12: a permanent with a finality counter that would be put into a graveyard from the
- `crates/engine/tests/game.rs:93443` — with the choice already made as `color`. (CR 614.12, CR 601)
- `crates/engine/tests/game.rs:93458` — CR 614.12: "As this creature enters, choose a color" is a replacement effect, not a
- `crates/engine/tests/game.rs:96199` — CR 614.12's choice is made per permanent, so a second Archangel asks again and each copy's
- `crates/engine/tests/game.rs:97427` — (CR 614.12 — an as-enters replacement choice, not a board-state condition.)

### CR 614.13

- `crates/engine/src/types/card.rs:601` — Whether this permanent enters the battlefield tapped, *unconditionally* (CR 614.13 — a
- `crates/engine/src/types/card.rs:818` — true CR 614.13 replacement (counters present the instant it enters, before any ETB trigger
- `crates/engine/src/types/effect/shared.rs:1659` — `Permanent::tapped`, which is set at creation from `Game::enters_tapped` (CR 614.13's own
- `crates/engine/tests/game.rs:11330` — "a land with enters_tapped enters the battlefield tapped (CR 614.13)"
- `crates/engine/tests/game.rs:51687` — stays at zero (CR 614.13: no counters would be placed, so no placement happens).
- `crates/engine/tests/game.rs:81414` — "Coastal Tower enters the battlefield tapped (CR 614.13)"
- `crates/engine/tests/game.rs:81456` — "Elfhame Palace enters the battlefield tapped (CR 614.13)"
- `crates/engine/tests/game.rs:81498` — "Seaside Citadel enters the battlefield tapped (CR 614.13)"

### CR 615

- `crates/engine/src/apply.rs:1900` — Masked (CR 615 — Illusionary Mask): a face-down creature it put onto the
- `crates/engine/src/cast.rs:1573` — Illusionary Mask's CR 615 replacement (see [`Game::flip_masked`]), which reveals a masked
- `crates/engine/src/cast.rs:1579` — Illusionary Mask's CR 615 self-replacement: if `object` is a masked face-down permanent, it
- `crates/engine/src/cast.rs:2032` — A morph cast is not masked — only Illusionary Mask sets the CR 615 replacement.
- `crates/engine/src/cast.rs:2044` — whether the result is `masked` (Illusionary Mask's CR 615 turn-face-up-on-interaction
- `crates/engine/src/characteristics.rs:1704` — (CR 615 — Tajic, Legion's Edge: "Prevent all noncombat damage that would be dealt to other
- `crates/engine/src/characteristics.rs:1715` — Whether `target` itself carries Phantom Centaur's self-shield (CR 615: "If damage would
- `crates/engine/src/characteristics.rs:1727` — dealt TO itself (CR 615 — Guard Gomazoa: "Prevent all combat damage that would be dealt
- `crates/engine/src/characteristics.rs:1738` — deals TO OTHERS (CR 615 — Fog Bank: "... and dealt by Fog Bank."). True iff `source` has
- `crates/engine/src/characteristics.rs:1748` — each prevented damage-dealing event (both CR 615): a `CountersPlaced` removing the
- `crates/engine/src/characteristics.rs:1752` — CR 615's replacement effect doesn't create counters from nothing). Phantom Centaur's own
- `crates/engine/src/combat.rs:322` — by [`Game::apply`]). A masked Illusionary Mask creature (CR 615) is turned face up first.
- `crates/engine/src/combat.rs:694` — CR 615: a masked Illusionary Mask attacker becoming tapped is turned face up first.
- `crates/engine/src/combat.rs:867` — CR 615: a masked attacker that would assign or deal combat damage is turned face up
- `crates/engine/src/combat.rs:896` — CR 615: a masked blocker that would deal combat damage is turned face up
- `crates/engine/src/combat.rs:917` — CR 615: a masked blocker that would be dealt combat damage is turned face up first, before
- `crates/engine/src/combat.rs:922` — Moment's Peace (CR 615, #150): a this-turn table-wide "prevent all combat damage"
- `crates/engine/src/combat.rs:928` — Fog Bank (CR 615, #220): a permanent "prevent all combat damage ... dealt by" static on
- `crates/engine/src/combat.rs:981` — Guard Gomazoa / Fog Bank (CR 615, #220): a permanent "prevent all combat damage ...
- `crates/engine/src/combat.rs:988` — share and removes +1/+1 counters instead (CR 615) — the same self-shield
- `crates/engine/src/combat.rs:1059` — Peace's table-wide this-turn shield (CR 615) — both silent, as on the creature path.
- `crates/engine/src/combat.rs:1091` — prevention static (CR 615), so combat damage passes it straight through.
- `crates/engine/src/combat.rs:1103` — CR 615: a masked Illusionary Mask creature that would be dealt damage is turned face up
- `crates/engine/src/combat.rs:1109` — Phantom Centaur (CR 615): "If damage would be dealt to Phantom Centaur, prevent that
- `crates/engine/src/combat.rs:1112` — `combat`. Bloatfly Swarm's scaling variant (also CR 615) shares this same check.
- `crates/engine/src/combat.rs:1125` — Guard Gomazoa / Fog Bank (CR 615, #220): a permanent combat-damage-prevention static —
- `crates/engine/src/combat.rs:1134` — Moment's Peace (CR 615, #150): a this-turn table-wide "prevent all combat damage"
- `crates/engine/src/combat.rs:1176` — CR 615: a masked Illusionary Mask creature that would deal damage is turned face up first
- `crates/engine/src/combat.rs:1204` — Prevention shield (CR 615 — Inkshield): if `player` has a this-turn "prevent all combat
- `crates/engine/src/combat.rs:1212` — Fog Bank (CR 615, #220): a permanent "prevent all combat damage ... dealt by" static on
- `crates/engine/src/combat.rs:1219` — Moment's Peace (CR 615, #150): the table-wide "prevent all combat damage" shield — like
- `crates/engine/src/combat.rs:1266` — Consult `player`'s combat-damage prevention shields (CR 615 — Inkshield). Returns `false`
- `crates/engine/src/combat.rs:1286` — One token per point prevented (CR 615 / Inkshield), routed through the token-creation
- `crates/engine/src/pending/handlers/library.rs:746` — Masked (CR 615): Illusionary Mask's face-down creature turns face up when it would
- `crates/engine/src/resolution/damage.rs:128` — prevents this damage outright and removes +1/+1 counters instead (CR 615).
- `crates/engine/src/resolution/damage.rs:141` — Tajic prevents noncombat damage to its controller's other creatures (CR 615).
- `crates/engine/src/resolution/damage.rs:259` — prevents its own share and removes +1/+1 counters instead (CR 615) — a
- `crates/engine/src/resolution/damage.rs:287` — of its own +1/+1 counters instead (CR 615).
- `crates/engine/src/resolution/damage.rs:362` — this damage outright and removes +1/+1 counters instead (CR 615).
- `crates/engine/src/resolution/damage.rs:366` — Tajic prevents noncombat damage to its controller's other creatures (CR 615).
- `crates/engine/src/resolution/resolve_misc.rs:175` — Inkshield (CR 615): arm a this-turn combat-damage prevention shield protecting the
- `crates/engine/src/state.rs:40` — "Prevent all combat damage that would be dealt to you this turn" (CR 615 — Inkshield):
- `crates/engine/src/types/card.rs:1631` — Whether this face-down spell was cast by Illusionary Mask's `{X}` ability (CR 615). Copied
- `crates/engine/src/types/card.rs:1935` — Whether this face-down permanent was put onto the battlefield by Illusionary Mask (CR 615):
- `crates/engine/src/types/stack.rs:2081` — Whether this face-down cast was Illusionary Mask's `{X}` (CR 615); see [`Spell::masked`].
- `crates/engine/src/types/stack.rs:2692` — entry (Inkshield, CR 615) — a marker replacing the `LifeChanged`/commander-damage this
- `crates/engine/tests/game.rs:4068` — Mirrors the protection case above for Phantom Centaur's self-shield (CR 615): the assigned
- `crates/engine/tests/game.rs:80309` — --- Illusionary Mask CR 615 turn-face-up-on-interaction replacement (slice 3, clause 2) ---
- `crates/engine/tests/game.rs:80340` — CR 615: a face-down creature Illusionary Mask put onto the battlefield that would be dealt
- `crates/engine/tests/game.rs:80376` — CR 615: a masked face-down creature that would become tapped is turned face up first.
- `crates/engine/tests/game.rs:80390` — CR 615: a masked face-down creature that would assign or deal combat damage is turned face up
- `crates/engine/tests/game.rs:81101` — ── Tajic, Legion's Edge — noncombat-damage prevention static (CR 615) ──────────────────
- `crates/engine/tests/game.rs:81249` — Phantom Centaur (CR 615): "Protection from black / Phantom Centaur enters with two +1/+1
- `crates/engine/tests/game.rs:81287` — CR 615's shield is symmetric across combat roles: it prevents the damage an attacker
- `crates/engine/tests/game.rs:81339` — At zero +1/+1 counters Phantom Centaur is a 0/0, but CR 615 says the shield still
- `crates/engine/tests/game.rs:81383` — The shield is self-only (CR 615 reads "Phantom Centaur", not "other creatures you
- `crates/engine/tests/game.rs:90295` — Guard Gomazoa / Fog Bank (CR 615, #220): a permanent combat-damage-prevention static, unlike
- `crates/engine/tests/game.rs:100692` — prevents all of it (CR 615), so no damage is dealt and no poison is placed.
- `crates/engine/tests/game.rs:101947` — ── Increment 22: Bloatfly Swarm — a scaling self-shield giving each player rad (CR 615, CR
- `crates/engine/tests/game.rs:101952` — way." Phantom Centaur's shape (a self-hosted CR 615 replacement removing +1/+1 counters), but
- `crates/engine/tests/game.rs:102178` — blocker-to-attacker paths (CR 615, mirroring Phantom Centaur's combat coverage).

### CR 616

- `crates/engine/src/apply.rs:257` — an enter-as-a-copy replacement (CR 614/616 — Altered Ego) pauses with the object briefly
- `crates/engine/src/effects.rs:428` — permanent enters (before the enters-with-counters / ETB steps, CR 616) so its
- `crates/engine/src/spawn.rs:146` — ponytail: a commander with a finality counter is a CR 616 choice between two

### CR 616.1

- `crates/engine/src/cast.rs:1107` — A land's own as-enters static (CR 616.1 — Vivid Crag's "enters with two charge
- `crates/engine/src/effects.rs:527` — [`Effect::Static(StaticEffect::EntersWithCounters)`] static (CR 616.1) or its [`CardDef::vanishing`…
- `crates/engine/src/replacements.rs:267` — `((base + Σadd) × Πtimes) ÷ 2^halvings`. CR 616.1 lets the *affected player* order
- `crates/engine/tests/game.rs:91165` — CR 616.1's as-enters static fires at `LandPlayed`, the land special action's own ETB

### CR 700

- `crates/engine/src/apply.rs:1001` — ponytail: the broader cast-spell *triggers* (magecraft, CR 700's "whenever you

### CR 700.2

- `crates/engine/src/cast.rs:158` — aren't folded into ward/cost-reduction — no pool modal card targets a warded permanent. (CR 702.21,…
- `crates/engine/src/cast.rs:640` — Whether `modes` is a legal modal choice for `def` (CR 700.2): between `modal_choose` and
- `crates/engine/src/cast.rs:733` — The chosen mode's independent target clauses (CR 601.2c/700.2), in printed order, if it
- `crates/engine/src/effects.rs:230` — target (CR 700.2 — modes are validated at cast, so `nth_mode` is `Some`); a
- `crates/engine/src/pending/handlers/fanout.rs:449` — "you return" (mandatory): a legal card must be chosen — declining is illegal (CR 700.2).
- `crates/engine/src/pending/handlers/library.rs:848` — (CR 700.2) — `modes` names `choose` distinct (printed-mode index, chosen Player target)
- `crates/engine/src/playable.rs:473` — Modal: mana first, then enough playable modes for `modal_choose` (CR 700.2) — an Abrade
- `crates/engine/src/query.rs:773` — A modal spell's targets travel *per mode* (CR 700.2), never in the top-level `target`, so a
- `crates/engine/src/query.rs:800` — The legal upper bound on modes `caster` may choose for `def`'s modal spell (CR 700.2 / CR
- `crates/engine/src/resolution/pause_edict.rs:202` — immediately instead (CR 700.2's "as many as possible").
- `crates/engine/src/triggers.rs:4232` — A modal *triggered* ability (CR 700.2 extended to a trigger — Shadrix Silverquill's
- `crates/engine/src/types/card.rs:550` — Whether this is a modal spell (CR 700.2). When set, the card's `Timing::Spell` abilities
- `crates/engine/src/types/card.rs:555` — How many distinct modes a modal spell's caster chooses at cast (CR 700.2) — 1 for a
- `crates/engine/src/types/card.rs:567` — cast time (CR 700.2, Nexus Mentality: "if you control a commander as you cast this spell,
- `crates/engine/src/types/card.rs:1521` — A modal spell's chosen modes (CR 700.2), each with its own target. An empty selection for
- `crates/engine/src/types/effect/choice.rs:166` — Witch of the Moors): a legal card *must* be chosen — declining is illegal (CR 700.2).
- `crates/engine/src/types/stack.rs:40` — leaves `modes` empty. A **modal** spell (CR 700.2) leaves `target` `None` and lists its
- `crates/engine/src/types/stack.rs:1613` — of them immediately instead of pausing (CR 700.2's "as many as possible"). Answered by
- `crates/engine/src/types/stack.rs:3067` — spell (CR 700.2 — a modal spell chooses exactly one of its modes at cast).
- `crates/engine/src/types/stack.rs:3325` — A modal spell's chosen modes (CR 700.2). Indexed by printed mode: `chosen[i] == Some(target)`
- `crates/engine/src/types/stack.rs:3392` — The `mode`-th mode of a modal "choose one" spell (CR 700.2): its `Timing::Spell` abilities
- `crates/engine/src/types/stack.rs:3412` — One ability's independent target clauses, in printed order (CR 601.2c/700.2) — Hull Breach's
- `crates/engine/tests/game.rs:12521` — A modal spell's targets travel per mode (CR 700.2), so `required_target` answers `None` for the
- `crates/engine/tests/game.rs:21923` — instead of pausing on a choice (CR 700.2's "as many as possible").
- `crates/engine/tests/game.rs:44650` — ── Modal "choose one" spells (CR 700.2) ────────────────────────────────────────────
- `crates/engine/tests/game.rs:44761` — ── Modal "choose two" spells (CR 700.2) — the Command shape ──────────────────────────
- `crates/engine/tests/game.rs:45463` — ── Modal triggered ability (CR 700.2 extended to a trigger, fidelity increment #110) ───────
- `crates/engine/tests/game.rs:57567` — ── Modal triggered ability (CR 700.2 "choose one" on a trigger) — Atsushi ──────────────
- `crates/engine/tests/game.rs:67130` — Answer Zimone's Hypothesis' resolution-time "choose odd or even" (CR 700.2 — a choose-one, so
- `crates/engine/tests/game.rs:68046` — living inside one mode of a modal spell (CR 700.2 + CR 601.2c together).
- `crates/engine/tests/game.rs:76916` — the pause lands the moment the dies trigger is placed — no further resolution. (CR 603.6, CR 700.2)

### CR 700.2d

- `crates/engine/src/cast.rs:641` — `modal_choose_max` entries (an exact count when `modal_choose_max` is `None` — CR 700.2d's
- `crates/engine/src/de.rs:333` — CR 700.2d "choose one or more" — the max of the range; `None` keeps the count
- `crates/engine/src/types/card.rs:560` — The maximum distinct modes a "choose one or more" spell's caster may choose (CR 700.2d) —
- `crates/engine/tests/game.rs:45204` — ── Modal "choose one or more" spells (CR 700.2d) — the Casualties of War shape ───────
- `crates/engine/tests/game.rs:45406` — CR 700.2d "choose one or more" — artifact and enchantment used to collapse into a single

### CR 700.4

- `crates/engine/src/apply.rs:1997` — CR 700.4/701.29 last-known information: a token ceasing to exist is a
- `crates/engine/src/apply.rs:2093` — CR 700.4/701.29 last-known information: read `is_modified` before
- `crates/engine/src/apply.rs:2097` — controller ("died under *your* control", CR 700.4) — the sibling
- `crates/engine/src/pending/handlers/library.rs:451` — (CR 700.4) and can't fire Dies (see `#183`'s from-battlefield gate).
- `crates/engine/src/triggers.rs:488` — CR 700.4: "died" means specifically "put into a graveyard from the battlefield" (CR 700.4, CR 603.6)
- `crates/engine/src/triggers.rs:521` — CR 700.4 "put into a graveyard from the battlefield" — the single source-zone
- `crates/engine/src/triggers.rs:1634` — CR 700.4 "put into a graveyard from the battlefield" — same gate as
- `crates/engine/src/triggers.rs:1686` — CR 700.4 "put into a graveyard from the battlefield" — same gate as
- `crates/engine/src/triggers.rs:1753` — CR 700.4 "put into a graveyard from the battlefield" — same gate as
- `crates/engine/src/types/card.rs:2083` — (CR 700.4).
- `crates/engine/src/types/effect/shared.rs:1602` — Level 3 — CR 700.4/701.29). Reads the controller's turn-scoped
- `crates/engine/src/types/filter.rs:505` — use — the arrival is never "put into a graveyard from the battlefield" (CR 700.4), so it
- `crates/engine/src/types/stack.rs:2822` — [`Self::MovedToGraveyard`] — CR 700.4 "died" is specifically "put into a graveyard *from
- `crates/engine/tests/game.rs:55870` — instead." (CR 603.4 intervening-if, CR 700.4 magecraft-shaped trigger)
- `crates/engine/tests/game.rs:78182` — CR 700.4 "died under *your* control": a modified creature owned by P1 but stolen by P0
- `crates/engine/tests/game.rs:86675` — ── Increment 183: "died" means put into a graveyard from the battlefield (CR 700.4/603.6) ────
- `crates/engine/tests/game.rs:86679` — CR 700.4: "died" is specifically "put into a graveyard from the battlefield" — Wild
- `crates/engine/tests/game.rs:86724` — CR 700.4: a library→graveyard mill is not "from the battlefield" — Millikin milling
- `crates/engine/tests/game.rs:86961` — CR 700.4: a library→graveyard search is not "from the battlefield" either — Buried Alive
- `crates/engine/tests/game.rs:87662` — battlefield, so it does not "die" (CR 700.4) and its Dies trigger must not fire.

### CR 700.9

- `crates/engine/src/pending/handlers/library.rs:275` — Answer a [`PendingChoice::ChooseCreatureType`] (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/pending/handlers/library.rs:308` — Answer a [`PendingChoice::ChooseColor`] — either an as-enters choice (CR 614.12/700.9-style
- `crates/engine/src/types/card.rs:1835` — The creature type named by an as-enters choice (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/types/card.rs:1840` — The color named by an as-enters choice (CR 614.12/700.9-style "as this Aura enters, choose
- `crates/engine/src/types/stack.rs:419` — Answer a [`PendingChoice::ChooseCreatureType`] (CR 614.12/700.9-style "as ~ enters,
- `crates/engine/src/types/stack.rs:423` — Answer a [`PendingChoice::ChooseColor`] (CR 614.12/700.9-style "as ~ enters, choose a
- `crates/engine/src/types/stack.rs:1647` — (CR 614.12/700.9-style "as ~ enters, choose a creature type" — Patchwork Banner's
- `crates/engine/src/types/stack.rs:1656` — `player` must name a color for `source` — either an as-enters choice (CR 614.12/700.9-style
- `crates/engine/src/types/stack.rs:2138` — An as-enters "choose a creature type" choice was answered (CR 614.12/700.9-style —
- `crates/engine/src/types/stack.rs:2145` — An as-enters "choose a color" choice was answered (CR 614.12/700.9-style — Flickering

### CR 701

- `crates/engine/src/cast.rs:2119` — CR 613.1e/701 "loses all abilities": a printed activated ability is suppressed while an
- `crates/engine/src/characteristics.rs:1199` — abilities and keywords (CR 613.1e/701). Only a battlefield permanent can be a host.
- `crates/engine/src/characteristics.rs:1210` — stripping them ([`Game::host_loses_all_abilities`], CR 613.1e/701 "loses all abilities"), in
- `crates/engine/src/characteristics.rs:1471` — CR 613.1e/701 "loses all abilities": a host under an ability-removing Aura (Darksteel
- `crates/engine/src/triggers.rs:3729` — CR 613.1e/701 "loses all abilities": a live host under an ability-removing Aura
- `crates/engine/tests/game.rs:29528` — activated ability (gain 1 life) — exercises the CR 613/701 "loses all abilities" removal:

### CR 701.x

- `crates/engine/src/types/stack.rs:1676` — ponytail: CR 701.x has every player choose without seeing anyone else's name; this

### CR 701.5

- `crates/engine/tests/game.rs:79340` — A hard counter (CR 701.5 — "counter target spell"), used to send Willbender's chosen spell to

### CR 701.5a

- `crates/engine/src/resolution/misc.rs:162` — Counter `spell` (CR 701.5a): move it from the stack to its owner's graveyard, so it never

### CR 701.5b

- `crates/engine/src/pending/handlers/optional.rs:314` — Answer a [`PendingChoice::ChooseCounteredSpellDestination`] (Hinder's CR 701.5b rider):
- `crates/engine/src/resolution/pause_counter_spell.rs:50` — Hinder's destination rider (CR 701.5b — `countered_dest`): pause this ability's
- `crates/engine/src/resolution/pause_counter_spell.rs:86` — Spell Crumple's destination rider (CR 701.5b — `countered_dest`): the same "would
- `crates/engine/src/types/effect/shared.rs:1126` — Where a countered spell goes instead of its owner's graveyard (CR 701.5b), the destination
- `crates/engine/src/types/stack.rs:451` — Answer a [`PendingChoice::ChooseCounteredSpellDestination`] (Hinder's CR 701.5b rider):
- `crates/engine/src/types/stack.rs:941` — (CR 701.5b — [`Effect::Misc(MiscEffect::CounterTargetSpell)`]'s `countered_dest` rider): `spell` is…
- `crates/engine/tests/game.rs:26229` — ── Hinder's countered-spell destination rider (CR 701.5b) ──────────────────────────
- `crates/engine/tests/game.rs:26362` — countered — CR 701.5b's tuck rider then has nothing to move (no tuck, no graveyard).
- `crates/engine/tests/game.rs:26426` — ── Spell Crumple's forced counter destination + self-tuck rider (CR 701.5b) ────────

### CR 701.5c

- `crates/engine/src/apply.rs:1036` — CR 701.5c/112.7a: a countered activated ability ceases to exist — remove the
- `crates/engine/src/resolution/misc.rs:78` — Counter target activated ability (CR 701.5c/112.7a — Azorius Guildmage). The target
- `crates/engine/src/types/stack.rs:2218` — An activated ability on the stack was countered (CR 701.5c / 112.7a — Azorius Guildmage):

### CR 701.5g

- `crates/engine/src/de.rs:325` — "This spell can't be countered" (CR 701.5g) — `uncounterable = true`; absent
- `crates/engine/src/resolution/misc.rs:170` — CR 701.5g: "this spell can't be countered" — the counter fizzles and the spell
- `crates/engine/src/types/card.rs:545` — "This spell can't be countered" (CR 701.5g, e.g. Altered Ego). Checked in
- `crates/engine/tests/game.rs:25934` — CR 701.5g: Altered Ego "can't be countered." P1's hard counter still legally targets it
- `crates/engine/tests/game.rs:25935` — (the target itself stays legal — only the counter effect fizzles at resolution, CR 701.5g),

### CR 701.7

- `crates/engine/tests/game.rs:55141` — Oversimplify ("Exile all creatures") — CR 701.18 exile is not "destroy" (CR 701.7), so

### CR 701.8

- `crates/engine/src/cast.rs:525` — CR 701.8/601.2h: a cost discard is still a discard — fires "whenever you discard"
- `crates/engine/src/pending/handlers/edict.rs:329` — firing the CR 701.8 discard marker — the shared tail [`Game::answer_discard`]'s effect
- `crates/engine/src/pending/handlers/fanout.rs:543` — CR 701.8: every discard fires "whenever you discard" watchers — a cleanup hand-size
- `crates/engine/src/state.rs:236` — event (CR 701.8), and only the **nonland** cards among it are threaded into
- `crates/engine/src/triggers.rs:903` — A discard (CR 701.8) — distinct from `MovedToGraveyard`, which also fires for a
- `crates/engine/src/triggers.rs:907` — Conspiracy Theorist's "one or more nonland cards" (CR 701.8): record every
- `crates/engine/src/triggers.rs:1056` — Conspiracy Theorist's "one or more nonland cards" (CR 701.8/603.3b): the whole
- `crates/engine/src/triggers.rs:1071` — "Nonland card" is any card that is not a land (CR 701.8) — an
- `crates/engine/src/triggers.rs:2625` — Queue `Trigger::YouDiscard` watchers on every permanent `player` controls (CR 701.8):
- `crates/engine/src/types/effect/choice.rs:146` — A batch nonland-discard payoff (CR 701.8 — Conspiracy Theorist's "you may exile one of
- `crates/engine/src/types/stack.rs:3009` — A card was discarded (CR 701.8): `card` is its new graveyard-object id (the same id
- `crates/engine/src/types/trigger.rs:290` — Whenever this permanent's controller discards a card (CR 701.8) — Containment Construct's
- `crates/engine/src/types/trigger.rs:294` — hand-size trim both count, CR 701.8). The discarded card's graveyard-object id rides in
- `crates/engine/src/types/trigger.rs:424` — Whenever the controller discards one or more **nonland** cards (CR 701.8 — Conspiracy
- `crates/engine/src/types/trigger.rs:682` — CR 701.8/603.10a last-known information: the graveyard-object ids of the **nonland** cards
- `crates/engine/tests/game.rs:34766` — Paying Wild Mongrel's discard cost is a real discard (CR 701.8/602.2b): it fires "whenever
- `crates/engine/tests/game.rs:57867` — ── Discard trigger (CR 701.8) — "whenever you discard a card" ─────────────────────────
- `crates/engine/tests/game.rs:57916` — multi-card batch (CR 701.8) behind Conspiracy Theorist's "discard one or more nonland cards."

### CR 701.9

- `crates/engine/src/pending/handlers/dig.rs:258` — ids top-to-bottom. Face-up (public, CR 701.17) unless `face_down` (CR 701.9 — Abstract
- `crates/engine/src/pending/handlers/dig.rs:494` — (CR 701.9 face-down — hidden from every viewer but `controller`) then the next four
- `crates/engine/src/types/stack.rs:2580` — Intet, the Dreamer instead exiles `face_down` (CR 701.9) and names the granting permanent in
- `crates/engine/src/types/stack.rs:2594` — unless `face_down` (Abstract Performance's first pile, CR 701.9 "exile a card face down" —
- `crates/engine/tests/game.rs:89576` — "\"exile the top card of your library face down\" (CR 701.9)"

### CR 701.12

- `crates/engine/src/combat.rs:1162` — Resolve a fight (CR 701.12): `a` and `b` each deal damage equal to their power to the
- `crates/engine/src/combat.rs:1167` — (the oracle text never says "fights"; CR 701.12 doesn't apply), just a plain "deals damage
- `crates/engine/src/combat.rs:1183` — Fight damage is noncombat (CR 701.12), so it passes `combat = false` — Tajic's static
- `crates/engine/src/resolution/pause_fight.rs:19` — Fight (CR 701.12): `target` is already the opponent's creature (chosen at cast);
- `crates/engine/src/resolution/pause_fight.rs:55` — Primal Might's mirror shape (CR 701.12): `target` is already the ally (the pumped
- `crates/engine/src/triggers.rs:940` — (CR 701.12) or other noncombat creature damage only emits that.
- `crates/engine/src/triggers.rs:969` — creature damage (fight, CR 701.12), so this one event arm covers both for
- `crates/engine/src/types/effect/misc.rs:34` — target creature you don't control" is one-directional — CR 701.12/119.3 fight
- `crates/engine/src/types/stack.rs:2670` — since noncombat creature damage (fight, CR 701.12) also emits `DamageMarked` but must not
- `crates/engine/src/types/trigger.rs:314` — (CR 701.12) or other noncombat creature damage only emits that, not this marker. The
- `crates/engine/tests/game.rs:4309` — Fight (CR 701.12) deals noncombat damage — the trigger only watches combat damage (CR
- `crates/engine/tests/game.rs:45606` — ── Fight (CR 701.12, fidelity increment #48) ───────────────────────────────────────────
- `crates/engine/tests/game.rs:81103` — durationless permanent static: effect damage and fight damage (CR 701.12) to the
- `crates/engine/tests/game.rs:81222` — Fight damage is noncombat (CR 701.12): your other creature deals its damage but takes
- `crates/engine/tests/game.rs:97749` — resolution-time target split as [`FIGHT_SPELL`], but `one_way: true` — no fight (CR 701.12
- `crates/engine/tests/game.rs:97773` — creature you don't control." — one-directional, not a fight (CR 701.12 doesn't apply, so
- `crates/engine/tests/game.rs:99518` — Infect reads "damage", not "combat damage" — a fight (CR 701.12) from an infect creature

### CR 701.12c

- `crates/engine/tests/game.rs:45733` — Both powers are read before either amount is marked (CR 510.2/701.12c), so a "dead" 2/2

### CR 701.13

- `crates/engine/src/cast.rs:2210` — A "mill a card" additional cost (CR 701.13/602.2b — Millikin's "{T}, Mill a card:
- `crates/engine/src/cast.rs:2469` — "Mill a card" as part of the cost (CR 701.13 — Millikin's "{T}, Mill a card: Add
- `crates/engine/src/types/effect/shared.rs:1472` — "Mill a card" as part of the cost (CR 701.13/118 — Millikin's "{T}, Mill a card: Add

### CR 701.15b

- `crates/engine/src/apply.rs:117` — regeneration instead (CR 701.15b) — the same substitution `DestroyTarget` already
- `crates/engine/src/apply.rs:1170` — Remove the regenerated creature from combat (CR 701.15b).
- `crates/engine/src/apply.rs:2656` — [`Event::Regenerated`]'s CR 701.15b removal, [`Event::RemovedFromCombat`]
- `crates/engine/src/core.rs:641` — How many regeneration shields the permanent at `id` currently has (CR 701.15b); 0 if it
- `crates/engine/src/priority.rs:1762` — Regeneration shields last only "this turn" (CR 701.15b) — any unused one expires.
- `crates/engine/src/resolution/destroy.rs:53` — regeneration (CR 701.15b), unless "can't be regenerated" turns it off (CR
- `crates/engine/src/types/card.rs:1816` — How many regeneration shields this permanent currently has (CR 701.15b): each is a
- `crates/engine/src/types/card.rs:1820` — reset to 0 at cleanup (CR 701.15b's "this turn"). Runtime state, not TOML-authored,
- `crates/engine/src/types/stack.rs:2236` — the same combat-list cleanup [`Self::Regenerated`]'s CR 701.15b removal already applies.
- `crates/engine/src/types/stack.rs:2238` — A regeneration shield was granted to a permanent (CR 701.15b — [`Effect::Control(ControlEffect::Reg…
- `crates/engine/src/types/stack.rs:2241` — A permanent was regenerated instead of destroyed (CR 701.15b): one regeneration shield is
- `crates/engine/src/types/stack.rs:2245` — A permanent's regeneration shields expired at cleanup (CR 701.15b's "this turn"). Resets
- `crates/engine/tests/game.rs:76522` — the shield is consumed (CR 701.15b).
- `crates/engine/tests/game.rs:76624` — Cross the cleanup step into the next turn; the unused shield expires (CR 701.15b's "this
- `crates/engine/tests/game.rs:76649` — shield replaces it exactly as it replaces an explicit `DestroyTarget` (CR 701.15b).
- `crates/engine/tests/game.rs:82873` — "{B}: Regenerate this creature." — a self-only regeneration shield (CR 701.15b), not a
- `crates/engine/tests/game.rs:93673` — same shield-honoring shape as a single-target `DestroyTarget` (CR 701.15b).

### CR 701.15d

- `crates/engine/src/types/card.rs:1819` — the destruction carries [`Effect::Destroy(DestroyEffect::DestroyTarget)::cant_be_regenerated`] (CR …
- `crates/engine/tests/game.rs:76560` — "It can't be regenerated" (CR 701.15d): the shield doesn't apply, so the creature dies...
- `crates/engine/tests/game.rs:85061` — "It can't be regenerated." (CR 701.15d) — a shield doesn't save the creature.
- `crates/engine/tests/game.rs:85082` — "It can't be regenerated." (CR 701.15d)
- `crates/engine/tests/game.rs:93698` — "They can't be regenerated" (CR 701.15d, Reiver Demon) turns the shield off even for a

### CR 701.16

- `crates/engine/src/pending/handlers/dig.rs:513` — `controller`'s library (all public, CR 701.16; a short library reveals only what's there,
- `crates/engine/src/pending/handlers/dig.rs:550` — top `count` of `controller`'s library (all public, CR 701.16; a short library reveals only
- `crates/engine/src/pending/handlers/optional.rs:494` — and sacrifice it (CR 701.16). Rupture Spire's own-ETB twin of [`Game::pay_echo`] — same
- `crates/engine/src/pending/handlers/optional.rs:582` — sacrifices `source` instead (CR 701.16).
- `crates/engine/src/spawn.rs:176` — The event for sacrificing the permanent at `id` (CR 701.16): it goes to the graveyard
- `crates/engine/src/types/stack.rs:1548` — (still library-resident and public, CR 701.16 "reveal" makes them visible to everyone) —
- `crates/engine/src/types/stack.rs:1561` — `controller`'s library (still library-resident and public, CR 701.16 "reveal") — to send
- `crates/engine/tests/game.rs:33009` — ── Multi-player sacrifice edicts (CR 701.16) ────────────────────────────────────────
- `crates/engine/tests/game.rs:33309` — ── Caster-directed keep-one-of-each-type sweep (Tragic Arrogance, CR 701.16) ──
- `crates/engine/tests/game.rs:67145` — CR 701.16 (return to hand) — "You may put a +1/+1 counter on a creature. Then choose odd or

### CR 701.16a

- `crates/engine/src/pending/handlers/edict.rs:56` — the choice's `options`, each distinct (CR 701.16a) — mandatory, unlike
- `crates/engine/src/types/stack.rs:1607` — `filter`) — a forced sacrifice cost/effect the affected player directs (CR 701.16a: "the
- `crates/engine/tests/game.rs:21949` — choice (CR 701.16a) — pauses on a ChooseOwnSacrifices, not an automatic pick.
- `crates/engine/tests/game.rs:62892` — candidate lands, which two go is the controller's own choice (CR 701.16a), not a
- `crates/engine/tests/game.rs:89224` — choice (CR 701.16a), not a target.

### CR 701.16b

- `crates/engine/src/pending/handlers/edict.rs:654` — CR 701.16b), then advance to the next player.
- `crates/engine/src/types/stack.rs:1299` — (CR 701.16b). `remaining` are the still-to-choose players (APNAP order) after this one; the

### CR 701.16d

- `crates/engine/src/playable.rs:578` — any nonempty pick outright. Control is enforced here directly (CR 701.16d — you can only
- `crates/engine/tests/game.rs:63501` — "CR 701.16d — you can only sacrifice what you control"

### CR 701.17

- `crates/engine/src/pending/handlers/dig.rs:87` — `count` cards of `controller`'s library face-up (public, CR 701.17), then raise a
- `crates/engine/src/pending/handlers/dig.rs:258` — ids top-to-bottom. Face-up (public, CR 701.17) unless `face_down` (CR 701.9 — Abstract
- `crates/engine/src/pending/handlers/dig.rs:417` — library face-up (CR 701.17, public) and add its mana value to the running tally, then
- `crates/engine/src/types/effect/shared.rs:1160` — own `controller` axis — CR 701.17, you can only sacrifice what you control). Which ones

### CR 701.18

- `crates/engine/tests/game.rs:55141` — Oversimplify ("Exile all creatures") — CR 701.18 exile is not "destroy" (CR 701.7), so

### CR 701.19

- `crates/engine/src/characteristics.rs:2031` — static ability (CR 701.19, Stranglehold's "Your opponents can't search libraries"): true if
- `crates/engine/src/de.rs:202` — common "up to N") or the `"any"` marker (CR 701.19's "any number of" — Trench Gorger),
- `crates/engine/src/pending/handlers/library.rs:485` — Always shuffle at the true end of the search (CR 701.19). ponytail: library order
- `crates/engine/src/pending/handlers/library.rs:504` — player's raise comes back without pausing, CR 701.19) so the rest of the fan-out still
- `crates/engine/src/pending/raise/library.rs:115` — Stranglehold's "Your opponents can't search libraries" (CR 701.19): a denied search never
- `crates/engine/src/resolution/pause_look.rs:60` — search to the exiled/destroyed permanent's controller instead (CR 701.19 doesn't
- `crates/engine/src/resolution/pause_look.rs:130` — Stranglehold denies `searching_player` (CR 701.19): the raise above came back
- `crates/engine/src/types/filter.rs:250` — Which library cards a [`Effect::Dig(DigEffect::SearchLibrary)`] may find (CR 701.19 — "search for a…
- `crates/engine/src/types/filter.rs:499` — Sterling Grove: "reveal it, then shuffle and put that card on top" — CR 701.19). A
- `crates/engine/src/types/filter.rs:553` — Whose library a [`Effect::Dig(DigEffect::SearchLibrary)`] searches (CR 701.19 — "search their libra…
- `crates/engine/src/zones.rs:112` — Shuffle `player`'s library, then put `card` on top (CR 701.19 — Enlightened Tutor/Sterling
- `crates/engine/tests/game.rs:34147` — ── Join forces (Collective Voyage): a shared X funded by every player (CR 101.4, CR 701.19) ──
- `crates/engine/tests/game.rs:51718` — ── Library search: tutors, ramp, and fetchlands (CR 701.19) ─────────────────────────
- `crates/engine/tests/game.rs:52210` — CR 701.19: "up to three" is a maximum, not a requirement — fetching just one of two
- `crates/engine/tests/game.rs:54050` — The library was still shuffled (CR 701.19): the draw order differs from the stacked order.
- `crates/engine/tests/game.rs:54234` — APNAP order (CR 101.4), each under that player's own control (CR 701.19), not the Explorer's
- `crates/engine/tests/game.rs:57382` — pick is deterministic given a fixed seed — replay determinism, CR 701.19.
- `crates/engine/tests/game.rs:70890` — "you may search your library" — fail to find is always legal (CR 701.19).
- `crates/engine/tests/game.rs:72555` — revealed, not after (CR 701.19).
- `crates/engine/tests/game.rs:84105` — `SearchDest::LibraryTop` (CR 701.19: reveal, then shuffle, then top — nothing leaves the
- `crates/engine/tests/game.rs:85417` — "may search" — the destroyed land's controller declines by failing to find (CR 701.19).
- `crates/engine/tests/game.rs:95360` — but the search it triggers never raises a pause at all — CR 701.19, the search and its

### CR 701.19a

- `crates/engine/src/resolution/resolve_misc.rs:98` — CR 701.19a: if there's nothing to exile, this is a no-op.
- `crates/engine/tests/game.rs:57479` — CR 701.19a: "if you can't" — an empty graveyard makes this a harmless no-op, not a panic.

### CR 701.19c

- `crates/engine/src/pending/handlers/library.rs:423` — Fail to find ends the search outright (CR 701.19c is always legal).
- `crates/engine/src/types/stack.rs:1173` — (`tapped` if it enters the battlefield), or none ("fail to find", CR 701.19c). Answered by
- `crates/engine/tests/game.rs:52293` — (CR 701.19c) that ends the whole search — no partial finds, but the mandatory shuffle
- `crates/engine/tests/game.rs:52685` — CR 701.19c: "up to two" searches for as many as possible. With only one basic land in the
- `crates/engine/tests/game.rs:54344` — Veteran Explorer: a player may decline their search (CR 701.19c — "fail to find" is always
- `crates/engine/tests/game.rs:67045` — A "may" search declined (fail to find, CR 701.19c) — no basic enters, no shuffle-less no-op.

### CR 701.19f

- `crates/engine/src/pending/handlers/library.rs:395` — shuffling (CR 701.19f — an "up to N" search shuffles once, after the last pick);
- `crates/engine/src/resolution/pause_look.rs:65` — shuffle, CR 701.19f) finishes — see that fan-out's doc comment.
- `crates/engine/src/types/filter.rs:572` — 101.4) starting with the active player; each shuffles their own library once (CR 701.19f)
- `crates/engine/src/types/stack.rs:1178` — shuffles only once, after the last pick (CR 701.19f). The library is shuffled once the
- `crates/engine/tests/game.rs:51982` — hand, then shuffle." (CR 701.19f — one search finding several cards shuffles once, after
- `crates/engine/tests/game.rs:52567` — (CR 701.19f).
- `crates/engine/tests/game.rs:54319` — P0's library was shuffled exactly once (CR 701.19f): a fresh game replaying only its

### CR 701.20

- `crates/engine/src/triggers.rs:897` — A sacrifice (CR 701.20) — distinct from `MovedToGraveyard`/`TokenCeasedToExist`,
- `crates/engine/src/triggers.rs:2580` — `def`) that player `by` just sacrificed (CR 701.20): `Trigger::YouSacrifice` on `by`'s own
- `crates/engine/src/triggers.rs:4974` — looks on) matches a sacrifice-trigger's `filter` (CR 701.20). Only the type and "another
- `crates/engine/src/types/stack.rs:2997` — A permanent was sacrificed (CR 701.20): `by` is the player who sacrificed it, `def` its

### CR 701.22

- `crates/engine/src/effects.rs:832` — Clash (CR 701.22): pick an opponent, both reveal + scry-1 their top, score the clash.
- `crates/engine/src/pending/handlers/dig.rs:740` — Begin a clash (CR 701.22, [`Effect::Dig(DigEffect::Clash)`]): the ability's controller picks a livi…
- `crates/engine/src/resolution/mod.rs:143` — Clash (CR 701.22): the controller's keep/bottom scry just cleared — raise the opponent's
- `crates/engine/src/resolution/resume.rs:17` — Clash (CR 701.22): opponent still owed a keep-on-top-or-bottom scry after the
- `crates/engine/src/types/effect/shared.rs:892` — Clash picks its opponent at resolution (CR 701.22), not via a cast/activation target.
- `crates/engine/src/types/stack.rs:794` — Lash Out (CR 701.22): once the opponent to clash with is known, both reveal their top card
- `crates/engine/tests/game.rs:89740` — --- Clash (CR 701.22) + Lash Out ---------------------------------------------------------------

### CR 701.22a

- `crates/engine/src/pending/handlers/dig.rs:677` — Clash reveals both top cards as it resumes (CR 701.22a), so it needs an events sink the
- `crates/engine/src/pending/handlers/dig.rs:771` — then takes a one-card scry to leave the card on top or bottom it (CR 701.22a) — the ability's

### CR 701.22d

- `crates/engine/src/lib.rs:123` — Whether the current resolution's [`Effect::Dig(DigEffect::Clash)`] was won by its controller (CR 70…
- `crates/engine/src/pending/handlers/dig.rs:795` — (CR 701.22d) — strictly greater; a controller who revealed nothing can't win, and an
- `crates/engine/src/triggers.rs:4040` — Lash Out (CR 701.22d): the resolution-scoped won-the-clash flag a preceding
- `crates/engine/src/types/effect/shared.rs:1767` — "If you win the clash" (Lash Out — CR 701.22d): reads the resolution-scoped

### CR 701.27

- `crates/engine/src/pending/handlers/targets.rs:252` — counter of every kind already there (CR 701.27). A nonzero `remaining` re-raises for the
- `crates/engine/src/pending/handlers/targets.rs:268` — Each option is chosen at most once (CR 701.27: a *set* of permanents/players, not a
- `crates/engine/src/pending/handlers/targets.rs:290` — Whenever you proliferate (CR 701.27, Scheming Aspirant) — one instance just resolved,
- `crates/engine/src/pending/handlers/targets.rs:291` — regardless of whether `chosen` is empty (CR 701.27 doesn't condition the instance on a
- `crates/engine/src/pending/handlers/targets.rs:306` — Give `id` another counter of each kind already on it (CR 701.27).
- `crates/engine/src/pending/handlers/targets.rs:352` — Give `seat` another counter of each kind already on them (CR 701.27 — poison).
- `crates/engine/src/pending/raise/optional.rs:8` — Every permanent and player that currently has a counter (CR 701.27 — proliferate can only
- `crates/engine/src/resolution/pause_choose.rs:90` — Proliferate (CR 701.27) pauses on a Proliferate choice over every counter-bearing
- `crates/engine/src/types/effect/shared.rs:1200` — Out of proliferate's reach for that reason (CR 701.27 chooses only permanents and players).
- `crates/engine/src/types/effect/shared.rs:1262` — "each kind already there" walks (proliferate, CR 701.27) can iterate both the same way.
- `crates/engine/src/types/effect/shared.rs:1286` — (Final Act) and "each kind already there" (CR 701.27, proliferate). The
- `crates/engine/src/types/stack.rs:312` — Answer a [`PendingChoice::Proliferate`]: the chosen permanents and players (CR 701.27 —
- `crates/engine/src/types/stack.rs:800` — One thing proliferate may choose (CR 701.27: "Choose any number of permanents and/or
- `crates/engine/src/types/stack.rs:1113` — every player that currently has a counter (CR 701.27: "Choose any number of permanents
- `crates/engine/src/types/trigger.rs:547` — Whenever the ability's controller proliferates (CR 701.27 — "proliferate twice"/"proliferate
- `crates/engine/src/types/trigger.rs:551` — [`Game::answer_proliferate`], even when the player proliferates nothing (CR 701.27 doesn't
- `crates/engine/tests/game.rs:70926` — ── Increment #93: counter-choice-verbs — proliferate, move counters, mass counter removal ── (CR 70…
- `crates/engine/tests/game.rs:70930` — Expansion Algorithm (soc): "Proliferate X times." A creature with a +1/+1 counter and a (CR 701.27,…
- `crates/engine/tests/game.rs:70969` — The same choice may be answered differently each of the X times (CR 701.27 is a fresh
- `crates/engine/tests/game.rs:71004` — CR 701.27: proliferate chooses a *set* of permanents/players, not a multiset — picking the
- `crates/engine/tests/game.rs:97919` — Drown in Ichor ({1}{B}) — "Target creature gets -4/-4 until end of turn. Proliferate." (CR 701.27, …
- `crates/engine/tests/game.rs:98563` — ── Contagion Clasp, Evolution Sage, Cathedral Acolyte (CR 121.4, CR 701.27) ───────────────────
- `crates/engine/tests/game.rs:98988` — ── Frank Horrigan grind: proliferate cards (CR 701.27) ──
- `crates/engine/tests/game.rs:98992` — Atomize: "Destroy target nonland permanent. Proliferate." (CR 701.27)
- `crates/engine/tests/game.rs:99022` — Karn's Bastion: "{T}: Add {C}." "{4}, {T}: Proliferate." (CR 701.27)
- `crates/engine/tests/game.rs:99086` — Thirsting Roots, mode 1: "Proliferate." (CR 701.27)
- `crates/engine/tests/game.rs:99111` — Proliferate." (CR 701.27)
- `crates/engine/tests/game.rs:100853` — "and a second poison counter on the poisoned player (CR 701.27)"
- `crates/engine/tests/game.rs:100881` — "toxic 1, then proliferate grows it (CR 701.27)"
- `crates/engine/tests/game.rs:101335` — ── Increment #17: proliferate's full CR 701.27 scope — "permanents and/or players" ──
- `crates/engine/tests/game.rs:101339` — CR 701.27: "Choose any number of permanents and/or players, then give each another counter
- `crates/engine/tests/game.rs:101366` — CR 701.27 only grows counters "already there" — a player with none is not a legal choice.
- `crates/engine/tests/game.rs:101451` — CR 701.27 chooses a *set* of permanents and/or players — a player can't be picked twice to
- `crates/engine/tests/game.rs:101653` — CR 701.27 gives "another counter of each kind already there," so a player holding rad
- `crates/engine/tests/game.rs:102543` — ── Increment #18: `Trigger::YouProliferate` (CR 701.27) — Scheming Aspirant ──────────────
- `crates/engine/tests/game.rs:102605` — CR 701.27 doesn't condition the proliferate instance on choosing anything — an empty

### CR 701.27b

- `crates/engine/tests/game.rs:98773` — Contagion Engine: "{4}, {T}: Proliferate twice." — CR 701.27b: "then do it again" is a
- `crates/engine/tests/game.rs:101412` — CR 701.27b ("then do it again" — Contagion Engine, Agent Frank Horrigan): two separate
- `crates/engine/tests/game.rs:102568` — CR 701.27b ("then do it again") is two separate proliferate instances, so "whenever you

### CR 701.28

- `crates/engine/tests/game.rs:99665` — Monstrosity (#12 fidelity increment, CR 701.28): Permanent::monstrous,

### CR 701.28a

- `crates/engine/src/resolution/counters.rs:322` — "Monstrosity N" (CR 701.28a): already monstrous is a total no-op (CR 701.28c) — not
- `crates/engine/src/types/effect/counters.rs:39` — "Monstrosity N" (CR 701.28a — Alpha Deathclaw's "{5}{B}{G}: Monstrosity 4"): a no-op if
- `crates/engine/tests/game.rs:99756` — CR 701.28a: "If this permanent isn't monstrous, put N +1/+1 counters on it and it becomes
- `crates/engine/tests/game.rs:99819` — uses (CR 614, CR 701.28a) — Doubling Season doubles four counters to eight.

### CR 701.28b

- `crates/engine/src/apply.rs:903` — CR 701.28b: a one-way flag, never cleared by the Untap `StepBegan` turn-boundary reset.
- `crates/engine/src/core.rs:682` — Whether the permanent at `id` is monstrous (CR 701.28b — has had a "Monstrosity N"
- `crates/engine/src/triggers.rs:441` — CR 701.28b: the flag is already set (the apply ran first) — same self-scan idiom
- `crates/engine/src/types/card.rs:1698` — Whether this permanent has become monstrous (CR 701.28b) — a one-way state, not
- `crates/engine/src/types/stack.rs:2123` — A permanent became monstrous (CR 701.28b — [`Effect::Counters(CountersEffect::Monstrosity)`]):
- `crates/engine/src/types/trigger.rs:57` — When this permanent becomes monstrous (CR 701.28b — Alpha Deathclaw's "…or becomes
- `crates/engine/tests/game.rs:99839` — "Monstrous" is a one-way state on the object, not the card (CR 701.28b) — a permanent that

### CR 701.28c

- `crates/engine/src/resolution/counters.rs:322` — "Monstrosity N" (CR 701.28a): already monstrous is a total no-op (CR 701.28c) — not
- `crates/engine/src/triggers.rs:442` — as `TurnedFaceUp` above. A no-op activation (CR 701.28c) mints no
- `crates/engine/src/types/card.rs:1703` — accompanying +1/+1 counters to zero (CR 701.28c only silences a *second* activation).
- `crates/engine/src/types/effect/counters.rs:40` — the source is already monstrous (CR 701.28c); otherwise puts `count` +1/+1 counters on it
- `crates/engine/src/types/trigger.rs:60` — second time on a later no-op activation (CR 701.28c means no event is minted at all).
- `crates/engine/tests/game.rs:99779` — CR 701.28c: if the permanent is already monstrous, "Monstrosity N" does nothing at all —
- `crates/engine/tests/game.rs:99791` — "a second activation is a complete no-op (CR 701.28c)"
- `crates/engine/tests/game.rs:99808` — activate_monstrosity(&mut game, beast); // already monstrous: CR 701.28c no-op

### CR 701.29

- `crates/engine/src/apply.rs:1997` — CR 700.4/701.29 last-known information: a token ceasing to exist is a
- `crates/engine/src/apply.rs:2093` — CR 700.4/701.29 last-known information: read `is_modified` before
- `crates/engine/src/query.rs:1518` — "Modified" (CR 701.29 — Silkguard's hexproof rider).
- `crates/engine/src/query.rs:1588` — Whether `id` is "modified" from `you`'s perspective (CR 701.29 / Silkguard's reminder
- `crates/engine/src/types/card.rs:2076` — Whether a *modified* creature (CR 701.29 — has a counter, is enchanted by an Aura, or is
- `crates/engine/src/types/effect/shared.rs:1602` — Level 3 — CR 700.4/701.29). Reads the controller's turn-scoped
- `crates/engine/src/types/filter.rs:227` — Imperial Advisor — CR 701.29 / "Equipment, Auras you control, and counters are
- `crates/engine/src/types/filter.rs:735` — "Modified" (CR 701.29 / Silkguard's reminder text "Equipment, Auras you control, and
- `crates/engine/tests/game.rs:68566` — ── Increment #90 (continued): the `modified` permanent-filter axis (CR 701.29 — has a
- `crates/engine/tests/game.rs:68696` — CR 701.29 / Silkguard's reminder text: "Equipment, Auras you control, and counters are
- `crates/engine/tests/game.rs:68757` — CR 701.29 / Silkguard's reminder text: "Equipment, Auras you control, and counters are
- `crates/engine/tests/game.rs:78061` — (CR 701.29), then kill it with Lightning Bolt.
- `crates/engine/tests/game.rs:78087` — True: an equipped (modified, CR 701.29) creature dying under P0's control sets the flag.
- `crates/engine/tests/game.rs:78194` — game.add_plus_counter(host); // a +1/+1 counter makes it modified (CR 701.29)

### CR 701.30

- `crates/engine/src/apply.rs:2604` — A reveal is not a zone change (CR 701.30) — the card stays exactly where it is;
- `crates/engine/src/types/stack.rs:2898` — The top card of `player`'s library was revealed (CR 701.30) — public to every player,
- `crates/engine/tests/game.rs:71793` — ── Reveal-until-a-count (#84, CR 701.30/120): open_the_way ────────────────────────────────

### CR 701.32

- `crates/engine/src/query.rs:955` — Populate: creature tokens the choosing player controls (CR 701.32).
- `crates/engine/src/types/filter.rs:113` — by Populate (CR 701.32), used with [`Effect::Token(TokenEffect::CreateCopy)`].
- `crates/engine/src/types/filter.rs:114` — ponytail: populate *chooses* a token, it doesn't *target* one (CR 701.32 is a choice, not a
- `crates/engine/src/types/stack.rs:1322` — `player` casts a council's-dilemma vote (CR 701.32 — Fateful Tempest's "Starting with you,

### CR 701.32a

- `crates/engine/src/pending/handlers/fanout.rs:153` — ever skipped: every living player votes (CR 701.32a).

### CR 701.34

- `crates/engine/src/apply.rs:2417` — Manifest (CR 701.34): the library card `from` enters face down as a 2/2 — its real
- `crates/engine/src/resolution/zones.rs:19` — Reality Shift's rider (CR 701.34): the *target's* controller manifests their top
- `crates/engine/src/triggers.rs:392` — A manifest enters the battlefield as a creature (CR 701.34); its own `Etb` scans
- `crates/engine/src/types/card.rs:1914` — Whether this permanent is *face down* (CR 708 — a manifested card, CR 701.34): while set,
- `crates/engine/src/types/stack.rs:2931` — A library card `from` was manifested (CR 701.34) — put onto the battlefield face down as
- `crates/engine/tests/game.rs:79050` — --- Manifest / face-down permanents (CR 701.34, CR 708 — Reality Shift) ---
- `crates/engine/tests/game.rs:79054` — face down as a 2/2 creature (CR 701.34), with no printed name/subtypes exposed (CR 708.2).

### CR 701.34d

- `crates/engine/src/resolution/zones.rs:27` — return Vec::new(); // an empty library manifests nothing (CR 701.34d).

### CR 701.34e

- `crates/engine/src/apply.rs:2435` — Turn face up (CR 701.34e): reveal the real card by clearing the face-down flag.
- `crates/engine/src/cast.rs:1525` — it has one, otherwise a manifest's hidden *printed* cost (CR 701.34e — Reality Shift) — then
- `crates/engine/src/cast.rs:1544` — CR 701.34e: only a creature card may be turned face up — but that restriction is
- `crates/engine/src/cast.rs:1551` — hidden card's printed cost (CR 701.34e).
- `crates/engine/src/priority.rs:1344` — hidden printed cost (CR 701.34e) — the same fork as `Game::turn_face_up`.
- `crates/engine/src/query.rs:371` — CR 701.34e: only a creature card may be turned face up — but that restriction is
- `crates/engine/src/types/stack.rs:138` — Turn a face-down manifested permanent face up (CR 701.34e): pay its hidden creature card's
- `crates/engine/src/types/stack.rs:2940` — A face-down permanent was turned face up (CR 701.34e — the turn-face-up special action):
- `crates/engine/src/types/stack.rs:3111` — Turn the face-down manifested `permanent` face up (CR 701.34e): pay its hidden creature
- `crates/engine/tests/game.rs:79107` — A manifested creature card can be turned face up any time for its mana cost (CR 701.34e — a
- `crates/engine/tests/game.rs:79159` — A manifested noncreature card can't be turned face up (CR 701.34e — only a creature card may

### CR 701.38

- `crates/engine/src/combat.rs:4` — Also: CR 701.38 (goad), CR 508.1g (attack costs / pillow-fort).
- `crates/engine/src/combat.rs:159` — The players who have goaded `creature` (CR 701.38); empty if it isn't goaded.
- `crates/engine/src/combat.rs:257` — Whether `creature` is currently goaded by anyone (CR 701.38): a one-shot `GoadTarget`
- `crates/engine/src/combat.rs:394` — declaration itself. (CR 508.1g, CR 701.38)
- `crates/engine/src/combat.rs:621` — attack at all ("if able") is simply not required — the same escape hatch goad uses. (CR 702.19, CR …
- `crates/engine/src/combat.rs:674` — afford. (CR 701.38, CR 508.1g)
- `crates/engine/src/priority.rs:1522` — beginning clears every goad they applied. (CR 701.38)
- `crates/engine/src/state.rs:4` — Side state for goad (CR 701.38), delayed triggers (CR 603.7), exile links,
- `crates/engine/src/state.rs:27` — Goad (CR 701.38): each entry is (goaded creature, the player who goaded it, source card
- `crates/engine/src/types/stack.rs:2466` — The creature `object` was goaded by player `by` (CR 701.38), until `by`'s next turn.
- `crates/engine/tests/game.rs:46` — an all-pass loop. (CR 701.38, CR 117)
- `crates/engine/tests/game.rs:40982` — ── Goad (CR 701.38) ─────────────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:40988` — game.goad(c, PlayerId(1)); // goaded by P1 (CR 701.38)
- `crates/engine/tests/game.rs:41007` — Sending it at a non-goader (P2) satisfies goad. (CR 701.38)
- `crates/engine/tests/game.rs:41017` — Dogfood: empty declare rejected (goad), then both seats passed — the step used to advance (CR 701.3…
- `crates/engine/tests/game.rs:41078` — game.goad(c, PlayerId(1)); // goaded only by P1 (CR 701.38)
- `crates/engine/tests/game.rs:41103` — game.goad(c, PlayerId(2)); // both opponents goaded it — no non-goader remains (CR 701.38)
- `crates/engine/tests/game.rs:41144` — Tapped ⇒ can't attack ⇒ goad's "if able" clause doesn't require it. (CR 701.38, CR 508)
- `crates/engine/tests/game.rs:41184` — Summoning-sick (no haste) ⇒ can't attack ⇒ not forced by goad. (CR 701.38, CR 508)
- `crates/engine/tests/game.rs:41854` — The goad is continuous (CR 701.38) for as long as the Aura stays attached — not the
- `crates/engine/tests/game.rs:41953` — Destroy the Aura (Fracture); the goad is a live query over the attachment, not an (CR 701.38, CR 30…
- `crates/engine/tests/game.rs:41954` — entry in `goaded`, so it disappears the instant the Aura is gone (CR 701.38).
- `crates/engine/tests/game.rs:62132` — target). (CR 701.38, CR 108.3, CR 601.2c)

### CR 701.38a

- `crates/engine/src/characteristics.rs:1019` — [`Effect::Static(StaticEffect::GrantToAttached)`] carries `goad = true` (CR 701.38a — the Impetus c…
- `crates/engine/src/combat.rs:268` — Creatures `player` controls that must attack this combat if able (CR 701.38a goad), each
- `crates/engine/src/combat.rs:280` — can pay count, and among those a non-goader is preferred (CR 701.38a). A goaded
- `crates/engine/src/combat.rs:389` — always affordable. Drives goad's "if able" clause (CR 701.38a): a goaded creature whose
- `crates/engine/src/combat.rs:584` — Goad requirements (CR 701.38a): every goaded creature the active player controls that
- `crates/engine/src/combat.rs:606` — CR 701.38a: "attacks a *player* other than you if able" — a non-goader only counts
- `crates/engine/src/combat.rs:672` — treats a defender whose tax `player` can't pay as "not able" (CR 701.38a "if able") via
- `crates/engine/tests/game.rs:30698` — (CR 701.38a's "may attack a goader" exception when no non-goader opponent exists).
- `crates/engine/tests/game.rs:41194` — CR 701.38a "if able" + CR 508.1g: a goaded creature must attack, but only if its controller
- `crates/engine/tests/game.rs:41248` — CR 701.38a's "attacks a player other than you if able" is gated on the tax too: the lone

### CR 701.38b

- `crates/engine/src/combat.rs:161` — next turn, CR 701.38b) with the continuous goad-on-attached static (Impetus cycle,
- `crates/engine/src/priority.rs:1521` — Goad ends "until your next turn" (CR 701.38b): the active player's turn
- `crates/engine/src/types/stack.rs:2472` — Every goad done by player `by` ended (the start of `by`'s turn — CR 701.38b).
- `crates/engine/tests/game.rs:41121` — Rolling to the goader's (P1) next turn clears the goad at its untap (CR 701.38b).

### CR 701.42

- `crates/engine/src/resolution/pause_arrange.rs:1` — ArrangeTop pause family — [`Effect::Dig(DigEffect::Scry)`] / [`Effect::Dig(DigEffect::Surveil)`] (C…
- `crates/engine/tests/game.rs:38770` — ── Scry & Surveil (CR 701.42 / 701.43) ──────────────────────────────────────────────

### CR 702

- `crates/engine/src/combat.rs:3` — Primary: CR 506–511 (combat phases/steps), CR 702 (evergreen combat keywords).
- `crates/engine/src/triggers.rs:2403` — Queue attached-host damage-watch triggers (CR 609.7/702, Armadillo Cloak: "Whenever
- `crates/engine/src/triggers.rs:3904` — count, CR 702), but `TriggerContext` carries no source object — only the ability's
- `crates/engine/src/triggers.rs:3907` — gate, Primordial Hydra's trample), or through `Game::ability_condition_holds` (CR 702)
- `crates/engine/src/triggers.rs:4047` — Whether `object` (a permanent) has `at_least` or more +1/+1 counters on it — CR 702's
- `crates/engine/src/types/card.rs:578` — Keywords granted only while a `Condition` holds (CR 702 conditional statics —
- `crates/engine/src/types/card.rs:1873` — the object its rewritten enchant ability names (CR 613.3/702: "it loses 'enchant creature
- `crates/engine/src/types/effect/shared.rs:1633` — "as long as this creature has `at_least` or more +1/+1 counters on it" (CR 702 counters;
- `crates/engine/src/types/effect/shared.rs:1644` — "if this permanent has no `kind` counters on it" (CR 702 counters; mana_bloom's upkeep
- `crates/engine/tests/game.rs:4019` — let attacker = game.spawn_on_battlefield(PlayerId(0), TRAMPLER.clone()); // 4/4 trample (CR 702)
- `crates/engine/tests/game.rs:5176` — let attacker = game.spawn_on_battlefield(PlayerId(0), BIG.clone()); // 4/4, no trample (CR 702)
- `crates/engine/tests/game.rs:5183` — Only 3 of the 4/4's 4 power assigned — without trample all must be assigned to blockers. (CR 702)
- `crates/engine/tests/game.rs:14035` — green Fungus Beast creature token with trample." Loaded from the real TOML pool (not a (CR 702, CR …
- `crates/engine/tests/game.rs:16460` — trample." A keyword anthem filtered to `has_counters` — a creature with a +1/+1 (CR 702)
- `crates/engine/tests/game.rs:16461` — counter on it gets trample, a bare creature you control doesn't. (CR 702, CR 108.3, CR 122)
- `crates/engine/tests/game.rs:16482` — its last counter loses trample the same recompute. (CR 702, CR 122)
- `crates/engine/tests/game.rs:35512` — and indestructible until end of turn." (CR 702, until-end-of-turn boost.) Cast for X=3
- `crates/engine/tests/game.rs:51436` — Primordial Hydra (soc): "This creature has trample as long as it has ten or more +1/+1 (CR 702)
- `crates/engine/tests/game.rs:51438` — (CR 702 counters), not an unconditional base keyword.
- `crates/engine/tests/game.rs:71646` — creatures you control gain trample and get +X/+X until end of turn, where X is the (CR 702, CR 108.…

### CR 702.2b

- `crates/engine/tests/game.rs:99543` — (CR 702.15) still gains life off the original amount, and deathtouch (CR 702.2b) still

### CR 702.2c

- `crates/engine/tests/game.rs:15189` — "2 damage from a deathtouch source is lethal to a 10-toughness blocker (CR 702.2c)",

### CR 702.3

- `crates/engine/src/types/card.rs:228` — Can't attack (CR 702.3).
- `crates/engine/tests/game.rs:18265` — A creature with Defender can't attack (CR 702.3) — `declare_attackers` rejects it, so

### CR 702.3c

- `crates/engine/tests/game.rs:89039` — "Defender" (CR 702.3c): Plumeveil, Wall of Denial, and Wall of Omens can't attack.

### CR 702.4

- `crates/engine/src/types/card.rs:224` — Deals combat damage in both the first-strike and the normal batch (CR 702.4).

### CR 702.4b

- `crates/engine/tests/game.rs:95898` — ── Global combat-static anthems (CR 508.1a, CR 702.4b — Avatar of Slaughter) ────────

### CR 702.6e

- `crates/engine/src/cast.rs:2087` — turn, a `−N` needs loyalty ≥ N); equip's sorcery-speed timing (CR 702.6e); a tap cost
- `crates/engine/src/cast.rs:2178` — Equip (CR 702.6e) is sorcery-speed. (Its creature-you-control target is a chosen
- `crates/engine/src/cast.rs:2332` — Equip targets a creature you control (CR 702.6e; its timing is gated above).
- `crates/engine/src/types/effect/shared.rs:691` — Equip targets "target creature you control" (CR 702.6e). The activation gate
- `crates/engine/tests/game.rs:18301` — Equip is sorcery-speed (CR 702.6e): it can't be activated in combat, so it must not
- `crates/engine/tests/game.rs:30437` — Equip targets "target creature you control" (CR 702.6e) — the activation gate already rejects

### CR 702.7

- `crates/engine/src/combat.rs:875` — but assigns no damage to it (CR 510.1a). Its *blockers* still deal theirs below. (CR 702.7, CR 506)
- `crates/engine/tests/game.rs:3827` — let attacker = game.spawn_on_battlefield(PlayerId(0), FIRST_STRIKER.clone()); // 2/2 first strike (…
- `crates/engine/tests/game.rs:3897` — let attacker = game.spawn_on_battlefield(PlayerId(0), FIRST_STRIKER.clone()); // 2/2 first strike (…
- `crates/engine/tests/game.rs:3918` — let attacker = game.spawn_on_battlefield(PlayerId(0), FIRST_STRIKER.clone()); // 2/2 first strike (…
- `crates/engine/tests/game.rs:3956` — let a1 = game.spawn_on_battlefield(PlayerId(0), FIRST_STRIKER.clone()); // 2/2 first strike (CR 702…
- `crates/engine/tests/game.rs:9846` — Tajic, Legion's Edge: "{R}{W}: Tajic gains first strike until end of turn." (CR 702.7, CR 500)
- `crates/engine/tests/game.rs:9858` — ability_index: 2, // {R}{W}: gains first strike (CR 702.7) — after Mentor (0) and the prevention st…
- `crates/engine/tests/game.rs:31123` — Angelic Destiny: +4/+4, flying, first strike; when enchanted creature dies, return this (CR 702.7)

### CR 702.8

- `crates/engine/src/query.rs:66` — step) still has no affordance; add "hold priority" if it's ever wanted. (CR 702.8, CR 117, CR 301.5)
- `crates/engine/src/resolution/misc.rs:58` — Alchemist's Refuge: "You may cast spells this turn as though they had flash." (CR 702.8, CR 601, CR…
- `crates/engine/src/resolution/misc.rs:60` — rather than a continuous "as though they had flash" static — behaviorally identical (CR 702.8)
- `crates/engine/src/types/card.rs:236` — May be cast any time you could cast an instant (CR 702.8).
- `crates/engine/tests/game.rs:14410` — ability_index: 0, // {G}{U}, {T}: grant flash permission this turn (CR 702.8, CR 500)
- `crates/engine/tests/game.rs:30187` — own bear; P1 responds with a flash control-changing Aura on the same bear, which resolves (CR 702.8…

### CR 702.8a

- `crates/engine/src/query.rs:60` — CR 702.8a) counts too: that reaction window is the whole point of the stack, and the
- `crates/engine/src/types/card.rs:1020` — spell with flash (CR 702.8a). The single timing predicate shared by the cast gate
- `crates/engine/tests/game.rs:16655` — [`TEST_ENCHANTMENT`] with Flash (CR 702.8a), so a non-active player can cast it the moment
- `crates/engine/tests/game.rs:89000` — Plumeveil: "Flash" — a non-active player holding priority can cast it (CR 702.8a).

### CR 702.9

- `crates/engine/src/types/card.rs:220` — Can block creatures with flying (CR 702.9).

### CR 702.9c

- `crates/engine/src/combat.rs:12` — that's attacking `player`; a flyer can only be blocked by a flyer/reacher (CR 702.9c);

### CR 702.10

- `crates/engine/src/types/card.rs:230` — Can't be blocked this turn/permanently (a fixed subset of CR 702.10's "unblockable" —
- `crates/engine/tests/game.rs:4638` — this turn." (CR 702.10, CR 201.2)
- `crates/engine/tests/game.rs:7268` — turn." (CR 509.1b, CR 702.10)
- `crates/engine/tests/game.rs:16539` — creature you control doesn't, and an opponent's token doesn't (controller-scoped). (CR 702.10)
- `crates/engine/tests/game.rs:97395` — Swiftfoot Boots ({2}) — "Equipped creature has hexproof and haste. ... Equip {1}" (CR 702.11, CR 70…

### CR 702.10b

- `crates/engine/src/combat.rs:74` — Unblockable (Rogue's Passage): no creature may block it at all (CR 702.10b).
- `crates/engine/tests/game.rs:43841` — same turn (CR 702.10b haste + slice-1's combat gate, CR 508.1a — attackers are the active
- `crates/engine/tests/game.rs:44023` — controller the same turn (CR 702.10b haste + slice-1's combat gate, CR 508.1a — attackers are

### CR 702.11

- `crates/engine/src/query.rs:1137` — chosen — so shroud/hexproof/protection (which only restrict *targeting*) don't filter (CR 702.11, C…
- `crates/engine/src/query.rs:1145` — own shroud/hexproof/protection (CR 702.11/702.16b/702.18) doesn't shield its
- `crates/engine/src/query.rs:1183` — Hexproof (CR 702.11): can't be targeted by an opponent.
- `crates/engine/src/types/card.rs:245` — Can't be the target of spells or abilities *opponents* control (CR 702.11). Its own
- `crates/engine/tests/game.rs:6497` — A test-only 2/2 with intrinsic hexproof (CR 702.11) — can't be the target of spells/
- `crates/engine/tests/game.rs:15196` — control have hexproof." Both statics are commander_only anthems — a commander creature (CR 702.11, …
- `crates/engine/tests/game.rs:19224` — controller — unlike protection/hexproof, there is no own-permanent bypass. (CR 702.11, CR 108.3)
- `crates/engine/tests/game.rs:19246` — CR 702.11: hexproof only rejects targeting by spells/abilities an opponent controls.
- `crates/engine/tests/game.rs:35511` — Tyvar's Stand ({X}{G}) — "Target creature you control gets +X/+X and gains hexproof (CR 702.11, CR …
- `crates/engine/tests/game.rs:44534` — CR 702.11 — the X ≥ 10 doubling clause is real targeting (chosen at placement), so an opponent's
- `crates/engine/tests/game.rs:44557` — "an opponent's hexproof creature can't be a doubling target (CR 702.11)"
- `crates/engine/tests/game.rs:68567` — counter / enchanted by an Aura / equipped) feeding Silkguard's hexproof rider. ─────────── (CR 702.…
- `crates/engine/tests/game.rs:68655` — Silkguard (soc): "...modified creatures you control gain hexproof until end of turn. (CR 702.11, CR…
- `crates/engine/tests/game.rs:68698` — no counters — same mass-hexproof grant, no +1/+1 counter this time. (CR 702.11, CR 122)
- `crates/engine/tests/game.rs:68808` — "modified", so the same resolution's hexproof rider reaches it. (CR 702.11)
- `crates/engine/tests/game.rs:68855` — clause above. (CR 702.11)
- `crates/engine/tests/game.rs:73031` — ── Per-card exotic: `keyword-strip` (arcane_lighthouse), CR 702.11/702.18 ─────────────────
- `crates/engine/tests/game.rs:73132` — lose hexproof and shroud and can't have hexproof or shroud." (CR 702.11)
- `crates/engine/tests/game.rs:73151` — "can't have hexproof or shroud": a fresh grant applied the same turn is filtered right (CR 702.11, …
- `crates/engine/tests/game.rs:97351` — Heroic Intervention ({1}{G}) — "Permanents you control gain hexproof and indestructible (CR 702.11,…
- `crates/engine/tests/game.rs:97395` — Swiftfoot Boots ({2}) — "Equipped creature has hexproof and haste. ... Equip {1}" (CR 702.11, CR 70…

### CR 702.11e

- `crates/engine/src/characteristics.rs:1543` — "Lose ... and can't have" (CR 702.11e/702.18d — arcane_lighthouse): strip these off

### CR 702.12

- `crates/engine/src/apply.rs:103` — 0-or-less toughness is a death SBA even for an indestructible creature (CR 702.12, CR 704)
- `crates/engine/src/characteristics_cache.rs:124` — first creature must drop that creature's cached indestructible). (CR 702.12)
- `crates/engine/src/types/card.rs:233` — "Destroy" and lethal damage don't destroy this (CR 702.12). A 0-or-less-toughness
- `crates/engine/tests/game.rs:6819` — Indestructible is live: the destroy spell resolves, but the bear survives. (CR 702.12, CR 601)
- `crates/engine/tests/game.rs:6862` — Both are active: a destroy fails (indestructible)... (CR 702.12)
- `crates/engine/tests/game.rs:30523` — Shielded by Faith: "Enchanted creature has indestructible. Whenever a creature enters, (CR 702.12)
- `crates/engine/tests/game.rs:30525` — indestructible grant — off its original host and onto the newly entering creature. (CR 702.12)
- `crates/engine/tests/game.rs:51547` — indestructible until end of turn." #72 self-target-spec: the grant always lands on (CR 702.12, CR 6…
- `crates/engine/tests/game.rs:51569` — unrelated stack item) — drain the whole stack, not just the indestructible grant. (CR 702.12, CR 40…
- `crates/engine/tests/game.rs:73991` — Gyome, Master Chef: "{1}, Sacrifice a Food: Target creature gains indestructible until (CR 702.12, …
- `crates/engine/tests/game.rs:97351` — Heroic Intervention ({1}{G}) — "Permanents you control gain hexproof and indestructible (CR 702.11,…

### CR 702.12b

- `crates/engine/src/apply.rs:105` — (CR 702.12b).
- `crates/engine/tests/game.rs:55142` — indestructible (CR 702.12b) does not save a creature the way it would from Winds of Rath.

### CR 702.13

- `crates/engine/tests/game.rs:2757` — A black creature with intimidate (CR 702.13): blockable only by artifact creatures and/or

### CR 702.13b

- `crates/engine/src/combat.rs:103` — Intimidate (CR 702.13b): can be blocked only by artifact creatures and/or creatures
- `crates/engine/src/types/card.rs:267` — (CR 702.13b) — the color-sharing sibling of [`Fear`](Self::Fear). See [`Game::can_block`].
- `crates/engine/tests/game.rs:9979` — CR 702.13b: "A creature with intimidate can't be blocked except by artifact creatures
- `crates/engine/tests/game.rs:9999` — CR 702.13b: intimidate's artifact-creature carve-out applies regardless of color.

### CR 702.15

- `crates/engine/src/combat.rs:1312` — Lifelink (CR 702.15): if `source` has lifelink and dealt `amount` (>0) damage, its
- `crates/engine/src/resolution/damage.rs:158` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source
- `crates/engine/src/resolution/damage.rs:385` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source deals.
- `crates/engine/src/resolution/damage.rs:407` — Lifelink (CR 702.15/119.3) triggers on ANY damage the source deals.
- `crates/engine/src/resolution/damage.rs:473` — Lifelink (CR 702.15): if `source` has lifelink and dealt `amount` (>0) damage to a
- `crates/engine/src/types/card.rs:226` — Damage this deals also causes its controller to gain that much life (CR 702.15).
- `crates/engine/tests/game.rs:11029` — target player: "{T}: deals 3 damage to target player." Exercises lifelink (CR 702.15) on the
- `crates/engine/tests/game.rs:99543` — (CR 702.15) still gains life off the original amount, and deathtouch (CR 702.2b) still

### CR 702.15b

- `crates/engine/src/combat.rs:1043` — (CR 702.15b — damage dealt is damage dealt).

### CR 702.15e

- `crates/engine/src/combat.rs:1206` — tally, and (CR 702.15e — no damage dealt) no lifelink. Instead each prevented point mints
- `crates/engine/src/resolution/damage.rs:318` — Lifelink (CR 702.15e): a source dealing damage to multiple players
- `crates/engine/src/resolution/damage.rs:343` — Lifelink (CR 702.15e): a source dealing damage to multiple players
- `crates/engine/src/types/trigger.rs:250` — whoever cast the Aura. Distinct from lifelink (CR 702.15e, a static replacement bundled
- `crates/engine/tests/game.rs:11081` — life at all (CR 119.3/702.15e — lifelink triggers on ANY damage the source deals).
- `crates/engine/tests/game.rs:11172` — as `DealDamage`'s `Target::Player` arm — CR 702.15e: a source dealing damage to multiple

### CR 702.16

- `crates/engine/src/characteristics.rs:401` — The [`ProtectionScope`]s `object` currently has (CR 702.16), collected from its
- `crates/engine/src/characteristics.rs:637` — through `legal_targets_for` if one ever does. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/characteristics.rs:648` — `ObjectId`) has — its color(s), a creature type (CR 702.16), or multicolored (CR 105.4).
- `crates/engine/src/query.rs:1191` — lenient form never rejects a legal play. (CR 702.16, CR 601.2c, CR 601)
- `crates/engine/src/types/card.rs:189` — The quality a [`Keyword::ProtectionFrom`] protects against (CR 702.16): a single fixed
- `crates/engine/src/types/card.rs:191` — "protection from creatures" (Spirit Mantle, CR 702.16 grants protection from a card type)
- `crates/engine/src/types/card.rs:241` — Protection from a color, card type, or color-count quality (CR 702.16): can't be
- `crates/engine/tests/game.rs:18468` — with protection from multicolored can't be blocked by a multicolored creature; a (CR 702.16)
- `crates/engine/tests/game.rs:19122` — grants protection from blue and not from red. (CR 702.16)
- `crates/engine/tests/game.rs:19168` — protection from the chosen color must not cause the Aura to fall off. (CR 702.16, CR 303.4)
- `crates/engine/tests/game.rs:93490` — it's on the battlefield (CR 702.16, CR 702.21, CR 303.4).
- `crates/engine/tests/game.rs:93535` — choice until end of turn." (CR 702.16, CR 702.21, CR 303.4)
- `crates/engine/tests/game.rs:93586` — CR 702.16, CR 702.21)

### CR 702.16b

- `crates/engine/src/cast.rs:2264` — The ability's source's own colors (CR 702.16b) — Nin, the Pain Artist (a UR source)
- `crates/engine/src/cast.rs:2286` — re-check is where protection (CR 702.16b) actually filters it, with these same
- `crates/engine/src/characteristics.rs:632` — an [`ObjectId`]) is (CR 702.16b/e). Used at the targeting site
- `crates/engine/src/effects.rs:90` — it leaves the stack with no effect. This is where protection (CR 702.16b)
- `crates/engine/src/query.rs:829` — An ability's source colors (CR 702.16b) are the object's own — casting and activating
- `crates/engine/src/query.rs:893` — `source_colors` is the acting spell's colors, tested against protection (CR 702.16b).
- `crates/engine/src/query.rs:1145` — own shroud/hexproof/protection (CR 702.11/702.16b/702.18) doesn't shield its
- `crates/engine/src/query.rs:1187` — Protection (CR 702.16b): can't be targeted by an opponent's spell of a color it has
- `crates/engine/src/triggers.rs:4619` — The ability source's own colors (CR 702.16b — protection filters its targets); a
- `crates/engine/tests/game.rs:8313` — CR 702.16b: a source can't target a permanent that has protection from one of its colors.
- `crates/engine/tests/game.rs:18312` — Infernal Grasp is black; White Knight has protection from black (CR 702.16b). The cast
- `crates/engine/tests/game.rs:18542` — so a red source (Lightning Bolt) can't target it (CR 702.16b).

### CR 702.16c

- `crates/engine/src/combat.rs:13` — protection stops blockers of the protected-from color (CR 702.16c). Menace is a rule
- `crates/engine/tests/game.rs:18278` — White Knight has protection from black (CR 702.16c): a black creature can't block it,
- `crates/engine/tests/game.rs:18417` — CR 702.16c: a creature with protection from creatures can't be blocked by a creature.
- `crates/engine/tests/game.rs:18467` — Stonecoil Serpent: "Reach, trample, protection from multicolored." CR 702.16c: a creature

### CR 702.16d

- `crates/engine/src/characteristics.rs:658` — Whether damage from `source` to `target` is prevented by protection (CR 702.16d).
- `crates/engine/src/combat.rs:977` — Protection prevents this blocker's share (CR 702.16d); it still counts as assigned.
- `crates/engine/src/combat.rs:1054` — Protection from the source's color prevents it entirely (CR 702.16d).
- `crates/engine/src/combat.rs:1086` — it entirely, CR 702.16d), notes deathtouch, and grants lifelink. The shared path behind
- `crates/engine/src/resolution/damage.rs:122` — unless protection from the source's color prevents it (CR 702.16d).
- `crates/engine/src/resolution/damage.rs:238` — Protection from the source's color prevents that permanent's share (CR 702.16d).
- `crates/engine/tests/game.rs:4037` — lethal to each blocker" requirement even if protection then prevents it (CR 702.16d) — the
- `crates/engine/tests/game.rs:18439` — CR 702.16d: protection also prevents damage from a source of the protected quality — an
- `crates/engine/tests/game.rs:18498` — CR 702.16d: a pro-multicolored blocker takes no combat damage from a multicolored

### CR 702.16e

- `crates/engine/src/apply.rs:44` — (CR 702.16e). Reuses [`Game::attachment_host_legal`] so the check stays one seam.
- `crates/engine/src/characteristics.rs:632` — an [`ObjectId`]) is (CR 702.16b/e). Used at the targeting site
- `crates/engine/tests/game.rs:19167` — "This effect doesn't remove this Aura" (CR 702.16e): the enchanted creature gaining

### CR 702.18

- `crates/engine/src/query.rs:1145` — own shroud/hexproof/protection (CR 702.11/702.16b/702.18) doesn't shield its
- `crates/engine/src/query.rs:1151` — Shroud/hexproof/protection (CR 702.18, 702.11, 702.16b) all restrict who can target a
- `crates/engine/src/query.rs:1175` — Shroud (CR 702.18): can't be targeted by anyone, even its own controller — checked
- `crates/engine/src/types/card.rs:249` — Can't be the target of any spell or ability, even its own controller's (CR 702.18).
- `crates/engine/tests/game.rs:6428` — A test-only 2/2 with intrinsic shroud (CR 702.18) — can't be the target of any
- `crates/engine/tests/game.rs:19223` — CR 702.18: shroud rejects targeting from anyone, including the permanent's own
- `crates/engine/tests/game.rs:73031` — ── Per-card exotic: `keyword-strip` (arcane_lighthouse), CR 702.11/702.18 ─────────────────
- `crates/engine/tests/game.rs:84271` — Sterling Grove: "Other enchantments you control have shroud." (CR 702.18) — a static,
- `crates/engine/tests/game.rs:89058` — controller's included (CR 702.18).

### CR 702.18a

- `crates/engine/tests/game.rs:91728` — CR 702.18a: shroud stops even its own controller's targeted spell.

### CR 702.18d

- `crates/engine/src/characteristics.rs:1543` — "Lose ... and can't have" (CR 702.11e/702.18d — arcane_lighthouse): strip these off

### CR 702.19

- `crates/engine/src/apply.rs:563` — flashback nor escape can only be its once-per-turn permission — flashback/escape (CR 702.34, CR 702…
- `crates/engine/src/cast.rs:4` — Also: alternative costs (CR 702.34 flashback, CR 702.19 escape), impulse play (CR 118.6).
- `crates/engine/src/cast.rs:25` — Flashback (CR 702.34), escape (CR 702.19), retrace (CR 702.83), or a fixed
- `crates/engine/src/cast.rs:236` — Flashback (CR 702.34), escape (CR 702.19), or a fixed cast-from-graveyard alternative
- `crates/engine/src/cast.rs:243` — (CR 702.34, CR 702.19, CR 702.83, CR 118.9)
- `crates/engine/src/combat.rs:621` — attack at all ("if able") is simply not required — the same escape hatch goad uses. (CR 702.19, CR …
- `crates/engine/src/de.rs:435` — Escape (CR 702.19) — `[escape]` (an `[escape.cost]` sub-table plus `exile`/
- `crates/engine/src/effects.rs:192` — escape Aura's target realistically fizzles in a test, so this residual is (CR 702.19, CR 303.4, CR …
- `crates/engine/src/effects.rs:193` — untested; extend with an `spell.escape` check if one needs it. (CR 702.19, CR 601)
- `crates/engine/src/effects.rs:501` — actually cast via escape (a card with escape usually has a normal cast mode (CR 702.19, CR 601)
- `crates/engine/src/effects.rs:621` — permanent's escape spell resolves through the `CardKind::Creature`/`Aura` arms (CR 702.19, CR 303.4…
- `crates/engine/src/effects.rs:623` — path — no pool escape card is a non-permanent, so this branch is exercised only (CR 702.19)
- `crates/engine/src/playable.rs:570` — Wrong or missing delve/escape exile picks — not a mana shortfall. (CR 702.19, CR 702.66, CR 406.5)
- `crates/engine/src/types/card.rs:772` — Escape (CR 702.19): "You may cast this card from your graveyard for its escape cost. Then
- `crates/engine/src/types/card.rs:1533` — Whether this spell was cast via escape (CR 702.19): from the graveyard for its escape
- `crates/engine/src/types/mana.rs:314` — Escape's cost (CR 702.19): the escape mana cost plus how many *other* graveyard cards must
- `crates/engine/src/types/stack.rs:2051` — Whether this was an escape cast (CR 702.19 — from the graveyard for its escape cost,
- `crates/engine/tests/game.rs:1330` — ── Delve (CR 702.66) / Escape (CR 702.19) — graveyard-exile payment ─────────────────────
- `crates/engine/tests/game.rs:2959` — CR 702.19). Unset (empty) for a spell with neither.

### CR 702.19a

- `crates/engine/tests/game.rs:1441` — Four named cards, but one of them is the spell itself — escape's "other cards" (CR 702.19a)

### CR 702.19c

- `crates/engine/src/effects.rs:499` — "Escapes with N +1/+1 counters" (CR 702.19c — Woe Strider): unlike a hydra's
- `crates/engine/src/types/mana.rs:315` — be exiled to cast it, and how many +1/+1 counters the permanent escapes with (CR 702.19c —
- `crates/engine/tests/game.rs:1423` — "CR 702.19c: Woe Strider escapes with two +1/+1 counters"

### CR 702.19d

- `crates/engine/src/effects.rs:191` — (CR 702.19d), not go to the graveyard — always graveyard-bound for now. No pool
- `crates/engine/src/effects.rs:578` — it's a copy (CR 707.10a), exile if it was cast via flashback/escape (CR 702.34e/702.19d),
- `crates/engine/src/effects.rs:619` — CR 702.34e/CR 702.19d — a flashback or escape spell is exiled as it leaves the
- `crates/engine/src/resolution/misc.rs:182` — CR 702.34e/CR 702.19d: a flashback or escape spell exiles "as it leaves the stack" —
- `crates/engine/src/resolution/pause_counter_spell.rs:53` — 701.5g), or exiles instead (flashback/escape, CR 702.34e/702.19d; Quintorius's CR
- `crates/engine/src/types/card.rs:776` — (CR 601.2f); the resolved spell is exiled like flashback's (CR 702.19d — only relevant to a
- `crates/engine/src/types/card.rs:1535` — exile-on-resolve treatment for a noncreature/nonland escape spell (CR 702.19d); a creature
- `crates/engine/tests/game.rs:1412` — "a creature escape spell becomes a permanent (CR 702.19d only exiles a spell that would \
- `crates/engine/tests/game.rs:26361` — CR 702.34e/702.19d: a flashback (or escape) spell exiles as it leaves the stack even when

### CR 702.21

- `crates/engine/src/cast.rs:142` — board-derived reduction (CR 601.2f, e.g. Blasphemous Act), and ward (CR 702.21). Shared by
- `crates/engine/src/cast.rs:156` — our single-pass casting; grow the trigger form if ward-on-the-stack interaction is needed. (CR 702.…
- `crates/engine/src/cast.rs:158` — aren't folded into ward/cost-reduction — no pool modal card targets a warded permanent. (CR 702.21,…
- `crates/engine/src/characteristics.rs:378` — The generic amount an opponent must pay to target `object`, if it has Ward {N} (CR 702.21).
- `crates/engine/src/characteristics_cache.rs:223` — own static reads its self (`object`), while Flickering Ward's Aura (`object`) grants (CR 702.21, CR…
- `crates/engine/src/resolution/pause_choose.rs:30` — Flickering Ward's "As this Aura enters, choose a color": pause on a ChooseColor for (CR 702.21, CR …
- `crates/engine/src/types/card.rs:238` — Ward {N} (CR 702.21): when an opponent targets this, counter that spell/ability unless
- `crates/engine/tests/game.rs:18525` — id with the choice already made as `color`. (CR 702.21, CR 601)
- `crates/engine/tests/game.rs:18540` — Flickering Ward: "As this Aura enters, choose a color. Enchanted creature has protection (CR 702.21…
- `crates/engine/tests/game.rs:32486` — pattern), alongside its already-modeled +1/+0/lifelink/ward {2} grant. (CR 702.21)
- `crates/engine/tests/game.rs:32565` — Chains of Custody: same ETB exile clause as Sheltered by Ghosts, alongside its ward {2} (CR 702.21,…
- `crates/engine/tests/game.rs:93490` — it's on the battlefield (CR 702.16, CR 702.21, CR 303.4).
- `crates/engine/tests/game.rs:93535` — choice until end of turn." (CR 702.16, CR 702.21, CR 303.4)
- `crates/engine/tests/game.rs:93586` — CR 702.16, CR 702.21)

### CR 702.24

- `crates/engine/src/de.rs:412` — Cumulative upkeep (CR 702.24) — `[cumulative_upkeep]` (`graveyard_cards = N`);
- `crates/engine/src/triggers.rs:1858` — cumulative upkeep follows its new controller, CR 702.24's "your."
- `crates/engine/src/types/card.rs:724` — Cumulative upkeep (CR 702.24 — Jotun Grunt): "At the beginning of your upkeep, put an age
- `crates/engine/src/types/effect/shared.rs:1292` — [`CardDef::cumulative_upkeep`](super::CardDef::cumulative_upkeep)'s upkeep cost (CR 702.24):
- `crates/engine/src/types/effect/shared.rs:1296` — a single graveyard on the bottom of their owner's library") — CR 702.24's upkeep cost may be
- `crates/engine/tests/game.rs:27097` — ── Cumulative upkeep (CR 702.24, jotun_grunt) ──────────────────────────────────────

### CR 702.24a

- `crates/engine/src/pending/handlers/optional.rs:385` — declines and sacrifices `source` (CR 702.24a), the same "declining does something"
- `crates/engine/src/triggers.rs:1853` — Queue Cumulative upkeep's age-counter-then-pay-or-sacrifice choice (CR 702.24a) for every
- `crates/engine/src/triggers.rs:1856` — of the controller's upkeeps with no gate — CR 702.24a's trigger condition is bare "at the
- `crates/engine/src/triggers.rs:4438` — Cumulative upkeep (CR 702.24a): put an age counter on the permanent, then offer
- `crates/engine/src/types/effect/shared.rs:1220` — An age counter (CR 122.1, CR 702.24a — cumulative upkeep, Jotun Grunt): placed on the
- `crates/engine/src/types/stack.rs:959` — instead (CR 702.24a — "sacrifice it unless you pay its upkeep cost for each age counter on

### CR 702.26

- `crates/engine/src/core.rs:676` — Whether the permanent at `id` is phased out (CR 702.26 — treated as though it doesn't
- `crates/engine/src/pending/handlers/targets.rs:373` — phases out (CR 702.26). An empty answer is a legal "phase out nothing" (CR "any number ...
- `crates/engine/src/resolution/pause_choose.rs:103` — Guardian of Faith's ETB (CR 702.26): pause to choose any number of the *other*
- `crates/engine/src/types/card.rs:1887` — Whether this permanent is *phased out* (CR 702.26): treated as though it doesn't exist —
- `crates/engine/src/types/stack.rs:1128` — creatures they control — to phase out (CR 702.26; [`Effect::Choice(ChoiceEffect::PhaseOut)`]). Answ…
- `crates/engine/src/types/stack.rs:2131` — `object` phased out (CR 702.26 — Guardian of Faith's [`Effect::Choice(ChoiceEffect::PhaseOut)`]): s…
- `crates/engine/tests/game.rs:76729` — ── Phasing (CR 702.26) — Guardian of Faith ───────────────────────────────────────────

### CR 702.26e

- `crates/engine/src/characteristics.rs:1853` — A phased-out Aura grants nothing (CR 702.26e), mirroring `attachment_grants`.
- `crates/engine/src/characteristics.rs:1880` — disappears the instant the Aura/Equipment leaves (CR 702.26e for a phased-out one).
- `crates/engine/src/characteristics.rs:1887` — A phased-out Aura/Equipment grants nothing (CR 702.26e), mirroring `attachment_grants`.
- `crates/engine/src/combat.rs:26` — A phased-out creature can't block (CR 702.26e — treated as though it doesn't exist).
- `crates/engine/src/combat.rs:226` — A phased-out creature can't attack (CR 702.26e — treated as though it doesn't exist).
- `crates/engine/src/combat.rs:420` — A phased-out permanent is treated as though it doesn't exist (CR 702.26e).
- `crates/engine/src/query.rs:1317` — Ids of all live permanents on the battlefield. Excludes phased-out permanents (CR 702.26e:
- `crates/engine/src/query.rs:1376` — A phased-out permanent matches no filter (CR 702.26e — treated as though it doesn't
- `crates/engine/src/types/card.rs:1893` — ponytail: a plain "did/didn't phase out" flag — no "phased in tapped" bit (CR 702.26e: a
- `crates/engine/tests/game.rs:76734` — A phased-out creature is treated as though it doesn't exist (CR 702.26e) — it can't be
- `crates/engine/tests/game.rs:76762` — A phased-out creature is treated as though it doesn't exist (CR 702.26e) — a targeted

### CR 702.26f

- `crates/engine/src/priority.rs:1543` — Phase in the active player's phased-out permanents (CR 702.26f) — as a turn-based
- `crates/engine/src/types/card.rs:1889` — it phases in at the start of its controller's next turn (CR 702.26f, before untapping).
- `crates/engine/src/types/stack.rs:2135` — `object` phased in (CR 702.26f — at the start of its controller's untap step, before
- `crates/engine/tests/game.rs:76841` — CR 702.26f: phased-out permanents phase in during their controller's untap step. Not at an

### CR 702.26g

- `crates/engine/src/apply.rs:913` — Phase out `object` and everything attached to it (CR 702.26g — indirect phasing);
- `crates/engine/src/priority.rs:1546` — its indirectly-phased attachments, which phase in together (CR 702.26g).
- `crates/engine/src/types/card.rs:1891` — phased-out permanent (CR 702.26g — indirect phasing); cleared at that untap step. `false`
- `crates/engine/src/types/stack.rs:2132` — [`Permanent::phased_out`] on it and on everything attached to it (CR 702.26g), so it's
- `crates/engine/tests/game.rs:76786` — CR 702.26g: anything attached to a phased-out permanent phases out with it, indirectly, and

### CR 702.27

- `crates/engine/src/de.rs:691` — `buyback = { generic = 3 }` spells Buyback (CR 702.27) — same table shape.
- `crates/engine/src/de.rs:739` — `[cost.additional.buyback]` — Buyback (CR 702.27), the same table shape as
- `crates/engine/src/types/mana.rs:239` — Buyback (CR 702.27) — "You may pay an additional [cost] as you cast this spell. If you

### CR 702.27c

- `crates/engine/src/cast.rs:165` — `bought_back` folds [`AdditionalCost::buyback`]'s cost on top (CR 702.27c), mirroring
- `crates/engine/src/cast.rs:292` — Buyback (CR 702.27c): the caster's chosen buyback cost, paid alongside the printed
- `crates/engine/src/playable.rs:19` — Whether the caster is paying the spell's buyback cost (CR 702.27c —
- `crates/engine/src/playable.rs:613` — Buyback (CR 702.27c): only payable if the spell actually has one, mirroring kicker's
- `crates/engine/src/types/card.rs:1591` — Whether the caster paid this spell's buyback cost (CR 702.27c — [`AdditionalCost::buyback`]),
- `crates/engine/src/types/mana.rs:242` — the caster chooses whether to pay when casting (CR 702.27c), recorded on the resulting
- `crates/engine/src/types/stack.rs:2063` — Whether the caster paid the spell's buyback cost (CR 702.27c); see
- `crates/engine/tests/game.rs:2979` — Pay the spell's buyback cost (CR 702.27c). Unset (`false` — decline) for a spell with no
- `crates/engine/tests/game.rs:82331` — Declining buyback (CR 702.27c) leaves Capsize an ordinary instant: it still bounces its
- `crates/engine/tests/game.rs:82349` — Buyback {3} is an *additional* cost (CR 702.27c) on top of the printed {1}{U}{U}: a
- `crates/engine/tests/game.rs:82436` — Declining buyback (CR 702.27c) leaves Constant Mists an ordinary instant: no land

### CR 702.27d

- `crates/engine/src/effects.rs:579` — its owner's hand if it was bought back (CR 702.27d), the bottom of its owner's library if
- `crates/engine/src/effects.rs:635` — Buyback (CR 702.27d): "If you do, put this card into your hand as it resolves" (Capsize)
- `crates/engine/tests/game.rs:82459` — but also returns Constant Mists to its owner's hand instead of the graveyard (CR 702.27d).

### CR 702.27f

- `crates/engine/src/cast.rs:302` — A non-mana buyback rider (CR 702.27f — Constant Mists' "Buyback—Sacrifice a
- `crates/engine/tests/game.rs:82399` — Constant Mists: "Buyback—Sacrifice a land." (CR 702.27f — a non-mana additional cost).

### CR 702.28

- `crates/engine/src/query.rs:117` — Still offer cycling from hand even when the card isn't otherwise playable (CR 702.28, CR 402.5)
- `crates/engine/tests/game.rs:9912` — CR 702.28: "This creature can block or be blocked by only creatures with shadow."
- `crates/engine/tests/game.rs:9931` — CR 702.28: the restriction runs both directions — Shadow can't block non-Shadow either.
- `crates/engine/tests/game.rs:61645` — After P0 casts and passes, P1 holds priority with one consecutive pass. Cycling must reset (CR 702.…

### CR 702.28b

- `crates/engine/src/combat.rs:90` — Shadow (CR 702.28b/c): a Shadow creature can only block/be blocked by other Shadow
- `crates/engine/src/types/card.rs:259` — Can only block or be blocked by other Shadow creatures (CR 702.28b/c). A *paired*

### CR 702.28c

- `crates/engine/src/combat.rs:90` — Shadow (CR 702.28b/c): a Shadow creature can only block/be blocked by other Shadow
- `crates/engine/src/types/card.rs:259` — Can only block or be blocked by other Shadow creatures (CR 702.28b/c). A *paired*

### CR 702.29

- `crates/engine/src/cast.rs:1187` — Cycling is an activated ability (CR 702.29) — requires priority (CR 117.1b).
- `crates/engine/src/types/card.rs:680` — a land"), on top of `cycling`'s mana. Cycling is an activated ability (CR 702.29), so the

### CR 702.29a

- `crates/engine/src/cast.rs:1175` — Activate a hand card's Cycling ability (CR 702.29a — "{N}, Discard this card: Draw a
- `crates/engine/src/cast.rs:1209` — before the discard), the sacrifice, and "discard this card" (CR 702.29a) — before the
- `crates/engine/src/cast.rs:1222` — CR 702.29a: cycling is an activated ability — its "Draw a card" goes on the stack as a
- `crates/engine/src/de.rs:396` — Cycling {N} (CR 702.29a) — `cycling = { generic = N }`; absent for a card with none.
- `crates/engine/src/types/card.rs:675` — Cycling {N} (CR 702.29a): "{N}, Discard this card: Draw a card," activatable from the
- `crates/engine/src/types/stack.rs:111` — Activate a hand card's Cycling ability (CR 702.29a — "{N}, Discard this card: Draw a
- `crates/engine/src/types/stack.rs:3101` — Cycle `card` from hand (CR 702.29a): pay its cycling cost, discard it, draw one.
- `crates/engine/tests/game.rs:53452` — CR 702.29a: Cycling {N} — "{N}, Discard this card: Draw a card." — an ability the card
- `crates/engine/tests/game.rs:53472` — CR 702.29a: cycling is an activated ability — its draw resolves off the stack, not inline.
- `crates/engine/tests/game.rs:53515` — "cycling only functions from hand (CR 702.29a)",
- `crates/engine/tests/game.rs:53889` — Cycling is an activated ability (CR 702.29a): P0 cycles a card, putting its draw on the
- `crates/engine/tests/game.rs:61553` — CR 702.29a: cycling's draw resolves off the stack.
- `crates/engine/tests/game.rs:81823` — Cycling {B} / {R} / {G} — CR 702.29a: discard from hand, draw a card off the stack.

### CR 702.29b

- `crates/engine/src/cast.rs:1119` — (CR 702.29b, Edge of Autumn's "Cycling—Sacrifice a land").
- `crates/engine/src/cast.rs:1176` — card."): pay the mana and any [`CardDef::cycling_sacrifice`] (CR 702.29b), discard `card`
- `crates/engine/src/de.rs:399` — A sacrifice folded into the cycling cost (CR 702.29b — Edge of Autumn's
- `crates/engine/src/query.rs:690` — The permanents that may pay `card`'s [`CardDef::cycling_sacrifice`] (CR 702.29b — Edge of
- `crates/engine/src/types/card.rs:679` — A sacrifice folded into the cycling cost (CR 702.29b — Edge of Autumn's "Cycling—Sacrifice
- `crates/engine/src/types/stack.rs:113` — `sacrifice` names the permanent paying [`CardDef::cycling_sacrifice`] (CR 702.29b — Edge
- `crates/engine/tests/game.rs:61712` — CR 702.29b: Edge of Autumn's "Cycling—Sacrifice a land" carries no mana at all, only a
- `crates/engine/tests/game.rs:61750` — CR 702.29b: naming a controlled land pays the cycling cost — the land is sacrificed (the

### CR 702.29d

- `crates/engine/src/cast.rs:1263` — "{U/R}{U/R}, Discard this card: Create a Treasure token."; CR 702.29d — Valley Rannet's
- `crates/engine/src/de.rs:492` — each), one per typecycling type (CR 702.29d — Valley Rannet's mountaincycling and
- `crates/engine/src/query.rs:221` — 113.6/602.5e — one per typecycling type, CR 702.29d: Valley Rannet's mountaincycling and
- `crates/engine/src/types/card.rs:692` — typecycling grants one ability *per named type* (CR 702.29d — Valley Rannet's
- `crates/engine/src/types/stack.rs:122` — "{U/R}{U/R}, Discard this card: Create a Treasure token."; CR 702.29d — typecycling grants
- `crates/engine/tests/game.rs:85621` — CR 702.29d: typecycling grants one ability per named type, so mountaincycling (index 0) can
- `crates/engine/tests/game.rs:85665` — the mirror of mountaincycling above — CR 702.29d's one-ability-per-type again.

### CR 702.29e

- `crates/engine/src/cast.rs:1238` — CR 702.29e: "when you cycle this card" triggers off the discard above and — placed by
- `crates/engine/src/types/trigger.rs:538` — "When you cycle this card" (CR 702.29e): a triggered ability on the cycled card's *own*
- `crates/engine/tests/game.rs:53540` — before you draw.)" — CR 702.29e: the cycled trigger stacks above cycling's own draw.
- `crates/engine/tests/game.rs:82728` — ── Typecycling (CR 702.29e — Noble Templar / Shoreline Ranger / Wirewood Guardian) ─────
- `crates/engine/tests/game.rs:85466` — "When you cycle this card, it deals 6 damage to any target." — CR 702.29e: the cycled

### CR 702.31

- `crates/engine/src/de.rs:408` — Echo (CR 702.31) — `[echo]` with the same `[cost]`-table shape; absent for a card
- `crates/engine/src/resolution/pause_may.rs:187` — (this is a real triggered ability, not Echo — CR 603.3b, not CR 702.31).
- `crates/engine/src/types/card.rs:717` — Echo (CR 702.31): "At the beginning of your upkeep, if this came under your control since
- `crates/engine/src/types/stack.rs:988` — keyword (CR 702.31) — conflating the two would misname what's happening on the stack.
- `crates/engine/tests/game.rs:26880` — ── Echo (CR 702.31, karmic_guide) ───────────────────────────────────────────────────

### CR 702.31c

- `crates/engine/src/triggers.rs:1836` — Queue Echo's pay-or-sacrifice choice (CR 702.31c) for every permanent `player` controls
- `crates/engine/src/triggers.rs:4391` — Echo (CR 702.31c/d): offer one queued pay-or-sacrifice choice at a time (a
- `crates/engine/src/types/stack.rs:947` — `source`, or decline and sacrifice it (CR 702.31c/d — "sacrifice it unless you pay its

### CR 702.31d

- `crates/engine/src/pending/handlers/optional.rs:344` — decline and sacrifice it (CR 702.31d). The permanent-scoped twin of
- `crates/engine/src/triggers.rs:4391` — Echo (CR 702.31c/d): offer one queued pay-or-sacrifice choice at a time (a
- `crates/engine/src/types/stack.rs:947` — `source`, or decline and sacrifice it (CR 702.31c/d — "sacrifice it unless you pay its

### CR 702.31e

- `crates/engine/src/pending/handlers/optional.rs:378` — CR 702.31e: this upkeep is now "since your last upkeep" — echo won't ask again.
- `crates/engine/src/types/card.rs:1829` — Echo (CR 702.31e) unpaid: set when a permanent with [`CardDef::echo`] enters, cleared at

### CR 702.33

- `crates/engine/src/de.rs:690` — `kicker = { generic = 5 }` spells Kicker (CR 702.33) — the same table shape as `[cost]`.
- `crates/engine/src/de.rs:737` — `[cost.additional.kicker]` — Kicker (CR 702.33), the same table shape as `[cost]`.
- `crates/engine/src/types/mana.rs:227` — Kicker (CR 702.33) — "You may pay an additional [cost] as you cast this spell" (Rite of
- `crates/engine/tests/game.rs:73015` — A client can't opt into a kicker rider a spell doesn't print (CR 702.33 only applies to a
- `crates/engine/tests/game.rs:96461` — A client can't opt into a Multikicker rider a spell doesn't print (CR 702.33 only applies

### CR 702.33c

- `crates/engine/src/cast.rs:180` — count itself (CR 702.33c — "any number of times," each payment a full extra instance of
- `crates/engine/src/cast.rs:349` — Multikicker (CR 702.33c): "you may pay an additional [cost] any number of times as you
- `crates/engine/src/cast.rs:870` — CR 601.2c/702.33c: Multikicker's own sibling, "1 + N" rather than "exactly N" — see the
- `crates/engine/src/cast.rs:888` — CR 601.2c/702.33c: Multikicker's own sibling — Comet Storm's "choose any target,
- `crates/engine/src/core.rs:923` — How many times the spell at `id` had its Multikicker cost paid (CR 702.33c —
- `crates/engine/src/de.rs:747` — `[cost.additional.multikicker]` — Multikicker (CR 702.33c), the same table shape
- `crates/engine/src/playable.rs:32` — How many times the caster paid the spell's Multikicker cost (CR 702.33c —
- `crates/engine/src/playable.rs:235` — Multikicker (CR 601.2c/702.33c): "choose any target, then choose another
- `crates/engine/src/playable.rs:634` — Multikicker (CR 702.33c): only declarable if the spell actually has one, mirroring
- `crates/engine/src/types/card.rs:1608` — How many times the caster paid this spell's Multikicker cost (CR 702.33c —
- `crates/engine/src/types/effect/shared.rs:154` — How many times the resolving spell's Multikicker cost (CR 702.33c) was paid — Lightkeeper
- `crates/engine/src/types/mana.rs:278` — Multikicker (CR 702.33c) — "You may pay an additional [cost] any number of times as you
- `crates/engine/src/types/mana.rs:280` — multikicker. A multikicker cost is a kicker cost (CR 702.33c), but unlike [`Self::kicker`]
- `crates/engine/src/types/stack.rs:93` — ([`AdditionalCost::multikicker`] — CR 702.33c), settled before the stack for the same
- `crates/engine/src/types/stack.rs:2072` — How many times the caster paid Multikicker (CR 702.33c), 0 for a spell with no
- `crates/engine/src/types/stack.rs:3207` — Multikicker's own sibling (CR 601.2c/702.33c) — Comet Storm's "Choose any target, then
- `crates/engine/tests/game.rs:3007` — Declare a Multikicker payment count (CR 702.33c) — how many times the caster paid the
- `crates/engine/tests/game.rs:96457` — ── Multikicker (CR 702.33c) ────────────────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:96499` — read off the resolved permanent (CR 702.33c) since the ETB trigger resolves after the
- `crates/engine/tests/game.rs:96540` — Declaring Multikicker paid twice needs 1 + 2 = 3 legal targets (CR 601.2c/702.33c); with

### CR 702.33d

- `crates/engine/src/cast.rs:162` — `kicked` folds [`AdditionalCost::kicker`]'s cost on top (CR 702.33d) — `false` for a
- `crates/engine/src/cast.rs:282` — Kicker (CR 702.33d): the caster's chosen kicker cost, paid alongside the printed cost.
- `crates/engine/src/core.rs:844` — Whether the spell at `id` was cast with its kicker cost paid (CR 702.33d —
- `crates/engine/src/de.rs:1022` — kicked-branch amount (`{ if_kicked = <Amount>, else = <Amount> }` — CR 702.33d), a
- `crates/engine/src/de.rs:1183` — `{ if_kicked = 5, else = 1 }` — [`Amount::IfSpellKicked`] (CR 702.33d).
- `crates/engine/src/playable.rs:17` — Whether the caster is paying the spell's kicker cost (CR 702.33d — [`AdditionalCost::kicker`]).
- `crates/engine/src/playable.rs:607` — Kicker (CR 702.33d): only payable if the spell actually has one — a client can't opt
- `crates/engine/src/resolution/damage.rs:183` — `Amount::IfSpellKicked` (CR 702.33d) reads the resolving *spell's* own kicked
- `crates/engine/src/types/card.rs:1586` — Whether the caster paid this spell's kicker cost (CR 702.33d — [`AdditionalCost::kicker`]),
- `crates/engine/src/types/effect/shared.rs:249` — `then` if the resolving spell was kicked (CR 702.33d), else `else_` — Rite of
- `crates/engine/src/types/mana.rs:229` — caster chooses whether to pay when casting (CR 702.33d), recorded on the resulting
- `crates/engine/src/types/stack.rs:2061` — Whether the caster paid the spell's kicker cost (CR 702.33d); see [`Spell::kicked`].
- `crates/engine/src/types/stack.rs:3216` — Kicker's own sibling (CR 702.33d/702.33g) — Orim's Thunder's "If this spell was kicked, it
- `crates/engine/tests/game.rs:2972` — Pay the spell's kicker cost (CR 702.33d). Unset (`false` — decline) for a spell with no
- `crates/engine/tests/game.rs:83650` — artifact; "If this spell was kicked, draw two cards" does nothing (CR 702.33d).

### CR 702.33g

- `crates/engine/src/cast.rs:872` — CR 702.33g: a kicked-conditional clause (Orim's Thunder's "If this spell was kicked, it
- `crates/engine/src/de.rs:1340` — Thunder), forced to `(0, 0)` otherwise (CR 702.33g) — unlike the others above, not a
- `crates/engine/src/types/stack.rs:3216` — Kicker's own sibling (CR 702.33d/702.33g) — Orim's Thunder's "If this spell was kicked, it
- `crates/engine/src/types/stack.rs:3221` — otherwise the clause is forced to `(0, 0)` — CR 702.33g's "the spell is cast as if it did
- `crates/engine/tests/game.rs:96678` — permanent's mana value (CR 702.33g).

### CR 702.34

- `crates/engine/src/apply.rs:563` — flashback nor escape can only be its once-per-turn permission — flashback/escape (CR 702.34, CR 702…
- `crates/engine/src/cast.rs:4` — Also: alternative costs (CR 702.34 flashback, CR 702.19 escape), impulse play (CR 118.6).
- `crates/engine/src/cast.rs:25` — Flashback (CR 702.34), escape (CR 702.19), retrace (CR 702.83), or a fixed
- `crates/engine/src/cast.rs:236` — Flashback (CR 702.34), escape (CR 702.19), or a fixed cast-from-graveyard alternative
- `crates/engine/src/cast.rs:243` — (CR 702.34, CR 702.19, CR 702.83, CR 118.9)
- `crates/engine/src/de.rs:404` — Flashback (CR 702.34) — `[flashback]` with the same `[cost]`-table shape (may carry
- `crates/engine/src/types/card.rs:710` — Flashback (CR 702.34): "You may cast this card from your graveyard for its flashback cost.
- `crates/engine/src/types/card.rs:1528` — Whether this spell was cast with flashback (CR 702.34): from the graveyard for its
- `crates/engine/src/types/stack.rs:2048` — Whether this was a flashback cast (CR 702.34 — from the graveyard for its flashback
- `crates/engine/tests/game.rs:498` — ── Flashback (CR 702.34) ─────────────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:699` — tap_forests(&mut game, 2); // flashback {2} (CR 702.34)
- `crates/engine/tests/game.rs:741` — Same body, but no flashback — an ordinary sorcery in the graveyard can't be cast. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:801` — Printed {5}, flashback {1}: with only one mana available, casting can only succeed if the (CR 702.3…
- `crates/engine/tests/game.rs:802` — flashback cost — not the printed cost — is charged. (CR 702.34)
- `crates/engine/tests/game.rs:857` — Deep Analysis's flashback: {1}{U}, Pay 3 life. (CR 702.34, CR 118.7)
- `crates/engine/tests/game.rs:26368` — tap_forests(&mut game, 2); // flashback {2} (CR 702.34)
- `crates/engine/tests/game.rs:37265` — target for the copy." A flashback cast (from the graveyard) reanimates its own target, (CR 702.34, …
- `crates/engine/tests/game.rs:48995` — A qualifying MV-5+ instant cast via flashback — from the graveyard, not the hand. (CR 702.34, CR 40…
- `crates/engine/tests/game.rs:49274` — fires on a flashback cast from the graveyard — proves the new gate is opt-in. (CR 702.34, CR 403.5,…
- `crates/engine/tests/game.rs:81060` — Flashback {2}{G} (CR 702.34): cast from the graveyard, it still prevents that turn's combat
- `crates/engine/tests/game.rs:85224` — Flashback {G} (CR 702.34): cast from the graveyard for the flashback cost.
- `crates/engine/tests/game.rs:100211` — ── Multikicker (CR 702.34, fidelity increment #11) ─────────────────────────────────────
- `crates/engine/tests/game.rs:100326` — A client can't opt into a multikicker rider a spell doesn't print (CR 702.34 only applies

### CR 702.34a

- `crates/engine/tests/game.rs:100217` — (CR 702.34a), for a total cost of {6}.

### CR 702.34b

- `crates/engine/tests/game.rs:100275` — "This artifact enters with a charge counter on it for each time it was kicked" (CR 702.34b).

### CR 702.34e

- `crates/engine/src/effects.rs:578` — it's a copy (CR 707.10a), exile if it was cast via flashback/escape (CR 702.34e/702.19d),
- `crates/engine/src/effects.rs:619` — CR 702.34e/CR 702.19d — a flashback or escape spell is exiled as it leaves the
- `crates/engine/src/resolution/misc.rs:182` — CR 702.34e/CR 702.19d: a flashback or escape spell exiles "as it leaves the stack" —
- `crates/engine/src/resolution/pause_counter_spell.rs:53` — 701.5g), or exiles instead (flashback/escape, CR 702.34e/702.19d; Quintorius's CR
- `crates/engine/src/types/card.rs:713` — the resolved spell is exiled instead of going to the graveyard (CR 702.34e). The cost may
- `crates/engine/src/types/card.rs:1530` — (CR 702.34e). A copy of a flashback spell inherits the flag but ceases to exist first, so
- `crates/engine/tests/game.rs:734` — "CR 702.34e: a resolved flashback spell is exiled, not put into the graveyard"
- `crates/engine/tests/game.rs:26361` — CR 702.34e/702.19d: a flashback (or escape) spell exiles as it leaves the stack even when
- `crates/engine/tests/game.rs:63737` — "CR 702.34e: a resolved flashback spell is exiled, not put into the graveyard"
- `crates/engine/tests/game.rs:81097` — "CR 702.34e: a resolved flashback spell is exiled, not put into the graveyard"
- `crates/engine/tests/game.rs:85235` — "CR 702.34e: a resolved flashback spell is exiled, not put into the graveyard"

### CR 702.36

- `crates/engine/tests/game.rs:2739` — A creature with fear (CR 702.36): can be blocked only by artifact and/or black creatures.

### CR 702.36b

- `crates/engine/src/combat.rs:96` — Fear (CR 702.36b): can be blocked only by artifact creatures and/or black creatures.
- `crates/engine/src/types/card.rs:263` — Can be blocked only by artifact creatures and/or black creatures (CR 702.36b). See
- `crates/engine/tests/game.rs:9945` — CR 702.36b: "A creature with fear can't be blocked except by artifact creatures and/or
- `crates/engine/tests/game.rs:9965` — CR 702.36b: fear's artifact-creature carve-out applies regardless of color.

### CR 702.37

- `crates/engine/src/de.rs:424` — Morph (CR 702.37) — `[morph]` with the same `[cost]`-table shape as `[bestow]` (the
- `crates/engine/src/types/card.rs:750` — Morph (CR 702.37 — Willbender): "You may cast this card face down as a 2/2 creature for
- `crates/engine/src/types/card.rs:1922` — 701.34) and the morph family (CR 702.37) — a morph card (Willbender, Chromeshell Crab) adds
- `crates/engine/tests/game.rs:79190` — --- Morph (CR 702.37 — cast a card face down as a 2/2 for {3}, turn up for its morph cost) ---
- `crates/engine/tests/game.rs:94890` — ── Zoetic Cavern: a Land with morph (CR 702.37 + CR 305) ──────────────────────────

### CR 702.37b

- `crates/engine/src/apply.rs:1896` — Morph (CR 702.37b/708): a face-down creature spell enters as a face-down 2/2 —
- `crates/engine/src/cast.rs:1992` — Cast a hand card face down as a 2/2 creature for {3} (CR 702.37b — morph). Any card whose
- `crates/engine/src/cast.rs:2022` — CR 702.37b: the face-down cast cost is a flat generic {3}, not the card's printed or
- `crates/engine/src/effects.rs:153` — face-down morph spell (CR 702.37b/708.2) resolves as a plain 2/2 colorless creature
- `crates/engine/src/priority.rs:1349` — A face-down morph cast pays a flat generic {3} (CR 702.37b).
- `crates/engine/src/query.rs:298` — Whether `card` may be offered a face-down morph cast (CR 702.37b): priority holder, a hand
- `crates/engine/src/types/card.rs:753` — generic {3} (CR 702.37b — [`Intent::CastFaceDown`]), and this cost is what turns the
- `crates/engine/src/types/card.rs:1626` — Whether this spell was cast face down (CR 702.37b — a morph cast, [`Intent::CastFaceDown`]):
- `crates/engine/src/types/stack.rs:190` — Cast a hand card face down as a 2/2 creature for {3} (CR 702.37b — morph). `card` is the
- `crates/engine/src/types/stack.rs:2078` — Whether this was a face-down morph cast (CR 702.37b — [`Intent::CastFaceDown`]); see
- `crates/engine/src/types/stack.rs:3120` — Cast `card` from hand face down as a 2/2 for {3} (CR 702.37b — morph). Offered only for a
- `crates/engine/tests/game.rs:79216` — Casting a morph card face down (CR 702.37b) pays the generic {3} face-down cost — not the
- `crates/engine/tests/game.rs:94892` — Casting Zoetic Cavern face down (CR 702.37b) pays the flat generic {3} face-down cost — not
- `crates/engine/tests/game.rs:94895` — (Land). Casting face down is casting a spell, not playing a land (CR 702.37b/601.2c), so it

### CR 702.37c

- `crates/engine/src/cast.rs:1524` — controller has priority. Pay the reveal cost — a morph card's *morph* cost (CR 702.37c) if
- `crates/engine/src/cast.rs:1528` — restriction (CR 702.37c — Zoetic Cavern's real kind is Land).
- `crates/engine/src/cast.rs:1545` — manifest's alone. A morph card (CR 702.37c) may be turned face up regardless of its
- `crates/engine/src/cast.rs:1550` — A morph card turns up for its morph cost (CR 702.37c); a manifest (no morph) pays the
- `crates/engine/src/priority.rs:1343` — Turning face up pays a morph card's morph cost (CR 702.37c), else a manifest's
- `crates/engine/src/query.rs:353` — stays a 2/2); a morph card (CR 702.37c) has no such restriction and may turn up regardless
- `crates/engine/src/query.rs:372` — manifest's alone. A morph card (CR 702.37c) may be turned face up regardless of its
- `crates/engine/src/query.rs:377` — Morph turns up for its morph cost (CR 702.37c); a manifest for its printed cost — the
- `crates/engine/src/types/card.rs:754` — resulting face-down permanent face up ([`Game::turn_face_up`], CR 702.37c) rather than the
- `crates/engine/tests/game.rs:79260` — A morph-cast face-down permanent turns face up for its **morph** cost (CR 702.37c — {1}{U},
- `crates/engine/tests/game.rs:94949` — A face-down Zoetic Cavern turns face up for its morph cost (CR 702.37c — {2}), revealing the

### CR 702.37f

- `crates/engine/src/triggers.rs:431` — Turned face up (CR 702.37f): scan the now-revealed permanent's own abilities for
- `crates/engine/src/types/trigger.rs:53` — When this permanent is turned face up (CR 702.37f — a morph/megamorph turned-face-up
- `crates/engine/tests/game.rs:79192` — A "when this is turned face up, draw a card" ability (CR 702.37f — a morph turned-face-up
- `crates/engine/tests/game.rs:79262` — (CR 702.37f).
- `crates/engine/tests/game.rs:79356` — Willbender's turned-face-up trigger (CR 702.37f): "When this creature is turned face up, change

### CR 702.37j

- `crates/engine/src/cast.rs:1552` — ponytail: a manifested *morph* card (CR 702.37j — pay either the {3}-back manifest turn

### CR 702.40a

- `crates/engine/src/resolution/copy.rs:242` — response) before this trigger resolved. A true Storm keyword (CR 702.40a)
- `crates/engine/src/triggers.rs:809` — CR 702.40a Storm's count: the game-wide tally of `spells_cast_this_turn`
- `crates/engine/src/types/effect/shared.rs:333` — CR 702.40a's storm count: "for each spell cast before it this turn" — the game-wide tally
- `crates/engine/src/types/effect/shared.rs:2047` — CR 702.40a Storm's copy count, locked in when a `Trigger::YouCastThis` ability goes on the
- `crates/engine/src/types/trigger.rs:790` — CR 702.40a's storm count: the game-wide tally of spells cast before this one this turn
- `crates/engine/tests/game.rs:62647` — CR 702.40a: long-established Storm rulings hold that countering the spell that has Storm
- `crates/engine/tests/game.rs:62649` — CR 702.40a) that copies the spell from its last-known information, independent of whether

### CR 702.42

- `crates/engine/src/cast.rs:172` — `strive_count.saturating_sub(1)` (CR 702.42 — "for each target beyond the first") — 0 for
- `crates/engine/src/cast.rs:315` — Strive (CR 601.2f/702.42): "{2}{R} more to cast for each target beyond the first" —
- `crates/engine/src/cast.rs:867` — CR 601.2c/601.2f/702.42: Strive's own sibling — Twinflame's target count is the
- `crates/engine/src/core.rs:901` — The spell at `id`'s declared Strive target count (CR 702.42 — [`AdditionalCost::strive`]),
- `crates/engine/src/de.rs:692` — `strive = { generic = 2, red = 1 }` spells Strive (CR 702.42) — same table shape, the
- `crates/engine/src/de.rs:742` — `[cost.additional.strive]` — Strive (CR 702.42), the same table shape as `[cost]`.
- `crates/engine/src/playable.rs:25` — The caster's declared Strive target count (CR 702.42 — [`AdditionalCost::strive`]); 0 for
- `crates/engine/src/playable.rs:223` — Strive (CR 601.2c/702.42): the caster commits to a target count before
- `crates/engine/src/playable.rs:624` — Strive (CR 702.42): only declarable if the spell actually has one, mirroring kicker's
- `crates/engine/src/types/card.rs:1596` — The caster's declared Strive target count (CR 702.42 — [`AdditionalCost::strive`]), 0 if
- `crates/engine/src/types/mana.rs:250` — Strive (CR 702.42) — "This spell costs [cost] more to cast for each target beyond the
- `crates/engine/src/types/stack.rs:2066` — The caster's declared Strive target count (CR 702.42), 0 for a spell with no Strive;
- `crates/engine/src/types/stack.rs:3181` — Strive's own sibling of [`Self::sacrifice_scaled`] (CR 601.2c/601.2f/702.42) — Twinflame's
- `crates/engine/tests/game.rs:2993` — Declare a Strive target count (CR 702.42). Unset (0) for a spell with no Strive, or

### CR 702.52

- `crates/engine/src/de.rs:506` — Dredge N (CR 702.52) — `dredge = N` for a dredger; absent (`None`) otherwise.
- `crates/engine/src/effects.rs:1173` — Each of these draws may be replaced by dredge (CR 702.52): `draw_with_dredge` draws one
- `crates/engine/src/pending/handlers/library.rs:659` — Answer a [`PendingChoice::ChooseDredge`] (CR 702.52). `dredger == Some(id)` replaces the draw:
- `crates/engine/src/pending/handlers/library.rs:660` — mill exactly that dredger's N off the top of `player`'s library (CR 702.52 — milled cards were
- `crates/engine/src/pending/handlers/library.rs:714` — draws — each re-checks dredge eligibility against the now-live graveyard/library (CR 702.52),
- `crates/engine/src/priority.rs:1697` — Dredge (CR 702.52): if a dredger is eligible, pause on the replacement choice
- `crates/engine/src/types/card.rs:886` — Dredge N (CR 702.52): a keyword ability that works from this card's graveyard. "If you would
- `crates/engine/src/types/stack.rs:260` — (mill its N, return it to hand, no draw); `None` declines and draws normally (CR 702.52).
- `crates/engine/src/zones.rs:133` — The dredgers `player` may use to replace a single draw (CR 702.52): each card in their own
- `crates/engine/src/zones.rs:149` — (CR 702.52 — every draw is its own event, CR 121.2). Pauses on the first draw for which
- `crates/engine/tests/game.rs:87236` — ── Increment #200 slice 1: dredge (CR 702.52) ──────────────────────────────
- `crates/engine/tests/game.rs:87310` — individual draws each get their own dredge choke (CR 702.52, #200 slice 2).
- `crates/engine/tests/game.rs:87445` — milled creature does NOT die (CR 702.52 mills from the library; #183 keys Dies off battlefield).
- `crates/engine/tests/game.rs:87706` — ── Increment #200 slice 2: multi-draw dredge sequencing (CR 702.52 / 121.2) ──
- `crates/engine/tests/game.rs:87737` — A "draw three" with an eligible dredger pauses before EACH of its three draws (CR 702.52 —

### CR 702.52a

- `crates/engine/src/types/card.rs:890` — which offers [`PendingChoice::ChooseDredge`] when the library holds at least N (CR 702.52a).
- `crates/engine/src/types/stack.rs:896` — Dredge N), offered only when the library holds at least N (CR 702.52a). Answered by
- `crates/engine/src/zones.rs:134` — graveyard carrying `dredge = Some(n)` whose N does not exceed the library size (CR 702.52a
- `crates/engine/tests/game.rs:87239` — not a trigger — no stack item. Illegal when the library holds fewer than N (CR 702.52a).
- `crates/engine/tests/game.rs:87637` — Only two cards — fewer than Dredge 3, so the option is illegal (CR 702.52a).
- `crates/engine/tests/game.rs:87739` — Six cards so the library stays ≥ 3 across all three draws (CR 702.52a keeps dredge legal).
- `crates/engine/tests/game.rs:87848` — CR 702.52a: dredge is illegal when the library holds fewer than N. In a "draw three" with a
- `crates/engine/tests/game.rs:87884` — "library dropped below N — dredge is no longer offered (CR 702.52a)"

### CR 702.57

- `crates/engine/src/cast.rs:1265` — [`CardDef::forecast`] (CR 702.57 — Skyscribing's Forecast, which *reveals* rather than
- `crates/engine/src/cast.rs:1323` — Forecast reveals rather than discards (CR 702.57) — the card stays in hand.
- `crates/engine/src/de.rs:496` — Forecast (CR 702.57, Skyscribing) — a `[forecast]` table (`[forecast.cost]` +
- `crates/engine/src/query.rs:223` — [`CardDef::forecast`] (CR 702.57 — affordable cost, plus the controller's own upkeep and
- `crates/engine/src/types/card.rs:699` — Forecast (CR 702.57 — Skyscribing's "Forecast — {2}{U}, Reveal this card from your hand:

### CR 702.57a

- `crates/engine/src/cast.rs:1294` — Forecast (CR 702.57a): activated only during the controller's own upkeep, and
- `crates/engine/src/types/card.rs:702` — during its owner's own upkeep, once each turn (CR 702.57a). Shares [`HandActivatedAbility`]'s
- `crates/engine/tests/game.rs:89375` — "Only once each turn" (CR 702.57a) — a second activation the same upkeep is illegal.

### CR 702.59

- `crates/engine/src/de.rs:416` — Recover (CR 702.59) — `[recover]` with the same `[cost]`-table shape as `[echo]`;
- `crates/engine/src/types/card.rs:733` — Recover (CR 702.59 — Grim Harvest): "When a creature is put into your graveyard from the
- `crates/engine/tests/game.rs:86756` — ── Increment 188: Recover (CR 702.59, Grim Harvest) ────────────────────────────────

### CR 702.59a

- `crates/engine/src/pending/handlers/optional.rs:455` — the graveyard to hand, or decline and exile it (CR 702.59a). The graveyard-scoped twin of
- `crates/engine/src/triggers.rs:593` — Recover (CR 702.59a): every recover-bearing card already in the dying
- `crates/engine/src/triggers.rs:4414` — Recover (CR 702.59a): offer one queued pay-or-exile choice at a time. A card
- `crates/engine/src/triggers.rs:4416` — earlier trigger from the same simultaneous batch of deaths — CR 702.59a's
- `crates/engine/src/types/stack.rs:972` — from their graveyard to hand, or decline and have it exiled instead (CR 702.59a — "When a
- `crates/engine/tests/game.rs:86886` — "declining recover exiles Grim Harvest (CR 702.59a)"
- `crates/engine/tests/game.rs:86896` — CR 702.59a's "put into your graveyard from the battlefield" reuses the #183 from-

### CR 702.59b

- `crates/engine/src/triggers.rs:595` — CR 702.59b — triggers once for this death. Queued like Echo's pay-or-
- `crates/engine/src/types/card.rs:738` — including the dying creature itself if it has recover (CR 702.59b). `[recover]` in TOML,

### CR 702.62

- `crates/engine/src/cast.rs:1366` — Suspend a hand card (CR 702.62 — Rousing Refrain): rather than cast it, pay its
- `crates/engine/src/core.rs:589` — How many time counters (CR 702.62 — suspend) the exiled card at `id` has (0 if it carries
- `crates/engine/src/de.rs:476` — Suspend N—[cost] (CR 702.62, Rousing Refrain) — a `[suspend]` table whose `cost`
- `crates/engine/src/effects.rs:721` — Move the card object `from` to exile with `counters` time counters on it (CR 702.62 —
- `crates/engine/src/lib.rs:203` — Time counters (CR 702.62 — suspend) on cards in exile, each `(exile object, count)`. Kept
- `crates/engine/src/query.rs:271` — Whether `card` may be offered as a Suspend action (CR 702.62): priority holder, in hand
- `crates/engine/src/types/card.rs:854` — Suspend N—[cost] (CR 702.62 — Rousing Refrain): "Rather than cast this card from your
- `crates/engine/src/types/card.rs:969` — Suspend N—[cost] (CR 702.62), carried by [`CardDef::suspend`]. `counters` is the N time
- `crates/engine/src/types/effect/shared.rs:1196` — A time counter (CR 702.62 — suspend, Rousing Refrain). Unlike the other kinds, a time
- `crates/engine/src/types/stack.rs:131` — Suspend a hand card (CR 702.62): pay its [`CardDef::suspend`] cost and exile it with N time
- `crates/engine/src/types/stack.rs:2481` — `count` time counters (CR 702.62 — suspend) were placed on the exiled card `card` as it
- `crates/engine/src/types/stack.rs:3106` — Suspend `card` from hand (CR 702.62): pay its suspend cost, exile it with time counters.
- `crates/engine/tests/game.rs:78460` — ── Suspend + board-dependent ritual (Rousing Refrain, CR 702.62) ─────────────────────────
- `crates/engine/tests/game.rs:78517` — Suspend 3—{1}{R} (Rousing Refrain, CR 702.62): rather than cast it, pay {1}{R} to exile the

### CR 702.62b

- `crates/engine/src/cast.rs:1368` — hand, usable any time the card could be cast (CR 702.62b — sorcery-speed for a sorcery).
- `crates/engine/src/cast.rs:1389` — CR 702.62b: a card may be suspended any time it could be cast (timing follows the card).
- `crates/engine/src/query.rs:272` — with a suspend cost the player can afford, at a time the card could be cast (CR 702.62b).

### CR 702.62c

- `crates/engine/src/cast.rs:1400` — then exile the card with its time counters (CR 702.62c).

### CR 702.62d

- `crates/engine/src/priority.rs:1602` — Suspend (CR 702.62d): at the start of its owner's upkeep, remove one time
- `crates/engine/src/types/card.rs:858` — [`Game::suspend`]); a time counter is removed at each of the owner's upkeeps (CR 702.62d),
- `crates/engine/src/types/stack.rs:2485` — One time counter was removed from the suspended card `card` (CR 702.62d — the upkeep
- `crates/engine/tests/game.rs:78557` — Suspend's tick-and-cast (Rousing Refrain, CR 702.62d/e): a time counter is removed at each of

### CR 702.62e

- `crates/engine/src/priority.rs:1604` — the owner may cast it from exile without paying its mana cost (CR 702.62e) —
- `crates/engine/src/priority.rs:1608` — last counter is removed, and the card gains haste (CR 702.62e/f). Modeled here
- `crates/engine/src/types/card.rs:860` — (CR 702.62e). `[suspend]` in TOML.
- `crates/engine/tests/game.rs:78557` — Suspend's tick-and-cast (Rousing Refrain, CR 702.62d/e): a time counter is removed at each of

### CR 702.62f

- `crates/engine/src/priority.rs:1608` — last counter is removed, and the card gains haste (CR 702.62e/f). Modeled here

### CR 702.63

- `crates/engine/src/de.rs:509` — Vanishing N (CR 702.63) — `vanishing = N` for a vanishing permanent; absent
- `crates/engine/src/types/card.rs:892` — Vanishing N (CR 702.63 — Deadwood Treefolk): "This permanent enters with N time counters
- `crates/engine/tests/game.rs:92207` — ── Vanishing (CR 702.63, deadwood_treefolk) ───────────────────────────────────────────

### CR 702.63a

- `crates/engine/src/effects.rs:528` — (CR 702.63a), placed as `perm` enters the battlefield. A `None` kind (+1/+1) is grown by any
- `crates/engine/src/types/card.rs:897` — of suspend's exile-zone time-counter store) as the permanent enters (CR 702.63a — answered
- `crates/engine/src/types/stack.rs:3471` — Vanishing N (CR 702.63a — Deadwood Treefolk) *is* an "enters with N time counters" static, so
- `crates/engine/tests/game.rs:92233` — "vanishing 3 places three time counters as it enters (CR 702.63a)"

### CR 702.63b

- `crates/engine/src/priority.rs:1657` — Vanishing (CR 702.63b — Deadwood Treefolk): at the beginning of its controller's
- `crates/engine/src/types/card.rs:899` — removes one at each of the controller's upkeeps (CR 702.63b, see `Game::advance_step`'s
- `crates/engine/tests/game.rs:92262` — "the controller's own upkeep removes exactly one time counter (CR 702.63b)"

### CR 702.63c

- `crates/engine/src/priority.rs:1660` — the last one comes off, its controller sacrifices it (CR 702.63c).
- `crates/engine/src/triggers.rs:1394` — (CR 702.63c).
- `crates/engine/src/types/card.rs:901` — controller sacrifices it" ability (CR 702.63c) so responses have a window, via the
- `crates/engine/tests/game.rs:92268` — "When the last is removed, sacrifice it." (CR 702.63c) — a real triggered ability, so it
- `crates/engine/tests/game.rs:92299` — "removing the last time counter sacrifices the vanishing permanent (CR 702.63c)"

### CR 702.66

- `crates/engine/src/cast.rs:160` — CR 702.66) — 0 for a non-delve spell, or when pricing without a chosen count (e.g. the
- `crates/engine/src/cast.rs:278` — Delve (CR 702.66): each exiled graveyard card pays for {1} of the generic cost.
- `crates/engine/src/de.rs:432` — Delve (CR 702.66) — `delve = true`; absent (`false`) for a card without delve.
- `crates/engine/src/playable.rs:438` — Delve (CR 702.66) can reduce generic — list as affordable when some exile count
- `crates/engine/src/playable.rs:570` — Wrong or missing delve/escape exile picks — not a mana shortfall. (CR 702.19, CR 702.66, CR 406.5)
- `crates/engine/src/query.rs:762` — Delve may exile any number from the GY (CR 702.66); cap only at GY size so the
- `crates/engine/src/query.rs:763` — client prompt never auto-skips when listability needed a positive delve count. (CR 702.66)
- `crates/engine/src/types/card.rs:766` — Delve (CR 702.66): "Each card you exile from your graveyard while casting this spell pays
- `crates/engine/src/types/stack.rs:53` — `discard_cost`'s shape: delve (CR 702.66) exiles a player-chosen number of the
- `crates/engine/src/types/stack.rs:480` — Graveyard cards paying delve or escape exile (CR 702.66 / 702.19); empty when neither.
- `crates/engine/tests/game.rs:1330` — ── Delve (CR 702.66) / Escape (CR 702.19) — graveyard-exile payment ─────────────────────
- `crates/engine/tests/game.rs:2958` — Name the graveyard cards paying a delve or escape graveyard-exile cost (CR 702.66 /
- `crates/engine/tests/game.rs:61494` — {7}{U} Treasure Cruise — with only {U} floating it needs delve 7. (CR 702.66)

### CR 702.72a

- `crates/engine/src/combat.rs:78` — Skulk (CR 702.72a): can't be blocked by creatures with greater power.
- `crates/engine/src/types/card.rs:257` — Can't be blocked by creatures with greater power (CR 702.72a). See [`Game::can_block`].
- `crates/engine/tests/game.rs:9893` — CR 702.72a: "This creature can't be blocked by creatures with greater power."

### CR 702.74

- `crates/engine/src/de.rs:428` — Evoke (CR 702.74) — `[evoke]` with the same `[cost]`-table shape as `[echo]`;
- `crates/engine/src/types/card.rs:758` — Evoke (CR 702.74 — Mulldrifter): "You may cast this spell for its evoke cost. If you do,
- `crates/engine/tests/game.rs:27489` — ── Evoke (CR 702.74, mulldrifter) ───────────────────────────────────────────────────

### CR 702.74a

- `crates/engine/src/apply.rs:1904` — Evoke (CR 702.74a): an evoked spell's resulting permanent is sacrificed the
- `crates/engine/src/cast.rs:168` — `evoked` charges [`CardDef::evoke`] instead of the printed cost (CR 702.74a) — `false` for
- `crates/engine/src/cast.rs:250` — Evoke (CR 702.74a): the caster's declared evoke cost replaces the printed cost —
- `crates/engine/src/playable.rs:22` — Whether the caster is casting the spell for its evoke cost (CR 702.74a —
- `crates/engine/src/playable.rs:619` — Evoke (CR 702.74a): only declarable if the card actually has an evoke cost — a client
- `crates/engine/src/triggers.rs:410` — Evoke (CR 702.74a): queued *before* the permanent's own `Etb` trigger
- `crates/engine/src/triggers.rs:1393` — enters" (CR 702.74a) and vanishing's "when the last time counter is removed, sacrifice it"
- `crates/engine/src/types/card.rs:761` — declares it (CR 702.74a — [`Spell::evoked`]); the resulting permanent is sacrificed the
- `crates/engine/src/types/card.rs:763` — an ETB payoff (Mulldrifter's draw two) still resolves first (CR 702.74a, CR 603.3b — see
- `crates/engine/src/types/card.rs:1637` — Whether this spell was cast for its evoke cost (CR 702.74a — [`CardDef::evoke`]). Copied
- `crates/engine/src/types/card.rs:1942` — Whether this permanent was cast for its evoke cost (CR 702.74a — [`CardDef::evoke`]): it is
- `crates/engine/src/types/stack.rs:72` — Whether the caster is casting the spell for its evoke cost (CR 702.74a —
- `crates/engine/src/types/stack.rs:2084` — Whether this was an evoke cast (CR 702.74a — for [`CardDef::evoke`]); see
- `crates/engine/tests/game.rs:2986` — Cast for the spell's evoke cost (CR 702.74a) instead of its printed cost. Unset (`false`
- `crates/engine/tests/game.rs:27543` — resolve_top_of_stack(&mut game); // the evoke self-sacrifice (CR 702.74a) resolves second
- `crates/engine/tests/game.rs:91491` — hand." + "Evoke {1}{U}{U}" (CR 702.74a — sacrificed the instant it enters, under its own
- `crates/engine/tests/game.rs:91511` — resolve_top_of_stack(&mut game); // evoke's self-sacrifice (CR 702.74a)

### CR 702.76a

- `crates/engine/src/triggers.rs:2705` — Queue enters-the-battlefield triggers (CR 702.76a constellation / CR 704.5n-kin
- `crates/engine/src/types/trigger.rs:391` — (CR 702.76a: "whenever an enchantment you control enters" — [`EnterController::You`],

### CR 702.82

- `crates/engine/src/de.rs:455` — Devour N (CR 702.82) — `devour = N`; absent for a card without devour.
- `crates/engine/src/effects.rs:410` — Devour N (CR 702.82): pause as the creature enters so its controller may
- `crates/engine/src/types/card.rs:810` — Devour N (CR 702.82): "As this creature enters, you may sacrifice any number of creatures.
- `crates/engine/src/types/stack.rs:1623` — other creatures they control) as `source` enters (CR 702.82 — "you may sacrifice any
- `crates/engine/tests/game.rs:75337` — ── #134 Devour N (CR 702.82): "As this creature enters, you may sacrifice any number of

### CR 702.83

- `crates/engine/src/cast.rs:25` — Flashback (CR 702.34), escape (CR 702.19), retrace (CR 702.83), or a fixed
- `crates/engine/src/cast.rs:243` — (CR 702.34, CR 702.19, CR 702.83, CR 118.9)
- `crates/engine/src/de.rs:439` — Retrace (CR 702.83) — `retrace = true`; absent (`false`) for a card without
- `crates/engine/src/types/card.rs:780` — Retrace (CR 702.83): "You may cast this card from your graveyard by discarding a land
- `crates/engine/tests/game.rs:1047` — ── Retrace (CR 702.83) ────────────────────────────────────────────────────────────────

### CR 702.83a

- `crates/engine/src/cast.rs:68` — `zone` gates retrace's discard-a-land rider (CR 702.83a): it's an "in addition to paying
- `crates/engine/src/cast.rs:90` — Retrace (CR 702.83a): can't pay without a land card in hand to discard (CR 601.2f —
- `crates/engine/src/cast.rs:239` — Retrace (CR 702.83a) has no alternative cost — it pays the printed `[cost]` as normal,
- `crates/engine/src/de.rs:730` — Retrace's "discard a land card" (CR 702.83a) — `discard_land = true`.
- `crates/engine/src/playable.rs:520` — Retrace's discard-a-land rider (CR 702.83a) shares the same discard-cost slot as the
- `crates/engine/src/types/card.rs:784` — resolved spell is put into the graveyard as usual — no exile rider (CR 702.83a), so it's
- `crates/engine/src/types/mana.rs:186` — Retrace's "discard a land card" (CR 702.83a) — an additional cost of discarding exactly
- `crates/engine/tests/game.rs:1080` — "CR 702.83a: a retrace spell goes to the graveyard as normal, not exiled"
- `crates/engine/tests/game.rs:1234` — "CR 702.83a: a retrace spell goes to the graveyard as normal, not exiled"
- `crates/engine/tests/game.rs:74519` — trigger above the spell. Cast from hand, retrace's discard-a-land rider (CR 702.83a) does

### CR 702.85

- `crates/engine/src/de.rs:447` — Cascade (CR 702.85) — `cascade = true`; absent (`false`) for a card without
- `crates/engine/src/effects.rs:863` — Cascade (CR 702.85): reveal-until a cheaper nonland, may cast it free, bottom the
- `crates/engine/src/pending/handlers/dig.rs:1292` — Resolve [`Effect::Dig(DigEffect::Cascade)`] (CR 702.85). Reveal cards from the top of `controller`'s
- `crates/engine/src/types/card.rs:795` — Cascade (CR 702.85): "When you cast this spell, exile cards from the top of your library
- `crates/engine/tests/game.rs:74298` — ── Cascade (CR 702.85) ───────────────────────────────────────────────────────────────
- `crates/engine/tests/game.rs:74302` — Cascade (CR 702.85): "When you cast this spell, exile cards from the top of your library

### CR 702.85b

- `crates/engine/src/triggers.rs:834` — spell. `mana_value` is baked in now as last-known information (CR 702.85b).

### CR 702.85c

- `crates/engine/src/pending/handlers/dig.rs:1296` — the library runs out (CR 702.85c "as many as possible"). A hit raises a may-cast-it-free

### CR 702.85e

- `crates/engine/src/triggers.rs:831` — Cascade (CR 702.85e): a rules-keyword "when you cast this spell" trigger, not
- `crates/engine/src/types/card.rs:800` — cascading spell when it's cast (CR 702.85e), wired at the cast choke like `retrace`/`echo`.
- `crates/engine/tests/game.rs:74322` — The cascade triggered ability sits above the still-unresolved sorcery (CR 702.85e).

### CR 702.86a

- `crates/engine/tests/game.rs:31738` — Eldrazi Conscription grants annihilator 2 (CR 702.86a): "Whenever [enchanted creature]
- `crates/engine/tests/game.rs:93216` — CR 702.86a: annihilator's "defending player" is the player being attacked — when the attack

### CR 702.90

- `crates/engine/src/types/card.rs:296` — Infect (CR 702.90): this source's damage to a creature is dealt as that many -1/-1 counters
- `crates/engine/src/types/effect/shared.rs:1271` — damage (CR 702.90), toxic (CR 702.164), and "gets a poison counter" effects.
- `crates/engine/tests/game.rs:99461` — Infect (#20 slice 2, CR 702.90): Keyword::Infect reshapes the damage a source deals —

### CR 702.90b

- `crates/engine/src/resolution/damage.rs:10` — every creature-damage mint routes through, so infect (CR 702.90b) reshapes all of them at
- `crates/engine/src/resolution/damage.rs:445` — counters rather than marked damage (CR 702.90b), and still satisfies "is dealt damage".
- `crates/engine/src/types/card.rs:297` — (CR 702.90b) and its damage to a player as that many poison counters (CR 702.90c). The
- `crates/engine/tests/game.rs:99490` — CR 702.90b: damage an infect source deals to a creature is dealt in the form of that many

### CR 702.90c

- `crates/engine/src/resolution/damage.rs:49` — in the form of that many poison counters (CR 702.90c) instead of as life loss.
- `crates/engine/src/types/card.rs:297` — (CR 702.90b) and its damage to a player as that many poison counters (CR 702.90c). The
- `crates/engine/tests/game.rs:99472` — CR 702.90c: damage an infect source deals to a player is dealt in the form of that many
- `crates/engine/tests/game.rs:100664` — Infect changes the *form* of the damage (CR 702.90c) and toxic adds counters *on top* of

### CR 702.103

- `crates/engine/src/cast.rs:1906` — Cast a card for its bestow cost (CR 702.103 — Eidolon of Countless Battles). `object` is the
- `crates/engine/src/characteristics.rs:1226` — Whether `id` is a bestowed permanent (CR 702.103) currently attached to a host: while so it
- `crates/engine/src/de.rs:420` — Bestow (CR 702.103) — `[bestow]` with the same `[cost]`-table shape as `[echo]`;
- `crates/engine/src/types/card.rs:741` — Bestow (CR 702.103 — Eidolon of Countless Battles): a permanent (enchantment) creature card
- `crates/engine/src/types/card.rs:1620` — Whether this spell was cast via bestow (CR 702.103 — Eidolon of Countless Battles): for its
- `crates/engine/src/types/card.rs:1907` — Whether this permanent was cast via bestow (CR 702.103 — Eidolon of Countless Battles) and
- `crates/engine/src/types/stack.rs:180` — Cast a permanent (enchantment) creature card for its bestow cost (CR 702.103 — Eidolon of
- `crates/engine/src/types/stack.rs:2075` — Whether this was a bestow cast (CR 702.103 — for [`CardDef::bestow`], as an Aura spell);
- `crates/engine/tests/game.rs:17287` — Bestow {2}{W}{W} (CR 702.103): cast as an Aura spell with enchant creature. While attached

### CR 702.103c

- `crates/engine/src/cast.rs:1908` — the stack as a bestowed Aura spell with "enchant creature" (CR 702.103c), targeting `target`.
- `crates/engine/src/cast.rs:1935` — Bestow grants "enchant creature" (CR 702.103c): the cast target must be a creature. Reuse
- `crates/engine/src/types/stack.rs:183` — creature", CR 702.103c). Casting pays the bestow cost and puts the card on the stack as a
- `crates/engine/tests/game.rs:17386` — Bestow gives the card enchant creature (CR 702.103c): its cast target must be a creature.

### CR 702.103d

- `crates/engine/src/apply.rs:1893` — Bestow (CR 702.103d): a bestowed spell enters as a dual-nature Aura/creature — it
- `crates/engine/src/characteristics.rs:1284` — CR 702.103d/e: while attached, a bestowed permanent is an Aura enchantment — it has the
- `crates/engine/src/effects.rs:150` — A bestowed spell (CR 702.103d) resolves as an Aura — it enters attached to its target

### CR 702.103e

- `crates/engine/src/apply.rs:99` — CR 702.103e: a bestowed permanent that's attached is an Aura, not a creature —
- `crates/engine/src/cast.rs:1910` — creature (CR 702.103e). Mirrors [`Game::cast_adventure`], but pays the bestow cost and marks
- `crates/engine/src/characteristics.rs:1227` — is an Aura enchantment and **not** a creature (CR 702.103e). An unattached bestowed
- `crates/engine/src/characteristics.rs:1244` — CR 702.103e: a bestowed permanent that's attached is an Aura enchantment, not a creature.
- `crates/engine/src/characteristics.rs:1284` — CR 702.103d/e: while attached, a bestowed permanent is an Aura enchantment — it has the
- `crates/engine/src/types/card.rs:744` — while attached it's an Aura, not a creature (CR 702.103e), and becomes a creature again when
- `crates/engine/src/types/card.rs:1909` — `Some`), it's an Aura enchantment and **not** a creature (CR 702.103e) — the "attached?"
- `crates/engine/tests/game.rs:17288` — it's an Aura enchantment, not a creature (CR 702.103e), and the buff pumps the enchanted
- `crates/engine/tests/game.rs:17311` — "while attached it's an Aura, not a creature (CR 702.103e)"

### CR 702.103i

- `crates/engine/src/characteristics.rs:1228` — permanent is a creature again (CR 702.103i), so this reads the live "attached?" gate, not the
- `crates/engine/src/effects.rs:152` — `kind` stays `Creature` for when it later stops being attached, CR 702.103i). A
- `crates/engine/src/types/card.rs:745` — it stops being attached (CR 702.103i — a state-based action). The bestowed status is runtime
- `crates/engine/src/types/card.rs:1911` — becomes a creature again (CR 702.103i). Set as it enters from the casting [`Spell::bestowed`];
- `crates/engine/tests/game.rs:17324` — CR 702.103i: when a bestowed permanent stops being attached it becomes a creature again
- `crates/engine/tests/game.rs:17365` — "a bestowed permanent stays on the battlefield when its host leaves (CR 702.103i), not \
- `crates/engine/tests/game.rs:17375` — "unattached, it's a creature again (CR 702.103i)"

### CR 702.108

- `crates/engine/src/cast.rs:176` — count itself (CR 702.108 — each payment is a full extra instance of the cost, unlike
- `crates/engine/src/core.rs:911` — How many times the spell at `id` had its Replicate cost paid (CR 702.108 —
- `crates/engine/src/de.rs:693` — per-extra-target cost. `replicate = { generic = 2 }` spells Replicate (CR 702.108) — same
- `crates/engine/src/de.rs:744` — `[cost.additional.replicate]` — Replicate (CR 702.108), the same table shape as
- `crates/engine/src/playable.rs:28` — How many times the caster paid the spell's Replicate cost (CR 702.108 —
- `crates/engine/src/playable.rs:629` — Replicate (CR 702.108): only declarable if the spell actually has one, mirroring
- `crates/engine/src/triggers.rs:3608` — Queue prowess triggers (CR 702.108): `spell_controller` just cast `def` (aimed at
- `crates/engine/src/types/card.rs:253` — of turn (CR 702.108). The whole ability *is* the keyword (CR 702.108a) — see
- `crates/engine/src/types/card.rs:1602` — How many times the caster paid this spell's Replicate cost (CR 702.108 —
- `crates/engine/src/types/mana.rs:264` — Replicate (CR 702.108) — "You may pay [cost] any number of times as you cast this spell.
- `crates/engine/src/types/stack.rs:86` — — CR 702.108), settled before the stack for the same reason as `strive_count` above
- `crates/engine/src/types/stack.rs:2069` — How many times the caster paid Replicate (CR 702.108), 0 for a spell with no Replicate;
- `crates/engine/tests/game.rs:3000` — Declare a Replicate payment count (CR 702.108) — how many times the caster paid the
- `crates/engine/tests/game.rs:23749` — ── Prowess (CR 702.108) ─────────────────────────────────────────────────────────────

### CR 702.108a

- `crates/engine/src/triggers.rs:3614` — `[[abilities]]` — the keyword *is* the whole ability (CR 702.108a), so there's nothing
- `crates/engine/src/types/card.rs:253` — of turn (CR 702.108). The whole ability *is* the keyword (CR 702.108a) — see
- `crates/engine/tests/game.rs:23752` — clauses — prowess is the whole ability (CR 702.108a), so no `[[abilities]]` is needed.

### CR 702.108b

- `crates/engine/src/cast.rs:333` — Replicate (CR 702.108b): "You may pay [cost] any number of times as you cast this
- `crates/engine/src/resolution/copy.rs:104` — Loyalty's Replicate copies, CR 702.108b/707.10a).
- `crates/engine/src/triggers.rs:3611` — of turn, provided the cast spell is noncreature (CR 702.108b — the pump is a real
- `crates/engine/src/types/card.rs:1606` — [`Game::mint_spell_copies`] (CR 702.108b).
- `crates/engine/src/types/mana.rs:272` — (CR 702.108b — reusing [`Game::mint_spell_copies`], the same rider [`Effect::Copy(CopyEffect::ThisS…
- `crates/engine/src/types/stack.rs:90` — that many copies (CR 702.108b).
- `crates/engine/tests/game.rs:32287` — Paying replicate once mints one copy at cast (CR 702.108b), which pauses on its own CR

### CR 702.111

- `crates/engine/src/types/card.rs:222` — Can't be blocked except by two or more creatures (CR 702.111).

### CR 702.114

- `crates/engine/src/triggers.rs:3659` — Queue Myriad (CR 702.114): `object` (an attacker carrying [`Keyword::Myriad`]) just
- `crates/engine/src/types/card.rs:287` — Myriad (CR 702.114): "Whenever this creature attacks, for each opponent other than the

### CR 702.114a

- `crates/engine/src/de.rs:358` — Devoid (CR 702.114a) — `devoid = true`; absent (`false`) for every ordinary card.
- `crates/engine/src/resolution/tokens.rs:318` — Myriad's payload (CR 702.114a): for each opponent other than the defending player,
- `crates/engine/src/triggers.rs:3660` — attacked `defender`. The whole ability *is* the keyword (CR 702.114a) — like Prowess,
- `crates/engine/src/types/card.rs:290` — *is* the keyword (CR 702.114a) — like Prowess, synthesized from the keyword at attack
- `crates/engine/src/types/card.rs:596` — Devoid (CR 702.114a): the card is colorless despite any colored mana-cost pips —
- `crates/engine/tests/game.rs:13314` — Smothering Abomination: "Devoid (This card has no color.)" (CR 702.114a) — colorless

### CR 702.121

- `crates/engine/tests/game.rs:9809` — Tajic, Legion's Edge — Mentor (CR 702.121): "Whenever this creature attacks, put a

### CR 702.121a

- `crates/engine/src/query.rs:1560` — Strictly lesser power than the filter's own source (Mentor, CR 702.121a). No-op
- `crates/engine/src/types/filter.rs:747` — Power strictly less than the filter's own source permanent's power (Mentor, CR 702.121a

### CR 702.130

- `crates/engine/src/apply.rs:232` — the flag (CR 702.130's "for the rest of the game").
- `crates/engine/src/types/card.rs:2151` — CR 702.130's "for the rest of the game." Feeds [`Condition::YouHaveCitysBlessing`].
- `crates/engine/tests/game.rs:73411` — (CR 702.130: "for the rest of the game"), so the anthem stays on.

### CR 702.131

- `crates/engine/src/types/card.rs:2149` — Whether this player has the city's blessing (CR 702.131 ascend). Sticky: set once by a
- `crates/engine/src/types/effect/shared.rs:1684` — "as long as you have the city's blessing" (CR 702.131, Ascend — tendershoot_dryad's
- `crates/engine/src/types/stack.rs:2972` — `player` got the city's blessing (CR 702.131 Ascend) — a state-based action fired once
- `crates/engine/tests/game.rs:73331` — ── Ascend / the city's blessing (CR 702.131) — tendershoot_dryad ──────────────────────

### CR 702.131b

- `crates/engine/src/apply.rs:229` — Ascend / the city's blessing (CR 702.131b): a living player who doesn't yet have it
- `crates/engine/src/types/effect/shared.rs:1687` — more permanents (CR 702.131b) and never cleared.
- `crates/engine/tests/game.rs:73395` — A tenth permanent tips the controller to the city's blessing (CR 702.131b); the next

### CR 702.131c

- `crates/engine/src/apply.rs:233` — ponytail: CR 702.131c says ascend is checked continuously; this checks only at each

### CR 702.135

- `crates/engine/tests/game.rs:24551` — A test-only free artifact spell — a minimal `SpellFilter::Historic` probe (CR 702.135:
- `crates/engine/tests/game.rs:24720` — card with mana value 3 or less from your graveyard to the battlefield." CR 702.135:

### CR 702.135a

- `crates/engine/src/characteristics.rs:2141` — CR 702.135a: an artifact, legendary, or Saga card is historic.
- `crates/engine/src/types/filter.rs:222` — Historic spells you cast (Teshar, Ancestor's Apostle) — CR 702.135a: an artifact,

### CR 702.137a

- `crates/engine/src/triggers.rs:807` — trigger resolves even if the spell is later countered, CR 702.137a).
- `crates/engine/src/types/trigger.rs:470` — later countered (CR 702.137a for Hydroid specifically). Fieldless: every pool consumer is
- `crates/engine/tests/game.rs:69762` — CR 702.137a: Hydroid Krasis's cast trigger is a separate object from the spell — it still

### CR 702.140

- `crates/engine/src/cast.rs:1411` — Encore a graveyard card (CR 702.140 — Angel of Indemnity): pay its [`CardDef::encore`]
- `crates/engine/src/cast.rs:1422` — ponytail: encore is a non-mana activated ability (CR 702.140), so its effect is meant to go
- `crates/engine/src/cast.rs:1435` — Encore is an activated ability (CR 702.140) — requires priority (CR 117.1b).
- `crates/engine/src/de.rs:485` — Encore [cost] (CR 702.140, Angel of Indemnity) — an `[encore]` table with the same
- `crates/engine/src/query.rs:111` — Encore (CR 702.140) — a keyword activated ability that functions from the
- `crates/engine/src/query.rs:327` — Whether `card` may be offered as an Encore action (CR 702.140): priority holder, in the
- `crates/engine/src/types/card.rs:871` — Encore [cost] (CR 702.140 — Angel of Indemnity): "[cost], Exile this card from your
- `crates/engine/src/types/stack.rs:134` — Encore a graveyard card (CR 702.140): pay its [`CardDef::encore`] mana cost and exile it
- `crates/engine/src/types/stack.rs:3108` — Encore `card` from the graveyard (CR 702.140): pay its encore mana cost, exile it, and mint
- `crates/engine/tests/game.rs:78891` — ── Encore (CR 702.140 — Angel of Indemnity) ──────────────────────────────────────────
- `crates/engine/tests/game.rs:78893` — Encore {6}{W}{W} (Angel of Indemnity, CR 702.140): "{6}{W}{W}, Exile this card from your

### CR 702.140a

- `crates/engine/src/cast.rs:1412` — mana cost and exile it from the graveyard (both halves of the cost, CR 702.140a) to create
- `crates/engine/src/cast.rs:1454` — Pay the cost (CR 702.140a): the encore mana cost (settled first, auto-tapping lands; an
- `crates/engine/tests/game.rs:78912` — The card is exiled from the graveyard (CR 702.140a — part of the cost).

### CR 702.140b

- `crates/engine/src/cast.rs:1415` — of the next end step. A sorcery-speed activated ability from the graveyard (CR 702.140b),
- `crates/engine/src/cast.rs:1450` — CR 702.140b: encore may be activated only as a sorcery.
- `crates/engine/src/query.rs:328` — owner's graveyard with an affordable encore cost, at sorcery speed (CR 702.140b — active
- `crates/engine/tests/game.rs:79005` — Encore may be activated only as a sorcery (CR 702.140b): outside a main phase (here P0's own

### CR 702.140c

- `crates/engine/src/cast.rs:1414` — opponent this turn if able (CR 702.140c), gains haste, and is sacrificed at the beginning
- `crates/engine/tests/game.rs:78951` — Each copy is forced to attack a *distinct* opponent (CR 702.140c).
- `crates/engine/tests/game.rs:78967` — Encore's "Sacrifice them at the beginning of the next end step" (CR 702.140c): the token copies

### CR 702.147

- `crates/engine/src/de.rs:451` — Demonstrate (CR 702.147) — `demonstrate = true`; absent (`false`) for a card
- `crates/engine/src/resolution/pause_choose.rs:73` — Demonstrate (CR 702.147): pause on a MayYesNo "copy it?" over the cast spell
- `crates/engine/src/types/card.rs:803` — Demonstrate (CR 702.147): "When you cast this spell, you may copy it. If you do, choose an
- `crates/engine/tests/game.rs:74543` — Demonstrate rider (CR 702.147) from any card-specific body. Its ability targets nothing, so
- `crates/engine/tests/game.rs:74578` — Demonstrate (CR 702.147): "you may copy it." A real, respondable trigger above the cast

### CR 702.147a

- `crates/engine/src/pending/handlers/optional.rs:49` — second copy (CR 702.147a "choose an opponent to also copy it") — see the
- `crates/engine/src/pending/handlers/targets.rs:224` — The chosen opponent (CR 702.147a) also gets a copy — mint the controller's own
- `crates/engine/src/resolution/resume.rs:22` — Demonstrate (CR 702.147a) second copy: `(opponent, spell)`, after the controller's
- `crates/engine/src/triggers.rs:856` — Demonstrate (CR 702.147a): another rules-keyword "when you cast this spell"
- `crates/engine/src/types/card.rs:807` — the cast spell (CR 702.147a), wired at the cast choke like `cascade`. `demonstrate = true`
- `crates/engine/tests/game.rs:2598` — Resolve a just-cast Demonstrate spell's fabricated trigger (CR 702.147a), declining the copy
- `crates/engine/tests/game.rs:74579` — spell (CR 702.147a); declining leaves just the one resolution.

### CR 702.148

- `crates/engine/src/types/card.rs:282` — Decayed (CR 702.148): can't block ([`Game::can_block`]), and "when it attacks, sacrifice
- `crates/engine/tests/game.rs:2773` — A creature with decayed (CR 702.148): can't block, sacrificed at end of combat if it attacks.

### CR 702.148b

- `crates/engine/src/combat.rs:54` — Decayed (CR 702.148b): "A creature with decayed can't block."
- `crates/engine/tests/game.rs:10130` — Decayed (CR 702.148b): "A creature with decayed can't block."

### CR 702.148c

- `crates/engine/src/combat.rs:698` — Decayed (CR 702.148c): "When it attacks, sacrifice it at the beginning of the end
- `crates/engine/src/types/card.rs:283` — it at the beginning of the end of combat step" (CR 702.148c) — a rules-defined delayed
- `crates/engine/tests/game.rs:10149` — Decayed (CR 702.148c): "When a creature with decayed attacks, sacrifice it at the

### CR 702.164

- `crates/engine/src/types/card.rs:303` — Toxic N (CR 702.164): "Players dealt combat damage by this creature also get N poison
- `crates/engine/src/types/effect/shared.rs:1271` — damage (CR 702.90), toxic (CR 702.164), and "gets a poison counter" effects.
- `crates/engine/tests/game.rs:100558` — Toxic (#20 slice 3, CR 702.164): Keyword::Toxic(N) — a creature with toxic N gives the player

### CR 702.164a

- `crates/engine/src/characteristics.rs:388` — How many poison counters `object` gives a player it deals combat damage to (CR 702.164a).
- `crates/engine/src/combat.rs:1247` — Toxic N (CR 702.164a): "Players dealt combat damage by this creature also get N poison
- `crates/engine/src/types/card.rs:304` — counters." Unlike Infect it does *not* reshape the damage (CR 702.164a) — the life loss
- `crates/engine/tests/game.rs:100559` — it deals combat damage to N poison counters *in addition to* that damage (CR 702.164a), and
- `crates/engine/tests/game.rs:100627` — CR 702.164a: toxic does not replace the damage the way infect does — the defending player
- `crates/engine/tests/game.rs:100665` — it (CR 702.164a) — they stack. Three damage from an infecting toxic-1 creature is three
- `crates/engine/tests/game.rs:100691` — CR 702.164a hangs the poison counters off combat damage actually dealt. Moment's Peace
- `crates/engine/tests/game.rs:100712` — CR 702.164a is combat damage only — a toxic creature's ETB ping costs life and nothing else.

### CR 702.164b

- `crates/engine/src/characteristics.rs:389` — Multiple instances of toxic add (CR 702.164b), so every instance is summed rather than
- `crates/engine/src/types/card.rs:306` — (CR 702.164b), so this is read as a sum by
- `crates/engine/tests/game.rs:100560` — multiple instances add (CR 702.164b).
- `crates/engine/tests/game.rs:100645` — CR 702.164b: a creature with more than one instance of toxic gives the sum — a printed
- `crates/engine/tests/game.rs:100773` — printed toxic 1 (CR 702.164b) for three poison counters.

### CR 702.165

- `crates/engine/src/triggers.rs:3992` — Corrupted (CR 702.165): "an opponent has three or more poison counters" — an
- `crates/engine/src/types/effect/shared.rs:1789` — word (CR 702.165), an existential over the ability controller's living opponents like
- `crates/engine/tests/game.rs:100891` — Poison readers (#20 slice 4): the Corrupted ability word (CR 702.165) gates on "an opponent

### CR 702.166

- `crates/engine/src/characteristics.rs:1514` — Backup / "it gains the following abilities until end of turn" (CR 702.166): a granted
- `crates/engine/src/characteristics_cache.rs:110` — A Backup grant (CR 702.166) adds the source's keywords to the target — drop its
- `crates/engine/src/lib.rs:193` — turn (CR 702.166 Backup — Guardian Scalelord). The granted set is read live off `source`'s
- `crates/engine/src/priority.rs:1782` — Backup / "gains the following abilities until end of turn" (CR 702.166 / 514.2)
- `crates/engine/src/resolution/control.rs:202` — Backup's rider (CR 702.166): the shared target creature gains the source's other
- `crates/engine/src/triggers.rs:3701` — The abilities `target` has gained from a live Backup grant (CR 702.166 — Guardian
- `crates/engine/src/triggers.rs:3744` — A Backup grant (CR 702.166) makes `source` gain another permanent's abilities until end
- `crates/engine/src/types/stack.rs:2420` — `target` gained `source`'s other abilities until end of turn (CR 702.166 Backup — Guardian
- `crates/engine/tests/game.rs:4938` — attack-trigger reanimation. Targeting another creature grants both until cleanup (CR 702.166).
- `crates/engine/tests/game.rs:5004` — The grant lasts only until end of turn (CR 702.166): flying is gone after cleanup.
- `crates/engine/tests/game.rs:5021` — must target itself, still gets the +1/+1 counter, but grants nothing (CR 702.166).

### CR 702.166b

- `crates/engine/src/triggers.rs:4993` — Whether an ability carries Backup's grant (CR 702.166b) — used to exclude the granting ability

### CR 703.2j

- `crates/engine/src/pending/handlers/fanout.rs:284` — puts it into their hand, a miss puts it on the bottom of their library (CR 201.2/703.2j) —
- `crates/engine/src/types/stack.rs:426` — Answer a [`PendingChoice::ChooseCardName`] (CR 201.2/703.2j "choose a card name" —
- `crates/engine/src/types/stack.rs:1670` — fan-out (CR 201.2/703.2j "choose a card name" — Conundrum Sphinx's attack trigger).

### CR 704

- `crates/engine/src/apply.rs:3` — Primary: CR 704 (state-based actions). Also the apply path that mutates board facts
- `crates/engine/src/apply.rs:103` — 0-or-less toughness is a death SBA even for an indestructible creature (CR 702.12, CR 704)
- `crates/engine/src/apply.rs:235` — sub-SBA window between a tenth permanent entering and the next sweep. (CR 704)
- `crates/engine/src/apply.rs:2064` — state-based action (lethal damage) or a direct effect (Destroy), the Aura's own (CR 704, CR 303.4, …
- `crates/engine/src/apply.rs:2065` — orphan-to-graveyard SBA hasn't run yet, so it's still attached right now. Read (CR 704, CR 303.4, C…
- `crates/engine/src/combat.rs:873` — The defender may have gone between substeps (CR 704 killed the defending player, or
- `crates/engine/src/pending/handlers/library.rs:142` — hostless-Aura state-based action, same as before this pause existed. (CR 704, CR 303.4)
- `crates/engine/src/pipeline.rs:8` — Primary: CR 704 (SBA fixpoint), CR 603 (trigger enqueue / APNAP placement), CR 608
- `crates/engine/src/pipeline.rs:16` — Sweep state-based actions to a fixpoint (CR 704).
- `crates/engine/src/priority.rs:1725` — SBA sweep and death triggers are handled by `submit` after this step, and a (CR 704, CR 603, CR 104…
- `crates/engine/src/resolution/damage.rs:121` — Damage to a creature is marked (an SBA later checks it against toughness), (CR 704, CR 120.3)
- `crates/engine/src/resolution/damage.rs:166` — Mass damage: mark `amount` on every creature; the SBA sweep clears the dead. (CR 704, CR 120.3)
- `crates/engine/src/resolution/destroy.rs:35` — `apply` will push them (as the SBA death sweep does). (CR 704)
- `crates/engine/src/resolution/pump.rs:300` — defender being someone other than the Aura's controller. An unattached Aura (mid-SBA) (CR 704, CR 3…
- `crates/engine/src/resolution/pump.rs:354` — TempBoost, cleared at cleanup). A 0-or-less-toughness creature dies to the next SBA. (CR 704, CR 51…
- `crates/engine/src/triggers.rs:493` — If the dying creature's owner left the game in this same SBA sweep, the (CR 704, CR 108.4)
- `crates/engine/src/triggers.rs:1620` — No pool effect kills in sequence within one resolution; revisit if one ever does. (CR 704, CR 108.3…
- `crates/engine/src/triggers.rs:2725` — triggers are queued (CR 704 SBAs precede trigger placement) — so read it as a possibly-
- `crates/engine/tests/game.rs:3451` — Any submitted intent triggers the SBA sweep that eliminates the doomed players. (CR 704, CR 603)
- `crates/engine/tests/game.rs:3891` — H2 repro: a first-strike 2/2 attacks a player at 2 life. The between-substeps SBA sweep (CR 704, CR…
- `crates/engine/tests/game.rs:9114` — "the SBA sweep eliminated P2 by the time the cast was processed (CR 704, CR 603)"
- `crates/engine/tests/game.rs:9171` — Shock the 1/1 token: lethal, so the SBA moves it off the battlefield. (CR 704, CR 111, CR 403.5)
- `crates/engine/tests/game.rs:9414` — resolve_top_of_stack(&mut game); // Shock resolves → SBA kills the Pest, which ceases to exist. (CR…
- `crates/engine/tests/game.rs:13616` — 3 >= toughness 2, so the SBA already moved the bear to the graveyard. (CR 704, CR 403.5)
- `crates/engine/tests/game.rs:14758` — Shock marks 2 damage; 2 >= toughness 2, so the SBA moves the bear to the graveyard. (CR 704, CR 403…
- `crates/engine/tests/game.rs:20864` — resolve_top_of_stack(&mut game); // Shock resolves → lethal damage → SBA kills the 1/1. (CR 704, CR…
- `crates/engine/tests/game.rs:20900` — resolve_top_of_stack(game); // Shock resolves → lethal → SBA kills the creature. (CR 704)
- `crates/engine/tests/game.rs:21022` — resolve_top_of_stack(game); // Shock resolves → lethal → SBA kills the creature. (CR 704)
- `crates/engine/tests/game.rs:21460` — resolve_top_of_stack(&mut game); // Shock resolves → lethal → SBA kills the watcher. (CR 704)
- `crates/engine/tests/game.rs:30615` — resolve_top_of_stack(&mut game); // bolt resolves; the bear dies to an SBA (CR 704)
- `crates/engine/tests/game.rs:32437` — the same SBA sweep. Deaths mint graveyard cards first, then `PlayerLost` tombstones (CR 704, CR 403…
- `crates/engine/tests/game.rs:36687` — `graveyard_or_command` choke point: lethal marked damage (the SBA death sweep) and a (CR 704, CR 12…
- `crates/engine/tests/game.rs:36715` — resolve_top_of_stack(&mut game); // Shock resolves → SBA sweep finds lethal damage. (CR 704, CR 120…
- `crates/engine/tests/game.rs:38280` — Any submitted intent triggers the SBA sweep that eliminates the doomed player. (CR 704, CR 603)
- `crates/engine/tests/game.rs:55224` — Toxic Deluge cast for X=2 gives every creature -2/-2 until end of turn: a 2/2 dies to an SBA, (CR 7…
- `crates/engine/tests/game.rs:57687` — resolve_top_of_stack(game); // Shock resolves → SBA kills the dragon → dies trigger placed. (CR 704…
- `crates/engine/tests/game.rs:65355` — resolve_top_of_stack(&mut game); // Shock resolves → lethal damage → SBA kills the 1/1. (CR 704, CR…

### CR 704.3

- `crates/engine/src/apply.rs:248` — Sweep state-based actions to a fixpoint (CR 704.3): one creature's death can drop a static
- `crates/engine/src/apply.rs:255` — CR 704.3: state-based actions are checked only when a player would receive priority.
- `crates/engine/src/triggers.rs:4291` — choice lands a sweep later than CR 704.3 strictly wants. Unobservable for every
- `crates/engine/src/types/effect/shared.rs:276` — object, and no state-based action clears it mid-resolution (CR 704.3 only checks between

### CR 704.5

- `crates/engine/src/apply.rs:89` — detaches simultaneously (CR 704.5), rather than one SBA sweep behind.
- `crates/engine/tests/game.rs:88633` — A resolution (and its CR 704.5 sweep) with another creature on the battlefield: the

### CR 704.5a

- `crates/engine/src/apply.rs:206` — commander damage loses (CR 704.5a/b/c, CR 903.10a).
- `crates/engine/tests/game.rs:97503` — is still offered, and paying puts them at 0 (a state-based loss, CR 704.5a).
- `crates/engine/tests/game.rs:102219` — a bare 0/0 dies to the CR 704.5a state-based action either way, which hides the difference.

### CR 704.5b

- `crates/engine/src/apply.rs:206` — commander damage loses (CR 704.5a/b/c, CR 903.10a).

### CR 704.5c

- `crates/engine/src/apply.rs:206` — commander damage loses (CR 704.5a/b/c, CR 903.10a).
- `crates/engine/src/apply.rs:215` — CR 704.5c: ten or more poison counters loses the game.
- `crates/engine/src/core.rs:195` — poison counters loses the game (CR 704.5c).
- `crates/engine/src/types/card.rs:2024` — Poison counters on a player that lose the game (CR 704.5c).
- `crates/engine/src/types/card.rs:2145` — (CR 704.5c).
- `crates/engine/src/types/effect/shared.rs:1270` — A poison counter (CR 122.1, CR 704.5c — ten or more loses the game). Placed by infect
- `crates/engine/src/types/stack.rs:2271` — [`Player::kind_counters`]; ten or more poison then loses the game via CR 704.5c's
- `crates/engine/tests/game.rs:99278` — CR 704.5c: "If a player has ten or more poison counters, that player loses the game."
- `crates/engine/tests/game.rs:99575` — CR 704.5c: poison from infect damage is ordinary poison — the tenth counter loses the game.
- `crates/engine/tests/game.rs:100733` — CR 704.5c: poison from toxic is ordinary poison — the tenth counter loses the game.

### CR 704.5f

- `crates/engine/src/apply.rs:104` — (CR 704.5f); lethal damage / deathtouch is not, if it's indestructible
- `crates/engine/tests/game.rs:64074` — it drops the creature to 0 toughness, which the SBA sweep (CR 704.5f) then kills.
- `crates/engine/tests/game.rs:70695` — which then dies to the SBA sweep for 0 toughness (CR 704.5f).
- `crates/engine/tests/game.rs:76600` — -0/-2 drops the 2/2 to 0 toughness: CR 704.5f puts it into the graveyard (not a "destroy"),
- `crates/engine/tests/game.rs:92559` — matching lands still sets base P/T to 0/0 (CR 613.3(7b)), which then dies to the CR 704.5f
- `crates/engine/tests/game.rs:97326` — CR 704.5r's annihilation and CR 704.5f's 0-toughness death are both state-based actions

### CR 704.5g

- `crates/engine/src/apply.rs:96` — A creature with lethal marked damage dies (CR 704.5g); a planeswalker with 0 loyalty
- `crates/engine/src/apply.rs:118` — honors, since CR 704.5g's lethal-damage/deathtouch destroy is a "destroy" too. CR
- `crates/engine/tests/game.rs:76648` — CR 704.5g's lethal-marked-damage state-based destroy is a "destroy" too — a regeneration
- `crates/engine/tests/game.rs:76665` — "the shield replaced the CR 704.5g lethal-damage state-based destroy"
- `crates/engine/tests/game.rs:83038` — "the shield replaced the CR 704.5g lethal-damage state-based destroy"

### CR 704.5i

- `crates/engine/src/apply.rs:97` — is put into its owner's graveyard (CR 704.5i).
- `crates/engine/tests/game.rs:50453` — −2 from 2 loyalty leaves 0; the state-based action moves it to the graveyard (CR 704.5i).
- `crates/engine/tests/game.rs:50507` — 3 damage to a loyalty-3 walker leaves 0, which the SBA (CR 704.5i) destroys.
- `crates/engine/tests/game.rs:60423` — it to the graveyard (CR 704.5i), the same as a creature dying to lethal damage.
- `crates/engine/tests/game.rs:102339` — the graveyard (CR 704.5i), and the emblem keeps working.

### CR 704.5j

- `crates/engine/src/apply.rs:271` — CR 704.5j: after event-producing SBAs settle, pause for the legend rule if a
- `crates/engine/src/apply.rs:288` — First legend-rule conflict (CR 704.5j), if any: a living controller with two or more
- `crates/engine/src/pending/handlers/edict.rs:728` — Answer a [`PendingChoice::ChooseLegendaryKeep`] (CR 704.5j): keep `keep` on the battlefield
- `crates/engine/src/types/stack.rs:439` — Answer a [`PendingChoice::ChooseLegendaryKeep`] (CR 704.5j legend rule): `keep` is the
- `crates/engine/src/types/stack.rs:1745` — Legend rule (CR 704.5j): `player` controls two or more legendary permanents named `name`
- `crates/engine/tests/game.rs:42131` — Non-legendary hosts so CR 704.5j does not pause before the re-attach MayYesNo.
- `crates/engine/tests/game.rs:93291` — CR 704.5j: two legendary permanents with the same name under one controller → choose one

### CR 704.5m

- `crates/engine/src/apply.rs:14` — Aura's cast-time legality re-check (CR 303.4f) and by the CR 704.5m/n state-based action
- `crates/engine/src/apply.rs:145` — CR 704.5m/n: an Aura attached to nothing/an illegal object is put into the graveyard;
- `crates/engine/src/apply.rs:168` — exemption naturally lapses — the ordinary CR 704.5m sweep then applies to it,
- `crates/engine/src/apply.rs:1976` — CR 603.6c/704.5m last-known information: capture the Aura(s) attached to this
- `crates/engine/src/apply.rs:2062` — CR 603.6c/704.5m last-known information: capture the Aura(s) attached to this
- `crates/engine/src/pending/handlers/dig.rs:1127` — Aura stays unattached and the existing Aura-legality state-based action (CR 704.5m) sweeps
- `crates/engine/src/triggers.rs:2724` — action (e.g. an unattached Aura, CR 704.5m) can sweep it away in the same batch before
- `crates/engine/src/types/card.rs:528` — of it. `Kind` stays `Aura` (CR 704.5m's Aura-orphan state-based action applies to it like
- `crates/engine/src/types/card.rs:1876` — [`Game::attachment_host_legal`], so the CR 704.5m sweep holds the Aura to exactly that
- `crates/engine/src/types/stack.rs:1734` — existing Aura-legality state-based action (CR 704.5m) and goes to the graveyard, unpaused.
- `crates/engine/src/types/trigger.rs:241` — a state-based action (CR 704.5m) triggered by the host's death — the pre-move attachment
- `crates/engine/tests/game.rs:18915` — anything is illegal, CR 704.5m): Copy Enchantment becoming a copy of Empyrial Armor pauses
- `crates/engine/tests/game.rs:29990` — "the Aura goes to the graveyard with its host (CR 704.5m)"
- `crates/engine/tests/game.rs:31085` — state-based-actioned (CR 704.5m).
- `crates/engine/tests/game.rs:32105` — (CR 704.5m); it never returns to the battlefield at all.
- `crates/engine/tests/game.rs:32567` — (CR 704.5m orphans the Aura the instant its host dies) — the linked exile must still
- `crates/engine/tests/game.rs:32936` — filter (CR 303.4a) legally attaches to a noncreature host — the CR 704.5m/n legality SBA
- `crates/engine/tests/game.rs:33005` — (CR 704.5m)"
- `crates/engine/tests/game.rs:36513` — Animate Dead is a real Aura (CR 303.4): CR 704.5m's Aura-orphan state-based action applies
- `crates/engine/tests/game.rs:36528` — "CR 704.5m: an Aura attached to nothing is put into its owner's graveyard as a \
- `crates/engine/tests/game.rs:72057` — by CR 704.5m's state-based action, which is exempted for exactly this Aura.
- `crates/engine/tests/game.rs:72096` — the existing Aura-legality state-based action (CR 704.5m) sweeps it straight to the
- `crates/engine/tests/game.rs:72217` — "an Aura with no legal host dies to CR 704.5m's state-based action"
- `crates/engine/tests/game.rs:88627` — the battlefield with this Aura.\"" — after the ETB attaches, the CR 704.5m sweep holds the

### CR 704.5n

- `crates/engine/src/apply.rs:14` — Aura's cast-time legality re-check (CR 303.4f) and by the CR 704.5m/n state-based action
- `crates/engine/src/apply.rs:145` — CR 704.5m/n: an Aura attached to nothing/an illegal object is put into the graveyard;
- `crates/engine/src/resolution/control.rs:29` — entering creature — moving it off any host it's already attached to (CR 704.5n
- `crates/engine/src/types/trigger.rs:392` — Ajani's Chosen/Archon of Sun's Grace) and landfall (CR 704.5n's kin: "whenever a land
- `crates/engine/tests/game.rs:17325` — (a state-based action) — it does NOT go to the graveyard as an unattached Aura (CR 704.5n).
- `crates/engine/tests/game.rs:30275` — The creature dies; the Equipment stays put and becomes unattached (CR 704.5n).
- `crates/engine/tests/game.rs:32936` — filter (CR 303.4a) legally attaches to a noncreature host — the CR 704.5m/n legality SBA
- `crates/engine/tests/game.rs:32954` — "an Aura enchanting a permanent it's legally allowed to enchant stays attached (CR 704.5n)"

### CR 704.5r

- `crates/engine/src/apply.rs:55` — CR 704.5r: if a permanent has both +1/+1 and −1/−1 counters on it, N of each are
- `crates/engine/tests/game.rs:93254` — CR 704.5r: if a permanent has both +1/+1 and -1/-1 counters, that many pairs are removed
- `crates/engine/tests/game.rs:97257` — CR 704.5r: "If a permanent has both a +1/+1 counter and a -1/-1 counter on it, N +1/+1
- `crates/engine/tests/game.rs:97295` — CR 704.5r: equal counts annihilate to zero of each, leaving the permanent at its printed
- `crates/engine/tests/game.rs:97326` — CR 704.5r's annihilation and CR 704.5f's 0-toughness death are both state-based actions
- `crates/engine/tests/game.rs:99595` — CR 704.5r: a permanent with both +1/+1 and -1/-1 counters has N of each removed, N being the

### CR 706

- `crates/engine/src/apply.rs:1355` — A permanent became a copy of another creature as it entered (CR 706/707.2). Overwrite
- `crates/engine/src/characteristics_cache.rs:180` — board (CR 706/707.2 — Altered Ego, Cursed Mirror; the same at the until-EOT revert).
- `crates/engine/src/de.rs:480` — Enter-as-a-copy replacement (CR 706/707.2) — an inline `enter_as_copy = { .. }`
- `crates/engine/src/effects.rs:427` — Enter-as-a-copy (CR 706/707.2 — Altered Ego, Cursed Mirror): pause as the
- `crates/engine/src/pending/handlers/edict.rs:236` — (CR 706/707.2; permanent, CR 400.7). `None` declines the "you may" and converts nothing.
- `crates/engine/src/pending/handlers/edict.rs:290` — card until end of turn (an [`Event::BecameCopy`] with `until_eot: true`, CR 706/707.2).
- `crates/engine/src/priority.rs:401` — "enchantment"`, CR 706/707.2) to enumerate its copyable candidates.
- `crates/engine/src/resolution/copy.rs:1` — Spell/ability copy choreography — CR 706 / 707 / 114.6 (copying spells, copying
- `crates/engine/src/types/card.rs:862` — Enter-as-a-copy replacement (CR 706/707.2), carried as a rules-keyword marker rather than a
- `crates/engine/src/types/card.rs:931` — The riders on an [`CardDef::enter_as_copy`] replacement (CR 706/707.2). `Copy` — all scalars,
- `crates/engine/src/types/card.rs:954` — The candidate-object type [`CardDef::enter_as_copy`] may copy (CR 706/707.2): `Creature` (the
- `crates/engine/src/types/card.rs:1949` — CR 706/613 — "become a copy … until end of turn"): when the copy is established, the
- `crates/engine/src/types/stack.rs:445` — declines the "you may" and it enters as its printed self (CR 706/707.2 — Altered Ego,
- `crates/engine/src/types/stack.rs:1688` — battlefield — CR 706/707.2: a creature for Altered Ego/Cursed Mirror, an enchantment
- `crates/engine/src/types/stack.rs:1707` — indefinite [`Event::BecameCopy`] per other token, CR 706/707.2), `None` declines the "you
- `crates/engine/src/types/stack.rs:1721` — with `until_eot: true`, CR 706/707.2), `None` declines the "you may". Only raised when at
- `crates/engine/src/types/stack.rs:2387` — `object` became a copy of another creature as it entered (CR 706/707.2 — Altered Ego,

### CR 706.2

- `crates/engine/tests/game.rs:4525` — or greater, create a token that's a copy of this creature." (CR 706.2)

### CR 706.9

- `crates/engine/src/resolution/copy.rs:59` — (`source`, not a chosen target). CR 706.9's "when you cast this spell" trigger
- `crates/engine/tests/game.rs:28715` — resolve_whole_stack(&mut g); // the retargeted copy, then the original — CR 706.9: a copy is
- `crates/engine/tests/game.rs:62487` — original (CR 706.9), so all three resolve before the original's draw/life step runs. It has
- `crates/engine/tests/game.rs:63388` — its 2 copies ON THE STACK ABOVE the original (CR 706.9), so all 3 resolve — the original

### CR 706.10

- `crates/engine/src/resolution/copy.rs:345` — `{X}` unchanged (CR 706.10 — an already-doubled X isn't re-doubled).

### CR 707

- `crates/engine/src/types/card.rs:1200` — (CR 613.1d/613.1f — Vraska, Betrayal's Sting's −2). Unlike a copy effect (CR 707) this is a
- `crates/engine/tests/game.rs:37265` — target for the copy." A flashback cast (from the graveyard) reanimates its own target, (CR 702.34, …
- `crates/engine/tests/game.rs:102714` — "\"becomes\" (CR 613) keeps the object's own name, unlike a CR 707 copy effect"

### CR 707.2

- `crates/engine/src/apply.rs:1355` — A permanent became a copy of another creature as it entered (CR 706/707.2). Overwrite
- `crates/engine/src/apply.rs:1406` — A copy made "except it has <keywords>" (CR 707.2): union the exception keywords into
- `crates/engine/src/characteristics.rs:898` — Copy-effect exception keywords (CR 707.2 — "except it has haste/myriad") are part of
- `crates/engine/src/characteristics_cache.rs:180` — board (CR 706/707.2 — Altered Ego, Cursed Mirror; the same at the until-EOT revert).
- `crates/engine/src/core.rs:382` — values (CR 707.2 — a copy made "except it has haste"/"except it has myriad"). The keyword
- `crates/engine/src/de.rs:480` — Enter-as-a-copy replacement (CR 706/707.2) — an inline `enter_as_copy = { .. }`
- `crates/engine/src/effects.rs:427` — Enter-as-a-copy (CR 706/707.2 — Altered Ego, Cursed Mirror): pause as the
- `crates/engine/src/pending/handlers/edict.rs:132` — `candidates`) has `source` become a copy of it — overwriting its `def` (CR 707.2) and
- `crates/engine/src/pending/handlers/edict.rs:197` — Cursed Mirror's "except it has haste" — a copiable value (CR 707.2): a copy of this
- `crates/engine/src/pending/handlers/edict.rs:210` — CR 707.2: the copied creature's own copy-effect exception rider is part of its copiable
- `crates/engine/src/pending/handlers/edict.rs:223` — Copy Enchantment copying an Aura (CR 707.2 read with CR 303.4f): the copy entered
- `crates/engine/src/pending/handlers/edict.rs:236` — (CR 706/707.2; permanent, CR 400.7). `None` declines the "you may" and converts nothing.
- `crates/engine/src/pending/handlers/edict.rs:257` — ponytail: the copyable values are the chosen token's `CardDef` values (CR 707.2), not a
- `crates/engine/src/pending/handlers/edict.rs:262` — CR 707.2: "a copy of that token" carries the chosen token's own copy-effect exception
- `crates/engine/src/pending/handlers/edict.rs:290` — card until end of turn (an [`Event::BecameCopy`] with `until_eot: true`, CR 706/707.2).
- `crates/engine/src/pending/handlers/edict.rs:292` — ponytail: the copyable values are the chosen card's printed `CardDef` (CR 707.2), not a
- `crates/engine/src/priority.rs:401` — "enchantment"`, CR 706/707.2) to enumerate its copyable candidates.
- `crates/engine/src/resolution/mill.rs:90` — copiable characteristics (CR 707.2) — `CreateTokenCopy`'s target-a-battlefield-
- `crates/engine/src/resolution/tokens.rs:220` — CR 707.2: a copy uses the copied object's *current copiable* values, which
- `crates/engine/src/resolution/tokens.rs:246` — Carry the copied object's own copiable rider (CR 707.2) onto the new copy —
- `crates/engine/src/resolution/tokens.rs:291` — CR 707.2: if the chosen creature is itself already a copy carrying a rider (a
- `crates/engine/src/resolution/tokens.rs:302` — "…except it has myriad" is a copiable value (CR 707.2): a copy of Muddle's
- `crates/engine/src/resolution/tokens.rs:384` — CR 707.2: "a copy of that token" carries the copied token's own copy-effect
- `crates/engine/src/resolution/zones.rs:247` — exiles, mints the token copy (CR 707.2) under `controller`, then adds `add_subtypes`
- `crates/engine/src/types/card.rs:862` — Enter-as-a-copy replacement (CR 706/707.2), carried as a rules-keyword marker rather than a
- `crates/engine/src/types/card.rs:931` — The riders on an [`CardDef::enter_as_copy`] replacement (CR 706/707.2). `Copy` — all scalars,
- `crates/engine/src/types/card.rs:935` — has haste"); `of` is the copyable type axis (Copy Enchantment's "any enchantment", CR 707.2,
- `crates/engine/src/types/card.rs:954` — The candidate-object type [`CardDef::enter_as_copy`] may copy (CR 706/707.2): `Creature` (the
- `crates/engine/src/types/stack.rs:445` — declines the "you may" and it enters as its printed self (CR 706/707.2 — Altered Ego,
- `crates/engine/src/types/stack.rs:1688` — battlefield — CR 706/707.2: a creature for Altered Ego/Cursed Mirror, an enchantment
- `crates/engine/src/types/stack.rs:1707` — indefinite [`Event::BecameCopy`] per other token, CR 706/707.2), `None` declines the "you
- `crates/engine/src/types/stack.rs:1721` — with `until_eot: true`, CR 706/707.2), `None` declines the "you may". Only raised when at
- `crates/engine/src/types/stack.rs:2318` — A copy effect made `object` a copy "except it has `keywords`" (CR 707.2 — Twinflame's
- `crates/engine/src/types/stack.rs:2387` — `object` became a copy of another creature as it entered (CR 706/707.2 — Altered Ego,
- `crates/engine/tests/game.rs:9574` — token's *copiable* values (CR 707.2), so its copiable snapshot reports haste — the rider a
- `crates/engine/tests/game.rs:9602` — haste (CR 707.2 — the copy uses the copied object's current copiable values).
- `crates/engine/tests/game.rs:9646` — "copying a copy keeps its 'except it has haste' rider (CR 707.2)"
- `crates/engine/tests/game.rs:10527` — copied form's *copiable* values (CR 707.2), so the copiable snapshot reports myriad — the
- `crates/engine/tests/game.rs:10554` — control), it keeps that creature's haste (CR 707.2) unioned with the myriad Muddle's own
- `crates/engine/tests/game.rs:10583` — "the copied token's 'except it has haste' rider is preserved (CR 707.2)"
- `crates/engine/tests/game.rs:10594` — a *copiable* rider (CR 707.2) — distinct from Brudiclad's own token-haste anthem, which
- `crates/engine/tests/game.rs:10628` — "the copied token's haste rider is preserved as a copiable value (CR 707.2)"
- `crates/engine/tests/game.rs:10635` — wholesale (CR 707.2), so a stale "except it has haste" rider left by a *prior* copied form
- `crates/engine/tests/game.rs:10694` — "the prior copy's haste rider is cleared by the new copy effect (CR 707.2)"
- `crates/engine/tests/game.rs:18606` — Bear with X=2 → a 3/3 Bear body carrying 2 +1/+1 counters (5/5), permanent (CR 707.2).
- `crates/engine/tests/game.rs:18854` — gets +1/+1 (CR 707.2, the copy's abilities function as the copy's own).
- `crates/engine/tests/game.rs:18914` — A copied Aura must enter attached (CR 707.2 read with CR 303.4f — an Aura not attached to
- `crates/engine/tests/game.rs:32382` — Confirms the copy is a full functional copy (CR 707.2): it carries Changing Loyalty's own

### CR 707.9

- `crates/engine/src/types/filter.rs:767` — (CR 707.9), so a copiable-name-vs-current-name distinction doesn't arise yet.

### CR 707.9a

- `crates/engine/src/types/stack.rs:2942` — is public game information (CR 707.9a).

### CR 707.10

- `crates/engine/src/apply.rs:867` — A copy isn't "cast" (CR 707.10) — no ambient timing to read.
- `crates/engine/src/apply.rs:884` — A copy pays no cost (CR 707.10) — nothing was spent to "cast" it.
- `crates/engine/src/cast.rs:2716` — CR 707.10: "Whenever you … activate an ability, if that ability's activation cost
- `crates/engine/src/cast.rs:2721` — (CR 707.10 copies value 0). No pool card is a non-`{X}` activated-ability copy consumer,
- `crates/engine/src/resolution/copy.rs:32` — CR 707.10: if the target spell has left the stack (countered/resolved), the copy
- `crates/engine/src/resolution/copy.rs:274` — Mirrorwing Dragon's watch payoff (CR 707.10): "that player copies that spell for
- `crates/engine/src/resolution/copy.rs:342` — Unbound Flourishing (CR 707.10): "copy that ability" — copy the activated ability
- `crates/engine/src/triggers.rs:3118` — ability … copy that ability", CR 707.10) when `activator` puts an `{X}`-cost activated
- `crates/engine/src/types/card.rs:1524` — Whether this spell is a *copy* (CR 707.10) rather than a cast card: it was put on the
- `crates/engine/src/types/trigger.rs:381` — Whenever a player activates an ability whose activation cost contains `{X}` (CR 707.10 —
- `crates/engine/tests/game.rs:8097` — (CR 707.10 — copy an activated ability whose cost contains {X}).
- `crates/engine/tests/game.rs:8195` — The copy trigger is gated on `{X}` in the activation cost (CR 707.10): a fixed-cost activated
- `crates/engine/tests/game.rs:8350` — Unbound Flourishing's ability half (CR 707.10): "... or activate an ability, if that

### CR 707.10a

- `crates/engine/src/apply.rs:183` — CR 111.7: a token Aura (a Replicate copy, CR 707.10a) that falls off ceases to
- `crates/engine/src/apply.rs:1922` — CR 707.10a: a copy of a permanent spell becomes a token as it resolves — it
- `crates/engine/src/cast.rs:783` — legality is evaluated from (the bent/copied spell's own controller — CR 114.6/707.10a);
- `crates/engine/src/effects.rs:195` — CR 707.10a/111.7: a copy that fails to resolve never becomes a card — it
- `crates/engine/src/effects.rs:578` — it's a copy (CR 707.10a), exile if it was cast via flashback/escape (CR 702.34e/702.19d),
- `crates/engine/src/effects.rs:590` — A copy ceases to exist (CR 707.10a); a cast instant/sorcery goes to the graveyard.
- `crates/engine/src/effects.rs:742` — Whether `object` is a still-on-stack spell that's a copy (CR 707.10a) — such a spell
- `crates/engine/src/resolution/copy.rs:54` — "you" is its new controller (CR 707.10a), unlike retargeting the ORIGINAL spell
- `crates/engine/src/resolution/copy.rs:104` — Loyalty's Replicate copies, CR 702.108b/707.10a).
- `crates/engine/src/resolution/misc.rs:175` — CR 707.10a: a countered spell that's a copy ceases to exist rather than going to any
- `crates/engine/src/resolution/pause_counter_spell.rs:89` — (whose pause answer never checks this), a copy (CR 707.10a) ceases to exist here
- `crates/engine/src/state.rs:252` — copy's dies-trigger, CR 707.10a). Read (not drained) per dying creature by
- `crates/engine/src/triggers.rs:2470` — card, a *token* Aura, CR 707.10a, straight to [`Object::Removed`]) fires its
- `crates/engine/src/types/card.rs:2002` — finished resolving (CR 707.10a), or an object owned by an eliminated player (CR 800.4a).
- `crates/engine/src/types/stack.rs:2112` — A spell *copy* finished resolving and ceased to exist (CR 707.10a / CR 111.7) — it leaves
- `crates/engine/tests/game.rs:25507` — CR 707.10a: a spell that's a copy ceases to exist rather than going to a graveyard when
- `crates/engine/tests/game.rs:25595` — "a countered copy ceases to exist (CR 707.10a), got {events:?}"
- `crates/engine/tests/game.rs:26533` — CR 707.10a: a spell that's a copy ceases to exist rather than reaching any zone when
- `crates/engine/tests/game.rs:26632` — "the countered storm copy ceases to exist (CR 707.10a), got {events:?}"
- `crates/engine/tests/game.rs:26656` — rider (setting the self-tuck mark), then ceases to exist (CR 707.10a) rather than reaching
- `crates/engine/tests/game.rs:26819` — CR 707.10a: a copy of an `ExileSelfOnResolve` spell runs the rider (setting the self-exile
- `crates/engine/tests/game.rs:26848` — exile — CR 707.10a.
- `crates/engine/tests/game.rs:26854` — "the copy ceases to exist (CR 707.10a) rather than reaching exile, got {events:?}"
- `crates/engine/tests/game.rs:32335` — CR 707.10a: "A copy of a permanent spell becomes a token as it resolves." A token that
- `crates/engine/tests/game.rs:62759` — choose new targets for the copies.)" End-to-end fidelity check for CR 707.10a on a real
- `crates/engine/tests/game.rs:62859` — "the countered storm copy ceases to exist (CR 707.10a), got {events:?}"
- `crates/engine/tests/game.rs:79765` — The copy resolves first (CR 707.10a — above the original), then the retargeted original.

### CR 707.10c

- `crates/engine/src/cast.rs:623` — its own per-copy CR 707.10c retarget.
- `crates/engine/src/cast.rs:762` — `def`'s first `Timing::Spell` ability with a target — spec and count (CR 707.10c/CR
- `crates/engine/src/cast.rs:781` — copy's or a bent original's CR 707.10c/114.6a retarget — same shape, just against an
- `crates/engine/src/pending/handlers/optional.rs:42` — `optional` gate, CR 707.10c — Sevinne's Reclamation) mints inline as part of the
- `crates/engine/src/pending/handlers/targets.rs:225` — copy now (with its usual CR 707.10c retarget); the opponent's copy is deferred to
- `crates/engine/src/resolution/copy.rs:26` — offer CR 707.10c's "you may choose new targets for the copy" — same
- `crates/engine/src/resolution/copy.rs:100` — same CR 707.10c retarget choice `CopyTargetSpell` offers, for one already-minted
- `crates/engine/src/resolution/copy.rs:259` — CR 707.10c declined: mint each copy keeping the triggering spell's own
- `crates/engine/src/resolution/copy.rs:344` — the stack above the original (CR 707.10c), carrying its effect/target and its chosen
- `crates/engine/src/resolution/copy.rs:353` — CR 603.4/707.10c: the triggering ability may have left the stack (countered in
- `crates/engine/src/resolution/copy.rs:372` — CR 707.10c: "you may choose new targets for the copy" — a real re-pick when the
- `crates/engine/src/resolution/copy.rs:378` — original's target(s) unchanged below — CR 707.10c's declined case.
- `crates/engine/src/resolution/copy.rs:390` — The copy is the same kind of ability as the original (CR 707.10c) — an activated
- `crates/engine/src/resolution/pause_choose.rs:75` — been countered in response before this trigger resolved (CR 707.10c guard, same
- `crates/engine/src/triggers.rs:4593` — and Unbound Flourishing's CR 707.10c copy-retarget of a targeted activated ability
- `crates/engine/src/triggers.rs:4887` — each ability — for an activated ability whose cost contains `{X}`, or a CR 707.10c copy of
- `crates/engine/src/types/stack.rs:843` — - an already-on-the-stack spell's independent target clause (CR 601.2c / CR 707.10c
- `crates/engine/src/types/stack.rs:2003` — CR 707.10c); `0` for every triggered ability. Read at resolution for `Amount::X`.
- `crates/engine/src/types/stack.rs:2008` — CR 707.10c copy (a copy is created, not activated, so no mana was spent on it —
- `crates/engine/src/types/stack.rs:2197` — (CR 707.10c); `0` for every triggered ability (abilities carry no `{X}` of their own).
- `crates/engine/src/types/stack.rs:2209` — every triggered ability and for a CR 707.10c copy.
- `crates/engine/tests/game.rs:8352` — copy." Nin is exactly the targeted {X}-cost activated ability that makes CR 707.10c's re-pick
- `crates/engine/tests/game.rs:8378` — "expected the copy's CR 707.10c retarget pause, got {:?}",
- `crates/engine/tests/game.rs:25246` — Resolving Twincast mints the copy and pauses on its CR 707.10c retarget choice; re-choose
- `crates/engine/tests/game.rs:25265` — CR 707.10c: the copier may choose new targets for the copy. P0 bolts a 0/7 ox, then
- `crates/engine/tests/game.rs:25313` — Resolve Twincast: the copy is minted and pauses on the CR 707.10c retarget choice.
- `crates/engine/tests/game.rs:25401` — Resolving Twincast mints the copy and pauses on its CR 707.10c retarget choice; re-choose
- `crates/engine/tests/game.rs:25481` — The copy pauses on its CR 707.10c retarget choice; re-choose the same target (P1) so the
- `crates/engine/tests/game.rs:25675` — Resolve stack items until Twincast itself resolves and pauses on the copy's CR 707.10c
- `crates/engine/tests/game.rs:34588` — Resolve Rootha's ability: the copy is minted and pauses on the CR 707.10c retarget choice.
- `crates/engine/tests/game.rs:37282` — "a flashback cast offers the CR 707.10c copy rider"
- `crates/engine/tests/game.rs:63548` — Paying mints a copy under the PAYER (CR 707.10c retarget), not the original caster.
- `crates/engine/tests/game.rs:74544` — minting a copy never pauses on a CR 707.10c retarget.
- `crates/engine/tests/game.rs:74672` — retargeting to a permanent they themselves control (CR 707.10c). Each player controls two
- `crates/engine/tests/game.rs:74878` — Casting the free copy is a genuine cast (CR 707.10c), so it fires Renegade Bull's own

### CR 708

- `crates/engine/src/apply.rs:1896` — Morph (CR 702.37b/708): a face-down creature spell enters as a face-down 2/2 —
- `crates/engine/src/core.rs:689` — Whether the permanent at `id` is face down (CR 708 — a manifested card): a 2/2 colorless
- `crates/engine/src/types/card.rs:1629` — so the permanent enters face down (CR 708). `false` for an ordinary face-up cast.
- `crates/engine/src/types/card.rs:1914` — Whether this permanent is *face down* (CR 708 — a manifested card, CR 701.34): while set,
- `crates/engine/tests/game.rs:79050` — --- Manifest / face-down permanents (CR 701.34, CR 708 — Reality Shift) ---

### CR 708.2

- `crates/engine/src/cast.rs:1995` — creature spell → face-down permanent (CR 708.2: a 2/2 colorless creature with no name,
- `crates/engine/src/cast.rs:2040` — Put `card` onto the stack as a face-down 2/2 creature spell (CR 708.2) controlled by
- `crates/engine/src/characteristics.rs:1216` — CR 708.2: a face-down permanent (a manifest) has no abilities.
- `crates/engine/src/characteristics.rs:1239` — CR 708.2: a face-down permanent (a manifest) is a creature and nothing else — its real
- `crates/engine/src/characteristics.rs:1280` — CR 708.2: a face-down permanent (a manifest) has no subtypes.
- `crates/engine/src/characteristics.rs:1379` — CR 708.2: a face-down permanent (a manifest) has base power and toughness 2/2, whatever
- `crates/engine/src/characteristics.rs:1467` — CR 708.2: a face-down permanent (a manifest) has no abilities, so no keyword abilities.
- `crates/engine/src/effects.rs:153` — face-down morph spell (CR 702.37b/708.2) resolves as a plain 2/2 colorless creature
- `crates/engine/src/pending/handlers/library.rs:726` — offered candidates) face down as a 2/2 creature spell (CR 708.2) without paying its mana
- `crates/engine/src/priority.rs:86` — CR 708.2: a face-down permanent has no abilities — a hidden land's `produces` sugar
- `crates/engine/src/priority.rs:140` — CR 708.2: a face-down permanent has no abilities — a hidden land's `produces` sugar
- `crates/engine/src/priority.rs:435` — CR 708.2: a face-down permanent (Zoetic Cavern morphed down) has no abilities.
- `crates/engine/src/priority.rs:613` — CR 708.2: a face-down permanent has no abilities.
- `crates/engine/src/priority.rs:1094` — CR 708.2: a face-down permanent has no abilities.
- `crates/engine/src/types/card.rs:1916` — other than creature, no subtypes, no mana cost, and no abilities (CR 708.2 — the
- `crates/engine/src/types/stack.rs:193` — enters as a face-down 2/2 permanent (CR 708.2). See [`Game::cast_face_down`].
- `crates/engine/tests/game.rs:79054` — face down as a 2/2 creature (CR 701.34), with no printed name/subtypes exposed (CR 708.2).
- `crates/engine/tests/game.rs:79217` — card's printed cost — and lands a face-down 2/2 colorless creature (CR 708.2), its real name,
- `crates/engine/tests/game.rs:79248` — "a face-down permanent is a 2/2 (CR 708.2), not its printed 4/4"
- `crates/engine/tests/game.rs:79953` — --- Illusionary Mask (lea, CR 708.2 — {X}: cast a hand creature whose mana cost the mana spent
- `crates/engine/tests/game.rs:79965` — (CR 708.2) without paying its mana cost.
- `crates/engine/tests/game.rs:80021` — "a face-down permanent is a 2/2 (CR 708.2), not its printed 2/2 by coincidence — MV-2 body"
- `crates/engine/tests/game.rs:94894` — colorless creature (CR 708.2) with no printed types, whatever the hidden card's real kind
- `crates/engine/tests/game.rs:94916` — "a face-down permanent is a 2/2 (CR 708.2), not a land"
- `crates/engine/tests/game.rs:94932` — "CR 708.2: a face-down permanent has no abilities — its hidden land's mana tap is hidden too"

### CR 709

- `crates/engine/src/de.rs:471` — A split card's two castable halves (CR 709, Fire // Ice) — `[[half]]` tables, each
- `crates/engine/src/types/card.rs:847` — A split card's two castable halves (CR 709 — Fire // Ice): this `CardDef` is the *fused*
- `crates/engine/src/types/stack.rs:2181` — One half of a split card (CR 709) was cast from hand and put on the stack. `source` is the
- `crates/engine/tests/game.rs:83414` — ── Fire // Ice (split card — one card, two independently castable halves, CR 709) ──────

### CR 709.1

- `crates/engine/src/apply.rs:801` — Both halves of a split card are instants or sorceries (CR 709.1).

### CR 709.4

- `crates/engine/src/apply.rs:733` — Only the cast half is on the stack (CR 709.4); the card moves from hand onto the
- `crates/engine/src/cast.rs:1812` — (CR 709.4) — the fused card is not itself castable. Mirrors [`Game::cast_adventure`], minus
- `crates/engine/src/core.rs:277` — CR 709.4: a split card is only the half that was cast while that half is on the stack;
- `crates/engine/src/state.rs:180` — the object is the whole split card again (CR 709.4), so [`Game::create_object`](crate::Game)
- `crates/engine/src/types/card.rs:848` — card (the combined characteristics every zone but the stack sees, CR 709.4 — combined name,
- `crates/engine/src/types/stack.rs:2185` — come from that one half rather than from the fused card (CR 709.4).
- `crates/engine/tests/game.rs:83420` — every other zone (CR 709.4).
- `crates/engine/tests/game.rs:83487` — "the combined mana value of both halves (CR 709.4)"
- `crates/engine/tests/game.rs:83526` — hand, cast Ice — the graveyard card is still the whole "Fire // Ice" (CR 709.4), not "Ice".

### CR 709.4a

- `crates/engine/src/cast.rs:1810` — Cast one half of a split card from hand (CR 709.4a — Fire // Ice). `source` is the card in
- `crates/engine/src/core.rs:749` — Target need and legal targets for casting half `half` of the split card `card` (CR 709.4a).
- `crates/engine/src/playable.rs:138` — A split card is cast as one of its halves (CR 709.4a — `Intent::CastSplitHalf`), never
- `crates/engine/src/query.rs:454` — card in hand (CR 709.4a) — the fused card itself is never castable, so this is the only way
- `crates/engine/src/state.rs:179` — CardId to restore)`. Only the cast half is on the stack (CR 709.4a); in every other zone
- `crates/engine/src/types/card.rs:850` — Only one half is ever cast (CR 709.4a), so casting goes through [`Game::cast_split_half`]
- `crates/engine/src/types/stack.rs:168` — Cast one half of a split card from hand (CR 709.4a — Fire // Ice). `source` is the card in
- `crates/engine/src/types/stack.rs:3117` — Cast one half of the split card `card` from hand (CR 709.4a). A split card lists one of
- `crates/engine/tests/game.rs:83419` — (CR 709.4a) puts only that half on the stack; the card is the whole split card again in
- `crates/engine/tests/game.rs:83427` — The fused card itself is never cast — only a half is (CR 709.4a).
- `crates/engine/tests/game.rs:83603` — CR 709.4a: only a half is ever cast, so the action list must offer *each* half — the fused

### CR 712

- `crates/engine/src/apply.rs:907` — CR 712: the permanent flips to its back face (one-way, permanent). Its live
- `crates/engine/src/characteristics.rs:1387` — CR 712: a flipped permanent's base P/T comes from its back face — read through `def_of`
- `crates/engine/src/characteristics_cache.rs:176` — Flipping (CR 712) swaps the front face's name/types/P/T/abilities for the back's.
- `crates/engine/src/core.rs:399` — CR 712: a flipped permanent (a Kamigawa flip card) permanently uses its back face's
- `crates/engine/src/core.rs:416` — The object's *printed front* card definition, ignoring any flip swap. For a CR 712 flip
- `crates/engine/src/resolution/misc.rs:151` — Nezumi Graverobber: the source permanent flips to its back face (CR 712). One-way and
- `crates/engine/src/types/card.rs:1926` — Whether this permanent has *flipped* (CR 712 — a Kamigawa flip card, Nezumi Graverobber →
- `crates/engine/src/types/effect/shared.rs:909` — Flipping (CR 712) always affects the ability's own source, never a chosen target.
- `crates/engine/src/types/stack.rs:2127` — `object` flipped (CR 712 — a Kamigawa flip card's [`Effect::Misc(MiscEffect::FlipSource)`]): sets
- `crates/engine/tests/game.rs:88117` — The flipped (back) face of [`FLIPPER_FRONT`] — a CR 712 Kamigawa flip card's "flips" face
- `crates/engine/tests/game.rs:88123` — A constructed CR 712 flip card: its front face is a plain 2/2 whose only activated ability
- `crates/engine/tests/game.rs:88162` — CR 712: a flip card "flips" and permanently uses its back face's name, P/T, and abilities.
- `crates/engine/tests/game.rs:88265` — are no cards in that player's graveyard, flip Nezumi Graverobber." (CR 712 flip; CR 608.2 —

### CR 712.5

- `crates/engine/src/apply.rs:909` — unchanged (CR 712.5 — counters, attachments, tapped state persist).
- `crates/engine/src/types/card.rs:1931` — unchanged (CR 712.5), so counters, attachments, and tapped state ride across untouched.
- `crates/engine/tests/game.rs:88204` — CR 712.5: flipping doesn't change the object — counters, tapped state, and object identity

### CR 715

- `crates/engine/src/cast.rs:1713` — Cast the adventure half of an adventure card from hand (CR 715 — Brazen Borrower's Petty
- `crates/engine/src/de.rs:466` — An adventure card's adventure half (CR 715, soc/sos) — an inline `[adventure]`
- `crates/engine/src/types/card.rs:839` — An adventure card's adventure half (CR 715 — soc/sos): the front face is the creature
- `crates/engine/src/types/stack.rs:156` — Cast the adventure half of an adventure card from hand (CR 715 — soc/sos). `source` is the
- `crates/engine/src/types/stack.rs:2167` — The adventure half of an adventure card (CR 715) was cast from hand and put on the stack.
- `crates/engine/src/types/stack.rs:2781` — An adventure spell (CR 715) finished resolving: its card `from` (the spell on the stack) is

### CR 715.2a

- `crates/engine/src/apply.rs:715` — The adventure half is always an instant/sorcery (CR 715.2a).

### CR 715.3c

- `crates/engine/src/cast.rs:1731` — The adventure half is cast from the card's owner's hand (CR 715.3c).

### CR 715.3d

- `crates/engine/src/apply.rs:618` — A card cast from exile "on an adventure" (CR 715.3d) consumes its permission —
- `crates/engine/src/apply.rs:2147` — grant the owner an open-ended permission to cast it from exile (CR 715.3d).
- `crates/engine/src/combat.rs:182` — (CR 715.3d) — an open-ended permission with the same "cast from exile" gate.
- `crates/engine/src/effects.rs:601` — CR 715.3d: an adventure spell is exiled "on an adventure" (as the creature front face,
- `crates/engine/src/state.rs:161` — Adventure (CR 715.3d): each entry is `(a card exiled "on an adventure", its owner)` — the
- `crates/engine/src/types/card.rs:842` — "on an adventure" (CR 715.3d) and its owner may cast the creature half from exile later at
- `crates/engine/src/types/stack.rs:2785` — creature half from exile later at normal cost (CR 715.3d). Distinct from
- `crates/engine/tests/game.rs:48249` — "CR 715.3d: the adventure card is exiled on an adventure, not put into the graveyard",

### CR 717.2

- `crates/engine/src/cast.rs:2141` — A Class's "Level N" ability (CR 717.2 — "Gain the next level as a sorcery"): activatable
- `crates/engine/src/resolution/counters.rs:319` — A Class's "Level N" ability (CR 717.2): the activation gate only offered this while
- `crates/engine/src/types/stack.rs:2120` — A Class permanent gained a level (CR 717.2 — [`Effect::Counters(CountersEffect::LevelUp)`]): sets `…
- `crates/engine/tests/game.rs:77642` — CR 717.2: a Class levels up one step at a time, at sorcery speed. "Level 3" isn't offered
- `crates/engine/tests/game.rs:78325` — {2}{B}: Level 3 (CR 717.2) moves the Class from level 2 to level 3 — observed through the

### CR 717.4

- `crates/engine/src/types/card.rs:1665` — This permanent's Class level (CR 717.4 — a Class enchantment's level counter). Raised one
- `crates/engine/tests/game.rs:77626` — A Class enters at level 1 (CR 717.4): its base (`min_level` 0) upkeep ability functions,

### CR 717.5

- `crates/engine/src/cast.rs:2150` — A level-gated activated ability functions only at or above its level (CR 717.5).
- `crates/engine/src/characteristics.rs:971` — A level-gated anthem functions only at or above its level (CR 717.5). A
- `crates/engine/src/characteristics.rs:1627` — A level-gated anthem functions only at or above its level (CR 717.5). A
- `crates/engine/src/characteristics.rs:2085` — above its level (CR 717.5).
- `crates/engine/src/de.rs:1940` — The minimum Class level this ability requires to function (CR 717.5 — a Class's
- `crates/engine/src/replacements.rs:135` — at or above its level (CR 717.5).
- `crates/engine/src/triggers.rs:3738` — level (CR 717.5). Every ordinary ability is `min_level = 0`, and a non-permanent
- `crates/engine/src/types/card.rs:444` — The minimum Class level this ability requires to function (CR 717.5 — a Class's
- `crates/engine/tests/game.rs:102514` — replacement functions only at level 3 (CR 717.5).

### CR 720

- `crates/engine/src/amount.rs:90` — "Creatures you control" is a *control* test (CR 109.4/CR 720) — a creature you own
- `crates/engine/src/amount.rs:112` — controller isn't you (CR 108.3/720). A permanent you own is controlled by you or an
- `crates/engine/src/core.rs:536` — The control-changing Aura (CR 720 — [`Effect::Static(StaticEffect::ControlAttached)`]) currently at…
- `crates/engine/src/priority.rs:1769` — A one-shot until-end-of-turn control change (CR 720) ends in the cleanup
- `crates/engine/src/priority.rs:1882` — summoning sickness, and meets goad requirements under its new controller (CR 720).
- `crates/engine/src/resolution/control.rs:81` — Reins of Power (CR 720): the mass, two-player until-EOT control exchange. `target` is
- `crates/engine/src/resolution/control.rs:133` — Insurrection (CR 720): the mass, one-sided, all-creatures-of-any-controller twin of
- `crates/engine/src/resolution/control.rs:170` — Homeward Path (CR 720): "Each player gains control of all creatures they own."
- `crates/engine/src/resolution/control.rs:244` — Donation (Zedruu, CR 720): `target` is the donated permanent (first clause);
- `crates/engine/src/resolution/control.rs:279` — Exchange control (Vedalken Plotter / Chromeshell Crab, CR 720): `target` is the first
- `crates/engine/src/state.rs:5` — once-per-turn flags, until-EOT control (CR 720), and inspect-ledger provenance.
- `crates/engine/src/state.rs:83` — Active one-shot until-end-of-turn control changes (CR 720), each entry (the controlled
- `crates/engine/src/state.rs:94` — Permanent control changes with no stated duration (CR 720 — Entrancing Melody), each
- `crates/engine/src/state.rs:111` — Control-changing Aura (CR 720 — [`Effect::Static(StaticEffect::ControlAttached)`](crate::Effect::St…
- `crates/engine/src/types/stack.rs:2409` — A one-shot control-changing effect (CR 720) took effect: `object` is now controlled by
- `crates/engine/src/types/stack.rs:2428` — A permanent control change with no stated duration (CR 720 — Entrancing Melody):
- `crates/engine/tests/game.rs:32779` — A test-only flash Aura with a static [`Effect::Static(StaticEffect::ControlAttached)`] grant (CR 72…
- `crates/engine/tests/game.rs:32854` — ── Control-changing Auras (CR 720) ──────────────────────────────────────────────────
- `crates/engine/tests/game.rs:32872` — "ownership never changes (CR 720)"
- `crates/engine/tests/game.rs:42412` — ── One-shot until-end-of-turn control change (CR 720) ──────────────────────────────
- `crates/engine/tests/game.rs:42504` — "ownership never changes (CR 720)"
- `crates/engine/tests/game.rs:42610` — "ownership never changes (CR 720)"
- `crates/engine/tests/game.rs:42755` — ── Permanent control change gated on mana value X (CR 720, entrancing_melody #99) ──
- `crates/engine/tests/game.rs:43355` — gates it (CR 720). P0 steals a Sol Ring P1 owns, then donates that borrowed permanent to P2:
- `crates/engine/tests/game.rs:43571` — "the donation hands control of the Forest to P1 (CR 720)"
- `crates/engine/tests/game.rs:43808` — "the donation hands control of the creature to P1 (CR 720)"
- `crates/engine/tests/game.rs:43996` — "the donation hands control of the creature to P1 (CR 720)"
- `crates/engine/tests/game.rs:44075` — ── Board-wide control reset (CR 720 — Homeward Path) ───────────────────────────────
- `crates/engine/tests/game.rs:44129` — "Each player gains control of all creatures they own" (CR 720): P0 steals P1's creature,
- `crates/engine/tests/game.rs:44244` — creatures they own." (CR 720)
- `crates/engine/tests/game.rs:98296` — control." CR 109.4/CR 720 — "you control" is a control test, not an ownership test, so a

### CR 720.3

- `crates/engine/src/apply.rs:1246` — CR 302.6/720.3: gaining control of a permanent (here via a control-changing

### CR 800.4a

- `crates/engine/src/apply.rs:1265` — CR 800.4a: record when this control Aura took hold so
- `crates/engine/src/apply.rs:2085` — CR 800.4a last-known information: def/owner for a death-watch scan that
- `crates/engine/src/apply.rs:2476` — CR 800.4a: everything the departing player owns leaves the game — including a
- `crates/engine/src/apply.rs:2494` — CR 800.4a: any effect that gives the departing player control of an object also
- `crates/engine/src/apply.rs:2524` — CR 800.4a also purges the departing player's own outstanding pending trigger/
- `crates/engine/src/combat.rs:122` — since been eliminated (CR 800.4a drops the target pair but leaves the attacker in combat).
- `crates/engine/src/core.rs:4` — Also: multiplayer elimination handoff (CR 800.4a). Deferred / gaps: see
- `crates/engine/src/core.rs:478` — The controller of the permanent at `id` under CR 800.4a: when several control-changing
- `crates/engine/src/core.rs:520` — The next control-change timestamp (CR 800.4a), consuming it. Called as each control
- `crates/engine/src/lib.rs:139` — Monotonic source of control-change timestamps (CR 800.4a — "the most recent control-changing
- `crates/engine/src/priority.rs:107` — The CR 800.4a elimination sweep — remove everything the leaver owns (even permanents others
- `crates/engine/src/priority.rs:1512` — discards to hand size. Their zones were emptied by the CR 800.4a sweep, so a draw here
- `crates/engine/src/priority.rs:1993` — library objects were removed by the CR 800.4a sweep, so drawing from it would panic.
- `crates/engine/src/resolution/control.rs:83` — steal can't feed the second — CR 800.4a), untap them all, swap each to the OTHER
- `crates/engine/src/resolution/control.rs:138` — earlier steal/donation — CR 800.4a), and grant haste. Ownership is untouched.
- `crates/engine/src/resolution/control.rs:283` — `ControlGained` events (CR 800.4a: the swap outranks any earlier steal), leaving
- `crates/engine/src/resolution/resolve_misc.rs:120` — CR 800.4a: no living opponents (a solitaire test rig) — nothing to choose.
- `crates/engine/src/resolution/resume.rs:31` — (CR 800.4a — nobody left to answer / no live object to finish).
- `crates/engine/src/state.rs:90` — can pick the most recent control-changing effect (CR 800.4a) when several apply to one
- `crates/engine/src/state.rs:98` — by timestamp (CR 800.4a).
- `crates/engine/src/state.rs:109` — by its timestamp (CR 800.4a).
- `crates/engine/src/state.rs:116` — three override registries by recency (CR 800.4a). Stamped in
- `crates/engine/src/state.rs:300` — tombstoned that id to [`Object::Removed`]. CR 800.4a: a creature whose owner leaves the
- `crates/engine/src/triggers.rs:397` — CR 800.4a: if this same event batch already eliminated the entering
- `crates/engine/src/triggers.rs:494` — creature left with them (CR 800.4a) and its arena slot is already Removed —
- `crates/engine/src/triggers.rs:515` — false, // include_self: its own controller left the game too (CR 800.4a)
- `crates/engine/src/triggers.rs:655` — game in this same SBA sweep (CR 800.4a), the moved card left with them and
- `crates/engine/src/triggers.rs:942` — CR 800.4a: if `source`'s owner left the game in this same SBA sweep (e.g. a
- `crates/engine/src/triggers.rs:1424` — false (CR 800.4a: the dying creature's own controller also left the game in this same
- `crates/engine/src/triggers.rs:1493` — the same SBA sweep as `dying` (CR 800.4a) doesn't get its own trigger either.
- `crates/engine/src/triggers.rs:2475` — Skip an Aura whose owner left the game in this same sweep (CR 800.4a) — its trigger must
- `crates/engine/src/triggers.rs:2519` — Skip an Aura whose owner left the game in this same sweep (CR 800.4a) — its
- `crates/engine/src/triggers.rs:3851` — "you have two or more opponents": living seats only (CR 800.4a) — an eliminated
- `crates/engine/src/types/card.rs:927` — (CR 800.4a losers drop out), read from [`Game::living_player_count`].
- `crates/engine/src/types/card.rs:2002` — finished resolving (CR 707.10a), or an object owned by an eliminated player (CR 800.4a).
- `crates/engine/src/types/effect/shared.rs:1566` — is an opponent (CR 102.3), but an eliminated seat no longer counts (CR 800.4a) — the
- `crates/engine/tests/game.rs:4263` — controller to 0 life — CR 800.4a removes that player's objects (including the blocker)
- `crates/engine/tests/game.rs:32440` — panic in `controller_of` (the leaving player's trigger simply doesn't fire; CR 800.4a).
- `crates/engine/tests/game.rs:38273` — CR 800.4a: a departing player's owned cards leave the game, and with no living opponents
- `crates/engine/tests/game.rs:42902` — ── CR 800.4a control-effect timestamp precedence + CR 602.2/509.1a controller gates ────────
- `crates/engine/tests/game.rs:42905` — creature), so one card exercises CR 800.4a timestamp precedence and the CR 602.2/509.1a
- `crates/engine/tests/game.rs:42925` — CR 800.4a: when several control-changing effects apply to one permanent, the most recent
- `crates/engine/tests/game.rs:42963` — "the most recent control-changing effect wins (CR 800.4a), not the first recorded",
- `crates/engine/tests/game.rs:42970` — until-EOT steal wins while it lasts (CR 800.4a); when it ends at cleanup, control reverts to
- `crates/engine/tests/game.rs:43001` — "the later until-EOT steal outranks the earlier permanent steal (CR 800.4a)"
- `crates/engine/tests/game.rs:43550` — exchange's result (P0 again), not the donation's (P1) — CR 800.4a "most recent wins". Its
- `crates/engine/tests/game.rs:43580` — "the exchange (most recent, CR 800.4a) outranks the earlier donation"
- `crates/engine/tests/game.rs:43747` — "each creature is now controlled by the other player (CR 800.4a)"
- `crates/engine/tests/game.rs:43788` — turn P0 (the Reins controller) wins (CR 800.4a "most recent wins"). At cleanup the until-EOT
- `crates/engine/tests/game.rs:43822` — "the mass until-EOT steal (most recent, CR 800.4a) outranks the earlier donation this turn"
- `crates/engine/tests/game.rs:43870` — CR 800.4a: when a player leaves the game, everything they OWN leaves with them (even a
- `crates/engine/tests/game.rs:43909` — "the donated creature P0 owns leaves the battlefield with P0 (CR 800.4a)"
- `crates/engine/tests/game.rs:43914` — "the control effect giving P0 the stolen creature ends — it returns to its owner (CR 800.4a)"
- `crates/engine/tests/game.rs:43947` — "every creature, any controller, is now controlled by the caster (CR 800.4a)"
- `crates/engine/tests/game.rs:43975` — turn P0 (Insurrection's controller) wins (CR 800.4a "most recent wins"). At cleanup the
- `crates/engine/tests/game.rs:44004` — "the mass until-EOT steal (most recent, CR 800.4a) outranks the earlier donation this turn"
- `crates/engine/tests/game.rs:86316` — CR 800.4a: a departing player's outstanding pending triggers must be purged along with
- `crates/engine/tests/game.rs:86366` — The purge (CR 800.4a) must drop only the departed player's own pending trigger work — a
- `crates/engine/tests/game.rs:88399` — ── Increment 184: death-watch survives a coincident player loss (CR 603.6e/800.4a) ──────
- `crates/engine/tests/game.rs:88403` — CR 800.4a: when a creature dies in the same SBA sweep as its owner's elimination, it
- `crates/engine/tests/game.rs:97559` — (CR 102.3 — every other player is an opponent; CR 800.4a — an eliminated player is no

### CR 800.4e

- `crates/engine/src/priority.rs:1510` — CR 800.4e: if the active player leaves the game mid-turn, that turn continues to its
- `crates/engine/src/priority.rs:1811` — Nobody is left to discard for a seat that left the game (CR 800.4e).
- `crates/engine/src/priority.rs:1991` — CR 800.4e: a turn whose active player has left the game runs to completion without an

### CR 802

- `crates/engine/src/combat.rs:665` — Pillow-fort attack taxes (CR 508.1g / CR 802, Ghostly Prison): the sum owed across the

### CR 903

- `crates/engine/src/lib.rs:2` — Primary: CR 117 (priority), CR 405 (stack), CR 903 (Commander).
- `crates/engine/src/query.rs:1299` — Whether `player` controls a commander on the battlefield right now (CR 903, "you control a
- `crates/engine/src/spawn.rs:3` — Seeded objects for tests and lobby setup; commander tax (CR 903). Deferred / gaps:
- `crates/engine/tests/game.rs:15196` — control have hexproof." Both statics are commander_only anthems — a commander creature (CR 702.11, …
- `crates/engine/tests/game.rs:65679` — no commander, the tap still legally happens (a mana ability never fails to activate) but (CR 605, C…

### CR 903.4

- `crates/engine/src/characteristics.rs:474` — `player`'s commander color identity (CR 903.4) — the [`color_identity`] of their
- `crates/engine/src/characteristics.rs:477` — commander) — CR 903.4 identity mana wouldn't apply without one.
- `crates/engine/src/characteristics.rs:485` — The mana credit "one mana of any color in your commander's color identity" (CR 903.4 —
- `crates/engine/src/de.rs:939` — color in your commander's color identity" (CR 903.4, Command Tower) — or the literal string
- `crates/engine/src/priority.rs:166` — "One mana of any color in your commander's color identity" (CR 903.4, Command Tower)
- `crates/engine/src/resolution/mana.rs:80` — "One mana of any color in your commander's color identity" (CR 903.4, Arcane
- `crates/engine/src/types/card.rs:584` — Extra colors a card's real rules text carries for color identity (CR 903.4) that the
- `crates/engine/src/types/card.rs:1045` — placeholder for full CR 903.4 color identity — Commander deck-identity validation lives in
- `crates/engine/src/types/effect/shared.rs:129` — The number of colors in the effect's controller's commander's color identity (CR 903.4) —
- `crates/engine/src/types/mana.rs:426` — static `Mana` kind — "one mana of any color in your commander's color identity" (CR 903.4 —
- `crates/engine/tests/game.rs:19661` — ── Commander color identity mana: Command Tower / Arcane Signet (CR 903.4) ─────────
- `crates/engine/tests/game.rs:19812` — Re-audit fold-in: a 3+-color commander identity (CR 903.4) now resolves to a genuine
- `crates/engine/tests/game.rs:19854` — No commander designated: the identity is all-false (CR 903.4 doesn't apply), so the

### CR 903.9

- `crates/engine/src/spawn.rs:129` — ponytail: the "may" (CR 903.9) always defaults to yes — no fixed soc-pool card needs

### CR 903.9a

- `crates/engine/src/spawn.rs:127` — command zone if it's a commander (a special-cased replacement effect, CR 903.9a).

### CR 903.9b

- `crates/engine/src/resolution/zones.rs:59` — CR 903.9b: a commander diverted to the command zone instead of exile was never
- `crates/engine/src/spawn.rs:134` — candidate either way. Same rationale covers `exile_or_command`'s CR 903.9b diversion
- `crates/engine/src/spawn.rs:166` — commander (CR 903.9b). `new_id` is the id the resulting card will take. See
- `crates/engine/src/types/stack.rs:2779` — instead (CR 903.9b) rather than emitting this event directly.
- `crates/engine/tests/game.rs:35108` — "CR 903.9b: a commander that would be exiled goes to the command zone instead"

### CR 903.10a

- `crates/engine/src/apply.rs:206` — commander damage loses (CR 704.5a/b/c, CR 903.10a).
- `crates/engine/src/combat.rs:1042` — toughness. No commander-damage tally (CR 903.10a is player-only); lifelink still applies
- `crates/engine/src/core.rs:189` — commanders that have actually connected appear. 21 from any single one is lethal (CR 903.10a).
- `crates/engine/src/resolution/damage.rs:148` — combat-only (CR 903.10a), so a burn spell never adds to it.

### CR 957

- `crates/engine/src/types/stack.rs:1980` — ponytail: Effect is ~CR 957B; boxing the large variant would add indirection without buying much.
