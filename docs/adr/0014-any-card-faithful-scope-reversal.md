# 0014 — North star: any card, faithfully

Status: **Accepted**

## Decision

- Open-ended fidelity target — design posture, not completeness claim. No card out of scope in principle.
- Five `soc` decks = first proving ground (reframes 0012, does not supersede it).
- Grow from real cards (0002), TDD, flag-don't-force (`approximates` + `ponytail:`). Gaps → `docs/FIDELITY_BACKLOG.md`.

## Consequences

- `.agents/skills/card-dsl/` is the authoring reference. Layers/replacement built when cards need them (0003 path).
