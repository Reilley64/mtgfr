/target
/data
/crates/server/data
/logs
/crates/server/logs
/node_modules
openapi.json
client/lib/wire/generated/

mutants*.out

/.idea
.DS_Store

# Local env overrides — never commit real secrets
.env
.env.*
!.env.example

# Optional local Settings override (prod/secrets stay in env / K8s)
config/mtgfr.local.toml

# Verify-skill / curl cookie jars (live session tokens)
jar.txt
**/jar.txt

# Local agent/dogfood artifacts (videos, screenshots, seed jars)
/tmp/

# Visual brainstorm companion sessions (obra/superpowers)
.superpowers/

# Local agent implementation plans (execution checklists; not feature SoT)
# Living behavior lives in docs/superpowers/specs/. Agents still write plans
# under docs/superpowers/plans/ in their workspace; do not commit them.
docs/superpowers/plans/

# Ephemeral CR-index hook state + atomic write scratch
.cursor/hooks/state/
docs/CR_INDEX.md.tmp
docs/CR_INDEX.md.lock

__pycache__/
*.py[cod]

# Scryfall fetch cache for tooling/set_fidelity_intake.py
.cache/

# Terraform (iac/) — deploy PRD §Terraform. State lives in the kubernetes backend, not locally;
# tfvars hold secrets (image tags, DB password, Cloudflare token, kubeconfig path).
# `.terraform.lock.hcl` is intentionally NOT ignored — commit it for reproducible provider installs.
iac/.terraform/
iac/terraform.tfvars
iac/*.auto.tfvars
iac/terraform.tfvars.json
iac/*.tfstate
iac/*.tfstate.*
iac/*.tfplan
